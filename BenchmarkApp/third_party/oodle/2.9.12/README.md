# Oodle 2.9.12 Placeholder

Drop a licensed Oodle `2.9.12` SDK here, or point `BENCHMARK_OODLE_ROOT` at a
private copy elsewhere.

The `include/` and `lib/` directories are committed as empty placeholders only.
