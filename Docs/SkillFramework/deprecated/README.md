# Deprecated Skills

这里收拢已经退出 live 工作流的旧 skill。

规则：

- 这些 skill 只做历史存档，不再同步到 `~/.codex/skills`
- 现行工作流只认 `Docs/SkillFramework/lower-level/` 和 `Docs/SkillFramework/higher-level/`
- 如果后续需要复用旧经验，必须先判断是否应该升级为新的 lower-level 或 higher-level skill，而不是直接复活旧目录

当前归档：

- `drawio-tech-diagram-review`
- `technical-talk-animation`
