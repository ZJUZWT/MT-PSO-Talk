# Page 19 Strong Parent Tree Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Convert `page_19` into a left-tree / right-flow layout where `stable.upipelinecache` contains the UE PSO children and the reclaimed horizontal space is used to rebalance the right side.

**Architecture:** The implementation updates both the formal harness contract and the production Remotion page so they describe the same tree structure. Geometry will be validated through the existing slide harness, then the page script and audits will be refreshed so the page, contract, and timing/storyboard records stay in sync.

**Tech Stack:** TypeScript, React, Remotion, slide geometry harness, Jest, Python audit scripts

---

### Task 1: Rewrite the formal page 19 geometry contract

**Files:**
- Modify: `SlideApp/src/harness/slide-geometry/contracts/page19-r1.ts`
- Test: `SlideApp/src/review/page19VerticalRelief.test.ts`
- Test: `SlideApp/src/harness/slide-geometry/review/geometryMetrics.test.ts`

**Step 1: Update the sketch structure**

- Remove the standalone `ue-group` container node from the left-to-right chain.
- Enlarge the `stable` node into a real parent container.
- Mark `ue-1`, `ue-2`, and `ue-3` as children of `stable`.
- Reposition `gpu`, `memory-group`, and `disk-group` leftward.

**Step 2: Run the most relevant page 19 geometry tests**

Run: `npm --silent --prefix SlideApp test -- src/review/page19VerticalRelief.test.ts`
Expected: either PASS or a targeted FAIL that reflects the new tree geometry assumptions.

**Step 3: Adjust any contract-level expectations**

- If the test fails on stale coordinates or review language, update the expectation to match the new contract.
- Keep the assertions focused on spacing, containment, and page-scoped geometry quality.

**Step 4: Re-run the same test**

Run: `npm --silent --prefix SlideApp test -- src/review/page19VerticalRelief.test.ts`
Expected: PASS

### Task 2: Rebuild the production page 19 layout in Remotion

**Files:**
- Modify: `SlideApp/src/remotion/pages/Page10Scene.tsx`
- Test: `SlideApp/src/remotion/Composition.test.tsx`
- Test: `SlideApp/src/remotion/pages/lateTailGeometry.test.ts`

**Step 1: Update the left tree rendering**

- Replace the old `UE PSO` outer group with a `stable` parent container.
- Render the three PSO boxes as children inside the parent.
- Keep geometry data attributes aligned with the new contract ids and labels.

**Step 2: Shift and rebalance the right-side groups**

- Move the GPU stack left.
- Move `内存中 PSO` and `硬盘中的 PSO` left.
- Tune widths only if necessary to preserve text fit and gap quality.

**Step 3: Run focused rendering tests**

Run: `npm --silent --prefix SlideApp test -- src/remotion/Composition.test.tsx -t "page 19"`
Expected: PASS

**Step 4: Run geometry budget coverage**

Run: `npm --silent --prefix SlideApp test -- src/remotion/pages/lateTailGeometry.test.ts -t "page_19"`
Expected: PASS

### Task 3: Refresh the page 19 script and review ledger

**Files:**
- Modify: `Docs/剧本/19-第十九页-正式动画.md`
- Modify: `SlideApp/src/review/formalPageReviewRegistry.ts`
- Test: `SlideApp/src/review/formalPageReviewRegistry.test.ts`

**Step 1: Update the script narrative**

- Rewrite the node and edge inventory to describe the strong parent tree.
- Update the geometry summary to reflect the new left-tree structure and the reopened right-side spacing.
- Keep the animation and continuity sections accurate to the current page flow.

**Step 2: Sync the formal review registry**

- Ensure `page_19` still resolves to the refreshed formal sketch.
- Update review text only where the new layout changes the facts.

**Step 3: Run the registry test**

Run: `npm --silent --prefix SlideApp test -- src/review/formalPageReviewRegistry.test.ts`
Expected: PASS

### Task 4: Run harness verification and audits

**Files:**
- Modify if needed: page 19 review output artifacts only when regenerated by existing tooling

**Step 1: Run mechanical review from page 19**

Run: `npm --silent --prefix SlideApp run review:mechanical -- --from page_19`
Expected: no blocker pages; updated page 19 metrics reflect the new tree geometry.

**Step 2: Run timing audit**

Run: `python3 scripts/slide-geometry-harness/audit_transition_timings.py`
Expected: PASS

**Step 3: Run storyboard sync audit**

Run: `python3 scripts/slide-geometry-harness/audit_storyboard_sync.py`
Expected: PASS

**Step 4: Review any newly surfaced failures**

- If the audits expose stale copy or timing facts, fix the source file rather than suppressing the check.

### Task 5: Final cleanup and commit

**Files:**
- Stage only files changed for this feature

**Step 1: Inspect final diff**

Run: `git status --short`
Expected: only intended page 19 files plus any pre-existing unrelated edits.

**Step 2: Commit the feature changes**

```bash
git add Docs/plans/2026-04-20-page19-strong-parent-tree-design.md \
        Docs/plans/2026-04-20-page19-strong-parent-tree-plan.md \
        SlideApp/src/harness/slide-geometry/contracts/page19-r1.ts \
        SlideApp/src/remotion/pages/Page10Scene.tsx \
        SlideApp/src/review/formalPageReviewRegistry.ts \
        Docs/剧本/19-第十九页-正式动画.md
git commit -m "feat: rebalance page 19 with stable parent tree"
```

Expected: commit succeeds without staging unrelated work.
