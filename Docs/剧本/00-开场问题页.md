# 第 00 页

> 文件类型：`正式动画页`
> Step / Sketch：`page_00` / `formal-page00`
> 上一页：`-`
> 下一页：`page_01`

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `7.5/10` | 来源：`npm --silent --prefix SlideApp run review:mechanical -- --from page_00` 中 `formal-page00` 当前结果；`blockerOpen=false`。 |
| Node 分数 | `9.3/10` | 参考 `单节点与单边 Review`；三张补充图与右侧步骤标签都稳定可读。 |
| Edge 分数 | `9.3/10` | 本页无显式结构 edge，沿用版式阅读路径评分。 |
| 动画分数 | `in_range` | `page_00 -> page_01` 当前时长 `1.500s`，处在 Harness 标准允许区间 `1.489s ~ 2.010s`。 |

## 页面目标

开场不再先问问题，而是直接把“PSO Cache 启用前 -> 编译着色器 -> PSO Cache 启用后”这条链路摆在第一页。左侧三张补充图负责给出证据，右侧三条步骤文本负责把顺序说清，让观众先接受“卡顿高峰可以因为预热而消失”。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | 画布内无标题；step label 仍为 `开场`。 |
| 页面副标题 | 无。 |
| 当前页核心信息 | 左侧三张补充图竖排，右侧三条步骤文本对齐各自图片中轴；从“PSO Cache 启用前”经过“编译着色器”再到“PSO Cache 启用后”。 |
| 与下一页关系 | 先让观众接受“卡顿出现 -> 正在编译 -> 卡顿消失”的三步因果，再整体淡出，把 `page_01` 的最小模型淡入接上。 |
| 这一页新增了什么 | 中间新增一张 `pso-compile-shader.png` 作为“编译着色器”步骤，右侧新增三条步骤标签。 |
| 这一页删除了什么 | 之前那版“只有上下两张图”的直接对照语法，以及右侧 2 条聚焦追问。 |
| 这一页保留了什么 | 无标题开场、整体淡出到 `page_01` 的过渡规则。 |
| 需要强调的元素 | 三步流程的因果顺序，而不是单纯上下对照。 |
| 暂不实现 / 暂不出现 | 右侧问题栏、额外说明卡片、背景色块。 |

## 本轮数学约束复核

- 上图框：`(120,60,760,176)`；右侧标签框：`(930,131,240,34)`，文本 `PSO Cache启用前`
- 中图框：`(120,296,760,128)`；右侧标签框：`(930,343,200,34)`，文本 `编译着色器`
- 下图框：`(120,484,760,176)`；右侧标签框：`(930,555,240,34)`，文本 `PSO Cache启用后`
- 手工约束检查：三张图左边统一对齐、右侧文本统一在独立列中；右侧文本基线分别对齐到三张图片的中心线；左列与右列最小横向间距约 `50px`；顶边 / 左边 / 右边 / 底边外边距分别约为 `60 / 120 / 110 / 60`
- Harness formal review：`mechanicalScore=7.5/10`，`blockerOpen=false`
- 当前主要建议：tightest internal padding 仍是 `8px`，左右质量分布 delta 约 `88%`；但作为开场页，这一版已经把“前 -> 编译 -> 后”的流程补完整

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | 全页 | 静态停留 | 左三图 + 右三标签 | 不先提问，直接把“启用前 -> 编译 -> 启用后”的链路立起来。 |
| 2 | `00 -> 01` | 整页淡出 | 全页内容 | 不回闪旧主图，不切背景色块。 |
| 3 | `00 -> 01` 尾段 | `page_01` 淡入 | `Input -> f(x) -> Output` | 让最小模型接住“为什么会这样”的解释需求。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | 中间编译条与右侧三条步骤标签。 |
| 需要复用的元素 | `pso-stutter.png`、`pso-compile-shader.png`、`pso-precompile-smooth-peak.png` |
| 当前共享元素 / 连续语义 carrier | `opening-to-model` 仍是整页 fade handoff |
| 是否需要镜头推进 / 缩放 | 不需要 |
| 是否需要逐步显隐 | 只保留整体淡出到下一页 |
| 风险点 | 这版几乎把信息权重全交给图片，如果后续观众不熟悉语境，需要靠你口播在 3 到 5 秒内补一句“中间这一步是在预热着色器，所以后面的高峰消失了”。 |

## Current screenshot

- 本轮没有单独补浏览器截图；当前以 `Page00OpeningScene.tsx` 终态、`formal-page00` mechanical review 与定向测试共同作为对齐依据。
- 上图：`SlideApp/public/supplement/pso-stutter.png`
- 中图：`SlideApp/public/supplement/pso-compile-shader.png`
- 下图：`SlideApp/public/supplement/pso-precompile-smooth-peak.png`

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `N/A` | `5.7/10` | 左图列与右标签列分工明确，三步开场比旧的直接对照更完整。 | `5.7/10` |
| `模块空间结构 Review` | `N/A` | `6.0/10` | 左图列和右文列分层更明确，三步顺序已经稳定成立。 | `6.0/10` |
| `单节点与单边 Review` | `N/A` | `9.3/10` | 单个图框和标签都稳定，没有 blocker。 | `9.3/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `before-image` | 上图是卡顿现场截图，只保留上半部分。 | 开场第一页，无上一页依赖。 | 裸图 | 作为流程起点很直接。 | `N/A` | 负责交代“启用前”的状态。 | `9.3/10` |
| `before-label` | 上图右侧文字，说明“PSO Cache启用前”。 | 开场第一页，无上一页依赖。 | 纯文本 | 让步骤语义不再含糊。 | `N/A` | 垂直对齐上图中轴。 | `9.0/10` |
| `compile-image` | 中图是“正在预热着色器”的编译条。 | 开场第一页，无上一页依赖。 | 裸图 | 把中间动作明确补出来。 | `N/A` | 作为中间步骤很关键。 | `9.2/10` |
| `compile-label` | 中图右侧文字，说明“编译着色器”。 | 开场第一页，无上一页依赖。 | 纯文本 | 让观众知道这不是第三个结果，而是过程。 | `N/A` | 与中图中轴对齐。 | `9.0/10` |
| `after-image` | 下图是 `page_18_img` 的平滑峰值图，只保留上半部分。 | 开场第一页，无上一页依赖。 | 裸图 | 给出结果终点。 | `N/A` | 负责交代“启用后”的状态。 | `9.3/10` |
| `after-label` | 下图右侧文字，说明“PSO Cache启用后”。 | 开场第一页，无上一页依赖。 | 纯文本 | 把收束语义说清。 | `N/A` | 垂直对齐下图中轴。 | `9.0/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `before-to-compile-to-after` | 由上图落到中图，再落到下图，完成“启用前 -> 编译 -> 启用后”的阅读路径。 | 开场第一页，无上一页依赖。 | 版式阅读路径 | 阅读顺序直接。 | `N/A` | 三步关系比旧的双图对照更完整。 | `9.2/10` |
| `opening-to-model` | 本页整体淡出，再把 `page_01` 的最小模型整体淡入。 | 连到 `page_01`。 | 跨页 handoff | 满足解释页先淡出再淡入主线页的规则。 | `in_range` | 当前 timing 仍通过 Harness 审计。 | `in_range` |

## 过渡动画剧本与时长审查

- 过渡：`page_00 -> page_01`
- frame window：`18 -> 108`（`90` frames @ 60fps）
- durationSec：`1.500`
- requiredSec：`1.489`
- allowedMinSec：`1.489`
- allowedMaxSec：`2.010`
- durationVerdict：`in_range`
- preset：`core_flow`

### 标准相位时间轴

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
| --- | ---: | ---: | ---: | --- |
| `hold_before` | 0 | 120 | 120 | 静态停留，让观众先看完整三步链路。 |
| `fade_out` | 120 | 340 | 220 | `page_00` 整页淡出。 |
| `node_motion` | 340 | 729 | 389 | 由三步开场切入最小模型。 |
| `edge_motion` | 729 | 1109 | 380 | `page_01` 主链建立。 |
| `fade_in` | 1109 | 1329 | 220 | `page_01` 元素整体淡入。 |
| `hold_after` | 1329 | 1489 | 160 | 留出最小模型的稳定阅读时间。 |

### 标准动作时间表

| 动作 id | kind | metric | duration(ms) | 说明 |
| --- | --- | --- | ---: | --- |
| `n_fade_out_old` | `fade_out` | `-` | 220 | 开场页旧元素退场。 |
| `n_move_primary` | `node_move` | `distancePx=220` | 389 | 最小模型主体建立。 |
| `e_grow_primary` | `edge_grow` | `lengthPx=320` | 380 | `page_01` 主链建立。 |
| `n_fade_in_new` | `fade_in` | `-` | 220 | 新页元素显现。 |

### 关键时间点

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
| --- | ---: | ---: | --- | --- |
| `Start` | 18 | 0.000 | 开场页静止展示 | 无边动作 |
| `Q1` | 41 | 0.375 | `n_move_primary` 开始主内容过渡 | 无显著边变化 |
| `Mid` | 63 | 0.750 | 主要节点运动基本完成 | `e_grow_primary` 建立主链 |
| `Q3` | 86 | 1.125 | `n_fade_in_new` 让最小模型显现 | 无显著边变化 |
| `End` | 108 | 1.500 | `page_01` 进入稳定终态 | 主链稳定可读 |

## 本轮改动说明

- 新增了：中间 `compile-count` 编译条，以及右侧三条步骤标签。
- 移动了：左侧图列统一改为 `x=120`；上图 `y=60, w=760, h=176`，中图 `y=296, w=760, h=128`，下图 `y=484, w=760, h=176`；右侧三条文本分别对齐三张图片中轴。
- 删除了：上一轮“单纯把双图做大”的方案，以及旧的右侧 2 条聚焦追问。
- 增加 / 删除的 node / edge：
  - 新增 node：无新的正式场景 node 类型
  - 删除 node：旧的 `prompt-list` 区域
  - 新增 node：`compile-image`、`compile-label`
  - 删除 node：旧的双图开场标签语法
  - 新增 edge：无
  - 删除 edge：无
- 被替换的行为：
  - 原来的“主图 + 底部结果图 + 右侧两问”开场，被替换成“左三图 + 右三标签”的三步流程语法。
  - `page_00 -> page_01` 的 fade handoff 保持不变，没有替换时长或 phase。

## 审批状态

`已按本轮要求改成：左侧三张图依次展示启用前、编译着色器、启用后，右侧三条文字同步对齐图片中轴，把开场直接讲成三步流程。`
