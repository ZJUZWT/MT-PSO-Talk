# 第 01 页

> 文件类型：`正式动画页`
> Step / Sketch：`page_01` / `-`
> 上一页：`-`
> 下一页：`page_02`


## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `8.8/10` | 来源：`2026-04-17` 截图复核（`ignore/review-refresh-20260417/page_01.png`） |
| Node 分数 | `8.9/10` | 三节点层级与字号清晰，中心焦点明确 |
| Edge 分数 | `9.0/10` | 两条主边直线、锚点居中、无扭曲 |
| 动画分数 | `10.0/10` | 首帧为静态建立页，无跨页转场压力 |

## 页面目标

用最简单的 `Input -> f(x) -> Output` 建立整场 talk 的起点。这一页先给观众一个稳定、可记忆的最小骨架，后续所有更复杂的结构都从这个骨架演化出来。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | Input -> f(x) -> Output |
| 页面副标题 | 最小公式骨架 |
| 当前页核心信息 | 输入经过一个函数，得到输出 |
| 与上一页关系 | 新起点，无需继承旧剧本 |
| 这一页新增了什么 | 一个水平主轴上的 `Input`、`f(x)`、`Output` 三点结构 |
| 这一页删除了什么 | 占位说明文案、待编写提示、无关卡片信息 |
| 这一页保留了什么 | 轻量背景、单一舞台框、稳定居中的舞台结构 |
| 需要强调的元素 | 中间的 `f(x)`，它是全页唯一主焦点 |
| 暂不实现 / 暂不出现 | 入场动画、镜头推进、额外注释框、更多节点 |

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | 全页 | 保持静态终态，不做入场动画 | `Input`、箭头、`f(x)`、箭头、`Output` | 第一页只需要让观众接受最基本的“输入 -> 变换 -> 输出”模型 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | `Input` 节点、`f(x)` 中心节点、`Output` 节点、两根水平箭头 |
| 需要复用的元素 | 现有右侧舞台框、当前页面的浅色背景语言 |
| 是否需要 draw.io 草稿 | 不需要，先直接用网页静态终态验证构图 |
| 是否需要镜头推进 / 缩放 | 不需要 |
| 是否需要逐步显隐 | 不需要 |
| 风险点 | 如果 `f(x)` 不够大或左右节点过重，会削弱“中间是唯一焦点”的阅读路径 |

## 审批状态

`已确认：静态终态版`

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `8.7/10` | `8.9/10` | 主舞台居中稳定、留白舒适。 | `8.8/10` |
| `模块空间结构 Review` | `8.8/10` | `8.9/10` | 三段式因果链清楚，中心模块权重正确。 | `8.9/10` |
| `单节点与单边 Review` | `8.9/10` | `9.0/10` | 节点和边都足够干净，几乎无噪声。 | `8.9/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `input-node` | 左侧输入节点，代表系统输入端。 | 首页起点，无上一页依赖 | 胶囊节点 | 起点语义明确。 | `8.8/10` | 尺寸与留白平衡。 | `8.8/10` |
| `fx-node` | 中央变换节点，代表核心处理。 | 首页核心语义 | 主焦点节点 | 焦点非常明确。 | `9.1/10` | 视觉重量与位置最稳定。 | `9.1/10` |
| `output-node` | 右侧输出节点，代表结果端。 | 首页终点语义 | 胶囊节点 | 终点语义清晰。 | `8.8/10` | 与左侧节点对称一致。 | `8.8/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `input-to-fx` | 输入流向核心处理。 | 首页起始边 | 主干箭头 | 因果方向明确。 | `9.0/10` | 直线与锚点都很干净。 | `9.0/10` |
| `fx-to-output` | 处理结果流向输出。 | 首页收束边 | 主干箭头 | 收束语义清楚。 | `9.0/10` | 与左侧边保持对称。 | `9.0/10` |
