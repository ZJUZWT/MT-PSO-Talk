# 第 02 页

> 文件类型：`正式动画页`
> Step / Sketch：`page_02` / `-`
> 上一页：`page_01`
> 下一页：`page_03`


## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `8.6/10` | 来源：`2026-04-21` 截图复核（`ignore/browser-api-captures/20260421-page02-vertexbuffer-big/page02.png`） |
| Node 分数 | `8.7/10` | 三节点语义升级后依旧清晰，图标可读性好 |
| Edge 分数 | `8.9/10` | 主边保持直线与对称，路径极简 |
| 动画分数 | `in_range` | `page_01 -> page_02` 为 `90` 帧，最新 transition timing audit verdict=`in_range` |

## 页面目标

把第一页的 `Input -> f(x) -> Output` 具体化成更接近图形渲染语义的版本，但仍然保留第一页的三段式空间骨架。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | VertexData -> GPU -> Pixels |
| 页面副标题 | 从抽象函数到图形语义 |
| 当前页核心信息 | 输入不再是抽象 `Input`，而是 `VertexData`；中间不再是抽象 `f(x)`，而是 `GPU`；输出变成像素块；上方再补一个最小 `Pipeline State`，并把 `PSO = Shader + State` 预告卡与 `VertexBuffer` 参考 inset 一起压到主轴下方的辅助带 |
| 与上一页关系 | 不是重画，而是在相同三槽位里做内容演化 |
| 这一页新增了什么 | `VertexData` 的两三角形图形表达、单层 `GPU`、`4x4` 像素输出、`GPU` 上方最小 `Pipeline State` 入口，以及主轴下方一组由 `PSO = Shader + State` 预告卡和 `VertexBuffer` 参考 inset 组成的辅助双图带 |
| 这一页删除了什么 | `Texture`、弯线、GPU 内框、GPU 四周装饰豆豆、旧版 `U` 型接口伏笔 |
| 这一页保留了什么 | 左中右三段结构、两根直线箭头、GPU 作为中间核心 |
| 需要强调的元素 | `GPU` 仍然是全页中心；`Pipeline State` 用统一 API 橙色提示“除了数据，还有状态”；下方双图带只做证据补充，不压住主轴，不提前抢第三页的配置层节奏 |
| 暂不实现 / 暂不出现 | 更多输入资源、更多渲染阶段、完整 API 名字列表 |

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | 01 -> 02 过渡开始 | `Input` 保持原位置，开始长高并替换内容 | 左侧 node | 不移动骨架，只替换语义 |
| 2 | 过渡中段 | `f(x)` 在原位置形变成 `GPU` | 中间 node | 让观众意识到抽象函数正在被具体化 |
| 3 | 过渡后段 | `GPU` 上方补一个最小 `Pipeline State`，并用 `GfxAPI设置` 竖箭头接到 GPU | 中间 node 上方 | 先让观众接受“GPU 还要吃状态” |
| 4 | 过渡后段 | 主轴下方同步补入缩略 `PSO = Shader + State` 预告卡与 `VertexBuffer` 参考 inset，组成辅助双图带 | 下方辅助双图带 | 提前埋下“状态里到底装了什么”与“输入布局也会进入 state”的双钩子，不额外压缩原主轴时长 |
| 5 | 过渡后段 | `Output` 在原位置替换成 `4x4` 像素块 | 右侧 node | 输出不再抽象，而是可见的像素结果 |
| 6 | 过渡结束 | 停在 `VertexData -> GPU -> Pixels` + `Pipeline State` + 下方辅助双图带 的静态终态 | 全页 | 为第三页 OpenGL 配置层和第二十八页 `PSO = Shader + State` 治理页同时埋钩子 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | 左侧两三角形图标、右侧 `4x4` 像素网格、`GPU` 上方 `Pipeline State`，以及主轴下方的 `PSO = Shader + State` 预告卡与 `VertexBuffer` 参考 inset |
| 需要复用的元素 | 第一页三槽位骨架、两根直线箭头、GPU 中轴位置 |
| 是否需要 draw.io 草稿 | 暂不需要 |
| 是否需要镜头推进 / 缩放 | 不需要 |
| 是否需要逐步显隐 | 需要，在节点内部做交叉显隐 |
| 风险点 | 如果下方 `PSO` 预告卡过高，会顶住主轴下边距；如果下方 `VertexBuffer` inset 过大，会挤掉底部留白；如果 `Pipeline State` 仍用红色，会和后续正式 API 配色脱节 |

## 审批状态

`已确认：A 结构 + VertexData 两三角形 + 单层 GPU + 4x4 Pixels`

## 2026-04-21 补图复核

- 新增元素：`page2-vertex-buffer-inset`，资源为 `SlideApp/public/supplement/VertexBuffer.png`
- 移动节点：`vertex-buffer-inset` 从左下补充带移动到 `Pipeline State` 右侧；`VertexData / GPU / Pixels / Pipeline State / PSO preview card` 原槽位保持不动
- 增加 / 删除 node / edge：新增 node `vertex-buffer-inset`；edge 无新增、无删除
- 行为替换：原“左上 `PSO` 预告卡单独淡入”替换为“左上 `PSO` 预告卡 + `Pipeline State` 右侧 `VertexBuffer` inset 同段淡入”；未改 frame anchors，原主轴时长未被压缩
- Harness 事实：本轮 formal review artifact 复核为 `overlap=0 / crossing=0 / nodePierce=0 / textOverflow=0`，但该页仍是早期紧凑页，当前 `minMargin=8`、`minNodeGap=2`

## 2026-04-21 下方双图带复核

- 新增元素：无新增页面元素。
- 移动节点：`pso-preview-card` 与 `vertex-buffer-inset` 从主轴上方辅图区一起下移到 `VertexData / GPU / Pixels` 主轴下方；`Pipeline State` 与主轴节点位置不变。
- 增加 / 删除 node / edge：无新增、无删除。
- 行为替换：保留原“`Pipeline State` 先建立，再补充辅助解释图”的淡入顺序，只把两块辅助图的终态位置改为主轴下方；未改 `page_01 -> page_02` frame anchors。
- Harness 事实：最新 transition timing audit 中 `page_01 -> page_02` 仍为 `1.500s`、verdict=`in_range`；latest `review:mechanical -- --from page_02` 返回 `page_02 status=ok / reviewSource=formal / sketchId=formal-page02 / blockerOpen=false`。

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260421-page02-vertexbuffer-big/page02.png)

- 当前 URL：`http://127.0.0.1:4173/?step=page_02`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260421-page02-vertexbuffer-big/page02.txt`
- Capture size：`4096 x 2404`

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `8.5/10` | `8.7/10` | 继承第一页骨架，空间稳定。 | `8.6/10` |
| `模块空间结构 Review` | `8.6/10` | `8.8/10` | `VertexData -> GPU -> Pixels` 一眼读通。 | `8.7/10` |
| `单节点与单边 Review` | `8.7/10` | `8.8/10` | 图标、文字和边线都较干净。 | `8.7/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `vertexdata-node` | 左侧 `VertexData` 输入节点（两三角形图标）。 | 承接 page01 输入槽位 | 图标节点 | 连续语义清楚。 | `8.7/10` | 图形简洁、识别度高。 | `8.7/10` |
| `gpu-node` | 中央 `GPU` 核心处理节点。 | 承接 page01 中央槽位 | 主焦点节点 | 核心语义明确。 | `8.9/10` | 焦点与字重控制良好，上方 `Pipeline State` 没有抢走中心权重。 | `8.9/10` |
| `pipeline-state` | `GPU` 上方最小 `Pipeline State` 节点。 | 新增状态入口 | 状态入口节点 | 用 API 橙色统一后，和后续 OpenGL / Vulkan 页连续性更强。 | `8.4/10` | 语义足够轻，不会提前展开成第三页的复杂配置带。 | `8.4/10` |
| `pso-preview-card` | 左上角缩略 `PSO = Shader + State` 预告卡。 | 借用 page28 的治理图式做提前预告 | 预告结构卡 | 只提示状态维度，不抢主轴。 | `8.1/10` | 作为左上角钩子成立，并给后续“平台差异”页预埋心智。 | `8.1/10` |
| `vertex-buffer-inset` | `Pipeline State` 右侧的 `VertexBuffer` 参考 inset。 | 新增输入布局证据 | screenshot inset | 成立。 | `8.2/10` | 贴着 `Pipeline State` 出现后，`Vertex Decl / Input Layout` 的证据关系更直接。 | `8.2/10` |
| `pixels-node` | 右侧 `Pixels` 输出节点（像素网格）。 | 承接 page01 输出槽位 | 图标节点 | 收束语义明确。 | `8.5/10` | 像素图标清楚，略小。 | `8.5/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `vertex-to-gpu` | `VertexData` 进入 `GPU`。 | 承接 page01 左侧主边 | 主干箭头 | 连续语义成立。 | `8.9/10` | 直线、居中、无噪声。 | `8.9/10` |
| `gpu-to-pixels` | `GPU` 输出到 `Pixels`。 | 承接 page01 右侧主边 | 主干箭头 | 收束语义成立。 | `8.9/10` | 与左边保持对称。 | `8.9/10` |
| `pipeline-to-gpu` | `Pipeline State` 通过 `GfxAPI设置` 竖箭头进入 `GPU`。 | 新增状态入口 | 状态箭头 | 用统一 API 橙色强调“状态也是输入”。 | `8.3/10` | 方向单纯清楚，和主轴不打架。 | `8.3/10` |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_01 -> page_02）

- 帧窗口：`108 -> 198`（`90` frames @ 60fps）
- 总时长：`1.500s`
- 时长评估：`in_range`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `90`，相对当前 `Δ+0` frames
- 标准时序评估：沿用 `core_flow` preset，最新 transition timing audit verdict=`in_range`
- 标准推荐最短时长：`1.489s`
- 标准允许上限：`2.010s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 120.0 | 120.0 | - |
| fade_out | 120.0 | 340.0 | 220.0 | n_fade_out_old |
| node_motion | 340.0 | 729.0 | 389.0 | n_move_primary |
| edge_motion | 729.0 | 1109.0 | 380.0 | e_grow_primary |
| fade_in | 1109.0 | 1329.0 | 220.0 | n_fade_in_new |
| hold_after | 1329.0 | 1489.0 | 160.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| n_fade_out_old | fade_out | fade_out | - | 220.0 | `core_flow` audit preset |
| n_move_primary | node_move | node_motion | primary semantic shift | 389.0 | `core_flow` audit preset |
| e_grow_primary | edge_grow | edge_motion | primary route growth | 380.0 | `core_flow` audit preset |
| n_fade_in_new | fade_in | fade_in | - | 220.0 | `core_flow` audit preset |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 108 | 0.000 | page_01 三槽位静置 | 主边静置 |
| Q1 | 130 | 0.367 | 旧 `Input / f(x) / Output` 开始淡出 | 主边无显著变化 |
| Mid | 153 | 0.750 | `VertexData / GPU / Pixels` 主骨架接管，`Pipeline State` 与辅助卡开始落位 | 主边进入切换 |
| Q3 | 176 | 1.133 | 新主轴与状态入口基本稳定，右侧 `VertexBuffer` inset 已进入终态区域 | 新主边完成生长 |
| End | 198 | 1.500 | 停在 `VertexData -> GPU -> Pixels` + `Pipeline State` + `VertexBuffer` inset 终态 | 无显著边变化 |

## Animation review verdict

- 2026-04-21 放大并右移 `VertexBuffer` inset 后，`page_01 -> page_02` 的 frame anchors 保持 `108 -> 198` 不变，没有为补图压缩原三段式主轴时长。
- 最新 `probe_transition_timeline.py --from-step page_01 --to-step page_02 --emit-markdown` 与 transition timing audit 都显示当前转场为 `in_range`。
- 当前转场仍按“先淡出上一页抽象三段式，再淡入新语义节点、`Pipeline State`、左上 `PSO` 预告卡和右侧 `VertexBuffer` inset”的顺序执行，没有额外插入无关闪回。

## 2026-04-21 左侧客观事实累积提示

- 新增元素：左侧 `客观事实` 区块在 `page_02` 新增条目 `PSO的复杂度是指数级别。`
- 移动节点：无页面主画板节点移动；左侧 notes 卡内部布局仅对新增事实行增加局部高亮容器，原 Session 区与主画板节点都不移动。
- 增加 / 删除 node / edge：Remotion 主画板 node / edge 无新增、无删除；左侧 notes DOM 新增的是“新增事实”局部扫光伪元素，不引入新的主画板连线。
- 行为替换：原“客观事实只做静态累积展示”替换为“新增事实在首次出现时播放一次局部波动发光 + 扫光，再回到稳定高亮”；未改 `page_01 -> page_02` 的 frame anchors，原主轴转场时长未被压缩。
- Harness 事实：本次改动只作用在左侧 notes 列，`page_01 -> page_02` 仍按原有 `1.500s` 过渡执行；转场解释继续以主画板 timing 为准，左侧新增事实提醒不替换现有 phase 顺序。
