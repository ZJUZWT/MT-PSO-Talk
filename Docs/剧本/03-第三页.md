# 第 03 页

> 文件类型：`正式动画页`
> Step / Sketch：`page_03` / `-`
> 上一页：`page_02`
> 下一页：`page_04`


## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `7.8/10` | 来源：`2026-04-17` 截图复核（`ignore/review-refresh-20260417/page_03.png`） |
| Node 分数 | `7.6/10` | 上层配置节点较多但仍可读，主焦点保持在 `GPU` |
| Edge 分数 | `7.2/10` | 边数量增多后可读性下降，仍无硬性交叉 |
| 动画分数 | `in_range` | 最新 transition timing audit：`page_02 -> page_03` 保持 `1.500s`，verdict=`in_range` |

## 页面目标

保留第二页底部 `VertexData -> GPU -> Pixels` 主轴，但把整组结构下移，为 GPU 上方让出一条新的配置带，开始讲 GPU 不是孤立工作的，它还要接收编译结果和状态配置。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | Compile / States -> GPU |
| 页面副标题 | 从单一 GPU 节点展开出上方配置层 |
| 当前页核心信息 | GPU 不只是吃 `VertexData`，还要接收 `ShaderBinary` 和状态配置 |
| 与上一页关系 | 不是改画布，而是把第二页整组往下压，在上方插入一层新的来源 |
| 这一页新增了什么 | 左侧 `ShaderCode -> ShaderBinary` 横向链路、右侧 `Depth` / `Blend`、带编号的 `Graphics API` 列表 |
| 这一页删除了什么 | 没有删除第二页主轴，只是把主轴下移并让 GPU 放大 |
| 这一页保留了什么 | 底部 `VertexData -> GPU -> Pixels` 主轴、水平主阅读骨架、GPU 仍然是全页收口点 |
| 需要强调的元素 | 上方多条线都向下收口到 GPU，且 `GFXAPI CALL` 的颜色与底部数据流颜色不同 |
| 暂不实现 / 暂不出现 | 更细的状态分类、更多输入资源、PSO 统一收口对象 |

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | 02 -> 03 过渡开始 | 底部 `VertexData -> GPU -> Pixels` 整组向下移动 | 底部三段主轴 | 先保住观众对第二页的空间记忆 |
| 2 | 过渡中段 | GPU 同步放大，变成更明确的收口对象 | 中间 GPU | 让观众意识到它要接收更多东西 |
| 3 | 过渡后段 | 左边出现 `ShaderCode -> ShaderBinary` 的横向链路，并带上 API 编号 | 上方左侧 | 不再写“编译”中文，而是用编号对应 API 名称 |
| 4 | 过渡后段 | 右边出现 `Depth`、`Blend`，两条线从上往下接入 GPU，并带上编号 | 上方右侧 | 强调这些不是运行时像素流，而是 CPU 通过图形 API 配置 GPU |
| 5 | 过渡结束 | 停在第三页终态 | 全页 | 为后续更复杂的对象打包做准备 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | `ShaderCode`、`ShaderBinary`、`Depth`、`Blend`、带编号的 `Graphics API` 文本列表 |
| 需要复用的元素 | 第二页底部主轴、GPU 的中心收口位置、左右节点位置 |
| 是否需要 draw.io 草稿 | 当前可直接在网页里验证，因为结构还不复杂 |
| 是否需要镜头推进 / 缩放 | 不需要镜头推进，只在图内做节点位移和放大 |
| 是否需要逐步显隐 | 需要，先移底部主轴，再出现上方配置带 |
| 风险点 | 如果 Shader 侧和状态侧没有明确左右分区，或者编号箭头和左侧 API 名称对应不上，第三页就会讲不清楚 |

## 审批状态

`已转为实现稿：等待继续细化`

## 2026-04-21 主轴复原复核

- 新增元素：无新增页面元素；本次恢复的是 `page_02 -> page_03` 开头的主轴下压动画。
- 移动节点：`VertexData / GPU / Pixels` 三段主轴恢复为从 page02 终态向 page03 更低终态过渡；`ShaderCode / ShaderBinary / Depth / Blend` 上方配置带也恢复到更靠下的历史落位。
- 增加 / 删除 node / edge：无新增、无删除。
- 行为替换：撤销“page03 与 page02 同高”的误改，恢复“先把底部主轴整组下压，再长出上方配置带”的原节奏；未改 `page_02 -> page_03` frame anchors。

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `7.7/10` | `7.9/10` | 上下双层结构成立，密度明显上升。 | `7.8/10` |
| `模块空间结构 Review` | `7.5/10` | `7.7/10` | 上层输入与底部主链关系可读，仍有拥挤感。 | `7.6/10` |
| `单节点与单边 Review` | `7.1/10` | `7.4/10` | 关键边方向明确，但编号线与竖线较多。 | `7.2/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `gpu-node` | 下层主轴核心 `GPU` 节点。 | 承接 page02 中央焦点 | 主焦点节点 | 连续语义稳定。 | `8.1/10` | 中心稳定，视觉锚点强。 | `8.1/10` |
| `shader-module-nodes` | 上层 `RawShaderCode -> BinaryShaderCode -> Program` 节点组。 | 从 page02 扩展出的新模块 | 配置模块节点组 | 新增语义清楚。 | `7.4/10` | 模块完整，但左上区域稍密。 | `7.4/10` |
| `state-nodes` | `Depth` 与 `Blend` 状态节点。 | 新增状态来源 | 状态节点组 | 语义正确。 | `7.3/10` | 节点可读，留白略紧。 | `7.3/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `bottom-main-edges` | `VertexData -> GPU -> Pixels` 底部主轴边。 | 连续承接 page02 主链 | 主干边组 | 连续语义清楚。 | `8.0/10` | 主轴边仍最清晰。 | `8.0/10` |
| `shader-to-gpu-edges` | `Program` 以及 shader 相关路径汇入 `GPU`。 | 新增配置输入边 | 配置边 | 汇入逻辑成立。 | `7.0/10` | 竖线和编号并置后略显拥挤。 | `7.0/10` |
| `state-to-gpu-edges` | `Depth/Blend` 向下进入 `GPU` 的状态边。 | 新增状态输入边 | 状态边 | 语义正确。 | `6.8/10` | 与邻近边距离偏近。 | `6.8/10` |
