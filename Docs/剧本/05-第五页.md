# 第 05 页（数据插页）

> 文件类型：`正式动画页`  
> Step：`page_04_data`  
> 上一页：`page_04`  
> 下一页：`page_05`


## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `8.6/10` | 来源：`三层 Review 总表` 的 `整体布局 Review` 行 |
| Node 分数 | `8.7/10` | 来源：`节点剧本与 Review 表` 综合分数列均值 |
| Edge 分数 | `8.9/10` | 来源：`边剧本与 Review 表` 综合分数列均值 |
| 动画分数 | `5.8/10` | 自动审计（`2026-04-18`）：`page_04_data -> page_05` 为 `too_long`，其余转场 in_range |

## Page goal

- 这一页只做一件事：在 UE Cook 主线前插入一张可投屏的数据表，给观众一个 OpenGL / Vulkan 耗时锚点。  
- 强约束：保持统一背景，不加大色块底板；最小字体 `20px`；不引入额外 node/edge 的新拓扑。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | 数据页：OpenGL / Vulkan 平均耗时 |
| 页面副标题 | 在进入 UE Cook 前单独停一页，用一张表展示 Link/Create/Bind 的 Min/Max/Avg |
| 当前页核心信息 | 纯表格，不讲新结构；仅提供性能量级与分布感知 |
| 与上一页关系 | 第四页主图先淡出，表格淡入 |
| 与下一页关系 | 表格淡出后回放第四页到第五页原始结构动画，再落到 UE Cook |
| 这一页新增了什么 | 标题、四列表格、四行 API 数据、来源行 |
| 这一页删除了什么 | 删除上一页中的流程节点强调（进入数据态时） |
| 这一页保留了什么 | 全局背景、舞台框架、左侧说明卡样式 |
| 需要强调的元素 | `Min/Max/Avg` 三列对比与 `Create` 行的峰值 |
| 字体约束 | 当前页最小字号 `20px`（已落地） |

## Node inventory

| id | text | semantic role | visual kind | anchor region | emphasis | alignment constraints | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `data_title` | `OpenGL / Vulkan 耗时对比表` | 数据页主标题 | text | 顶部左 | high | 与表格左边界对齐 | 否 | n/a |
| `data_subtitle` | 指标单位说明 | 辅助说明 | text | 标题下方 | medium | 与主标题左对齐 | 否 | n/a |
| `data_table` | API/Min/Max/Avg | 主信息容器 | grid/table | 画面中部 | high | 左右边距对称 | 否 | n/a |
| `data_source` | 数据来源行 | 追溯说明 | text | 底部左 | low | 与表格左边界对齐 | 否 | n/a |

## Edge inventory

| id | from | to | meaning | route grammar | max bends | segment label | arrow style | sibling / symmetry note | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `table_col_dividers` | `data_table` | `data_table` | 列切分 | 垂直直线 | 0 | 无 | 无箭头 | 三列等语义并行 | 否 | n/a |
| `table_row_dividers` | `data_table` | `data_table` | 行切分 | 水平直线 | 0 | 无 | 无箭头 | 四行数据等语义并行 | 否 | n/a |
| `table_header_divider` | `data_table` | `data_table` | 表头分隔 | 水平直线 | 0 | 无 | 无箭头 | 与行切分同风格 | 否 | n/a |

## Spatial constraints

- 舞台按 `1280x720` 计算：主表框固定 `x=80, y=194, w=1120, h=404`。  
- 表格左右外边距固定 `80px / 80px`；标题与来源左对齐到 `x=80`。  
- 最小可读字号 `20px`，不允许再降到 `18px`。

## Element styles

- 背景保持全局统一，不新增深浅底板。  
- 表格只使用细线分割，不加卡片阴影。  
- 文本颜色遵循当前主题深色墨水系，数值列使用棕色强调。

## Assembly order

1. 渲染标题与副标题。  
2. 渲染表格外框与列/行分割线。  
3. 渲染表头，再渲染四行数据。  
4. 渲染来源行。  
5. 在转场期叠加透明度与 sceneFrame 回放策略。

## Acceptance checklist

1. `page_04_data` 锚点帧必须是完整表格，不允许空白或半透明残影。  
2. 前后过渡必须遵守：淡入表格/淡出主图 -> 淡出表格/淡入主图 -> 回放原动画。  
3. 当前页最小字号必须 `>=20px`。  
4. 不新增大色块背景，不破坏全局背景一致性。  
5. 下一页 UE Cook 落位后，表格必须完全退出。

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260417-194605/p4data-f144-r2.png)

- 当前 URL：`http://127.0.0.1:4173/?step=page_04_data`
- 捕获方式：`browser-api page`

## 数学评测（先数学，后读图）

- 画布：`1280x720`  
- 主表框：`x=80, y=194, w=1120, h=404`  
- 表格外边距：左 `80px` / 右 `80px` / 上 `194px` / 下 `122px`  
- 当前页最小字号：`20px`（实测 DOM 文本集合最小值）  
- 线条交叉：`0`  
- 节点覆盖：`0`（表格为单容器网格）  
- 可见超框：`0`

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | 信息集中、左右边界稳定 | 边距对称，重心稳定 | 通过 | `8.6/10` |
| `模块空间结构 Review` | 标题-表格-来源三段清晰 | 网格结构规整、无交叉 | 通过 | `8.7/10` |
| `单节点与单边 Review` | 文本可读、列标题明确 | 最小字号满足 `20px`，线条全直 | 通过 | `8.8/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `data_title` | 先给出“这是数据页” | 否 | 标题 text | 无连续要求 | `10` | 字重和字号均可读 | `8.7` |
| `data_table` | 主体信息承载 | 否 | table grid | 无连续要求 | `10` | 网格规整、留白合理 | `8.9` |
| `data_source` | 给出处，方便追溯 | 否 | 底部 text | 无连续要求 | `10` | 20px 可读性达标 | `8.6` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `table_col_dividers` | 竖线切列，便于横向比较 | 否 | line | 无连续要求 | `10` | 全直线、无偏折 | `9.0` |
| `table_row_dividers` | 横线切行，便于纵向读取 | 否 | line | 无连续要求 | `10` | 全直线、无交叉 | `9.0` |
| `table_header_divider` | 头身分区，先读列再读值 | 否 | line | 无连续要求 | `10` | 分区清晰 | `8.8` |

## 过渡动画剧本与时长审查

### page_04 -> page_04_data

- 帧窗口：`288 -> 348`（`60` frames @ 60fps）
- 总时长：`1.000s`
- 时长评估：`in_range`
- 当前动作（已恢复）：上一页主图淡出、数据表淡入

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 288 | 0.000 | 第四页节点全量可见 | 第四页流程线全量可见 |
| Q1 | 303 | 0.250 | 表格标题开始淡入 | 第四页线条开始降权 |
| Mid | 318 | 0.500 | 表格主体进入可读状态 | 第四页线条继续淡出 |
| Q3 | 333 | 0.750 | 表格接近满显 | 第四页线条接近不可见 |
| End | 348 | 1.000 | 数据页完整落位 | 表格网格线完全接管 |

### page_04_data -> page_05

- 帧窗口：`348 -> 492`（`144` frames @ 60fps）
- 总时长：`2.400s`
- 时长评估：`too_long`（按当前 v1.0 overlay 公式）
- 原始动画保护检查：`pass`

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 348 | 0.000 | 数据表完整保持 | 表格网格线保持 |
| HoldEnd | 366 | 0.300 | 仍停留在数据表 | 表格线条保持 |
| FadeEnd | 372 | 0.400 | 表格完成淡出，第四页主图回显 | 第四页线条回显完成 |
| ReplayMid | 432 | 1.400 | 进入“第四页 -> 第五页”原始回放中段 | Material/Cooked/Binary 主线进入建立阶段 |
| End | 492 | 2.400 | 第五页稳定落位 | `Material -> Cooked -> Binary -> GPU` 完整可读 |

## 审批状态

`已修订：第五页时序已按当前代码对齐（含 2.4s 回放），并记录 too_long 风险`
