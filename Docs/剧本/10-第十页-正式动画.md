# 第 10 页正式动画：`page_10`

> 文件类型：`正式动画页`
> Step / Sketch：`page_10` / `-`
> 上一页：`page_09`
> 下一页：`page_11`
>
> 2026-04-12 更新：第 10-15 页整章节拍整体后移一页。当前文档上半部分已同步为新节拍；下方旧评分仅保留作历史参考，待新一轮 critic/capture 刷新。


## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `6.5/10` | 来源：`三层 Review 总表` 的 `整体布局 Review` 行 |
| Node 分数 | `6.9/10` | 来源：`当前页面评分` 的 `单节点与单边 Review` 综合分（本轮先按同值回填 Node/Edge） |
| Edge 分数 | `6.9/10` | 来源：`当前页面评分` 的 `单节点与单边 Review` 综合分（本轮先按同值回填 Node/Edge） |
| 动画分数 | `8.5/10` | 自动 probe：page_09 -> page_10，durationVerdict=in_range |

## 页面目标

先闪回第 05 页的旧问题，先收缩成问号，再恢复第五页，然后让 `?` 收成 `!`，最后把 `Material / CookedShaderCode` 收成 `ShaderLibrary`，不提前露出后面的电脑 / 手机基础舞台。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | 先回到第 5 页，再回答 `ShaderLibrary` |
| 页面副标题 | 这一页先回退到第五页的旧问题，再把问号弹成感叹号：最后由 `Material / CookedShaderCode` 收成 `ShaderLibrary`，后面的电脑 / 手机舞台整体后移一页。 |
| 当前页核心信息 | 第五页的问题先被正式回答成 `ShaderLibrary`。 |
| 与上一页关系 | 从第 09 页的 SharedCode 索引问题，短暂回拉到第 05 页的资产 cook 问号。 |
| 这一页新增了什么 | `? -> !` 的 answer beat，以及把原 `CookedShaderCode` 节点正式改名成 `ShaderLibrary`。 |
| 这一页删除了什么 | 第 09 页里 SharedCode / LibraryShaderIndex 的硬索引结构。 |
| 这一页保留了什么 | 第 05 页的问题源头，以及 `Vertex -> GPU` 的旧世界骨架。 |
| 需要强调的元素 | `!`、`ShaderLibrary`。 |
| 暂不实现 / 暂不出现 | `Computer`、`Phone`、`.ushaderbytecode`、`.scl.csv`、stable band、`rec.upipelinecache`、`expand`、最终闭环回流。 |

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | `0.00s - 0.18s` | 第 09 页整个旧画面先整体缩小。 | 第 09 页 legacy world | 先明确“上一页整张图被收起来”，不是直接淡出。 |
| 2 | `0.40s - 0.70s` | 中央问号出现，先独立站住。 | `?` | 先把问题抽象成一个明确的提问符号。 |
| 3 | `0.66s - 1.41s` | 第 05 页旧骨架重新恢复出来。 | `Mesh`、`Material`、`CookedShaderCode`、`BinaryShaderCode`、`GPU` | 让观众重新看到旧问题的完整上下文。 |
| 4 | `0.92s - 1.58s` | 问号从中央移回第 05 页原来的提问位置。 | `?`、第 05 页问号锚点 | 这一步是“回到旧问题原位”，不是空中消失。 |
| 5 | `1.36s - 2.02s` | `?` 变成 `!`，同时开始答案节拍。 | `!` | 用更从容的 beat 表明“现在开始回答”。 |
| 6 | `1.63s - 2.11s` | `Material` 汇入右上旧节点，原 `CookedShaderCode` 节点改名成 `ShaderLibrary`。 | `Material`、`CookedShaderCode/ShaderLibrary` | `ShaderLibrary` 不应另起新框，而应复用旧的 `CookedShaderCode` 节点位置。 |

## 当前截图

![Page10 formal capture](../../ignore/page10-transition-fix-capture/page10-transition-fix.png)

- 当前 URL：`http://127.0.0.1:4178/?step=page_10`
- 捕获类型：正式 Remotion 场景静帧
- 评分来源：Art Critic 与 Geometry Critic 双盲看图后人工整理，综合分为简单平均后四舍五入到 0.1

## 当前页面评分

| 维度 | Art Critic | Geometry Critic | 综合 |
| --- | --- | --- | --- |
| `整体布局 Review` | `6.2/10` | `6.8/10` | `6.5/10` |
| `模块空间结构 Review` | `5.8/10` | `6.1/10` | `6.0/10` |
| `单节点与单边 Review` | `6.5/10` | `7.2/10` | `6.9/10` |

## 关键模块剧本与 Review

| 模块 | 中文剧本描述 | Review 观点 | 综合分数 |
| --- | --- | --- | --- |
| 闪回旧问题 | 先回到第 05 页，把“旧问号”重新摆到观众眼前。 | 叙事逻辑成立，但单看静帧时，这一页仍像过渡页而不是完整结论页。 | `6.4/10` |
| `? -> !` 回答拍点 | 用一个短促、明确的 answer beat 把语义从“困惑”切到“答案”。 | 核心设计正确，但还需要更强的舞台标题感去托住这个 beat。 | `6.8/10` |
| `ShaderLibrary` 回答页 | 把第五页里左侧的 `Material` 汇入右上的 `CookedShaderCode` 节点，并把那个旧节点改名成 `ShaderLibrary`。 | 终态不应该再出现一个额外的新框，而是明确复用旧节点。 | `待刷新` |
| 延后展开 | 明确把电脑 / 手机母版延后到第 11 页再出现。 | 当前静帧不再提前泄题，但后续页的连续评分还没刷新。 | `待刷新` |

## 当前问题与下一轮重点

- `page_09 -> page_10` 已从原来的 `108` 帧放慢到 `132` 帧，也就是从约 `1.8s` 放慢到约 `2.2s`。
- 当前页已改成真正的“第五页答案页”，旧的 `.ushaderbytecode` 描述已废弃。
- 当前文档下半部历史评分尚未按新截图刷新；如果继续迭代，下一步应该重新跑 critic，而不是继续沿用旧分数。

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `6.2/10` | `6.8/10` | 闪回与收束关系成立，但终态仍带过渡页气质。 | `6.5/10` |
| `模块空间结构 Review` | `5.8/10` | `6.1/10` | `? -> ! -> ShaderLibrary` 主模块能读通，舞台承托还偏弱。 | `6.0/10` |
| `单节点与单边 Review` | `6.5/10` | `7.2/10` | 关键节点复用逻辑正确，局部边的舞台感仍可强化。 | `6.9/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `flashback-context` | 先回到第 05 页旧问题骨架。 | 承接 page09 后的语义回拉 | 过渡舞台模块 | 连续语义成立，观众能识别“先回旧问题”。 | `6.4/10` | 叙事正确，但单帧易被读成过渡。 | `6.4/10` |
| `answer-beat` | `? -> !` 的回答拍点。 | 问题符号延续并转义为答案 | 符号节点 | 转义关系清楚，答题节拍明确。 | `6.8/10` | 节拍清楚，托底视觉可再加强。 | `6.8/10` |
| `shaderlibrary-node` | `Material` 汇入并复用旧节点为 `ShaderLibrary`。 | 延续第 05 页右上节点语义 | 复用旧节点 carrier | 复用关系正确，不应再额外新建框。 | `6.9/10` | 终态节点语义正确，仍需更强定格感。 | `6.9/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `flashback-edge` | 从 page09 收缩并回拉到第 05 页语义。 | 连续承接上一页 | 过渡边群 | 逻辑连续，承接关系清楚。 | `6.5/10` | 仍偏“过渡性”，线条存在感偏弱。 | `6.5/10` |
| `question-to-answer-edge` | `?` 移位并转成 `!` 的关键边。 | 问题符号连续语义 | 关键动作边 | 连续语义清晰。 | `6.8/10` | 核心动作成立，停帧张力可再提高。 | `6.8/10` |
| `material-to-shaderlibrary` | `Material` 汇入旧节点并完成命名收束。 | 承接第 05 页旧路径 | 收束边 | 复用语义正确。 | `6.9/10` | 边逻辑正确，终态识别度中等。 | `6.9/10` |
