# 第 11 页正式动画：`page_11`

> 文件类型：`正式动画页`
> Step / Sketch：`page_11` / `-`
> 上一页：`page_10`


## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `8.5/10` | 来源：`三层 Review 总表` 的 `整体布局 Review` 行 |
| Node 分数 | `8.2/10` | 来源：`节点剧本与 Review 表` 综合分数列均值 |
| Edge 分数 | `8.3/10` | 来源：`边剧本与 Review 表` 综合分数列均值 |
| 动画分数 | `8.5/10` | 自动 probe：page_10 -> page_11，durationVerdict=in_range |

## Page goal

- 让新章节的双设备舞台正式落位：左侧 `Computer`，右侧 `Phone`，中间只保留 `.ushaderbytecode` 作为 page10 答案的落点。
- 这页不提前讲 `cook` 分叉和回流，重点是把 page10 的答案与 page11 的 runtime 舞台建立连续语义。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | 测试环境收集回路 |
| 页面副标题 | 左边是 `Computer`，右边是 `Phone`，中间保留 `.ushaderbytecode`，先把新章节的基础舞台站稳。 |
| 当前页核心信息 | `ShaderLibrary` 在这一页正式落成 `.ushaderbytecode`，runtime trio 同时进入 `Phone`。 |
| 与上一页关系 | 继承 page10 的 `ShaderLibrary` 答案，并把它落进新的设备母版。 |
| 这一页新增了什么 | `Computer shell`、`Phone shell`、`Phone` 内部 runtime trio 的正式舞台。 |
| 这一页删除了什么 | `? / !` 节拍、page10 的旧问题世界。 |
| 这一页保留了什么 | page10 的答案语义，以及 `VertexData -> GPU -> Pixels` 的 runtime 三段关系。 |
| 需要强调的元素 | `.ushaderbytecode`、`Phone` 内部 runtime trio、左右双设备锚点。 |
| 暂不实现 / 暂不出现 | `cook`、`.scl.csv`、`rec.upipelinecache`、`stablepc.csv`、`stable.upipelinecache`、`expand`。 |

## Node inventory

| id | text | semantic role | visual kind | anchor region | emphasis | alignment constraints | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `computer-shell` | 无文字 | 左侧 source 设备锚点 | device shell | 左下 1/3 | 中 | 与 `phone-shell` 共享页面水平中线 | 无 | 无 |
| `bytecode-node` | `.ushaderbytecode` | page10 答案落点 | artifact pill | 中下带 | 高 | 位于两台设备之间，承担桥位 | 连续承接 page10 `ShaderLibrary` | 必须复用 page10 shared carrier |
| `phone-shell` | 无文字 | 右侧 runtime 设备锚点 | device shell | 右下 1/3 | 中 | 与 `computer-shell` 共享页面水平中线 | 无 | 无 |
| `phone-runtime-vertex` | 无文字 | runtime 输入起点 | icon in phone | `Phone` 上半部 | 中 | 与 `GPU`、`pixels` 共用同一垂直中轴 | 连续承接 page10 `VertexData` | 必须复用 page10 runtime carrier |
| `phone-runtime-gpu` | `GPU` | runtime 核心执行点 | text in phone | `Phone` 中部 | 高 | 与 `vertex`、`pixels` 共用同一垂直中轴 | 连续承接 page10 `GPU` | 必须复用 page10 runtime carrier |
| `phone-runtime-pixels` | 无文字 | runtime 输出终点 | pixel icon in phone | `Phone` 下半部 | 中 | 与 `vertex`、`GPU` 共用同一垂直中轴 | 连续承接 page10 `Pixels` | 必须复用 page10 runtime carrier |

## Edge inventory

| id | from | to | meaning | route grammar | max bends | segment label | arrow style | sibling / symmetry note | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `runtime-edge-1` | `phone-runtime-vertex` | `phone-runtime-gpu` | runtime 输入进入 GPU | 竖直向下 | 0 | 无 | single arrow | 与 `runtime-edge-2` 共用同一垂直主轴 | 连续承接 page10 `VertexData -> GPU` | 必须复用 page10 runtime edge carrier |
| `runtime-edge-2` | `phone-runtime-gpu` | `phone-runtime-pixels` | GPU 输出到像素结果 | 竖直向下 | 0 | 无 | single arrow | 与 `runtime-edge-1` 共用同一垂直主轴 | 连续承接 page10 `GPU -> Pixels` | 必须复用 page10 runtime edge carrier |

## Spatial constraints

- `Computer` 与 `Phone` 的视觉中心线必须彼此对齐，并共同落在页面水平中线。
- `.ushaderbytecode` 位于两台设备之间，不能读成居中标题条。
- `Phone` 内部 trio 必须共享一条清晰的垂直主轴。
- settled frame 不出现 page12 之后的任何新增流程节点。

## Element styles

- 设备外壳使用中性浅底与低饱和描边，本页不显示 `Computer / Phone` 文字标签。
- `.ushaderbytecode` 使用更强的强调描边，承担答案卡片角色。
- 连续语义 actor 优先复用同一个 render carrier，禁止靠 duplicate fade 伪装 continuity。

## Assembly order

1. 先摆出左右两个设备锚点。
2. 把 page10 的 `ShaderLibrary` 变形成 `.ushaderbytecode`。
3. 把 page10 的 runtime trio 收束进 `Phone` 内部的垂直主轴。
4. 保持 settled frame 只展示双设备舞台与 `.ushaderbytecode`。

## Acceptance checklist

1. `Computer` 与 `Phone` 的共同中心线必须落在页面水平中线。
2. `.ushaderbytecode` 必须读成 page10 `ShaderLibrary` 的同元素延续。
3. `Phone` 内部 trio 必须读成 page10 `VertexData -> GPU -> Pixels` 的同元素落位。
4. settled frame 不出现 `cook`、`.scl.csv`、`rec`、stable band、`expand`。
5. `Phone` 内部两条箭头保持同轴、稳定、无漂移感。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | `page10 -> page11` 前半段 | page10 的旧世界退出，新双设备母版开始落位。 | `Computer shell`、`Phone shell` | 先让新章节的舞台出现。 |
| 2 | `page10 -> page11` 中段 | `ShaderLibrary` 变形成 `.ushaderbytecode` 并向桥位落下。 | `bytecode-node` | 必须是同 carrier 变化，不是新卡片替换。 |
| 3 | `page10 -> page11` 后半段 | runtime trio 进入 `Phone` 内部并在 settled frame 稳住。 | runtime trio | 进入 `Phone` 的过程必须可追踪，不应断层。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | `Computer shell`、`Phone shell` |
| 需要复用的元素 | page10 `ShaderLibrary` carrier、page10 runtime travelling carriers |
| 当前共享元素 / 连续语义 carrier | `ShaderLibrary -> .ushaderbytecode` 与 `VertexData/GPU/Pixels -> Phone runtime trio` 共用 carrier |
| 是否需要镜头推进 / 缩放 | 不需要镜头推进，允许局部落位与轻微 scale settle |
| 是否需要逐步显隐 | 允许设备母版显现，但连续语义 actor 不允许靠 duplicate fade 解决 |
| 风险点 | `.ushaderbytecode` 的桥位仍偏低，与 `Phone` 主轴之间的收束感还不够强 |

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/formal-pages-11-15/page11-formal-browser.png)

- 当前 URL：`http://127.0.0.1:4178/?step=page_11&debugFrame=546&surface=stage`
- 捕获方式：`browser-api stage`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/formal-pages-11-15/page11-formal-browser.txt`

## Blockers

- 无硬性 blocker。

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `8.5/10` | `8.6/10` | 左右设备与页面中线对齐明确，停帧很稳；主要问题是 `.ushaderbytecode` 偏低。 | `8.5/10` |
| `模块空间结构 Review` | `7.9/10` | `7.8/10` | `Computer / bytecode / Phone` 三段式成立，但 bytecode 与 Phone 内主轴的桥接仍偏“并置”。 | `7.9/10` |
| `单节点与单边 Review` | `8.7/10` | `8.2/10` | 节点与边的语言统一，Phone 内竖向边已不再像装饰，但箭头端点仍可再贴近。 | `8.5/10` |

## Top 3 fixes

1. 抬高 `.ushaderbytecode`，把它拉回与左右设备更接近的水平叙事带。
2. 让 bytecode 与 Phone 内入口建立更明确的对位关系，减少“桥牌感”。
3. 轻微加长 Phone 内两段竖向箭头并贴近上下对象，增强机械确定性。

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `computer-shell` | 左侧新的 source 设备母版。 | 无 | 独立 `Computer` 设备壳体 | 不涉及 continuity，主要承担左侧锚点职责。 | `8.6/10` | 上下留白对称，轮廓稳定，仍略轻于右侧 `Phone`。 | `8.4/10` |
| `bytecode-node` | page10 答案在这一页的正式落点。 | 连续承接 page10 `ShaderLibrary` | page10 shared bytecode carrier | continuity 成立，也能读到“同元素换名”。 | `8.2/10` | 桥位清晰，但位置仍略低。 | `7.6/10` |
| `phone-shell` | 右侧新的 runtime 母版。 | 无 | 独立 `Phone` 设备壳体 | 不涉及 continuity，作为接收端母版已站稳。 | `8.6/10` | 内部纵轴清楚，但视觉重量偏重。 | `8.3/10` |
| `phone-runtime-vertex` | page10 `VertexData` 在 `Phone` 内的输入起点。 | 连续承接 page10 `VertexData` | page10 runtime carrier | continuity 成立，位置已进入 `Phone` 内主轴。 | `7.9/10` | 图标干净，但与外部桥位关系仍偏弱。 | `8.2/10` |
| `phone-runtime-gpu` | `Phone` 中部主焦点。 | 连续承接 page10 `GPU` | page10 runtime carrier | continuity 成立，焦点延续正确。 | `8.1/10` | `GPU` 焦点足够稳，但与 `.ushaderbytecode` 缺少更强承接。 | `8.4/10` |
| `phone-runtime-pixels` | page10 `Pixels` 在 `Phone` 内的输出终点。 | 连续承接 page10 `Pixels` | page10 runtime carrier | continuity 成立，终点身份能读出来。 | `7.9/10` | 图形语言稳定，终点感较前一页更清楚。 | `8.1/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `runtime-edge-1` | `VertexData` 在 `Phone` 内部继续流向 `GPU`。 | 连续承接 page10 `VertexData -> GPU` | page10 runtime edge carrier | continuity 成立，能认出是延续下来的流程边。 | `8.1/10` | 直线、同轴、无漂移；端点还能再更贴近对象。 | `8.3/10` |
| `runtime-edge-2` | `GPU` 在 `Phone` 内部继续输出到像素结果。 | 连续承接 page10 `GPU -> Pixels` | page10 runtime edge carrier | continuity 成立，不再掉成装饰线。 | `8.1/10` | 竖向节奏稳定，终点锚定已明确。 | `8.3/10` |

## 审批状态

`正式页已完成一轮 browser-api capture + blind critic 复核。当前版本无硬性 blocker；剩余问题集中在 .ushaderbytecode 桥位偏低，以及它与 Phone 主轴之间的收束感仍偏弱。`
