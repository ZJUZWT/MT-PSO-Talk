# 第 12 页正式动画：`page_12`

> 文件类型：`正式动画页`
> Step / Sketch：`page_12` / `-`
> 上一页：`page_11`


## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `8.1/10` | 来源：`三层 Review 总表` 的 `整体布局 Review` 行 |
| Node 分数 | `7.8/10` | 来源：`节点剧本与 Review 表` 综合分数列均值 |
| Edge 分数 | `7.8/10` | 来源：`边剧本与 Review 表` 综合分数列均值 |
| 动画分数 | `5.0/10` | 自动 probe：page_11 -> page_12，durationVerdict=too_short |

## Page goal

- 在 page11 的双设备母版之上，第一次正式讲清 `cook` 分叉与两份 UE5 输出。
- 这一页只讲 `Computer -> cook -> A -> (.ushaderbytecode / .scl.csv)`，先不让任何文件落到 `Phone`。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | 首次 Cook 分出两份 UE5 输出 |
| 页面副标题 | 舞台已经站稳之后，这一页才把首次 `cook` 讲清：电脑侧分叉，正式分出 `.ushaderbytecode` 和 `.scl.csv`。 |
| 当前页核心信息 | 电脑侧首次 `cook` 会同时产生两份 UE5 格式产物。 |
| 与上一页关系 | 保留 page11 的基础母版，只让电脑侧长出 `cook` 分叉。 |
| 这一页新增了什么 | `cook` 主线、split junction `A`、`.scl.csv`。 |
| 这一页删除了什么 | 无。 |
| 这一页保留了什么 | `Computer`、`Phone`、`.ushaderbytecode`、Phone 内部 runtime trio。 |
| 需要强调的元素 | `Computer`、`cook`、junction `A`、`.scl.csv`。 |
| 暂不实现 / 暂不出现 | `.ushaderbytecode -> Phone`、`rec.upipelinecache`、stable band、`expand`。 |

## Node inventory

| id | text | semantic role | visual kind | anchor region | emphasis | alignment constraints | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `computer-shell` | `Computer` | 左侧 source 设备 | device shell | 左下 1/3 | 高 | 与 `phone-shell` 继续共享基础舞台 | 连续承接 page11 `computer-shell` | 必须复用同一设备 carrier，允许 label 打开 |
| `split-a` | 无文字 | `cook` 分叉点 | junction circle | 中左 | 高 | 位于 `Computer` 右侧、两份输出左侧 | 无 | 新建 junction carrier |
| `scl-node` | `.scl.csv` | UE5 metadata 输出 | artifact pill | 中部上带 | 高 | 与 `bytecode-node` 形成上下输出簇 | 无 | 新建 artifact carrier |
| `bytecode-node` | `.ushaderbytecode` | bytecode 输出 | artifact pill | 中部下带 | 高 | 与 `scl-node` 形成上下输出簇 | 连续承接 page11 `bytecode-node` | 必须复用 page11 bytecode carrier |
| `phone-shell` | `Phone` | 右侧 runtime 设备 | device shell | 右下 1/3 | 中 | 继续与 `Computer` 构成左右双锚点 | 连续承接 page11 `phone-shell` | 必须复用同一设备 carrier，允许 label 打开 |
| `phone-runtime-vertex` | 无文字 | runtime 输入起点 | icon in phone | `Phone` 上半部 | 中 | 与 `GPU`、`pixels` 共用垂直中轴 | 连续承接 page11 `phone-runtime-vertex` | 必须复用同一 runtime carrier |
| `phone-runtime-gpu` | `GPU` | runtime 核心执行点 | text in phone | `Phone` 中部 | 中 | 与 `vertex`、`pixels` 共用垂直中轴 | 连续承接 page11 `phone-runtime-gpu` | 必须复用同一 runtime carrier |
| `phone-runtime-pixels` | 无文字 | runtime 输出终点 | pixel icon in phone | `Phone` 下半部 | 中 | 与 `vertex`、`GPU` 共用垂直中轴 | 连续承接 page11 `phone-runtime-pixels` | 必须复用同一 runtime carrier |

## Edge inventory

| id | from | to | meaning | route grammar | max bends | segment label | arrow style | sibling / symmetry note | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `computer-to-a` | `computer-shell` | `split-a` | 首次 `cook` 主干从电脑发出 | rounded orthogonal | 2 | `cook` | single arrow | 与两条分叉边共同组成 Y 形主因果 | 无 | 新建 edge carrier |
| `a-to-scl` | `split-a` | `scl-node` | metadata 输出支线 | rounded orthogonal | 1 | `UE5 formats` | single arrow | 与 `a-to-bytecode` 形成同源分叉 | 无 | 新建 edge carrier |
| `a-to-bytecode` | `split-a` | `bytecode-node` | bytecode 输出支线 | rounded orthogonal | 1 | 无 | single arrow | 与 `a-to-scl` 形成同源分叉 | 无 | 新建 edge carrier |
| `runtime-edge-1` | `phone-runtime-vertex` | `phone-runtime-gpu` | runtime 内部输入链路 | 竖直向下 | 0 | 无 | single arrow | 作为背景保留 | 连续承接 page11 `runtime-edge-1` | 必须复用同一 runtime edge carrier |
| `runtime-edge-2` | `phone-runtime-gpu` | `phone-runtime-pixels` | runtime 内部输出链路 | 竖直向下 | 0 | 无 | single arrow | 作为背景保留 | 连续承接 page11 `runtime-edge-2` | 必须复用同一 runtime edge carrier |

## Spatial constraints

- `Computer` 保持左侧锚点，`Phone` 保持右侧锚点，中部让出给 cook split。
- `split-a`、`.scl.csv`、`.ushaderbytecode` 必须组成一眼可读的分叉簇。
- `.scl.csv` 位于 `.ushaderbytecode` 上方，形成“上 metadata / 下 bytecode”的双产物关系。
- `Phone` 本页仍然保留，但不能让任何外部文件线提前落进它。

## Element styles

- `cook` 和 `UE5 formats` 都是线段标签，不是独立节点。
- `Computer` 与 `Phone` 本页可以打开设备标签，但不能压住内部结构。
- 输出簇继续沿用浅底 + 橙色强调描边的 artifact 语言。

## Assembly order

1. 保持 page11 的双设备母版和中部 bytecode。
2. 从 `Computer` 拉出 `cook` 主干并放下 `split-a`。
3. 从 `split-a` 分出 `.scl.csv` 与 `.ushaderbytecode` 两支。
4. 保持 `Phone` 只作为后续 receiver plane，不接入外部文件。

## Acceptance checklist

1. 本页必须一眼读出 `Computer -> cook -> A -> (.scl.csv / .ushaderbytecode)`。
2. `Phone` 仍在画面中，但不应提前出现 `.ushaderbytecode -> Phone` 落线。
3. `.scl.csv` 与 `.ushaderbytecode` 必须读成同一次 cook 的两份产物。
4. `rec.upipelinecache`、`stablepc.csv`、`stable.upipelinecache`、`expand` 不能提前出现。
5. runtime trio 只作为保留背景，不能抢走 cook split 的主语义。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | `page11 -> page12` 前半段 | `Computer` 标签打开，cook 主干从电脑侧长出。 | `computer-shell`、`computer-to-a` | 先讲 source 端开始工作。 |
| 2 | `page11 -> page12` 中段 | junction `A` 与分叉两支出现。 | `split-a`、`a-to-scl`、`a-to-bytecode` | 把“同一次 cook 分出两份产物”讲清。 |
| 3 | `page11 -> page12` 后半段 | `.scl.csv` 稳住，上下双产物带成形。 | `scl-node`、`bytecode-node` | 本页不提前让任何线接进 `Phone`。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | `split-a`、`.scl.csv` |
| 需要复用的元素 | page11 `computer-shell`、`phone-shell`、`.ushaderbytecode`、runtime trio |
| 当前共享元素 / 连续语义 carrier | 设备壳体与 runtime trio 沿用上一页 carrier；`.ushaderbytecode` 沿用上一页 bytecode carrier |
| 是否需要镜头推进 / 缩放 | 不需要镜头推进，强调通过线条与节点出现完成 |
| 是否需要逐步显隐 | 允许 `cook` 主线和输出簇逐步长出 |
| 风险点 | `Phone` 标题与 `RUNTIME` 容易叠压；cook 分叉一旦不够规整，会让中部舞台显忙 |

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/formal-pages-11-15/page12-formal-browser.png)

- 当前 URL：`http://127.0.0.1:4178/?step=page_12&debugFrame=600&surface=stage`
- 捕获方式：`browser-api stage`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/formal-pages-11-15/page12-formal-browser.txt`

## Blockers

- `Phone` 顶部的 `Phone` 与灰色 `RUNTIME` 叠在同一高度带，存在轻微可读性风险。

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `8.0/10` | `8.1/10` | 左侧 `cook` 主线清楚，但中部输出簇偏左，右侧 `Phone` 更像背景。 | `8.1/10` |
| `模块空间结构 Review` | `8.4/10` | `8.4/10` | `Computer -> cook -> 分叉 -> 两个 UE5 输出` 的结构一眼可读，是本页最强部分。 | `8.4/10` |
| `单节点与单边 Review` | `8.1/10` | `7.7/10` | 分叉逻辑成立，但 `cook` 主干弯折稍大，分叉两支的几何对称性还不够硬。 | `7.9/10` |

## Top 3 fixes

1. 处理 `Phone / RUNTIME` 顶部叠压，让设备标题与内部标签分层更清楚。
2. 收紧 `cook` 主线弯折，让从 `Computer` 出来的主干更像主梁而不是装饰曲线。
3. 强化 `split-a -> (.scl.csv / .ushaderbytecode)` 的几何对称，提升停帧可讲性。

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `computer-shell` | source 端第一次打开 `Computer` 标签。 | 连续承接 page11 `computer-shell` | page11 device carrier | 同一设备 actor 延续正确，只增加 label。 | `8.5/10` | 位置稳定，但本页重心仍主要在其右侧的 cook split。 | `8.2/10` |
| `split-a` | 第一次出现的 cook 分叉点。 | 无 | 新建 junction carrier | 新节点语义明确，是本页主枢纽。 | `8.0/10` | 分叉成立，但与双产物的对称性还可更强。 | `7.8/10` |
| `scl-node` | metadata 产物，暂不落进 `Phone`。 | 无 | 新建 artifact carrier | 新节点成立，功能语义清晰。 | `8.0/10` | 标签稳定，但与 `UE5 formats` 标注略近。 | `7.8/10` |
| `bytecode-node` | 从 page11 延续下来的 bytecode 产物。 | 连续承接 page11 `.ushaderbytecode` | page11 bytecode carrier | continuity 成立，当前页语义转成“cook 输出”。 | `8.3/10` | 在输出簇里站位明确，是两产物中的下支主角。 | `8.0/10` |
| `phone-shell` | 右侧 runtime 设备，本页仍只做 receiver plane。 | 连续承接 page11 `phone-shell` | page11 device carrier | continuity 成立，允许 label 打开。 | `8.2/10` | `Phone` 与 `RUNTIME` 顶部叠压，影响第一眼清晰度。 | `7.2/10` |
| `phone-runtime-vertex` | 继续作为后续接收的预留入口。 | 连续承接 page11 `phone-runtime-vertex` | page11 runtime carrier | continuity 成立。 | `8.0/10` | 作为背景保留是合理的。 | `7.9/10` |
| `phone-runtime-gpu` | 继续作为后续接收的核心落点。 | 连续承接 page11 `phone-runtime-gpu` | page11 runtime carrier | continuity 成立。 | `8.0/10` | 焦点被成功退到背景。 | `8.0/10` |
| `phone-runtime-pixels` | 继续作为后续输出终点。 | 连续承接 page11 `phone-runtime-pixels` | page11 runtime carrier | continuity 成立。 | `8.0/10` | 本页存在感合适，没有抢戏。 | `7.9/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `computer-to-a` | `cook` 主干从 `Computer` 发出。 | 无 | 新建 cook edge carrier | 新边语义明确，是本页主因果起点。 | `8.0/10` | 弯折略大，主干感还可更硬。 | `7.5/10` |
| `a-to-scl` | 从分叉点指向 `.scl.csv` 的 metadata 支线。 | 无 | 新建 split edge carrier | 与 `a-to-bytecode` 组成同源分叉。 | `8.0/10` | 出射角与下支还不够对称。 | `7.7/10` |
| `a-to-bytecode` | 从分叉点指向 `.ushaderbytecode` 的下支。 | 无 | 新建 split edge carrier | 与 `a-to-scl` 一起讲清双产物。 | `8.0/10` | 路由较稳，是两支里更清楚的一条。 | `8.0/10` |
| `runtime-edge-1` | `Phone` 内部输入链继续保留。 | 连续承接 page11 `runtime-edge-1` | page11 runtime edge carrier | continuity 成立。 | `8.0/10` | 作为背景保留合适。 | `8.0/10` |
| `runtime-edge-2` | `Phone` 内部输出链继续保留。 | 连续承接 page11 `runtime-edge-2` | page11 runtime edge carrier | continuity 成立。 | `8.0/10` | 作为背景保留合适。 | `8.0/10` |

## 审批状态

`正式页已完成 browser-api capture + blind critic 复核。当前页的主逻辑与主结构已经成立，真正需要先修的是 Phone 顶部文字叠压，以及 cook 分叉的几何规整度。`
