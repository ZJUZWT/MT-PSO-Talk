# 第 13 页正式动画：`page_13`

> 文件类型：`正式动画页`
> Step / Sketch：`page_13` / `-`
> 上一页：`page_12`


## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `8.0/10` | 来源：`三层 Review 总表` 的 `整体布局 Review` 行 |
| Node 分数 | `7.8/10` | 来源：`节点剧本与 Review 表` 综合分数列均值 |
| Edge 分数 | `7.6/10` | 来源：`边剧本与 Review 表` 综合分数列均值 |
| 动画分数 | `5.0/10` | 自动 probe：page_12 -> page_13，durationVerdict=too_short |

## Page goal

- 在保留 `cook` split 的基础上，第一次把 `.ushaderbytecode` 真正接进 `Phone`。
- 这一页的主信息不是“两份文件都存在”，而是“最先进入 runtime 的是 bytecode 这一支”。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `.ushaderbytecode` 先进入 `Phone` |
| 页面副标题 | 先保留 cook 分叉和两份输出，再让 `.ushaderbytecode` 第一次真正落进 `Phone`，把 runtime landing 讲清。 |
| 当前页核心信息 | 最先直接喂给包体运行的是 bytecode 这一支。 |
| 与上一页关系 | 保留 page12 的 `cook` split，只新增 `.ushaderbytecode -> Phone`。 |
| 这一页新增了什么 | `.ushaderbytecode -> Phone` 外部 landing 边。 |
| 这一页删除了什么 | 无。 |
| 这一页保留了什么 | `Computer`、`cook`、junction `A`、`.scl.csv`、Phone 内部 runtime trio。 |
| 需要强调的元素 | `.ushaderbytecode -> Phone`、`Phone`。 |
| 暂不实现 / 暂不出现 | `rec.upipelinecache`、`stablepc.csv`、`stable.upipelinecache`、`expand`。 |

## Node inventory

| id | text | semantic role | visual kind | anchor region | emphasis | alignment constraints | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `computer-shell` | `Computer` | 左侧 source 设备 | device shell | 左下 1/3 | 中 | 与 `Phone` 继续构成左右锚点 | 连续承接 page12 `computer-shell` | 必须复用同一设备 carrier |
| `split-a` | 无文字 | cook 分叉点 | junction circle | 中左 | 中 | 继续位于 `Computer` 与输出簇之间 | 连续承接 page12 `split-a` | 必须复用同一 junction carrier |
| `scl-node` | `.scl.csv` | metadata 输出 | artifact pill | 中上 | 中 | 继续位于 `.ushaderbytecode` 上方 | 连续承接 page12 `scl-node` | 必须复用同一 artifact carrier |
| `bytecode-node` | `.ushaderbytecode` | runtime 输入来源 | artifact pill | 中下 | 高 | 与新 landing 边构成当前页主轴 | 连续承接 page12 `bytecode-node` | 必须复用同一 bytecode carrier |
| `phone-shell` | `Phone` | runtime 接收端 | device shell | 右下 1/3 | 高 | 与新 landing 边形成明确接收关系 | 连续承接 page12 `phone-shell` | 必须复用同一设备 carrier |
| `phone-runtime-vertex` | 无文字 | runtime 输入入口 | icon in phone | `Phone` 上半部 | 中 | 与 `GPU`、`pixels` 共用垂直中轴 | 连续承接 page12 `phone-runtime-vertex` | 必须复用同一 runtime carrier |
| `phone-runtime-gpu` | `GPU` | runtime 核心执行点 | text in phone | `Phone` 中部 | 高 | 与 `bytecode-node` 的 landing 关系应被读出 | 连续承接 page12 `phone-runtime-gpu` | 必须复用同一 runtime carrier |
| `phone-runtime-pixels` | 无文字 | runtime 输出终点 | pixel icon in phone | `Phone` 下半部 | 中 | 与 `vertex`、`GPU` 共用垂直中轴 | 连续承接 page12 `phone-runtime-pixels` | 必须复用同一 runtime carrier |

## Edge inventory

| id | from | to | meaning | route grammar | max bends | segment label | arrow style | sibling / symmetry note | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `computer-to-a` | `computer-shell` | `split-a` | `cook` 主干 | rounded orthogonal | 2 | `cook` | single arrow | 与 page12 保持同一来源 | 连续承接 page12 `computer-to-a` | 必须复用同一 edge carrier |
| `a-to-scl` | `split-a` | `scl-node` | metadata 输出支线 | rounded orthogonal | 1 | `UE5 formats` | single arrow | 保留为背景支线 | 连续承接 page12 `a-to-scl` | 必须复用同一 edge carrier |
| `a-to-bytecode` | `split-a` | `bytecode-node` | bytecode 输出支线 | rounded orthogonal | 1 | 无 | single arrow | 作为新 landing 边的上游 | 连续承接 page12 `a-to-bytecode` | 必须复用同一 edge carrier |
| `bytecode-to-phone` | `bytecode-node` | `phone-shell` | bytecode 首次进入 runtime | rounded orthogonal | 2 | 无 | single arrow | 当前页主线 | 无 | 新建 landing edge carrier |
| `runtime-edge-1` | `phone-runtime-vertex` | `phone-runtime-gpu` | runtime 内部输入链路 | 竖直向下 | 0 | 无 | single arrow | 作为 receiver plane 内部骨架 | 连续承接 page12 `runtime-edge-1` | 必须复用同一 runtime edge carrier |
| `runtime-edge-2` | `phone-runtime-gpu` | `phone-runtime-pixels` | runtime 内部输出链路 | 竖直向下 | 0 | 无 | single arrow | 作为 receiver plane 内部骨架 | 连续承接 page12 `runtime-edge-2` | 必须复用同一 runtime edge carrier |

## Spatial constraints

- `bytecode-node -> Phone` 必须成为本页第一眼能读到的主关系。
- `cook` split 保留，但其优先级应低于新的 landing 边。
- `Phone` 仍位于右侧 receiver plane，landing 点必须明确靠向其左边界。
- `rec`、stable band 与 `expand` 不能提前出现。

## Element styles

- `bytecode-to-phone` 继续使用主流程橙色边，但要比背景支线更有存在感。
- `Phone` 本页可以打开标签，但不能压住 `RUNTIME` 和内部链路。
- `.scl.csv` 保持为背景支线产物，不应喧宾夺主。

## Assembly order

1. 保留 page12 的 `cook` split 和双产物簇。
2. 强化 `Phone` 作为 receiver plane 的存在。
3. 从 `.ushaderbytecode` 长出 landing 边接入 `Phone`。
4. 保持 `rec` 和 stable 系统继续隐藏。

## Acceptance checklist

1. 本页必须一眼读出“.ushaderbytecode 先进入 Phone”。
2. `cook` split 仍然存在，但只能当作这条 landing 边的上游背景。
3. `.scl.csv` 继续保留，且不能提前接到 `Phone`。
4. `rec.upipelinecache`、stable band、`expand` 仍然不能出现。
5. `bytecode-to-phone` 的落点必须明确，不应读成“只是指向附近”。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | `page12 -> page13` 前半段 | 保留 cook split，提升 `Phone` 的存在感。 | `phone-shell`、existing split | 先把“接收端”抬出来。 |
| 2 | `page12 -> page13` 中段 | 从 `.ushaderbytecode` 拉出 landing 边并接到 `Phone`。 | `bytecode-to-phone` | 讲清最先进入 runtime 的是 bytecode。 |
| 3 | `page12 -> page13` 后半段 | `Phone` 稳住为当前页主角。 | `phone-shell`、runtime trio | 本页不提前讲回流。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | 无新增节点，只新增 `bytecode-to-phone` |
| 需要复用的元素 | page12 的全部主舞台节点 |
| 当前共享元素 / 连续语义 carrier | cook split、双产物簇、Phone 内部 trio 全部延续上一页 carrier |
| 是否需要镜头推进 / 缩放 | 不需要镜头推进，允许通过强调权重把 Phone 拉成主角 |
| 是否需要逐步显隐 | 允许 `bytecode-to-phone` 逐步长出 |
| 风险点 | `bytecode-to-phone` 的最后一段入射过硬时，会让“进入 runtime”的语义显得像绕行管线 |

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/formal-pages-11-15/page13-formal-browser.png)

- 当前 URL：`http://127.0.0.1:4178/?step=page_13&debugFrame=654&surface=stage`
- 捕获方式：`browser-api stage`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/formal-pages-11-15/page13-formal-browser.txt`

## Blockers

- 无硬性 blocker。

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `8.2/10` | `7.8/10` | 右侧 `Phone` 被强调后重心更集中，但中部到右侧的长 landing 边拉出了较强张力。 | `8.0/10` |
| `模块空间结构 Review` | `8.3/10` | `7.9/10` | “cook split 仍在 + bytecode 进入 runtime” 的顺序成立，但两条主线仍有轻微争抢。 | `8.1/10` |
| `单节点与单边 Review` | `7.9/10` | `7.3/10` | `bytecode-to-phone` 的长路径能读通，但最后一段的折角偏硬，落点不够果断。 | `7.6/10` |

## Top 3 fixes

1. 让 `.ushaderbytecode -> Phone` 的落点更像明确 dock，而不是指向 `Phone` 边界附近。
2. 继续处理 `Phone / RUNTIME` 顶部的叠压，让右侧 receiver plane 更安静。
3. 把 `cook` split 的存在感再往后退一点，避免它与 landing 主线争第一视线。

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `computer-shell` | 左侧 source 设备继续保留。 | 连续承接 page12 `computer-shell` | page12 device carrier | continuity 成立，成功退为背景来源。 | `8.2/10` | 稳定，但已不是本页主角。 | `7.9/10` |
| `split-a` | 继续保留 cook 分叉点。 | 连续承接 page12 `split-a` | page12 junction carrier | continuity 成立。 | `8.0/10` | 作为背景分叉存在合理，但优先级还能更低。 | `7.7/10` |
| `scl-node` | 保留 metadata 输出，暂不落进 `Phone`。 | 连续承接 page12 `scl-node` | page12 artifact carrier | continuity 成立。 | `8.0/10` | 节点稳定，但本页应当更像背景支线。 | `7.7/10` |
| `bytecode-node` | 本页真正的起点主角。 | 连续承接 page12 `bytecode-node` | page12 bytecode carrier | continuity 成立，当前页主语义转成“进入 runtime”。 | `8.4/10` | 节点本身稳定，向右 landing 的动机明确。 | `8.0/10` |
| `phone-shell` | runtime 接收端，本页主角。 | 连续承接 page12 `phone-shell` | page12 device carrier | continuity 成立，允许 label 打开。 | `8.2/10` | `Phone / RUNTIME` 叠压仍然存在。 | `7.2/10` |
| `phone-runtime-vertex` | 接收后的第一层内部入口。 | 连续承接 page12 `phone-runtime-vertex` | page12 runtime carrier | continuity 成立。 | `8.0/10` | 作为背景骨架合理。 | `7.9/10` |
| `phone-runtime-gpu` | `Phone` 内部的视觉焦点。 | 连续承接 page12 `phone-runtime-gpu` | page12 runtime carrier | continuity 成立。 | `8.1/10` | 当前页焦点仍稳。 | `8.1/10` |
| `phone-runtime-pixels` | runtime 输出终点。 | 连续承接 page12 `phone-runtime-pixels` | page12 runtime carrier | continuity 成立。 | `8.0/10` | 终点感存在，但不应抢外部 landing 主线。 | `7.8/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `computer-to-a` | `cook` 主干继续保留。 | 连续承接 page12 `computer-to-a` | page12 edge carrier | continuity 成立。 | `8.0/10` | 稳，但应更像背景。 | `7.6/10` |
| `a-to-scl` | metadata 背景支线继续保留。 | 连续承接 page12 `a-to-scl` | page12 edge carrier | continuity 成立。 | `8.0/10` | 出射路径仍略显随手。 | `7.5/10` |
| `a-to-bytecode` | 继续作为 landing 主线的上游。 | 连续承接 page12 `a-to-bytecode` | page12 edge carrier | continuity 成立。 | `8.0/10` | 稳，是中部最清楚的背景边。 | `7.8/10` |
| `bytecode-to-phone` | `.ushaderbytecode` 第一次真正接入 `Phone`。 | 无 | 新建 landing edge carrier | 新边语义明确，是当前页最重要的一条关系。 | `8.2/10` | 路由偏长、最后一段折角偏硬，落点不够“入手机”。 | `7.1/10` |
| `runtime-edge-1` | `Phone` 内部输入链继续作为 receiver plane 骨架。 | 连续承接 page12 `runtime-edge-1` | page12 runtime edge carrier | continuity 成立。 | `8.0/10` | 退到背景后读感稳定。 | `7.9/10` |
| `runtime-edge-2` | `Phone` 内部输出链继续作为 receiver plane 骨架。 | 连续承接 page12 `runtime-edge-2` | page12 runtime edge carrier | continuity 成立。 | `8.0/10` | 退到背景后读感稳定。 | `7.9/10` |

## 审批状态

`正式页已完成 browser-api capture + blind critic 复核。当前页逻辑清楚，没有硬性 blocker；最值得继续修的是 bytecode-to-phone 的最终落点，以及右侧 Phone 标题区的拥挤。`
