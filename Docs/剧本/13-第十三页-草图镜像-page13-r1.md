# 第 13 页草图镜像：`page13-r1`

> 2026-04-12 更新：第 10-15 页整章节拍整体后移一页。当前页语义已改成“手机先吃进 `.ushaderbytecode`”；下方旧评分仅保留作历史参考。

## 页面目标

在保持 `Phone` 为主角的前提下，先只新增 `.ushaderbytecode -> Phone` 这一条真正落地到包体运行时的路径。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | 手机先吃进 `.ushaderbytecode` |
| 页面副标题 | 两份文件关系讲清之后，这一页才第一次把 `.ushaderbytecode` 真正接到手机运行时。 |
| 当前页核心信息 | 最先直接喂给包体运行的是 bytecode 这一支。 |
| 与上一页关系 | 保留 page12 的 `cook` split，只让下支真正接入 `Phone`。 |
| 这一页新增了什么 | `.ushaderbytecode -> Phone` 长水平 + 斜入路径。 |
| 这一页删除了什么 | 无。 |
| 这一页保留了什么 | `cook` split、`.scl.csv`、`Computer`。 |
| 需要强调的元素 | `Phone`、`.ushaderbytecode -> Phone`。 |
| 暂不实现 / 暂不出现 | `Phone -> rec` 回流、stable band、`expand`、最终 stable 回到手机。 |

## 当前截图

![Page13 sketch capture](../../ignore/page13-r1-capture/page13-r1.png)

- 草图 URL：`http://127.0.0.1:4178/?mode=sketch&sketch=page13-r1`
- Review URL：`http://127.0.0.1:4178/?mode=sketch&sketch=page13-r1&review=1`
- 当前 capture 来源：浏览器 API stage 导出

## 最新事实层

- `Receiver plane`：`Phone return arc`
- `Primary line`：`Phone -> rec.upipelinecache -> Computer`
- `Mechanical score`：`6.3 / 10`
- `Verdict`：`Clean the primary route before critic pass`
- `Node piercings / bad endpoint anchors / overflow`：`0 / 0 / 0`
- `Minimum node gap`：`41px`
- `Minimum rendered font`：`28px (~21pt)`
- `Directional margins`：`T 186 / R 30 / B 117 / L 112 px`
- `Worst anchor offsets`：`computer-to-a: from Computer right +11px`、`bytecode-to-phone: to Phone left +10px`
- `Top fixes`：继续拉直主路线；最紧节点栈仍低于舒适线；外边距还偏小。

## 浏览器文字探针

- 浏览器实测最小内部 padding：`8.0px`
- `Phone`：`tightest 9.4px`
- `rec.upipelinecache`：`tightest 8.4px`
- `.ushaderbytecode`：`tightest 8.0px`
- `.scl.csv`：`tightest 8.0px`

## 当前页面评分

| 维度 | Art Critic | Geometry Critic | 综合 |
| --- | --- | --- | --- |
| `整体布局 Review` | `7.6/10` | `8.0/10` | `7.8/10` |
| `模块空间结构 Review` | `7.4/10` | `7.6/10` | `7.5/10` |
| `单节点与单边 Review` | `7.2/10` | `7.5/10` | `7.4/10` |

## 关键模块剧本与 Review

| 模块 | 中文剧本描述 | Review 观点 | 综合分数 |
| --- | --- | --- | --- |
| 顶部回流外环 | 用一条大外环把 `Phone` 与 `Computer` 真正绑进同一个闭环语义。 | 一旦这条边出现，整页终于有“成页”感。 | `7.9/10` |
| `rec.upipelinecache` 顶部节点 | 把回流结果变成一个可被点名的中间产物。 | 顶部节点很稳，但外环的存在感已经开始和内部链路争夺注意力。 | `7.4/10` |
| 内部主链保留 | 下方 `cook` split 与 `bytecode -> Phone` 继续作为背景保留。 | 内外两层结构成立，但优先级还可以再更硬。 | `7.3/10` |
| 端点垂直对齐 | `Phone` 顶边正中起笔、`Computer` 顶边正中收口。 | 几何语义正确，是这一页最稳定的地方。 | `7.6/10` |

## 当前问题与下一轮重点

- 这一页最大的收获是“回流半圈”已经讲清。
- 如果再迭代，应优先处理“外环太抢”而不是继续加节点。
