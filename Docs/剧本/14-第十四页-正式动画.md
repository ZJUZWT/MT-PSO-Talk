# 第 14 页正式动画：`page_14`

> 文件类型：`正式动画页 / placeholder onepage`
> Step / Sketch：`page_14` / `formal-page14`
> 上一页：`page_13`


## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 机械评分 | `6.7/10` | 来源：`formal-page14` + `buildGeometryReviewArtifact(...)`；`overlap / crossing / nodePierce / textOverflow` 全部为 `0`，`.rec.upipelinecache` 盒宽与三条 relation pill 已同步进入正式 harness 链路。 |
| 整体布局 Review | `4.0/10` | 四分位锚点与外边距继续稳定，解释板内部仍偏紧，但 `minNodeGap` 已从 `2px` 提到 `6px`。 |
| 模块空间结构 Review | `4.3/10` | `UE PSO / Gfx PSO / GPU` 主链成立，`record / save` 已不再压住 `UE -> rec` 的蓝色虚线，`bind / use` 也收回到 `Gfx -> GPU` 主链上方。 |
| 单节点与单边 Review | `9.3/10` | 三条边现在无穿框、无交叉、无越界；`UE -> rec` 的 detour 已从 `314%` 降到 `0%`，剩余问题转为整体 tightness 与底部说明 padding。 |
| 动画分数 | `4.5/10` | `2026-04-21` timing probe：基础区间 `in_range`，Harness 标准 `too_long`。 |

## 当前截图

![page14 review](../../ignore/page14-browser-capture-updated/page14-updated-headless.png)

- 最新浏览器复核截图：`ignore/page14-browser-capture-updated/page14-updated-headless.png`
- 几何约束复核：`UE PSO / Gfx PSO / GPU` 的渲染中心 X 继续落在 `320 / 640 / 960`；`UE / Gfx / GPU` 与 `UE -> Gfx`、`Gfx -> GPU` 主链共用同一条 Y 中线；`.rec.upipelinecache` 盒宽已扩到 `304px` 并解除 live 超框；`record / save` 已移出 `UE -> rec` 主 lane，`bind / use` 已回到 `Gfx -> GPU` 主链上方。
- Formal harness 载体：`SlideApp/src/review/formalPageReviewRegistry.ts` 中新增 `formal-page14`，正式 mechanical review 不再回退到旧的 loop sketch。

## Page goal

- 在 `page13` 的 `.ushaderbytecode -> Phone` 之后插入一个解释停顿，先讲清运行时看到的并不是“一个 PSO 名字”这么简单，而是至少有 `UE PSO` 与 `Gfx PSO` 两层。
- 这页不是拓扑推进页，而是轻量 onepage 图解页：核心是 `UE PSO -> Gfx PSO -> GPU` 与 `UE PSO -> .rec.upipelinecache` 的关系。
- 需要明确避免误解：`GPU` 不是把 `UE PSO` 转成 `Gfx PSO` 的转换器，`GPU` 是后面消费 `Gfx PSO` 的执行端。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `Phone` 如何收集 `PSO` |
| 页面副标题 | 先区分 `UE PSO` 与 `Gfx PSO`：前者是引擎侧记录 / 描述，后者是 `RHI / driver` 侧运行时对象 / 编译结果。 |
| 当前页核心信息 | `.rec.upipelinecache` 记录的是 `UE PSO` 这层 `ShaderHash + State`，不是把 driver 对象原样落盘。 |
| 页面类型 | `placeholder onepage 内容页` |
| 与上一页关系 | 承接 page13 的 bytecode landing，暂停主流程推进，先解释运行时收集这件事。 |
| 与下一页 narrative 关系 | 下一页回到主舞台，正式把 `Phone -> rec -> Computer` 的回流半圈长出来。 |
| 这一页新增了什么 | `UE PSO` / `Gfx PSO` 双胶囊、`UE -> Gfx` 的 `create / resolve`、`Gfx -> GPU` 的 `bind / use`、`UE -> rec` 的 `record / save`。 |
| 这一页删除了什么 | 原来的单一 `PSO` 胶囊被拆分，不再把引擎记录和图形后端对象混成一个框。 |
| 这一页保留了什么 | 保留上一页主流程作为淡化 memory plate，不改变主舞台编号和章节骨架。 |
| 移动的节点 | explanation layer 保持四分位锚点：`UE PSO` 中心 X 固定到 `320`，`Gfx PSO` 中心 X 固定到 `640`；右侧 `Phone` runtime 共享载体整体左移 `174px`，使 `GPU` 渲染中心 X 落到 `960`；`.rec.upipelinecache` 继续与 `UE PSO` 同轴，但盒宽从 `248px` 扩到 `304px`；`record / save` pill 收窄到 `156px` 并移到 `UE -> rec` 主 lane 左侧，`create / resolve` pill 收窄到 `196px`，`bind / use` pill 左移回 `Gfx -> GPU` 主链上方。 |
| 新增 node / edge | 本轮微调无新增；仍保留 `UE PSO`、`Gfx PSO`、`GPU`、`.rec.upipelinecache` 与三条解释关系边。 |
| 删除 node / edge | 本轮微调无删除；仍沿用这一版 `UE / Gfx` 双层解释图。 |
| 行为替换 | 不替换插页过渡逻辑；继续沿用“保留 runtime carrier continuity，只调 explanation layer”的策略。本轮保持 `UE PSO -> Gfx PSO -> GPU` 的 `左 1/4 -> 中轴 -> 右 1/4` 水平构图和共享 Y 中线，但把 `UE -> rec` 从左侧绕行替换成近竖直的中心 lane，把 `record / save` 从压线标签改成侧挂标签，并把 `bind / use` 从偏右漂浮标签改成主链上方标签。 |
| 需要强调的元素 | `UE PSO`、`Gfx PSO`、`.rec.upipelinecache`、`OpenGL` vs `Metal`。 |
| 暂不实现 / 暂不出现 | `Phone -> rec -> Computer` 正式回流线、`stablepc.csv`、`stable.upipelinecache`、precompile。 |

## Onepage 内容结构

| 区块 | 内容 | 讲述重点 |
| --- | --- | --- |
| 顶部标题行 | 标题 | 这是解释插页，不是新拓扑页。 |
| 左中 | `UE PSO` 胶囊 | 解释这层是 `ShaderHash + State` 的引擎侧记录 / 描述。 |
| 中右 | `Gfx PSO` 胶囊 | 解释这层是 `RHI / driver` 侧运行时对象 / 编译结果。 |
| 左上 | `.rec.upipelinecache` 文件胶囊 | 只从 `UE PSO` 这层记录落盘。 |
| 右侧终点 | `GPU` / 运行时消费端 | 只消费 `Gfx PSO`，不承担转换职责。 |
| 底部说明 | `UE PSO 记录描述；Gfx PSO 是运行时对象 / 编译结果` + `OpenGL / Vulkan / Metal` 差异 | 同时收口层级关系与 API 差异。 |

## 插页转场规则

1. 主流程页整体缩小并淡出，短暂保留为背景 memory plate。
2. 标题和 `UE PSO -> Gfx PSO -> GPU` / `UE PSO -> .rec.upipelinecache` 轻量图解浮入，先讲层级，再讲记录与消费端。
3. 离场时 onepage 整体缩小并淡出，短暂恢复上一张主流程静态帧，再进入下一张主流程动画。

## 版式与内容约束

- 这页必须读成“解释板”，不能伪装成新的流程拓扑。
- 标题下不再堆大段说明，只保留一眼能懂的图形关系。
- `UE PSO -> Gfx PSO` 解释 create / resolve，`Gfx PSO -> GPU` 解释 bind / use，`UE PSO -> .rec.upipelinecache` 解释 record / save。
- 不能把 `GPU` 画成 `UE PSO -> Gfx PSO` 的中转节点。
- 不要出现完整的 `Computer` / `Phone` 双端闭环线稿，只保留轻量说明图形和标签。

## Acceptance checklist

1. 一眼读得出这是在解释 `.rec.upipelinecache` 的来源，而不是在新增闭环 routing。
2. 观众必须能一眼区分 `UE PSO` 和 `Gfx PSO` 不是同一个层级。
3. `GPU` 必须被读成消费 `Gfx PSO` 的执行端，而不是“转换站”。
4. `OpenGL` 与 `Vulkan / Metal` 的对比必须落到“只有 `BSS` vs 带更多 `State`”。
5. 页面视觉密度必须明显低于主流程页，不能重新堆成一张路由图。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | `page13 -> page14` 前半段 | `page13` 主舞台整体缩小并淡出，保留成浅色 memory plate。 | previous flow frame | 先让观众知道主流程暂时停一下。 |
| 2 | `page13 -> page14` 中段 | 标题、`UE PSO`、`Gfx PSO`、`.rec.upipelinecache` 先后浮入。 | title row、ue box、gfx box、rec file | 点名运行时不是只“用” bytecode，也在维护引擎记录和图形后端对象两层。 |
| 3 | `page13 -> page14` 后半段 | `UE -> Gfx`、`Gfx -> GPU`、`UE -> rec` 三条关系补齐，底部层级说明与 API 差异落稳。 | relation arrows、api note | 用“记录 / 对象 / 消费端”的拆层，顺手收束 `OpenGL` vs `Vulkan / Metal`。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 主体形态 | 轻量 onepage diagram board |
| 背景策略 | page13 缩小淡出后保留为 memory plate |
| 内容块数量 | `2` 个 PSO 层级胶囊 + `1` 个 `rec` 文件 + `3` 条关系箭头 + `2` 条底部说明 |
| 动画节奏 | 先缩主流程，再进图解；离场时先恢复旧静态帧，再接下一页新增线路 |
| 字号策略 | 双胶囊标题、正文、底部说明整体放大；本轮重点放大的是三条解释关系 label，改成更大的 pill label，并同步加粗三条解释线；不再单独改 phone runtime 内部几何。 |
| 风险点 | 如果把 `UE PSO` / `Gfx PSO` 再做成一页大流程图，或让 `GPU` 看起来像转换站，这页就会重新失焦。 |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_13 -> page_14）

- 帧窗口：`1558 -> 1665`（`107` frames @ 60fps）
- 总时长：`1.783s`
- 时长评估：`in_range`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `107`，相对当前 `Δ+0` frames
- 标准时序评估：`too_long`（标准下限 `1.241s`，上限 `1.675s`）
- 标准调整建议：目标帧长 `89`，相对当前 `Δ-18` frames
- 本轮处理：保留用户已确认的插页停顿感，不替换现有帧锚点；先完成版式与字号修正，并把 timing 结论显式记账。

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`1.241s`
- 标准允许上限：`1.675s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 120.0 | 120.0 | - |
| fade_out | 120.0 | 340.0 | 220.0 | fade_out_page13_main_stage |
| node_motion | 340.0 | 611.2 | 271.2 | move_page14_explanation_board |
| edge_motion | 611.2 | 800.7 | 189.5 | grow_ue_to_gfx_relation, grow_gfx_to_gpu_relation, grow_ue_to_rec_relation |
| fade_in | 800.7 | 1020.7 | 220.0 | fade_in_page14_boxes, fade_in_page14_notes |
| hold_after | 1020.7 | 1240.7 | 220.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_page13_main_stage | fade_out | fade_out | - | 220.0 | formula |
| move_page14_explanation_board | node_move | node_motion | distancePx=96.0 | 271.2 | formula |
| grow_ue_to_gfx_relation | edge_grow | edge_motion | lengthPx=24.0 | 158.0 | formula |
| grow_gfx_to_gpu_relation | edge_grow | edge_motion | lengthPx=46.0 | 174.5 | formula |
| grow_ue_to_rec_relation | edge_grow | edge_motion | lengthPx=66.0 | 189.5 | formula |
| fade_in_page14_boxes | fade_in | fade_in | - | 220.0 | formula |
| fade_in_page14_notes | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 1558 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 1585 | 0.450 | move_page14_explanation_board(node_move) | 无显著边变化 |
| Mid | 1612 | 0.900 | fade_in_page14_boxes(fade_in)、fade_in_page14_notes(fade_in) | 无显著边变化 |
| Q3 | 1638 | 1.333 | 无显著节点变化 | 无显著边变化 |
| End | 1665 | 1.783 | 无显著节点变化 | 无显著边变化 |

## 审批状态

`page14 仍保持“phone runtime 连续 + 四分位 explanation layer + formal harness 已接入”的版本：UE / Gfx 双层解释图保留不变，右侧 runtime 共享载体只做整体平移，不拆 continuity；本轮把 `.rec.upipelinecache` 盒宽扩到 live 可读范围，把 `record / save` 挪出主 lane，并把 `bind / use` 收回主链上方，`UE -> rec` 的 detour 已从 `314%` 降到 `0%`。当前残留项不再是绕路线，而是 explanation board 仍偏紧、底部说明 padding 仍低，以及 `page13 -> page14` 插页在 Harness v1.0 下仍被记为 `too_long`，待 14-15 插页链统一收时长时再一起收口。`

## 三层 Review 总表

| Review 层级 | 当前分数 | 综合判断 |
| --- | --- | --- |
| `整体布局 Review` | `4.0/10` | explanation board 的外边距已经稳定，主问题收敛为内部 pocket 仍偏紧。 |
| `模块空间结构 Review` | `4.3/10` | `UE / Gfx / GPU` 主链与 `.rec` 上挂关系成立，正式 harness 识别到剩余问题主要是 pill gap 太小。 |
| `单节点与单边 Review` | `9.3/10` | `overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`；关系线已从“会出错”变成“可读但还可更顺”。 |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `ue-pso-card` | `UE PSO` 卡说明引擎侧记录 / 描述。 | 上一页没有同名卡，允许新增解释节点。 | `StageBox + text` | 新节点，但语义清楚，不会被误读成主流程新站点。 | `7.0/10` | 本轮字号抬高后可读性明显改善。 | `6.2/10` |
| `gfx-pso-card` | `Gfx PSO` 卡说明运行时对象 / 编译结果。 | 上一页没有同名卡，允许新增解释节点。 | `StageBox + text` | 与 `UE PSO` 形成同层对照，语义成立。 | `7.0/10` | 盒体与正文已可读，但和 `UE PSO` 间距仍偏短。 | `6.0/10` |
| `rec-card` | `.rec.upipelinecache` 只记录 `UE PSO` 这层。 | 承接后续 `page15` 的 `rec` 语义。 | `StageBox + text` | 作为上方落盘目标，语义稳定。 | `7.4/10` | 本轮把盒宽扩到 `304px` 后，live 渲染里的文件名已不再挤出框外，同时仍保持和 `UE PSO` 同轴。 | `6.6/10` |
| `gpu-runtime-stack` | 右侧 `GPU` 竖列只负责消费 `Gfx PSO`。 | 延续 `Phone` runtime 竖列语义。 | `VertexTriangles + GPU text + PixelGrid` | 连续语义最强，能直接借到手机侧 runtime 读法。 | `7.8/10` | 本轮恢复 shared carrier 几何后，右侧读法重新稳定。 | `6.8/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `ue-to-gfx` | `UE PSO -> Gfx PSO`，讲清 create / resolve。 | 新增解释边。 | `StrokeArrow + ArrowLabelPill` | 语义明确，读者能知道不是同一层对象。 | `6.6/10` | 主链改成橙色后，和记录线不再混色；`UE / Gfx` 间距拉开后，箭头本体终于有了足够长度。 | `6.2/10` |
| `gfx-to-gpu` | `Gfx PSO -> GPU`，讲清 bind / use。 | 承接手机 runtime 消费语义。 | `StrokeArrow + ArrowLabelPill` | 明确把 `GPU` 固定在消费端。 | `7.2/10` | `bind / use` pill 已收回主链上方，`Gfx -> GPU` 的对应关系比上一轮更直接。 | `7.0/10` |
| `ue-to-rec` | `UE PSO -> .rec.upipelinecache`，讲清 record / save。 | 为下一页 `rec` 回流半圈预埋语义。 | `StrokeArrow + ArrowLabelPill` | 语义稳定，是下一页 continuity 的关键。 | `7.0/10` | `record / save` 已从主 lane 侧挂，蓝色虚线回到近竖直中心 lane，detour 从 `314%` 降到 `0%`。 | `7.2/10` |
