# 第 14 页正式动画：`page_14`

> 文件类型：`正式动画页`
> Step / Sketch：`page_14` / `-`
> 上一页：`page_13`

## Page goal

- 在保留 bytecode landing 的基础上，第一次把 `Phone -> rec.upipelinecache -> Computer` 的回流半圈讲清。
- 这页的核心不是 stable，而是“运行时开始把收集结果回送到电脑侧”。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `Phone` 开始回传 `.rec.upipelinecache` |
| 页面副标题 | 保持 bytecode 已经进入 `Phone`，再从 `Phone` 顶部拉出回流：运行时开始把 `.rec.upipelinecache` 往电脑侧送。 |
| 当前页核心信息 | `Phone` 不再只是接收端，它开始把运行时收集结果送回 `Computer`。 |
| 与上一页关系 | 保留 `bytecode -> Phone`，只新增顶层回流半圈。 |
| 这一页新增了什么 | `rec.upipelinecache` 节点、`Phone -> rec -> Computer` 顶层回环。 |
| 这一页删除了什么 | 无。 |
| 这一页保留了什么 | `cook` split、`.ushaderbytecode -> Phone`。 |
| 需要强调的元素 | `rec.upipelinecache`、顶层回流半圈、`Phone`。 |
| 暂不实现 / 暂不出现 | `stablepc.csv`、`stable.upipelinecache`、`expand`。 |

## Node inventory

| id | text | semantic role | visual kind | anchor region | emphasis | alignment constraints | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `computer-shell` | `Computer` | 回流接收端 | device shell | 左下 1/3 | 中 | 与 `Phone` 继续构成左右双锚点 | 连续承接 page13 `computer-shell` | 必须复用同一设备 carrier |
| `split-a` | 无文字 | cook 分叉点 | junction circle | 中左 | 低 | 继续作为背景主干枢纽 | 连续承接 page13 `split-a` | 必须复用同一 junction carrier |
| `scl-node` | `.scl.csv` | metadata 输出 | artifact pill | 中上 | 低 | 继续位于 `.ushaderbytecode` 上方 | 连续承接 page13 `scl-node` | 必须复用同一 artifact carrier |
| `bytecode-node` | `.ushaderbytecode` | 已进入 runtime 的输入来源 | artifact pill | 中下 | 中 | 保留通往 `Phone` 的既有关系 | 连续承接 page13 `bytecode-node` | 必须复用同一 bytecode carrier |
| `rec-node` | `rec.upipelinecache` | 回流中继产物 | artifact pill | 顶部中带 | 高 | 处于 `Phone` 与 `Computer` 顶部回路之间 | 无 | 新建 artifact carrier |
| `phone-shell` | `Phone` | runtime 发送端兼接收端 | device shell | 右下 1/3 | 高 | 与顶部回流和下方 landing 同时发生关系 | 连续承接 page13 `phone-shell` | 必须复用同一设备 carrier |
| `phone-runtime-vertex` | 无文字 | runtime 输入起点 | icon in phone | `Phone` 上半部 | 中 | 与 `GPU`、`pixels` 共用垂直中轴 | 连续承接 page13 `phone-runtime-vertex` | 必须复用同一 runtime carrier |
| `phone-runtime-gpu` | `GPU` | runtime 核心执行点 | text in phone | `Phone` 中部 | 中 | 继续承担内部焦点 | 连续承接 page13 `phone-runtime-gpu` | 必须复用同一 runtime carrier |
| `phone-runtime-pixels` | 无文字 | runtime 输出终点 | pixel icon in phone | `Phone` 下半部 | 中 | 与 `vertex`、`GPU` 共用垂直中轴 | 连续承接 page13 `phone-runtime-pixels` | 必须复用同一 runtime carrier |

## Edge inventory

| id | from | to | meaning | route grammar | max bends | segment label | arrow style | sibling / symmetry note | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `computer-to-a` | `computer-shell` | `split-a` | `cook` 主干 | rounded orthogonal | 2 | `cook` | single arrow | 作为下层背景主线保留 | 连续承接 page13 `computer-to-a` | 必须复用同一 edge carrier |
| `a-to-scl` | `split-a` | `scl-node` | metadata 输出支线 | rounded orthogonal | 1 | `UE5 formats` | single arrow | 作为下层背景主线保留 | 连续承接 page13 `a-to-scl` | 必须复用同一 edge carrier |
| `a-to-bytecode` | `split-a` | `bytecode-node` | bytecode 输出支线 | rounded orthogonal | 1 | 无 | single arrow | 作为 landing 的上游背景 | 连续承接 page13 `a-to-bytecode` | 必须复用同一 edge carrier |
| `bytecode-to-phone` | `bytecode-node` | `phone-shell` | bytecode 已进入 runtime 的既有路径 | rounded orthogonal | 2 | 无 | single arrow | 继续保留 | 连续承接 page13 `bytecode-to-phone` | 必须复用同一 landing carrier |
| `phone-to-rec` | `phone-shell` | `rec-node` | runtime 把记录缓存送到上方中继 | rounded orthogonal | 2 | 无 | single arrow | 与 `rec-to-computer` 组成回流半圈 | 无 | 新建 return edge carrier |
| `rec-to-computer` | `rec-node` | `computer-shell` | 记录缓存回到电脑侧 | rounded orthogonal | 2 | 无 | single arrow | 与 `phone-to-rec` 组成回流半圈 | 无 | 新建 return edge carrier |
| `runtime-edge-1` | `phone-runtime-vertex` | `phone-runtime-gpu` | runtime 内部输入链路 | 竖直向下 | 0 | 无 | single arrow | 作为 receiver plane 骨架保留 | 连续承接 page13 `runtime-edge-1` | 必须复用同一 runtime edge carrier |
| `runtime-edge-2` | `phone-runtime-gpu` | `phone-runtime-pixels` | runtime 内部输出链路 | 竖直向下 | 0 | 无 | single arrow | 作为 receiver plane 骨架保留 | 连续承接 page13 `runtime-edge-2` | 必须复用同一 runtime edge carrier |

## Spatial constraints

- 顶部 `rec` 回路必须一眼读出 `Phone -> rec -> Computer` 的方向。
- `rec-node` 必须读成回流中继，而不是悬在顶上的孤立标题条。
- 下方 cook split 与 bytecode landing 继续保留，但优先级要低于新回流半圈。
- stable band 与 `expand` 仍然不能出现。

## Element styles

- 顶部回流线继续使用主流程橙色边，但不能挤压 `Phone` 壳体边界。
- `rec.upipelinecache` 沿用 artifact pill 语言，保证可被单独点名。
- 设备标签可以继续打开，但不能牺牲内部链路可读性。

## Assembly order

1. 保留 page13 的 cook split 与 bytecode landing。
2. 在顶部放出 `rec.upipelinecache` 中继节点。
3. 从 `Phone` 顶部拉出回流到 `rec`，再从 `rec` 落回 `Computer`。
4. 保持 stable 系统继续隐藏。

## Acceptance checklist

1. 本页必须一眼读出 `Phone -> rec -> Computer` 的回流半圈。
2. `rec.upipelinecache` 必须像中继节点，而不是孤立悬浮条。
3. `bytecode -> Phone` 仍要保留，但优先级应低于新的回流半圈。
4. `stablepc.csv`、`stable.upipelinecache`、`expand` 仍不能出现。
5. 顶部回流线不能贴着 `Phone` 壳体边缘走得过紧。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | `page13 -> page14` 前半段 | 保留 bytecode landing，把视线往上引。 | existing stage | 先告诉观众“流程不只往右走”。 |
| 2 | `page13 -> page14` 中段 | `rec.upipelinecache` 出现在顶部中带。 | `rec-node` | 点名运行时回传的记录产物。 |
| 3 | `page13 -> page14` 后半段 | `Phone -> rec -> Computer` 半圈完整长出。 | `phone-to-rec`、`rec-to-computer` | 这是第一次读到回流。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | `rec-node`、`phone-to-rec`、`rec-to-computer` |
| 需要复用的元素 | page13 的下层主舞台全部元素 |
| 当前共享元素 / 连续语义 carrier | 下层 split 与 landing 全部沿用上一页 carrier；顶部回流为新系统 |
| 是否需要镜头推进 / 缩放 | 不需要镜头推进，强调通过顶部回流长出完成 |
| 是否需要逐步显隐 | 允许 `rec-node` 与回流边逐步长出 |
| 风险点 | 顶部回流线一旦过长或贴 Phone 边界过紧，就会压过中层舞台并制造紧张感 |

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/formal-pages-14-15-routes-20260412/page14-routes-v1.png)

- 当前 URL：`http://127.0.0.1:4178/?step=page_14&debugFrame=708&surface=stage`
- 捕获方式：`browser-api stage`
- 捕获结果：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/formal-pages-14-15-routes-20260412/page14-routes-v1.png`
- 捕获备注：本轮专门复核 `Phone -> rec` 顶部回流是否脱离 `Phone` 壳体顶边。

## Blockers

- 无硬性 blocker。

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `8.2/10` | `8.0/10` | 顶部回流终于和 `Phone` 屋顶脱开，画面上沿更像一条独立回流走廊。 | `8.1/10` |
| `模块空间结构 Review` | `8.4/10` | `8.1/10` | `Phone -> rec -> Computer` 的半圈现在一眼可读，`rec` 的中继身份更明确。 | `8.3/10` |
| `单节点与单边 Review` | `8.3/10` | `8.1/10` | 最大问题从“贴壳体擦边”变成了仅剩轻微的路径偏长，已不构成停帧阻塞。 | `8.2/10` |

## Top 3 fixes

1. 把 `rec.upipelinecache` 更明确地对位到 `Phone` 与 `Computer` 之间的回流中继位置。
2. 如果后续还要打磨，可以再轻微缩短 `rec -> Computer` 的左侧长横段，让下落更利落。
3. 继续保持上回路为第一主线，下层 `cook split` 只做背景，不要重新加粗抢戏。

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `computer-shell` | 回流最终落回的电脑侧接收端。 | 连续承接 page13 `computer-shell` | page13 device carrier | continuity 成立，本页语义从 source 兼转为回流落点。 | `8.1/10` | 左侧锚点稳定，但并非当前页第一焦点。 | `7.7/10` |
| `split-a` | 作为下层背景保留的 cook 分叉点。 | 连续承接 page13 `split-a` | page13 junction carrier | continuity 成立。 | `8.0/10` | 退到背景后合理。 | `7.5/10` |
| `scl-node` | metadata 背景支线。 | 连续承接 page13 `scl-node` | page13 artifact carrier | continuity 成立。 | `8.0/10` | 作为背景存在稳定。 | `7.5/10` |
| `bytecode-node` | 已进入 runtime 的 bytecode 来源。 | 连续承接 page13 `bytecode-node` | page13 bytecode carrier | continuity 成立。 | `8.2/10` | 节点存在感合适，不与顶部回路冲突。 | `7.8/10` |
| `rec-node` | 顶部回流的中继节点。 | 无 | 新建 artifact carrier | 新节点语义明确，是本页新增核心。 | `8.0/10` | 可读性好，但中继对位感仍偏弱。 | `7.6/10` |
| `phone-shell` | runtime 发送端兼接收端。 | 连续承接 page13 `phone-shell` | page13 device carrier | continuity 成立。 | `8.1/10` | 顶部回流线已经脱离壳体屋顶，设备边界更干净。 | `8.1/10` |
| `phone-runtime-vertex` | 保留内部输入起点。 | 连续承接 page13 `phone-runtime-vertex` | page13 runtime carrier | continuity 成立。 | `8.0/10` | 作为内部骨架存在合理。 | `7.8/10` |
| `phone-runtime-gpu` | 保留内部核心焦点。 | 连续承接 page13 `phone-runtime-gpu` | page13 runtime carrier | continuity 成立。 | `8.0/10` | 焦点清楚，但不应压过顶部回流。 | `7.9/10` |
| `phone-runtime-pixels` | 保留内部输出终点。 | 连续承接 page13 `phone-runtime-pixels` | page13 runtime carrier | continuity 成立。 | `8.0/10` | 作为背景骨架存在合理。 | `7.8/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `computer-to-a` | cook 主干继续保留。 | 连续承接 page13 `computer-to-a` | page13 edge carrier | continuity 成立。 | `8.0/10` | 本页应退成背景。 | `7.5/10` |
| `a-to-scl` | metadata 背景支线继续保留。 | 连续承接 page13 `a-to-scl` | page13 edge carrier | continuity 成立。 | `8.0/10` | 退成背景后合理。 | `7.4/10` |
| `a-to-bytecode` | bytecode 上游背景继续保留。 | 连续承接 page13 `a-to-bytecode` | page13 edge carrier | continuity 成立。 | `8.0/10` | 退成背景后合理。 | `7.5/10` |
| `bytecode-to-phone` | bytecode 已进入 runtime 的既有路径。 | 连续承接 page13 `bytecode-to-phone` | page13 landing carrier | continuity 成立。 | `8.1/10` | 本页继续保留即可，不宜再抢戏。 | `7.4/10` |
| `phone-to-rec` | 从 `Phone` 顶部拉出的回流上半段。 | 无 | 新建 return edge carrier | 新边语义明确。 | `8.0/10` | 先上提再左转，已不再擦过 `Phone` 顶边，顶部走廊读起来更从容。 | `8.4/10` |
| `rec-to-computer` | 从 `rec` 落回 `Computer` 的下半段。 | 无 | 新建 return edge carrier | 与 `phone-to-rec` 一起讲清半圈。 | `8.0/10` | 落点明确，仍有一点“左段偏长”的感觉，但已不突兀。 | `7.8/10` |
| `runtime-edge-1` | `Phone` 内部输入链继续保留。 | 连续承接 page13 `runtime-edge-1` | page13 runtime edge carrier | continuity 成立。 | `8.0/10` | 稳。 | `7.8/10` |
| `runtime-edge-2` | `Phone` 内部输出链继续保留。 | 连续承接 page13 `runtime-edge-2` | page13 runtime edge carrier | continuity 成立。 | `8.0/10` | 稳。 | `7.8/10` |

## 审批状态

`正式页已完成 headless-stage fallback capture + blind critic 复核。当前页没有硬性 blocker；后续若继续打磨，应该优先处理 rec 的中继对位和顶部回流线的紧张感，而不是再加新节点。`
