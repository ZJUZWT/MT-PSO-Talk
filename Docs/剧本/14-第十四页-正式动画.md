# 第 14 页正式动画：`page_14`

> 文件类型：`正式动画页 / placeholder onepage`
> Step / Sketch：`page_14` / `-`
> 上一页：`page_13`


## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `7.2/10` | 来源：`2026-04-17` 截图复核（`ignore/review-refresh-20260417/page_14-headless.png`） |
| Node 分数 | `6.0/10` | 本页为“整图证据页”，按可读锚点节点（主图+高亮框）评估 |
| Edge 分数 | `5.8/10` | 本页连线语义主要由高亮标注承担，边语义密度较低 |
| 动画分数 | `5.0/10` | 自动 probe：page_13 -> page_14，durationVerdict=too_short |

## Page goal

- 在 `page13` 的 `.ushaderbytecode -> Phone` 之后插入一个解释停顿，先讲清运行时收集到的 `PSO` 信息最终如何落进 `.rec.upipelinecache`。
- 这页不是拓扑推进页，而是轻量 onepage 图解页：只保留 `Draw -> PSO -> .rec.upipelinecache` 的抽象关系和 API 差异。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `Phone` 如何收集 `PSO` |
| 页面副标题 | `Draw` 侧触发的运行时组合会被抽取成 `PSO` 记录，然后写入 `.rec.upipelinecache`。其中 `BSS` 这一块就是前面的 `VS Hash / PS Hash`。 |
| 当前页核心信息 | `.rec.upipelinecache` 来自运行时观测，不是电脑侧静态生成。 |
| 页面类型 | `placeholder onepage 内容页` |
| 与上一页关系 | 承接 page13 的 bytecode landing，暂停主流程推进，先解释运行时收集这件事。 |
| 与下一页 narrative 关系 | 下一页回到主舞台，正式把 `Phone -> rec -> Computer` 的回流半圈长出来。 |
| 这一页新增了什么 | `Draw -> PSO capsule -> .rec.upipelinecache` 图解、`OpenGL` vs `Vulkan / Metal` 状态差异说明。 |
| 这一页删除了什么 | 不推进正式 routing，不引入 `Computer` 侧 `expand / build`，不再使用左右说明卡。 |
| 这一页保留了什么 | 保留上一页主流程作为淡化 memory plate，不改变主舞台编号和章节骨架。 |
| 需要强调的元素 | runtime collection chain、`.rec.upipelinecache`、`OpenGL` vs `Metal`。 |
| 暂不实现 / 暂不出现 | `Phone -> rec -> Computer` 正式回流线、`stablepc.csv`、`stable.upipelinecache`、precompile。 |

## Onepage 内容结构

| 区块 | 内容 | 讲述重点 |
| --- | --- | --- |
| 顶部标题行 | 标题 | 这是解释插页，不是新拓扑页。 |
| 左下角 | `Draw` 小胶囊 | 作为运行时触发源，不展开冗余调用链。 |
| 中央主体 | `PSO` 横向胶囊，内部放 `BSS(VS Hash / PS Hash)` 与 `State` | `BSS` 是前面的 shader hash 大块，不再和 `VS / PS Hash` 并列。 |
| 右侧 | `.rec.upipelinecache` 文件胶囊 | 和下一页的正式回流节点直接接上。 |
| 底部说明 | `OpenGL: BSS` / `Vulkan / Metal: BSS + State` | 强调 API 暴露状态信息的差异。 |

## 插页转场规则

1. 主流程页整体缩小并淡出，短暂保留为背景 memory plate。
2. 标题和 `Draw -> PSO -> .rec.upipelinecache` 轻量图解浮入，`Draw -> PSO` 用虚线表达“抽取”。
3. 离场时 onepage 整体缩小并淡出，短暂恢复上一张主流程静态帧，再进入下一张主流程动画。

## 版式与内容约束

- 这页必须读成“解释板”，不能伪装成新的流程拓扑。
- 标题下不再堆大段说明，只保留一眼能懂的图形关系。
- `Draw -> PSO` 是虚线抽取，`PSO -> .rec.upipelinecache` 是实线落盘。
- 不要出现完整的 `Computer` / `Phone` 双端闭环线稿，只保留轻量说明图形和标签。

## Acceptance checklist

1. 一眼读得出这是在解释 `.rec.upipelinecache` 的来源，而不是在新增闭环 routing。
2. `Draw` 只作为抽取来源存在，不能扩展成新的复杂子图。
3. `OpenGL` 与 `Vulkan / Metal` 的对比必须落到“只有 `BSS` vs 带更多 `State`”，并且 `BSS` 要明确包住 `VS Hash / PS Hash`。
4. 页面视觉密度必须明显低于主流程页，不能重新堆成一张路由图。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | `page13 -> page14` 前半段 | `page13` 主舞台整体缩小并淡出，保留成浅色 memory plate。 | previous flow frame | 先让观众知道主流程暂时停一下。 |
| 2 | `page13 -> page14` 中段 | 标题、`PSO` 胶囊、`.rec.upipelinecache` 文件先后浮入。 | title row、pso capsule、rec file | 点名运行时不是只“用” bytecode，也在“记”状态。 |
| 3 | `page13 -> page14` 后半段 | `Draw -> PSO` 虚线抽取与 `PSO -> rec` 实线关系补齐，底部 API 差异落稳。 | draw lane、api note | 用 `OpenGL` vs `Vulkan / Metal` 的状态差异收束这一页。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 主体形态 | 轻量 onepage diagram board |
| 背景策略 | page13 缩小淡出后保留为 memory plate |
| 内容块数量 | `1` 个 `PSO` 主胶囊 + `1` 个 `Draw` 来源 + `1` 个 `rec` 文件 + `2` 条 API 备注 |
| 动画节奏 | 先缩主流程，再进图解；离场时先恢复旧静态帧，再接下一页新增线路 |
| 风险点 | 如果又长成左右信息卡或大背景板，这页会重新变成 AI 式说明页。 |

## 审批状态

`placeholder onepage 文档版已切到轻量图解。当前重点是把 runtime collection 的来源和 API 差异讲清，而不是在这一页推进正式闭环。`

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `7.1/10` | `7.3/10` | 主证据图占幅充分，阅读顺序可读。 | `7.2/10` |
| `模块空间结构 Review` | `6.8/10` | `7.0/10` | “主图 + 热点标注”结构成立，但解释区仍可更聚焦。 | `6.9/10` |
| `单节点与单边 Review` | `5.9/10` | `6.1/10` | 节点/边不是本页主表达载体，可读但不够结构化。 | `6.0/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `runtime-profiler-image` | 运行时卡顿证据主图（性能分析器截图）。 | 承接上页“运行时问题”主题 | 主证据图 | 语义连续，观众能理解这是问题证据。 | `7.2/10` | 主体清楚，但局部文本较密。 | `7.2/10` |
| `hotspot-right-panel` | 右侧 `PSOPrecache` 耗时热点锚点。 | 新增证据锚点 | 高亮框节点 | 证据意图明确。 | `6.0/10` | 锚点明显，解释文字可更靠近。 | `6.0/10` |
| `hotspot-timeline` | 顶部与中部时间轴热点锚点。 | 新增证据锚点 | 高亮框节点 | 与右侧热点形成互证关系。 | `5.8/10` | 可读但缺少更强的导引线。 | `5.8/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `timeline-to-bottleneck` | 时间轴热点指向性能瓶颈结论。 | 承接运行时叙事 | 隐式注释边 | 语义成立。 | `5.8/10` | 边关系主要靠口播补全。 | `5.8/10` |
| `worker-to-bottleneck` | Worker 轨道热点与瓶颈结论的对应关系。 | 新增证据路径 | 隐式注释边 | 互证关系清楚。 | `5.7/10` | 缺少明确箭头导引，结构感一般。 | `5.7/10` |
| `panel-to-bottleneck` | 右侧统计面板热点与瓶颈结论对应。 | 新增证据路径 | 隐式注释边 | 可读性尚可。 | `5.9/10` | 重点能看见，但边路径表达弱。 | `5.9/10` |
