# 第 14 页正式动画：`page_14`

> 文件类型：`正式动画页 / placeholder onepage`
> Step / Sketch：`page_14` / `-`
> 上一页：`page_13`

## Page goal

- 在 `page13` 的 `.ushaderbytecode -> Phone` 之后插入一个解释停顿，先讲清 `Phone` 侧运行时到底在收集什么。
- 这页不是拓扑推进页，而是 onepage 内容页：把 `.rec.upipelinecache` 的来源和 API 差异先说透，再回到主流程。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `Phone` 侧运行时如何收集 `rec.upipelinecache` |
| 页面副标题 | `Phone` 不只是消费 `.ushaderbytecode`，它还会把运行时看到的组合状态记录下来，最后汇总成 `.rec.upipelinecache`。 |
| 当前页核心信息 | `rec.upipelinecache` 来自运行时观察记录，不是电脑侧凭空生成的文件。 |
| 页面类型 | `placeholder onepage 内容页` |
| 与上一页关系 | 承接 page13 的 bytecode landing，暂停主流程推进，先解释运行时收集这件事。 |
| 与下一页 narrative 关系 | 下一页回到主舞台，正式把 `Phone -> rec -> Computer` 的回流半圈长出来。 |
| 这一页新增了什么 | 运行时收集链说明、`OpenGL` vs `Metal / modern gfx api` 对比、底部 takeaway strip。 |
| 这一页删除了什么 | 不推进正式 routing，不引入 `Computer` 侧 `expand / build`。 |
| 这一页保留了什么 | 保留上一页主流程作为淡化 memory plate，不改变主舞台编号和章节骨架。 |
| 需要强调的元素 | runtime collection chain、`.rec.upipelinecache`、`OpenGL` vs `Metal`。 |
| 暂不实现 / 暂不出现 | `Phone -> rec -> Computer` 正式回流线、`stablepc.csv`、`stable.upipelinecache`、precompile。 |

## Onepage 内容结构

| 区块 | 内容 | 讲述重点 |
| --- | --- | --- |
| 顶部标题行 | 标题 + 一句 thesis | 这是解释插页，不是新拓扑页。 |
| 左侧内容卡 | `Draw / ShaderHash / State -> rec.upipelinecache` | 讲清 `Phone` 运行时会把 draw 相关观测记录下来。 |
| 右侧内容卡 | `OpenGL` vs `Metal / modern gfx api` | 讲清不同 API 对显式 pipeline-state 组合的暴露程度不同。 |
| 底部 takeaway strip | `OpenGL` 记录数通常更少，`Metal` 通常更多 | 强调“显式性越强，记录与组合通常越多”。 |

## 插页转场规则

1. 主流程页整体缩小并淡出，短暂保留为背景 memory plate。
2. onepage 内容板从中心轻微放大浮入，标题、两张内容卡、底部 takeaway 分两拍落位。
3. 离场时 onepage 整体缩小并淡出，短暂恢复上一张主流程静态帧，再进入下一张主流程动画。

## 版式与内容约束

- 这页必须读成“解释板”，不能伪装成新的流程拓扑。
- 标题下的 thesis 只能有一句，不要扩展成长段说明。
- 左卡负责“收集逻辑”，右卡负责“API 差异”，底部 strip 负责“结论”，三者分工不能混。
- 不要出现完整的 `Computer` / `Phone` 双端闭环线稿，只保留轻量说明图形和标签。

## Acceptance checklist

1. 一眼读得出这是在解释 `.rec.upipelinecache` 的来源，而不是在新增闭环 routing。
2. `OpenGL` 与 `Metal / modern gfx api` 的对比必须落到“显式 pipeline-state 组合数量不同”。
3. 底部 takeaway 必须能单独复述：`OpenGL` 通常记录更少，`Metal` 通常更多。
4. 页面视觉密度必须明显低于主流程页，不能重新堆成一张路由图。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | `page13 -> page14` 前半段 | `page13` 主舞台整体缩小并淡出，保留成浅色 memory plate。 | previous flow frame | 先让观众知道主流程暂时停一下。 |
| 2 | `page13 -> page14` 中段 | 标题、thesis 和两张内容卡从中心浮入。 | title row、left card、right card | 点名 `Phone` 侧运行时正在做“收集”。 |
| 3 | `page13 -> page14` 后半段 | 底部 takeaway strip 落稳。 | takeaway strip | 用 `OpenGL` vs `Metal` 的数量差异收束这一页。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 主体形态 | 轻量 onepage explanation board |
| 背景策略 | page13 缩小淡出后保留为 memory plate |
| 内容块数量 | `2` 张中心卡 + `1` 条底部 takeaway |
| 动画节奏 | 先缩主流程，再进内容板；离场时反向执行 |
| 风险点 | 如果说明卡太多或箭头太多，这页会误读成新的拓扑页。 |

## 审批状态

`placeholder onepage 文档版已定。当前重点是把 runtime collection 的来源和 API 差异讲清，而不是在这一页推进正式闭环。`
