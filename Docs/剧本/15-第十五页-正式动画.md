# 第 15 页正式动画：`page_15`

> 文件类型：`正式动画页`
> Step / Sketch：`page_15` / `-`
> 上一页：`page_14`


## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `7.1/10` | 来源：`2026-04-17` 截图复核（`ignore/review-refresh-20260417/page_15-f960.png`） |
| Node 分数 | `6.9/10` | 来源：同一截图下节点可读性与间距复核 |
| Edge 分数 | `6.8/10` | 来源：同一截图下主干与示例边线读通性复核 |
| 动画分数 | `8.5/10` | 自动 probe：page_15_img -> page_15，durationVerdict=in_range |

## Page goal

- 在 page14 插页解释完运行时收集后，回到主流程，第一次把 `Phone -> rec.upipelinecache -> Computer` 的回流半圈讲清。
- 这页的核心不是 stable，而是“运行时已经开始把收集结果送回电脑侧”。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `Phone` 开始回传 `.rec.upipelinecache` |
| 页面副标题 | 先保留 `.ushaderbytecode -> Phone` 的 landing，再从 `Phone` 顶部拉出回流，让 `.rec.upipelinecache` 正式回到 `Computer`。 |
| 当前页核心信息 | `Phone` 不再只是接收端，它开始把运行时收集结果送回 `Computer`。 |
| 与上一页关系 | page14 先解释 `rec` 的来源，这一页回到主舞台，把 `Phone -> rec -> Computer` 的回流半圈正式长出来。 |
| 与下一页 narrative 关系 | 下一页暂停主流程，专门解释 `expand / build` 和四类文件各自承载的信息。 |
| 这一页新增了什么 | `rec.upipelinecache` 节点、`Phone -> rec -> Computer` 顶层回流半圈。 |
| 这一页删除了什么 | 无。 |
| 这一页保留了什么 | `cook` split、`.ushaderbytecode -> Phone`、`Phone` 内部 runtime trio。 |
| 需要强调的元素 | `rec.upipelinecache`、顶层回流半圈、`Phone`。 |
| 暂不实现 / 暂不出现 | `stablepc.csv`、`stable.upipelinecache`、`expand`、precompile。 |

## Node inventory

| id | text | semantic role | visual kind | emphasis |
| --- | --- | --- | --- | --- |
| `computer-shell` | `Computer` | 回流接收端 | device shell | 中 |
| `split-a` | 无文字 | `cook` 分叉点 | junction circle | 低 |
| `scl-node` | `.scl.csv` | metadata 输出 | artifact pill | 低 |
| `bytecode-node` | `.ushaderbytecode` | 已进入 runtime 的输入来源 | artifact pill | 中 |
| `rec-node` | `rec.upipelinecache` | 回流中继产物 | artifact pill | 高 |
| `phone-shell` | `Phone` | runtime 发送端兼接收端 | device shell | 高 |
| `phone-runtime-vertex` | 无文字 | runtime 输入起点 | icon in phone | 中 |
| `phone-runtime-gpu` | `GPU` | runtime 核心执行点 | text in phone | 中 |
| `phone-runtime-pixels` | 无文字 | runtime 输出终点 | pixel icon in phone | 中 |

## Edge inventory

| id | from | to | meaning | route grammar | emphasis |
| --- | --- | --- | --- | --- | --- |
| `computer-to-a` | `computer-shell` | `split-a` | `cook` 主干 | rounded orthogonal | 低 |
| `a-to-scl` | `split-a` | `scl-node` | metadata 输出支线 | rounded orthogonal | 低 |
| `a-to-bytecode` | `split-a` | `bytecode-node` | bytecode 输出支线 | rounded orthogonal | 低 |
| `bytecode-to-phone` | `bytecode-node` | `phone-shell` | bytecode 已进入 runtime 的既有路径 | rounded orthogonal | 中 |
| `phone-to-rec` | `phone-shell` | `rec-node` | runtime 把记录缓存送到上方中继 | rounded orthogonal | 高 |
| `rec-to-computer` | `rec-node` | `computer-shell` | 记录缓存回到电脑侧 | rounded orthogonal | 高 |
| `runtime-edge-1` | `phone-runtime-vertex` | `phone-runtime-gpu` | runtime 内部输入链路 | 竖直向下 | 低 |
| `runtime-edge-2` | `phone-runtime-gpu` | `phone-runtime-pixels` | runtime 内部输出链路 | 竖直向下 | 低 |

## Spatial constraints

- 顶部 `Phone -> rec -> Computer` 必须一眼读出方向，不能像悬在标题区的装饰线。
- `rec-node` 必须明确对位成回流中继，而不是独立标题条。
- 下层 `cook` split 与 bytecode landing 继续保留，但优先级必须低于新回流半圈。
- stable band 仍然不能提前出现。

## Assembly order

1. 让 page14 插页整体缩小并淡出。
2. 短暂恢复 page13 的 landing 主舞台，作为回到拓扑页的记忆接点。
3. 在顶部放出 `rec.upipelinecache` 节点。
4. 从 `Phone` 顶部拉出回流到 `rec`，再从 `rec` 落回 `Computer`。

## Acceptance checklist

1. 本页必须一眼读出 `Phone -> rec -> Computer` 的回流半圈。
2. `rec.upipelinecache` 必须像中继节点，而不是悬浮标签。
3. `.ushaderbytecode -> Phone` 仍要保留，但优先级应低于新的回流半圈。
4. `stablepc.csv`、`stable.upipelinecache`、`expand` 仍不能出现。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | `page14 -> page15` 前半段 | onepage 插页缩小淡出，短暂恢复 page13 landing 静态帧。 | placeholder board、restored landing frame | 先把“解释板”收回，再回到主流程。 |
| 2 | `page14 -> page15` 中段 | `rec.upipelinecache` 出现在顶部中带。 | `rec-node` | 点名这是运行时带回来的记录产物。 |
| 3 | `page14 -> page15` 后半段 | `Phone -> rec -> Computer` 半圈完整长出。 | `phone-to-rec`、`rec-to-computer` | 这是第一次正式读到回流。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | `rec-node`、`phone-to-rec`、`rec-to-computer` |
| 需要复用的元素 | page13 的 `cook` split、`.ushaderbytecode -> Phone`、`Phone` 内部 trio |
| 转场要求 | page14 离场后先短暂回到 page13 主舞台，再播放本页回流新增动作 |
| 风险点 | 如果顶部回流线贴 `Phone` 壳体过紧，整页会重新变得局促。 |

## 审批状态

`旧 page14 语义已顺延为新 page15。当前页只负责把 runtime 回流讲清，不提前吞入 stable 构建。`

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `7.0/10` | `7.2/10` | 主链 `rec + scl -> expand -> stablepc` 成立，左下示例区仍偏挤。 | `7.1/10` |
| `模块空间结构 Review` | `6.8/10` | `7.0/10` | 主模块与 example 模块关系清楚，但上右注释区略抢视线。 | `6.9/10` |
| `单节点与单边 Review` | `6.7/10` | `7.1/10` | 节点可读性整体合格，边线末段和示例分叉仍可更干净。 | `6.9/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `rec-node` | `rec.upipelinecache` 作为历史 runtime 记录输入。 | 承接上一页 runtime 回传语义 | artifact 节点 | 连续语义稳定。 | `7.1/10` | 左侧定位稳定、文本可读。 | `7.1/10` |
| `scl-node` | 历史 `.scl.csv` 作为映射字典输入。 | 承接 `.scl.csv` 语义并切换为历史版本 | artifact 节点 | 连续语义成立。 | `6.9/10` | 顶部占位合理，右侧注释略近。 | `6.9/10` |
| `expand-merge` | 中央 `+` + `expand` 语义转换点。 | 新增处理节点 | merge 节点 | 功能语义清楚。 | `6.8/10` | 尺寸可读，周边边线仍稍密。 | `6.8/10` |
| `stablepc-node` | `stablepc.csv` 输出节点。 | 新增输出节点 | artifact 节点 | 输出语义明确。 | `6.9/10` | 右侧承接稳，整体仍偏右重。 | `6.9/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `rec-to-expand` | `rec` 水平进入 `expand` 合流点。 | 承接上一页回传链路 | 主干边 | 连续语义成立。 | `7.0/10` | 线段干净、节奏稳定。 | `7.0/10` |
| `scl-to-expand` | `.scl.csv` 垂直注入合流点。 | 承接 `.scl.csv` 参与处理语义 | 注入边 | 语义成立。 | `6.7/10` | 入点清楚，但与注释区距离偏近。 | `6.7/10` |
| `expand-to-stablepc` | 合流后输出到 `stablepc.csv`。 | 新增输出关系 | 输出边 | 输出逻辑明确。 | `6.9/10` | 主干可读，箭头收口顺。 | `6.9/10` |
| `example-branch` | `Hash_rec -> StableKey_A/B` 示例分叉边。 | 新增示例关系 | example 边组 | 示例语义正确。 | `6.6/10` | 分叉可读，局部可再拉开。 | `6.6/10` |
