# 第 15 页正式动画：`page_15`

> 文件类型：`正式动画页`
> Step / Sketch：`page_15` / `-`
> 上一页：`page_14`

## Page goal

- 在 page14 的回流半圈之上，补出 `expand` 前置 `+`、`stablepc.csv`、stable merge node、`stable.upipelinecache` 和回到 `Phone` 的最终落线。
- 这页负责把“收集、电脑整理、稳定结果再回到手机使用”的闭环真正讲完整。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `expand` 生成 stable，最终回到 `Phone` |
| 页面副标题 | 最后把电脑侧的 `expand`、`stablepc.csv`、`stable.upipelinecache` 和回到 `Phone` 的那一条线压进同一页，让收集、构建、再使用真正闭合。 |
| 当前页核心信息 | `Computer` 负责整理成 stable，stable 结果再回到 `Phone`，闭环才算完成。 |
| 与上一页关系 | 承接 page14 的回流半圈，在同一页补出 stable 构建与最终 landing。 |
| 这一页新增了什么 | `expand` 前置 `+`、`stablepc.csv`、stable merge node `B`、`stable.upipelinecache`、`stable -> Phone`。 |
| 这一页删除了什么 | 无。 |
| 这一页保留了什么 | `cook` split、`bytecode -> Phone`、`Phone -> rec -> Computer`。 |
| 需要强调的元素 | `expand`、`stablepc.csv`、`stable.upipelinecache -> Phone`、闭环完成。 |
| 暂不实现 / 暂不出现 | 额外的新文件类型或更多支线。 |

## Node inventory

| id | text | semantic role | visual kind | anchor region | emphasis | alignment constraints | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `computer-shell` | `Computer` | 电脑侧整理端 | device shell | 左下 1/3 | 高 | 与右侧 `Phone` 继续构成闭环两端 | 连续承接 page14 `computer-shell` | 必须复用同一设备 carrier |
| `split-a` | 无文字 | cook 分叉点 | junction circle | 中左下 | 中 | 继续位于 `Computer` 与双产物之间 | 连续承接 page14 `split-a` | 必须复用同一 junction carrier |
| `scl-node` | `.scl.csv` | metadata 支线 | artifact pill | 中部 | 中 | 既保留 cook 输出，又参与 stable merge | 连续承接 page14 `scl-node` | 必须复用同一 artifact carrier |
| `bytecode-node` | `.ushaderbytecode` | 原始 runtime 输入产物 | artifact pill | 中下 | 中 | 保留与 `Phone` 的既有关系 | 连续承接 page14 `bytecode-node` | 必须复用同一 bytecode carrier |
| `rec-node` | `rec.upipelinecache` | 顶部回流中继 | artifact pill | 顶部中带 | 中 | 继续处于 `Phone` 与 `Computer` 的回流半圈上 | 连续承接 page14 `rec-node` | 必须复用同一 artifact carrier |
| `merge-a` | `+` | `expand` 前置合流点 | merge point | 中部左带 | 高 | 位于 `Computer`、`.scl.csv` 与 `stablepc.csv` 之间 | 无 | 新建 merge carrier |
| `stablepc-node` | `stablepc.csv` | expand 阶段产物 | artifact pill | 中部左带 | 高 | 与 `merge-a`、`merge-b`、`stable-upipe-node` 同属 stable band | 无 | 新建 artifact carrier |
| `merge-b` | `+` | stable merge junction | merge point | 中部偏右 | 高 | 位于 `stablepc` / `.scl.csv` / `stable.upipelinecache` 之间 | 无 | 新建 merge carrier |
| `stable-upipe-node` | `stable.upipelinecache` | 稳定产物 | artifact pill | 中右 | 高 | 与 `Phone` 形成最终 landing 关系 | 无 | 新建 artifact carrier |
| `phone-shell` | `Phone` | 最终使用端 | device shell | 右下 1/3 | 高 | 同时承接 bytecode 旧路径与 stable 新路径 | 连续承接 page14 `phone-shell` | 必须复用同一设备 carrier |
| `phone-runtime-vertex` | 无文字 | runtime 输入起点 | icon in phone | `Phone` 上半部 | 中 | 与 `GPU`、`pixels` 共用垂直中轴 | 连续承接 page14 `phone-runtime-vertex` | 必须复用同一 runtime carrier |
| `phone-runtime-gpu` | `GPU` | runtime 核心执行点 | text in phone | `Phone` 中部 | 中 | 与 stable landing 一起组成“使用端” | 连续承接 page14 `phone-runtime-gpu` | 必须复用同一 runtime carrier |
| `phone-runtime-pixels` | 无文字 | runtime 输出终点 | pixel icon in phone | `Phone` 下半部 | 中 | 与 `vertex`、`GPU` 共用垂直中轴 | 连续承接 page14 `phone-runtime-pixels` | 必须复用同一 runtime carrier |

## Edge inventory

| id | from | to | meaning | route grammar | max bends | segment label | arrow style | sibling / symmetry note | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `computer-to-a` | `computer-shell` | `split-a` | cook 主干 | rounded orthogonal | 2 | `cook` | single arrow | 保留为下层来源主干 | 连续承接 page14 `computer-to-a` | 必须复用同一 edge carrier |
| `a-to-scl` | `split-a` | `scl-node` | metadata 输出支线 | rounded orthogonal | 1 | `UE5 formats` | single arrow | 同时作为 stable merge 输入之一 | 连续承接 page14 `a-to-scl` | 必须复用同一 edge carrier |
| `a-to-bytecode` | `split-a` | `bytecode-node` | bytecode 输出支线 | rounded orthogonal | 1 | 无 | single arrow | 保留 bytecode 路径上游 | 连续承接 page14 `a-to-bytecode` | 必须复用同一 edge carrier |
| `bytecode-to-phone` | `bytecode-node` | `phone-shell` | 原始 bytecode 进入 runtime 的旧路径 | rounded orthogonal | 2 | 无 | single arrow | 与 stable 路径并存 | 连续承接 page14 `bytecode-to-phone` | 必须复用同一 landing carrier |
| `phone-to-rec` | `phone-shell` | `rec-node` | runtime 回流上半段 | rounded orthogonal | 2 | 无 | single arrow | 与 `rec-to-computer` 组成回流半圈 | 连续承接 page14 `phone-to-rec` | 必须复用同一 return carrier |
| `rec-to-computer` | `rec-node` | `computer-shell` | runtime 回流下半段 | rounded orthogonal | 2 | 无 | single arrow | 与 `phone-to-rec` 组成回流半圈 | 连续承接 page14 `rec-to-computer` | 必须复用同一 return carrier |
| `computer-to-expand-merge` | `computer-shell` | `merge-a` | `Computer` 先接入前置合流点 | horizontal | 0 | 无 | single arrow | stable band 左侧引入段 | 无 | 新建 stable edge carrier |
| `scl-to-expand-merge` | `scl-node` | `merge-a` | `.scl.csv` 也接入 `expand` 前置 `+` | rounded orthogonal | 2 | 无 | single arrow | 与 `computer-to-expand-merge` 在 `merge-a` 汇合 | 无 | 新建 stable edge carrier |
| `expand-merge-to-stablepc` | `merge-a` | `stablepc-node` | `expand` 合流后产出 `stablepc.csv` | horizontal | 0 | `expand` | single arrow | stable band 上游第二段 | 无 | 新建 stable edge carrier |
| `stablepc-to-b` | `stablepc-node` | `merge-b` | stablepc 输入 merge | horizontal / slight rounded | 1 | 无 | single arrow | 与 `scl-to-b` 在 `merge-b` 汇合 | 无 | 新建 stable edge carrier |
| `scl-to-b` | `scl-node` | `merge-b` | `.scl.csv` 输入 merge | rounded orthogonal | 2 | 无 | single arrow | 与 `stablepc-to-b` 在 `merge-b` 汇合 | 无 | 新建 stable edge carrier |
| `b-to-stable-upipe` | `merge-b` | `stable-upipe-node` | merge 之后形成 stable 产物 | horizontal / slight rounded | 1 | 无 | single arrow | stable band 主输出 | 无 | 新建 stable edge carrier |
| `stable-upipe-to-phone` | `stable-upipe-node` | `phone-shell` | stable 结果回到 `Phone` 使用 | horizontal | 0 | 无 | single arrow | 当前页收束边 | 无 | 新建 stable landing carrier |
| `runtime-edge-1` | `phone-runtime-vertex` | `phone-runtime-gpu` | runtime 内部输入链路 | 竖直向下 | 0 | 无 | single arrow | 作为接收端内部骨架保留 | 连续承接 page14 `runtime-edge-1` | 必须复用同一 runtime edge carrier |
| `runtime-edge-2` | `phone-runtime-gpu` | `phone-runtime-pixels` | runtime 内部输出链路 | 竖直向下 | 0 | 无 | single arrow | 作为接收端内部骨架保留 | 连续承接 page14 `runtime-edge-2` | 必须复用同一 runtime edge carrier |

## Spatial constraints

- 当前页必须一眼读出“回流 -> expand -> merge -> stable -> Phone”的完整闭环。
- `expand` 左侧必须明确存在一个前置 `+`，不能再被读成 `Computer` 直接生成 `stablepc.csv`。
- `merge-a`、`stablepc.csv`、`merge-b`、`stable.upipelinecache` 这一组现在必须共享同一条水平中心轴，形成稳定的 middle spine。
- stable band 必须像一个独立层，而不是几条线临时堆在中右区域。
- `merge-b` 周围要能清楚看见三条输入/输出关系，不能读成一团粘连。
- `stable-upipelinecache` 的文本区必须保持干净，任何边都不应穿过主要文字区域。

## Element styles

- `expand` 是线段标签，不是独立节点。
- stable band 继续沿用 artifact / merge point 语言，但要明显区别于下方 cook split。
- `Phone` 仍是最终使用端，不应被 stable 大块压成背景。

## Assembly order

1. 保留 page14 的回流半圈与下层 split / landing。
2. 从 `Computer` 长出 `expand` 到前置 `merge-a`，同时让 `.scl.csv` 也补入 `merge-a`。
3. 让 `merge-a` 输出 `stablepc.csv`，再让 `stablepc.csv` 与 `.scl.csv` 在 `merge-b` 汇合，并形成 `stable.upipelinecache`。
4. 从 stable 产物拉出最终 landing 回到 `Phone`。

## Acceptance checklist

1. 当前页必须一眼读出完整闭环，而不是“又多了几条线”。
2. `merge-a`、`stablepc.csv`、`merge-b`、`stable.upipelinecache` 必须读成同一 stable band。
3. `.scl.csv` 必须至少有一条明确的边接入 `expand` 前置 `+`。
4. `stable-upipelinecache` 的文字区不能被线条穿过或严重贴压。
5. `Phone` 必须保持“最终使用端”的身份，不应完全被 stable 大块压住。
6. `bytecode` 旧路径和 stable 新路径应并存，但必须能区分主次。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | `page14 -> page15` 前半段 | 保留回流半圈，电脑侧长出 `expand`，先进入前置 `+`。 | `computer-to-expand-merge`、`merge-a` | 先讲电脑开始整理。 |
| 2 | `page14 -> page15` 中段 | `.scl.csv` 先补入 `expand` 前置 `+`，再产出 `stablepc.csv`；随后进入 `merge-b` 形成 stable。 | `scl-to-expand-merge`、`expand-merge-to-stablepc`、`merge-b`、`b-to-stable-upipe`、`stable-upipe-node` | 讲清 stablepc 和 stable 都不是凭空出现。 |
| 3 | `page14 -> page15` 后半段 | stable 产物回到 `Phone`，闭环收束。 | `stable-upipe-to-phone` | 这一页必须读成真正的闭环完成。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | `merge-a`、`stablepc-node`、`merge-b`、`stable-upipe-node`、stable band 边 |
| 需要复用的元素 | page14 的回流半圈、cook split、bytecode landing、Phone 内部 trio |
| 当前共享元素 / 连续语义 carrier | page14 的既有系统全部延续；stable band 是当前页新增系统 |
| 是否需要镜头推进 / 缩放 | 不需要镜头推进，靠 stable band 长出和最终 landing 收束完成 |
| 是否需要逐步显隐 | 允许 stable band 和最终 landing 逐步长出 |
| 风险点 | stable band 与 `Phone` 一旦过近，就会把中右区域挤成读不顺的一团；当前版本已经触发了这个风险 |

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/formal-pages-15-expand-merge-20260413/page15-expand-merge-headless-v9-arrowfix.png)

- 当前 URL：`http://127.0.0.1:4178/?step=page_15&debugFrame=762&surface=stage`
- 首选捕获：`browser-api stage`，结果：`timeout`
- 前台窗口捕获：失败，前台窗口不是 SlideApp 页
- 实际捕获方式：`headless-stage fallback`
- 捕获结果：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/formal-pages-15-expand-merge-20260413/page15-expand-merge-headless-v9-arrowfix.png`
- 捕获备注：本轮主要是修正 `.scl.csv -> merge-b` 这条线在正式页里的箭头朝向；其余布局与 v8 保持一致。

## Blockers

- 几何硬 blocker 已解除：当前 `crossingCount = 0`、`nodePierceCount = 0`、`edgeOverlapCount = 0`。
- `browser-api stage` 仍超时，所以本轮截图只能以 `headless-stage fallback` 归档。

## 2026-04-12 几何重评

> 这一节是本轮真实运行出来的 mechanical review，不伪造新的 blind critic 分数。上面的 Art Critic / Geometry Critic 表可视为历史记录。

| 指标 | 分数 / 数值 | 说明 |
| --- | --- | --- |
| `mechanicalScore` | `4.0/10` | 总分维持不变，但扣分结构明显更健康了：不再是 detour 和扭曲，而主要剩在局部拥挤。 |
| `stageLayout` | `7/10` | 电脑左移、手机右移后，左右边距收缩到 `52px`，外轮廓略紧。 |
| `layoutDensity` | `4/10` | 最小节点间距进一步提到 `25px`，但仍低于 `48px` 的舒适阈值。 |
| `balance` | `4/10` | 右侧手机仍然显著更重，`leftRightMassDelta = 48.1%`。 |
| `lineStraightness` | `0/10` | 主线弯折已经继续降到 `6`，`phone-to-rec` 的 detour 也清零，但评分仍被中部一条长绕行线拖住。 |
| `crossingRisk` | `9/10` | `crossingCount = 0`、`nodePierceCount = 0`、`edgeOverlapCount = 0`。 |
| `primaryLineClarity` | `0/10` | 主脊柱更稳了，但 `.scl.csv -> merge-b` 仍是一条较长的中部占道线，削弱了主线的一口气感。 |
| `minRenderedFontPx` | `26px` | 当前字体仍满足本页最基本可读性。 |
| `minInternalPadding` | `8px` | `stable.upipelinecache` 内边距保住了下限，没有再贴边。 |

### 本轮几何结论

1. 这一版继续满足你刚补的两个点：`cook` 已经回到左下横线中点，顶部 `rec` 右侧回流也减少了一次不必要扭折。
2. `crossingCount = 0`、`nodePierceCount = 0`、`edgeOverlapCount = 0` 仍然保持清零，而且主线弯折已经从 `8` 再降到 `6`，`detourEdgeCount` 也降到了 `0`。
3. 当前主要问题已经换成更局部的两处：一是 `.scl.csv -> merge-b` 仍然偏长，二是 `Phone` 左侧两个落点的竞争还存在。

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `8.2/10` | `8.1/10` | 这版整体已经明显更“能顺着讲”，顶层回流和中部 stable spine 的分工更清楚，但重心仍略偏右。 | `8.2/10` |
| `模块空间结构 Review` | `8.4/10` | `7.6/10` | 平衡后的 spine 让 stable 这一层终于像独立模块，不过 `.scl.csv -> merge-b` 还在中部占出一条较长车道。 | `8.0/10` |
| `单节点与单边 Review` | `7.8/10` | `7.2/10` | `cook` 与 `phone-to-rec` 都明显顺了，但 `Phone` 左边界双入口竞争和 `.scl.csv -> merge-b` 长绕行仍是主要扣分点。 | `7.5/10` |

## Top 3 fixes

1. 优先继续处理 `.scl.csv -> merge-b`：它现在已经是最明显的一条长绕行线，并且占用了中部横向视线。
2. 继续打开 `Phone` 左边界的入口关系，`stable -> Phone` 和 `bytecode -> Phone` 目前仍然会竞争注意力。
3. 如果下一轮继续抛光，优先继续给左侧 `expand + .scl.csv` 合流区更多呼吸感，而不是再改主脊柱。

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `computer-shell` | 电脑侧整理端，同时保持回流落点身份。 | 连续承接 page14 `computer-shell` | page14 device carrier | continuity 成立，当前页职责扩展为 stable 源头。 | `8.0/10` | 左侧锚点稳定。 | `7.5/10` |
| `split-a` | cook 分叉点继续作为原始来源。 | 连续承接 page14 `split-a` | page14 junction carrier | continuity 成立。 | `8.0/10` | 退成背景后合理。 | `7.3/10` |
| `scl-node` | 既是 cook 输出，也是 stable merge 输入。 | 连续承接 page14 `scl-node` | page14 artifact carrier | continuity 成立，当前页语义增加。 | `8.0/10` | 左移后与 `merge-a` 的因果关系终于更直接，但仍会被 `merge-b` 那一带牵出额外关注。 | `7.3/10` |
| `bytecode-node` | 保留原始 runtime 输入路径。 | 连续承接 page14 `bytecode-node` | page14 bytecode carrier | continuity 成立。 | `8.0/10` | 当前页存在感应再退后一点。 | `6.8/10` |
| `rec-node` | 回流中继继续保留。 | 连续承接 page14 `rec-node` | page14 artifact carrier | continuity 成立。 | `8.0/10` | 顶部回流可读，但已不是主角。 | `7.4/10` |
| `merge-a` | `expand` 前置合流点。 | 无 | 新建 merge carrier | 新节点成立，补足了 `stablepc.csv` 的上游逻辑。 | `7.8/10` | 现在已经和 `.scl.csv`、`stablepc.csv` 真正共轴，角色更清楚，但左中区域仍不算松。 | `7.4/10` |
| `stablepc-node` | `expand` 产生的 stable 输入之一。 | 无 | 新建 artifact carrier | 新节点语义明确。 | `7.8/10` | 终于落在 stable 主脊柱上，读起来像流程中继而不是飘在上面的支块。 | `7.3/10` |
| `merge-b` | stable 系统的合流点。 | 无 | 新建 merge carrier | 新节点成立。 | `7.6/10` | 水平主干已经成立，但它和 `Phone` 左侧入口仍共同挤占中右区域注意力。 | `7.0/10` |
| `stable-upipe-node` | 最终 stable 产物。 | 无 | 新建 artifact carrier | 新节点语义正确，是当前页 stable band 的终点。 | `7.8/10` | 文本区干净、位置稳定，但体量与右侧 Phone 靠得较近，导致右半边重心偏强。 | `7.7/10` |
| `phone-shell` | 最终使用端。 | 连续承接 page14 `phone-shell` | page14 device carrier | continuity 成立。 | `8.0/10` | 作为终点身份成立，但和 `stable.upipelinecache`、两条入射线一起把右侧压得比左侧更重。 | `6.9/10` |
| `phone-runtime-vertex` | 使用端内部输入起点。 | 连续承接 page14 `phone-runtime-vertex` | page14 runtime carrier | continuity 成立。 | `8.0/10` | 当前页应继续退居背景。 | `7.4/10` |
| `phone-runtime-gpu` | 使用端内部执行焦点。 | 连续承接 page14 `phone-runtime-gpu` | page14 runtime carrier | continuity 成立。 | `8.0/10` | 因右侧拥挤，焦点被外层 stable 结构压弱。 | `7.1/10` |
| `phone-runtime-pixels` | 使用端输出终点。 | 连续承接 page14 `phone-runtime-pixels` | page14 runtime carrier | continuity 成立。 | `8.0/10` | 继续退居背景是合理的。 | `7.3/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `computer-to-a` | cook 主干继续保留。 | 连续承接 page14 `computer-to-a` | page14 edge carrier | continuity 成立。 | `8.0/10` | `cook` 标签终于落回左下横线中点，读起来更像这条底部主干的标签而不是游离文字。 | `7.8/10` |
| `a-to-scl` | metadata 输出支线继续保留。 | 连续承接 page14 `a-to-scl` | page14 edge carrier | continuity 成立。 | `8.0/10` | 位置开始受 stable 区挤压。 | `6.9/10` |
| `a-to-bytecode` | bytecode 输出支线继续保留。 | 连续承接 page14 `a-to-bytecode` | page14 edge carrier | continuity 成立。 | `8.0/10` | 与新 stable 区同时存在后，存在感偏多。 | `6.8/10` |
| `bytecode-to-phone` | 原始 bytecode 路径继续保留。 | 连续承接 page14 `bytecode-to-phone` | page14 landing carrier | continuity 成立。 | `8.0/10` | 本页应更明确退为旧路径背景。 | `6.5/10` |
| `phone-to-rec` | 回流上半段继续保留。 | 连续承接 page14 `phone-to-rec` | page14 return carrier | continuity 成立。 | `8.0/10` | 现在已经简化成一次上去一次横进，detour 清零，终于不再显得扭。 | `8.1/10` |
| `rec-to-computer` | 回流下半段继续保留。 | 连续承接 page14 `rec-to-computer` | page14 return carrier | continuity 成立。 | `8.0/10` | 顶部路径仍偏长。 | `7.1/10` |
| `computer-to-expand-merge` | `Computer` 先接入前置 `+`。 | 无 | 新建 stable edge carrier | 新边语义明确。 | `7.8/10` | 经过重新分配段长后，这一段和后续几段的节奏更接近，不再一长一短。 | `7.8/10` |
| `scl-to-expand-merge` | `.scl.csv` 垂直补入 `expand` 前置 `+`。 | 无 | 新建 stable edge carrier | 新边把你要求的逻辑补上了。 | `7.8/10` | 垂直直连是这次最明显的提升之一，终于不再扭。 | `8.0/10` |
| `expand-merge-to-stablepc` | 前置 `+` 输出 `stablepc.csv`。 | 无 | 新建 stable edge carrier | 语义成立。 | `7.8/10` | `expand` 标签回到这条横向主干后，逻辑终于与视觉对齐。 | `7.8/10` |
| `stablepc-to-b` | stablepc 输入 merge。 | 无 | 新建 stable edge carrier | 与 `scl-to-b` 一起解释 merge。 | `7.6/10` | 重新均分后，这条线已经不再显得短促，水平 spine 的节奏更自然。 | `7.8/10` |
| `scl-to-b` | `.scl.csv` 输入 merge。 | 无 | 新建 stable edge carrier | 语义正确。 | `7.6/10` | 它仍是当前 stable 区最明显的一条长绕行线，虽然逻辑正确，但还没有压到最顺。 | `7.1/10` |
| `b-to-stable-upipe` | merge 之后形成 stable 产物。 | 无 | 新建 stable edge carrier | 主因果正确。 | `7.6/10` | 主干已经压直，读者能一眼看见 stable 在这里生成。 | `7.8/10` |
| `stable-upipe-to-phone` | stable 结果最终回到 `Phone`。 | 无 | 新建 stable landing carrier | 闭环收束语义正确。 | `7.8/10` | 纯水平 landing 很干净，但它和右侧其他入口一起加重了 Phone 左边缘的拥挤感。 | `7.4/10` |
| `runtime-edge-1` | 使用端内部输入链继续保留。 | 连续承接 page14 `runtime-edge-1` | page14 runtime edge carrier | continuity 成立。 | `8.0/10` | 背景保留合理。 | `7.3/10` |
| `runtime-edge-2` | 使用端内部输出链继续保留。 | 连续承接 page14 `runtime-edge-2` | page14 runtime edge carrier | continuity 成立。 | `8.0/10` | 背景保留合理。 | `7.3/10` |

## 审批状态

`正式页已完成代码改动、测试和 headless-stage fallback 校对。本轮已经把 cook 标签归回底部横线中点、把 stable spine 段长重新均衡，并把 phone-to-rec 压成更直接的回流；下一轮如果继续做，优先应继续处理 .scl.csv -> merge-b 和 Phone 左侧双入口，而不是再推翻这套拓扑。`
