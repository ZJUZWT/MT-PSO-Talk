# 第 15 页草图镜像：`page15-r1`

> 2026-04-12 更新：第 10-15 页整章节拍整体后移一页。当前页现在同时承担“电脑 expand 生成 stable 产物”和“stable 再回到手机”的双重职责；下方旧评分仅保留作历史参考，待新一轮 critic/capture 刷新。

## 页面目标

把原来拆开的“电脑侧 expand 生成 stable”与“stable 再回到手机”压进同一页，让“手机收集、电脑整理、稳定结果再回到手机使用”的闭环真正讲完整。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | 电脑 `expand`，stable 再回到手机，闭环完成 |
| 页面副标题 | 最后把电脑侧的 `expand`、`stablepc.csv`、`stable.upipelinecache` 和回到手机的那一条线压进同一页，让收集、构建、再使用真正闭合。 |
| 当前页核心信息 | 电脑负责整理成 stable，stable 结果再回到手机，闭环才算完成。 |
| 与上一页关系 | 承接 page14 的回流半圈，在同一页补出 stable 构建与最终 landing。 |
| 这一页新增了什么 | `expand`、`stablepc.csv`、`stable.upipelinecache`、`stable -> Phone`。 |
| 这一页删除了什么 | 无。 |
| 这一页保留了什么 | `cook` split、`bytecode -> Phone`、`Phone -> rec -> Computer`。 |
| 需要强调的元素 | `expand`、`stablepc.csv`、`stable.upipelinecache -> Phone`、闭环完成。 |
| 暂不实现 / 暂不出现 | 新文件类型或额外支线。 |

## 当前截图

![Page15 sketch capture](../../ignore/page15-shifted-capture/page15-shifted.png)

- 当前 URL：`http://127.0.0.1:4178/?step=page_15`
- 当前 capture 来源：浏览器 API page 导出

## 最新事实层

- `Receiver plane`：`Full PSO loop`
- `Primary line`：`Computer -> A -> .ushaderbytecode / .scl.csv -> Phone, plus expand -> stablepc.csv / .scl.csv -> B -> stable.upipelinecache -> Phone`
- `Mechanical score`：`5.2 / 10`
- `Verdict`：`Clean the primary route before critic pass`
- `Node piercings / bad endpoint anchors / overflow`：`0 / 0 / 0`
- `Minimum node gap`：`22px`
- `Minimum rendered font`：`28px (~21pt)`
- `Directional margins`：`T 186 / R 30 / B 117 / L 112 px`
- `Cramped nodes`：`b:22px, stable-upipe:22px, scl:27px`
- `Worst anchor offsets`：`computer-to-a: from Computer right +11px`、`computer-to-stablepc: from Computer right +11px`、`bytecode-to-phone: to Phone left +10px`
- `Top fixes`：继续拉直主路线；把最紧节点栈从 `22px` 拉开；把最小外边距从 `30px` 拉回安全区。

## 浏览器文字探针

- 浏览器实测最小内部 padding：`8.0px`
- `stable.upipelinecache`：`tightest 8.7px`
- `stablepc.csv`：`tightest 8.0px`
- `Phone`：`tightest 9.4px`
- `Computer`：`tightest 9.0px`

## 当前页面评分

| 维度 | Art Critic | Geometry Critic | 综合 |
| --- | --- | --- | --- |
| `整体布局 Review` | `7.5/10` | `7.8/10` | `7.7/10` |
| `模块空间结构 Review` | `6.9/10` | `7.4/10` | `7.2/10` |
| `单节点与单边 Review` | `6.7/10` | `6.6/10` | `6.7/10` |

## 关键模块剧本与 Review

| 模块 | 中文剧本描述 | Review 观点 | 综合分数 |
| --- | --- | --- | --- |
| `stable -> Phone` 最终 landing | 用一条新的手机落点，把 stable 产物送回包体侧。 | 闭环终于讲完整，但这也是全页最容易挤的最后一段。 | `6.9/10` |
| Phone 双入口 | `bytecode` 与 `stable` 两条路径分别命中手机左侧上下两个落点。 | 双入口的因果分流已经成立，但仍然需要更大的外部气口。 | `6.8/10` |
| stable band + merge zone | 让 stable 结果不是凭空出现，而是先过 `B` 再进入设备端。 | 概念成立，主要问题仍是右侧读感偏挤。 | `6.7/10` |
| 整体闭环 | 下方直达、上方回流、右侧再使用，三段终于闭合。 | 是目前这组草图里最“完整”的一页。 | `7.5/10` |

## 当前问题与下一轮重点

- 这一页现在逻辑完整、几何硬错误清零，已经能稳定代表“闭环完成”。
- 如果后面还有时间，最值得继续抛光的是右侧 landing 区的呼吸感，而不是再改流程结构。
