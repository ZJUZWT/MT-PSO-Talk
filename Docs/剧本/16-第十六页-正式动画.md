# 第 16 页正式动画：`page_16`

> 文件类型：`正式动画页 / placeholder onepage`
> Step / Sketch：`page_16` / `-`
> 上一页：`page_15`


## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `6.9/10` | 来源：`三层 Review 总表` 的 `整体布局 Review` 行 |
| Node 分数 | `7.0/10` | 来源：`节点剧本与 Review 表` 综合分数列均值 |
| Edge 分数 | `6.9/10` | 来源：`边剧本与 Review 表` 综合分数列均值 |
| 动画分数 | `8.5/10` | 自动 probe：page_15 -> page_16，durationVerdict=in_range |

## Page goal

- 在 page15 把 `Phone -> rec -> Computer` 回流讲清之后，先暂停主流程，只解释为什么电脑侧必须先做 `expand`。
- 这页不讲 `build`，只回答一个问题：旧版本运行时记录里的 `ShaderHash + State`，为什么不能直接拿来给当前版本使用。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `为什么需要 expand` |
| 页面副标题 | `expand` 不是在“加工一下文件”，而是在把旧 `ShaderHash` 记录提升回跨版本还能理解的 stable 语义。 |
| 当前页核心信息 | `历史 rec.upipelinecache + 历史 scl.csv -> stablepc.csv` |
| 页面类型 | `placeholder onepage 内容页` |
| 与上一页关系 | 承接 page15 的 runtime 回流，暂停主流程推进，专门解释为什么要借助历史版本 `scl.csv`。 |
| 与下一页 narrative 关系 | 下一页再解释 `build` 如何结合当前版本 `scl.csv`，把 stable 语义重新落回当前版本。 |
| 这一页新增了什么 | `rec.upipelinecache`、`历史 scl.csv`、`stablepc.csv`、stable 含义说明、`H_old -> K1 / K2` 示例。 |
| 这一页删除了什么 | 不讲 `build`，不讲 `stable.upipelinecache`，不回到 `Phone`。 |
| 这一页保留了什么 | 保留 page15 主舞台作为背景记忆，不改主线骨架。 |
| 需要强调的元素 | `历史 scl.csv`、`stablepc.csv`、`ShaderStableKey 关键参数表`、`expand`。 |
| 暂不实现 / 暂不出现 | `stable.upipelinecache`、`build`、`stable -> Phone`、precompile。 |

## Onepage 内容结构

| 区块 | 内容 | 讲述重点 |
| --- | --- | --- |
| 顶部标题行 | 标题 | 强调这一页只回答 `expand`。 |
| 左上卡 | `rec.upipelinecache` | 旧版本运行时只记到 `ShaderHash + State`。 |
| 左下卡 | `历史 scl.csv` | 旧版本 `StableKey <-> Hash` 字典。 |
| 右上卡 | `stablepc.csv` | `expand` 之后得到的 `StableKey + State`。 |
| 右下卡 | ShaderStableKey 参数表 | 展示关键字段与占位示例，待补真实样例值。 |
| 底部 example strip | `H_old -> K1 / K2` | 一条旧 hash 记录可能被提升成两条 stable 语义记录。 |

## 插页转场规则

1. page15 主舞台整体缩小并淡出。
2. `rec.upipelinecache`、`历史 scl.csv` 和 `stablepc.csv` 以简图方式浮入，中间只保留一个 `expand` 合流点。
3. 离场时 onepage 整体缩小并淡出，不直接画 stable 闭环。

## 版式与内容约束

- 必须一眼读出 `历史 rec + 历史 scl -> stablepc`，不能混入 `build`。
- `历史` 这个限定词必须明确出现，不允许把 `scl.csv` 读成当前版本。
- 右下信息块必须是 `ShaderStableKey` 参数表，不再使用二元结论文案。
- 例子必须短，但要能让观众理解为什么一条旧 hash 记录可能展开成多条 stable 记录。

## Acceptance checklist

1. 一眼读得出这一页在解释 `expand`，不是在解释整个电脑侧加工流程。
2. `历史 rec.upipelinecache` 和 `历史 scl.csv` 的关系必须清楚。
3. 能单独指出 `ShaderStableKey` 至少 4 个关键参数字段。
4. 观众应该能带走一句话：`expand` 是把旧 hash 记录提升回 stable 语义。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | `page15 -> page16` 前半段 | page15 主舞台缩小并淡出。 | previous flow frame | 先把回流拓扑退到背景。 |
| 2 | `page15 -> page16` 中段 | `rec.upipelinecache`、`历史 scl.csv`、`stablepc.csv` 和合流点浮入。 | three file cards、expand merge | 点名要借助历史字典，把旧 hash 提升成 stable 语义。 |
| 3 | `page15 -> page16` 后半段 | stable 含义说明和底部 example strip 落稳。 | stable note、example strip | 用 `H_old -> K1 / K2` 收束这一页。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 主体形态 | 三卡 + 一条说明卡的轻量 onepage |
| 背景策略 | page15 缩小淡出后作为透明背景下的记忆接点 |
| 内容块数量 | `3` 张主卡 + `1` 张 stable 含义说明 + `1` 条 example strip |
| 动画节奏 | 先缩主流程，再进 `expand` 图解，最后落 stable 含义和 example |
| 风险点 | 如果把 `build` 也塞进来，这页会重新变得拥挤且难讲。 |

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260413-190949/review-capture.png)

- 当前 URL：`http://127.0.0.1:4179/?variant=bus-clean&step=page_16&debugFrame=875`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260413-190949/review-capture.txt`

## Blockers

- `H_old / K1 / K2` 虽然已经从主链里拆出来，但仍更像右下角脚注，不像一个真正自成一块的示例区。
- 主链 `rec -> expand -> stablepc` 和右下示例区在同一中右势能带里，主次关系还不够果断，观众仍可能把示例当装饰跳过。
- `stablepc.csv` 下方竖线、右侧 stable 解释文字、浅色背景带之间仍然太贴，存在“线挤文本区”的停帧压力。

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `6.2/10` | `7.6/10` | 大结构仍能读懂，但“示例区”还没有真正站住，主链与示例区在同一画面势能里互相抢解释权。 | `6.9/10` |
| `模块空间结构 Review` | `5.8/10` | `7.0/10` | `历史输入 -> expand -> stable 输出` 主链成立，但右下示例区仍像附注岛，和主链的映射关系不够硬。 | `6.4/10` |
| `单节点与单边 Review` | `7.0/10` | `6.6/10` | `H_old / K1 / K2` 已从“埋在主链里”提升到“可见”，但线条主次、示例边界和文本车道仍在拖分。 | `6.8/10` |

## Top 3 fixes

1. 把 `H_old / K1 / K2` 做成真正的示例卡片或旁注容器，不再只是几个漂浮 token。
2. 拉开示例区与主链的留白断层，并建立更明确的映射关系，让观众一眼知道这是“为什么需要 expand”的例子，而不是装饰。
3. 重排 `stablepc.csv` 下方的竖线与右侧文字区，避免主干线继续压着解释文本车道。

## 2026-04-19 补丁（Step 19 缺项修复）

- 恢复了 `历史 runtime`、`历史映射` 两个阶段标签，避免观众把 `rec/scl` 误读成当前版本数据。
- 右下信息块改为 `ShaderStableKey` 参数表（字段 + 占位示例列），去掉原有二元结论文案，等待后续补入真实例子。
- 过渡时长复核：`page_15 -> page_16` 当前 `107` 帧（`1.783s`），`durationVerdict=in_range`，无需改动锚点。

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `rec-node` | page15 回流到电脑后的历史 runtime 观察。 | 连续承接 page15 `rec.upipelinecache` 语义 | 文件胶囊 | 观众仍能认出它来自回流半圈，continuity 成立。 | `8.0/10` | 盒体本身稳定，但右侧支线关系还没有完全锁死。 | `7.5/10` |
| `hold-node` | `H_old` 作为旧 hash 的例子标签。 | 新增示例节点 | 小胶囊标签 | 允许新载体，不要求上一页 continuity。 | `8.0/10` | 现在终于不再埋进主链，但仍像脚注 token，还不像独立示例起点。 | `7.0/10` |
| `k-branch` | `K1 / K2` 表示被提升出的 stable 语义分支。 | 新增示例分支 | 两个小胶囊 + 分叉线 | 新增元素，不涉及 continuity。 | `8.0/10` | 正交分叉比之前清楚，但整体仍缺少“示例区边界”，容易被读成附注。 | `6.6/10` |
| `scl-node` | 历史 `.scl.csv` 映射字典。 | 与 page15 `.scl.csv` 语义相关，但版本切为历史，允许新载体 | 文件胶囊 | 版本切换已在文案里说明，不构成 continuity 伪装。 | `7.6/10` | 位置清楚，但与上方 `rec`、右侧 `stablepc` 的分区关系仍偏松。 | `7.0/10` |
| `expand-merge` | 中央 `+` 与 `expand` 共同承担提升动作。 | 新增动作节点 | 合流圆点 + pill | 动作节点新增合理。 | `8.0/10` | 功能能懂，但线条进入点还不够工程制图式整齐。 | `6.9/10` |
| `stablepc-node` | `expand` 之后的 `stablepc.csv` 输出。 | 新增输出节点 | 强调文件胶囊 | 作为本页主产物，语义明确。 | `8.3/10` | 是当前页最稳的节点，但周边空白过大，独自承压。 | `7.8/10` |
| `stablekey-table` | `ShaderStableKey` 关键字段表。 | 新增参数说明 | 右下表格说明 | 新增元素合理。 | `8.0/10` | 表格可读性提升，后续需要替换占位示例为真实案例。 | `7.2/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `rec-to-expand` | 历史 runtime 观察进入 `expand`。 | 连续承接 page15 回流后的电脑侧处理起点 | 橙色主链箭头 | 能读成 page15 之后的下一步。 | `8.0/10` | 主向是清楚的，但落点还不够正交、居中、像素对齐。 | `7.0/10` |
| `scl-to-expand` | 历史映射字典参与 `expand`。 | 与 page15 `.scl.csv` 语义相关，但此页切为历史版本 | 橙色辅链箭头 | 版本切换已讲明，不算 continuity 错误。 | `7.5/10` | 进入点合理，但与 `rec-to-expand` 还没有形成更漂亮的双路合流。 | `6.8/10` |
| `expand-to-stablepc` | `expand` 输出 `stablepc.csv`。 | 新增输出边 | 橙色主链箭头 | 新边合理。 | `8.0/10` | 是本页最清楚的一段边，但仍可更直、更像单脊柱输出。 | `7.4/10` |
| `hold-to-k` | `H_old` 被提升成 `K1 / K2` 的分叉示意。 | 新增示例边 | 灰色分叉线 | 新增示例关系合理。 | `8.0/10` | 已经比上一版更像结构分叉，但仍缺少容器和主次层级支撑。 | `6.4/10` |

## 审批状态

`review 账本已恢复并同步到最新截图。当前页比上一版更好找节点了，但仍未通过美感验收：现在的核心问题不再是“节点消失”，而是示例区仍像脚注、主链与示例区主次关系不够硬。`
