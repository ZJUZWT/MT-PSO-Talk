# 第 17 页正式动画：`page_17`

> 文件类型：`正式动画页 / placeholder onepage`
> Step / Sketch：`page_17` / `-`
> 上一页：`page_16`


## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `7.5/10` | 来源：`三层 Review 总表` 的 `整体布局 Review` 行 |
| Node 分数 | `6.9/10` | 来源：`节点剧本与 Review 表` 综合分数列均值 |
| Edge 分数 | `6.7/10` | 来源：`边剧本与 Review 表` 综合分数列均值 |
| 动画分数 | `8.5/10` | 自动 probe：page_16 -> page_17，durationVerdict=in_range |

## Page goal

- 在 page16 把 `expand` 讲清之后，再单独解释 `build` 如何回到当前版本。
- 这页只回答：为什么有了 `stablepc.csv` 之后，仍然需要当前版本的 `scl.csv`，才能生成 `stable.upipelinecache`。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `build 如何回到当前版本` |
| 页面副标题 | `build` 不是重复 `expand`，而是把 stable 语义重新投影回当前版本可用的 hash 世界。 |
| 当前页核心信息 | `stablepc.csv + 当前 scl.csv -> stable.upipelinecache` |
| 页面类型 | `placeholder onepage 内容页` |
| 与上一页关系 | 承接 page16 的 stable 语义页，继续停在说明层，不回主舞台。 |
| 与下一页 narrative 关系 | 下一页回到主舞台，正式把 `stable.upipelinecache -> Phone` 接回去。 |
| 这一页新增了什么 | `当前 scl.csv`、`stable.upipelinecache`、当前版本映射示例。 |
| 这一页删除了什么 | 不画 `expand`，不画 `Phone`，不讲 precompile。 |
| 这一页保留了什么 | 保留 stable 语义的认知前提，但换成当前版本映射视角。 |
| 需要强调的元素 | `当前 scl.csv`、`stable.upipelinecache`、`build`、当前版本。 |
| 暂不实现 / 暂不出现 | `stable -> Phone`、precompile、compile-count。 |

## Onepage 内容结构

| 区块 | 内容 | 讲述重点 |
| --- | --- | --- |
| 顶部标题行 | 标题 | 强调这一页只回答 `build`。 |
| 左上卡 | `stablepc.csv` | 输入已经是 `StableKey + State`。 |
| 左下卡 | `当前 scl.csv` | 当前版本 `StableKey <-> Hash` 字典。 |
| 右上卡 | `stable.upipelinecache` | 当前版本真正可用的缓存表达。 |
| 右下卡 | build 含义说明 | stable 语义不能直接运行，必须落回当前版本 hash。 |
| 底部 example strip | `K1 / K2 -> H_a / H_b` | 两条 stable 语义记录会重新映射成两条当前版本记录。 |

## 插页转场规则

1. page16 onepage 缩小并淡出。
2. `stablepc.csv`、`当前 scl.csv`、`stable.upipelinecache` 和中间 `build` 合流点浮入。
3. 离场时 onepage 整体缩小并淡出，为主舞台闭环页让路。

## 版式与内容约束

- 必须一眼读出 `stablepc + 当前 scl -> stable.upipelinecache`。
- `当前` 这个限定词必须明确出现，不能再和 page16 的历史字典混淆。
- 这页不能再出现 `expand` 字样，避免观众把两步重新揉回一起。
- 例子必须说明 stable 语义如何重新映射成当前版本 hash。

## Acceptance checklist

1. 一眼读得出这一页在解释 `build`，不是在解释 `expand`。
2. `当前 scl.csv` 必须和历史版本字典形成明确区分。
3. 观众应该能带走一句话：`build` 是把 stable 语义落回当前版本。
4. 页面必须保持轻量，不要长成主舞台续画。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | `page16 -> page17` 前半段 | page16 onepage 缩小并淡出。 | previous explain page | 先结束 `expand` 解释。 |
| 2 | `page16 -> page17` 中段 | `stablepc.csv`、`当前 scl.csv`、`stable.upipelinecache` 与 `build` 合流点浮入。 | three file cards、build merge | 点名 stable 语义还要重新回到当前版本。 |
| 3 | `page16 -> page17` 后半段 | build 含义说明和底部 example strip 落稳。 | build note、example strip | 用 `K1 / K2 -> H_a / H_b` 收束。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 主体形态 | 三卡 + 一条说明卡的轻量 onepage |
| 背景策略 | page16 说明页缩退后再进入 build 图解 |
| 内容块数量 | `3` 张主卡 + `1` 张 build 含义说明 + `1` 条 example strip |
| 动画节奏 | 先收 expand，再进 build 图解，最后落当前版本示例 |
| 风险点 | 如果把 `Phone` 和主流程线条也拉回来，这页会重新丢失“build-only”的清晰度。 |

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260413-1827-rework5/page17-rework5.png)

- 当前 URL：`http://127.0.0.1:4179/?variant=bus-clean&step=page_17&debugFrame=970`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260413-1827-rework5/page17-rework5.txt`

## Blockers

- 中央 `build` 合流点、上方 `stablepc.csv`、右侧 `stable.upipelinecache` 还没有形成一条足够硬的主脊柱，因果方向清楚但骨架还偏软。
- 下方 `.scl.csv` 到合流点的水平线过长，`StableKey -> 当前 Hash` 这个 pill 像是贴在线上，而不是线本身的结构标签。
- 右侧大画布虽然干净，但仍有 explanation board 的空板感；`stable.upipelinecache` 的两行标题也略挤。

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `7.0/10` | `8.0/10` | 左中右逻辑已经成立，比 page16 更干净，但主脊柱还不够“焊死”，仍有说明板味道。 | `7.5/10` |
| `模块空间结构 Review` | `6.8/10` | `7.2/10` | `stablepc + 当前 scl -> stable.upipelinecache` 能读懂，但 `.scl.csv` 这一路过长、合流点不够像真正的机械中心。 | `7.0/10` |
| `单节点与单边 Review` | `7.2/10` | `6.6/10` | 节点风格基本统一，但 line label、下路由长度、输出框行距与对齐仍在拖分。 | `6.9/10` |

## Top 3 fixes

1. 把 `stablepc.csv -> build -> stable.upipelinecache` 收成更刚性的水平主脊柱，减少“上来一下、再横出去”的松散感。
2. 缩短 `.scl.csv` 的横向路径，让 `StableKey -> 当前 Hash` 变成真正的线标签，而不是一块临时贴上去的说明 pill。
3. 给右侧输出区更强的结果落点，缓解 `stable.upipelinecache` 标题拥挤和右半边空板的问题。

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `stablepc-node` | 承接 page16 的 `stablepc.csv`，作为 build 的输入。 | 连续承接 page16 主产物 | 文件胶囊 | 语义连续清楚，观众能认出它来自上一页。 | `8.5/10` | 框体本身稳，但与中间 `build` 的骨架连接还不够硬朗。 | `7.5/10` |
| `current-scl-node` | 当前版本 `.scl.csv` 映射字典。 | 与上一页 `历史 scl.csv` 同类但版本切换为当前，允许新载体 | 文件胶囊 | 当前/历史区分清楚，不会误读成上一页同一张字典卡。 | `7.8/10` | 位置清楚，但离中心过远，拖长了下方连线路径。 | `6.7/10` |
| `build-merge` | 中央 `+` 与 `build` 一起承担“回到当前版本”的动作。 | 新增动作节点 | 合流圆点 + build pill | 新动作节点合理。 | `8.0/10` | 能懂，但还不像一个真正锁定全页节奏的机械中心。 | `6.8/10` |
| `mapping-pill` | `StableKey -> 当前 Hash` 的线标签。 | 新增线标签 | ArrowLabelPill | 新元素合理。 | `8.0/10` | 现在更像贴片说明，和下方线路没有完全融合。 | `6.3/10` |
| `stable-cache-node` | `stable.upipelinecache` 作为当前版本可直接消费的结果。 | 与后续 page18 主线目标语义连续 | 强调文件胶囊 | 语义准确，是这页的落点。 | `8.4/10` | 两行标题略挤，右侧承重太高，气质偏“结果卡”但抓眼还不够。 | `7.0/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `stablepc-to-build` | `stablepc.csv` 进入 `build`。 | 连续承接 page16 `stablepc` 之后的下一步 | 橙色主链箭头 | 读得出它是上一页之后的承接动作。 | `8.4/10` | 方向清楚，但还可以更严格地收成一条中心主线。 | `7.0/10` |
| `scl-to-build` | 当前 `.scl.csv` 参与 `build`。 | 与上一页 `scl` 同类但切为当前版本 | 橙色辅链箭头 | 版本切换已被文字说明，continuity 不构成问题。 | `7.7/10` | 当前页最低分主边之一，水平段偏长，进入中心的姿态不够利落。 | `6.1/10` |
| `build-to-stablecache` | `build` 输出 `stable.upipelinecache`。 | 与下一页 page18 的稳定缓存主线连续 | 橙色主链箭头 | 能作为下一页的清晰接点。 | `8.5/10` | 输出方向清楚，但结果框偏大导致线看起来略短、略像贴近式连接。 | `7.1/10` |

## 审批状态

`review 账本已恢复。当前页叙事已经比 page16 清楚，但仍未通过美感验收：综合卡在 7 分附近，主要问题是主脊柱不够硬、下方映射线过长、右侧输出区仍有 explanation board 味道。`
