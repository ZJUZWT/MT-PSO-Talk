# 第 17 页正式动画：`page_17`

> 文件类型：`正式动画页`
> Step / Sketch：`page_17` / `-`
> 上一页：`page_16`

## Page goal

- 在 page16 解释完 `expand / build` 和文件语义后，回到主流程，把 stable band 与 `stable -> Phone` 的最终落线正式补齐。
- 这页负责把“收集、电脑整理、再回到手机使用”的闭环真正讲完整。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `expand` 生成 stable，最终回到 `Phone` |
| 页面副标题 | 在已经解释过文件含义的前提下，把 `expand`、`stablepc.csv`、`stable.upipelinecache` 和回到 `Phone` 的那一条线压回主舞台，让闭环真正闭合。 |
| 当前页核心信息 | `Computer` 负责整理成 stable，stable 结果再回到 `Phone`，闭环才算完成。 |
| 与上一页关系 | page16 先解释 `expand / build` 和四类文件的语义，这一页回到主舞台，把 stable band 和最终 landing 正式长出来。 |
| 与下一页 narrative 关系 | 下一页暂停主流程，专门解释 precompile 如何消费 stable cache，以及 `OpenGL` / `Metal` 的 compile-count 差异。 |
| 这一页新增了什么 | `expand` 前置 `+`、`stablepc.csv`、stable merge node、`stable.upipelinecache`、`stable -> Phone`。 |
| 这一页删除了什么 | 无。 |
| 这一页保留了什么 | `cook` split、`.ushaderbytecode -> Phone`、`Phone -> rec -> Computer`。 |
| 需要强调的元素 | `expand`、`stablepc.csv`、`stable.upipelinecache -> Phone`、闭环完成。 |
| 暂不实现 / 暂不出现 | precompile、compile-count 比较、额外支线。 |

## Node inventory

| id | text | semantic role | visual kind | emphasis |
| --- | --- | --- | --- | --- |
| `computer-shell` | `Computer` | 电脑侧整理端 | device shell | 高 |
| `split-a` | 无文字 | `cook` 分叉点 | junction circle | 中 |
| `scl-node` | `.scl.csv` | metadata 支线 | artifact pill | 中 |
| `bytecode-node` | `.ushaderbytecode` | 原始 runtime 输入产物 | artifact pill | 中 |
| `rec-node` | `rec.upipelinecache` | 顶部回流中继 | artifact pill | 中 |
| `merge-a` | `+` | `expand` 前置合流点 | merge point | 高 |
| `stablepc-node` | `stablepc.csv` | `expand` 阶段产物 | artifact pill | 高 |
| `merge-b` | `+` | stable merge junction | merge point | 高 |
| `stable-upipe-node` | `stable.upipelinecache` | 稳定产物 | artifact pill | 高 |
| `phone-shell` | `Phone` | 最终使用端 | device shell | 高 |
| `phone-runtime-vertex` | 无文字 | runtime 输入起点 | icon in phone | 中 |
| `phone-runtime-gpu` | `GPU` | runtime 核心执行点 | text in phone | 中 |
| `phone-runtime-pixels` | 无文字 | runtime 输出终点 | pixel icon in phone | 中 |

## Edge inventory

| id | from | to | meaning | route grammar | emphasis |
| --- | --- | --- | --- | --- | --- |
| `computer-to-a` | `computer-shell` | `split-a` | `cook` 主干 | rounded orthogonal | 低 |
| `a-to-scl` | `split-a` | `scl-node` | metadata 输出支线 | rounded orthogonal | 低 |
| `a-to-bytecode` | `split-a` | `bytecode-node` | bytecode 输出支线 | rounded orthogonal | 低 |
| `bytecode-to-phone` | `bytecode-node` | `phone-shell` | 原始 bytecode 进入 runtime 的旧路径 | rounded orthogonal | 低 |
| `phone-to-rec` | `phone-shell` | `rec-node` | runtime 回流上半段 | rounded orthogonal | 中 |
| `rec-to-computer` | `rec-node` | `computer-shell` | runtime 回流下半段 | rounded orthogonal | 中 |
| `computer-to-expand-merge` | `computer-shell` | `merge-a` | `Computer` 接入前置合流点 | horizontal | 高 |
| `scl-to-expand-merge` | `scl-node` | `merge-a` | `.scl.csv` 补入 `expand` 前置 `+` | rounded orthogonal | 高 |
| `expand-merge-to-stablepc` | `merge-a` | `stablepc-node` | `expand` 合流后产出 `stablepc.csv` | horizontal | 高 |
| `stablepc-to-b` | `stablepc-node` | `merge-b` | stablepc 输入 merge | horizontal / slight rounded | 高 |
| `scl-to-b` | `scl-node` | `merge-b` | `.scl.csv` 输入 merge | rounded orthogonal | 高 |
| `b-to-stable-upipe` | `merge-b` | `stable-upipe-node` | merge 之后形成 stable 产物 | horizontal / slight rounded | 高 |
| `stable-upipe-to-phone` | `stable-upipe-node` | `phone-shell` | stable 结果回到 `Phone` 使用 | horizontal | 高 |
| `runtime-edge-1` | `phone-runtime-vertex` | `phone-runtime-gpu` | runtime 内部输入链路 | 竖直向下 | 低 |
| `runtime-edge-2` | `phone-runtime-gpu` | `phone-runtime-pixels` | runtime 内部输出链路 | 竖直向下 | 低 |

## Spatial constraints

- 当前页必须一眼读出“回流 -> expand -> merge -> stable -> Phone”的完整闭环。
- `expand` 左侧必须明确存在前置 `+`，不能读成 `Computer` 直接生成 `stablepc.csv`。
- `stablepc.csv`、`merge-b`、`stable.upipelinecache` 必须读成同一 stable band。
- `Phone` 仍是最终使用端，不应被 stable 区压成背景。

## Assembly order

1. 让 page16 插页整体缩小并淡出。
2. 短暂恢复 page15 的 runtime return 主舞台，作为回到主流程的记忆接点。
3. 从 `Computer` 长出 `expand` 到前置 `merge-a`，同时让 `.scl.csv` 补入 `merge-a`。
4. 让 `merge-a` 输出 `stablepc.csv`，再由 `merge-b` 形成 `stable.upipelinecache`。
5. 从 stable 产物拉出最终 landing 回到 `Phone`。

## Acceptance checklist

1. 当前页必须一眼读出完整闭环，而不是“又多了几条线”。
2. `merge-a`、`stablepc.csv`、`merge-b`、`stable.upipelinecache` 必须读成同一 stable band。
3. `.scl.csv` 必须至少有一条明确的边接入 `expand` 前置 `+`。
4. `Phone` 必须保持“最终使用端”的身份。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | `page16 -> page17` 前半段 | onepage 插页缩小淡出，短暂恢复 page15 runtime return 静态帧。 | placeholder board、restored return frame | 先把说明页收回，再回到主舞台。 |
| 2 | `page16 -> page17` 中段 | `expand` 前置 `+`、`stablepc.csv`、merge、stable band 依次长出。 | `merge-a`、`stablepc-node`、`merge-b`、`stable-upipe-node` | 讲清电脑侧是如何把回流记录整理成 stable。 |
| 3 | `page16 -> page17` 后半段 | stable 产物回到 `Phone`，闭环收束。 | `stable-upipe-to-phone` | 这一页必须读成闭环真正完成。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | `merge-a`、`stablepc-node`、`merge-b`、`stable-upipe-node` 及对应 stable edges |
| 需要复用的元素 | page15 的回流半圈、bytecode landing、`Phone` 内部 trio |
| 转场要求 | page16 离场后先短暂回到 page15 主舞台，再播放 stable band 的新增动作 |
| 风险点 | 如果 stable band 被挤到右侧，会让 `Phone` 终点读感过重且闭环层次变乱。 |

## 审批状态

`旧 page15 语义已顺延为新 page17。当前页负责把 stable 闭环完成，precompile 与 API compile-count 差异留到下一张插页解释。`
