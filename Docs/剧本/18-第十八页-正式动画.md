# 第 18 页正式动画：`page_18`

> 文件类型：`正式动画页 / placeholder onepage`
> Step / Sketch：`page_18` / `-`
> 上一页：`page_17`

## Page goal

- 在 page17 把 stable 闭环讲完整之后，插入第三张 onepage 内容页，解释 precompile 如何消费 `stable.upipelinecache`，以及为什么不同 API 的 compile-count 差异会很大。
- 这页负责把“闭环之后还会发生什么”讲清，而不是继续改动当前拓扑。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | precompile 如何消费 stable cache |
| 页面副标题 | `stable.upipelinecache` 只是稳定输入，真正的预编译工作量仍然强烈取决于 API 的显式程度。 |
| 当前页核心信息 | precompile 消费的是稳定化后的 cache 表达，但 `OpenGL` 和 `Metal` 的 compile-count 会因为 API 显式性差异而明显不同。 |
| 页面类型 | `placeholder onepage 内容页` |
| 与上一页关系 | 承接 page17 的 stable 闭环，暂停主流程推进，专门解释 precompile 输入、处理过程和 compile-count 差异。 |
| 与下一段 narrative 关系 | 为后续 precompile / benchmark / API 讨论提供解释桥梁，不再改动当前闭环骨架。 |
| 这一页新增了什么 | precompile input / process / comparison 三栏内容和底部 takeaway strip。 |
| 这一页删除了什么 | 不新增新的闭环 routing，不重新讨论 `expand / build` 细节。 |
| 这一页保留了什么 | 保留 page17 主舞台作为淡化 memory plate，不改变当前章节主骨架。 |
| 需要强调的元素 | `stable.upipelinecache`、precompile process、`OpenGL` vs `Metal` compile-count。 |
| 暂不实现 / 暂不出现 | 新的拓扑节点、额外文件类型、更多 runtime 支线。 |

## Onepage 内容结构

| 区块 | 内容 | 讲述重点 |
| --- | --- | --- |
| 顶部标题行 | 标题 + 一句 thesis | 强调 precompile 消费 stable 输入，但工作量仍和 API 显式度强相关。 |
| 左侧内容卡 | precompile input: `stable.upipelinecache` | 讲清输入是稳定 cache 表达，不是原始 runtime hash。 |
| 中部内容卡 | `StableKey / State -> build pipeline -> precompile` | 讲清处理过程。 |
| 右侧内容卡 | compile-count comparison | `OpenGL` 通常更少，`Metal` 通常更多。 |
| 底部 takeaway strip | explicit APIs cost more upfront but give stronger control | 收束成一句更偏章节结论的话。 |

## 插页转场规则

1. 主流程页整体缩小并淡出，短暂保留为背景 memory plate。
2. onepage 内容板从中心轻微放大浮入，标题、三张内容卡、底部 takeaway 分两拍落位。
3. 离场时 onepage 整体缩小并淡出，短暂恢复上一张主流程静态帧，再进入下一段章节内容。

## 版式与内容约束

- 三栏必须形成清晰的 `input -> process -> comparison` 顺序，不能只是三块孤立文字卡。
- `OpenGL` 和 `Metal` 的对比要落在 compile target 数量与 API 显式性，而不是泛泛性能判断。
- 底部 takeaway 要比标题更偏“结论”，让观众带着一句完整判断离开这页。
- 页面仍要保持 explanation board 的轻量感，不能回退成主流程拓扑。

## Acceptance checklist

1. 一眼读得出 precompile 消费的是 `stable.upipelinecache`。
2. `input -> process -> comparison` 的三段关系必须清楚。
3. `OpenGL` 通常更少、`Metal` 通常更多的 compile-count 结论必须能单独复述。
4. 页面要像章节解释板，而不是 page17 的续画。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | `page17 -> page18` 前半段 | `page17` 主舞台整体缩小并淡出，保留成浅色 memory plate。 | previous flow frame | 先把闭环主舞台退到背景。 |
| 2 | `page17 -> page18` 中段 | 标题、三张内容卡从中心浮入。 | title row、three content cards | 点名 stable cache 之后还有 precompile 这一步。 |
| 3 | `page17 -> page18` 后半段 | 底部 takeaway strip 落稳。 | takeaway strip | 用 API 显式性与 compile-count 的差异收束。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 主体形态 | 三栏 onepage explanation board |
| 背景策略 | page17 缩小淡出后保留为 memory plate |
| 内容块数量 | `3` 张内容卡 + `1` 条底部 takeaway |
| 动画节奏 | 先缩主流程，再进三栏内容，最后压出 takeaway |
| 风险点 | 如果右侧 comparison 卡写成大段论述，这页会失去“一眼比较”的解释效率。 |

## 审批状态

`placeholder onepage 文档版已定。当前页只解释 precompile 与 compile-count 差异，不再改动 page17 已经完成的 stable 闭环。`
