# 第 18 页正式动画：`page_18`

> 文件类型：`正式动画页`
> Step / Sketch：`page_18` / `-`
> 上一页：`page_17`


## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `6.8/10` | 来源：`三层 Review 总表` 的 `整体布局 Review` 行 |
| Node 分数 | `6.2/10` | 来源：`节点剧本与 Review 表` 综合分数列均值 |
| Edge 分数 | `6.0/10` | 来源：`边剧本与 Review 表` 综合分数列均值 |
| 动画分数 | `8.5/10` | 自动 probe：page_17 -> page_18，durationVerdict=in_range |

## Page goal

- 在 page16 和 page17 分别讲清 `expand` 与 `build` 之后，回到主流程舞台，把 `stable.upipelinecache` 真正送回 `Phone`。
- 这页负责把“收集、整理、再使用”的闭环正式闭合。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `stable` 再回到手机，闭环完成 |
| 页面副标题 | 解释页已经讲完，这一页只把 stable band 和 `stable -> Phone` 正式压回主舞台。 |
| 当前页核心信息 | `Computer` 整理出 `stable.upipelinecache`，再回到 `Phone` 使用，闭环才算完成。 |
| 与上一页关系 | page17 只解释 `build`，这一页回到主舞台，把 stable band 与最终 landing 正式长出来。 |
| 与下一页 narrative 关系 | 下一页暂停主流程，专门解释 precompile 如何消费 `stable.upipelinecache`。 |
| 这一页新增了什么 | `expand` 前置 `+`、`stablepc.csv`、stable merge、`stable.upipelinecache`、`stable -> Phone`。 |
| 这一页删除了什么 | 不再出现 onepage 说明卡。 |
| 这一页保留了什么 | `cook` split、`.ushaderbytecode -> Phone`、`Phone -> rec -> Computer`。 |
| 需要强调的元素 | `expand`、`stablepc.csv`、`stable.upipelinecache -> Phone`、闭环完成。 |
| 暂不实现 / 暂不出现 | precompile、compile-count 比较、额外支线。 |

## Node inventory

| id | text | semantic role | visual kind | emphasis |
| --- | --- | --- | --- | --- |
| `computer-shell` | `Computer` | 电脑侧整理端 | device shell | 高 |
| `split-a` | 无文字 | `cook` 分叉点 | junction circle | 中 |
| `scl-node` | `.scl.csv` | metadata 支线 | artifact pill | 中 |
| `bytecode-node` | `.ushaderbytecode` | runtime 输入产物 | artifact pill | 中 |
| `rec-node` | `rec.upipelinecache` | 顶部回流中继 | artifact pill | 中 |
| `merge-a` | `+` | `expand` 前置合流点 | merge point | 高 |
| `stablepc-node` | `stablepc.csv` | stable 中间产物 | artifact pill | 高 |
| `merge-b` | `+` | build 后段 merge | merge point | 高 |
| `stable-upipe-node` | `stable.upipelinecache` | 最终稳定产物 | artifact pill | 高 |
| `phone-shell` | `Phone` | 最终使用端 | device shell | 高 |

## Edge inventory

| id | from | to | meaning | route grammar | emphasis |
| --- | --- | --- | --- | --- | --- |
| `computer-to-a` | `computer-shell` | `split-a` | `cook` 主干 | rounded orthogonal | 低 |
| `a-to-scl` | `split-a` | `scl-node` | metadata 支线 | rounded orthogonal | 低 |
| `a-to-bytecode` | `split-a` | `bytecode-node` | bytecode 支线 | rounded orthogonal | 低 |
| `bytecode-to-phone` | `bytecode-node` | `phone-shell` | 原始 runtime 输入路径 | rounded orthogonal | 低 |
| `phone-to-rec` | `phone-shell` | `rec-node` | runtime 回流上半段 | rounded orthogonal | 中 |
| `rec-to-computer` | `rec-node` | `computer-shell` | runtime 回流下半段 | rounded orthogonal | 中 |
| `computer-to-expand-merge` | `computer-shell` | `merge-a` | 电脑接入 stable band | horizontal | 高 |
| `scl-to-expand-merge` | `scl-node` | `merge-a` | `.scl.csv` 接入前置 `+` | rounded orthogonal | 高 |
| `expand-merge-to-stablepc` | `merge-a` | `stablepc-node` | `expand` 输出 | horizontal | 高 |
| `stablepc-to-b` | `stablepc-node` | `merge-b` | build merge 输入 | horizontal | 高 |
| `scl-to-b` | `scl-node` | `merge-b` | `.scl.csv` 接入 build merge | rounded orthogonal | 高 |
| `b-to-stable-upipe` | `merge-b` | `stable-upipe-node` | 最终 stable 产物 | horizontal | 高 |
| `stable-upipe-to-phone` | `stable-upipe-node` | `phone-shell` | stable 结果回到 `Phone` | horizontal | 高 |

## Spatial constraints

- 当前页必须一眼读出“回流 -> stable band -> Phone”的完整闭环。
- `Phone` 仍是最终使用端，不应被 stable 区压成背景。
- `stablepc.csv`、`merge-b`、`stable.upipelinecache` 必须读成同一 stable band。
- 所有 stable 新增动作都要建立在 page15 的回流主舞台上，而不是重新生成一张新图。

## Assembly order

1. 让 page17 onepage 整体缩小并淡出。
2. 短暂恢复 page15 runtime return 静态帧。
3. 从 `Computer` 长出 stable band：`merge-a`、`stablepc.csv`、`merge-b`、`stable.upipelinecache`。
4. 从 `stable.upipelinecache` 拉出最终 landing 回到 `Phone`。

## Acceptance checklist

1. 当前页必须一眼读出完整闭环，而不是“又多了几条线”。
2. stable band 必须读成同一组动作，而不是散落的新节点。
3. `stable.upipelinecache -> Phone` 必须是这一页最明确的收束。
4. 这一页必须像主舞台页，而不是说明页。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | `page17 -> page18` 前半段 | page17 onepage 缩小淡出，短暂恢复 page15 主舞台。 | placeholder board、restored return frame | 先把 build 解释页收回。 |
| 2 | `page17 -> page18` 中段 | stable band 依次长出。 | `merge-a`、`stablepc-node`、`merge-b`、`stable-upipe-node` | 讲清 stable 结果在电脑侧成形。 |
| 3 | `page17 -> page18` 后半段 | `stable.upipelinecache -> Phone` 补上，闭环完成。 | `stable-upipe-to-phone` | 这一页必须读成完整闭合。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | stable band 与 `stable -> Phone` |
| 需要复用的元素 | page15 的回流半圈、bytecode landing、`Phone` 内部 trio |
| 转场要求 | page17 离场后先短暂回到 page15 主舞台，再播放 stable band 的新增动作 |
| 风险点 | 如果 stable band 被挤成附属支线，闭环完成感会明显变弱。 |

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260413-162648/page18-fix.png)

- 当前 URL：`http://127.0.0.1:4179/?step=page_18`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260413-162648/page18-fix.txt`

## Blockers

- 这一页该读成 `stable.upipelinecache -> Phone` 的闭环收束，但顶部 `rec` 回路、中部 stable band、底部 bytecode 链三条车道同时抢戏，没有唯一主脊柱。
- 右侧 `Phone` 左边界同时接收 `stable` 和 `.ushaderbytecode` 两条入射，落点过近，闭环完成的终局感被挤成了“线路堆在一起”。
- 左侧 source 端锚点太弱，当前 `Computer` 壳体缺字，加上两枚 `+` 与 `.scl.csv` 区域挤压，让电脑侧整理动作没有稳稳立住。

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `6.8/10` | `6.8/10` | 大结构能读懂，但主焦点跑偏，页面没有把“闭环完成”锁成唯一第一视觉。 | `6.8/10` |
| `模块空间结构 Review` | `6.1/10` | `6.2/10` | `expand -> stablepc -> stable.upipelinecache -> Phone` 这条链是对的，但旧回流和旧 bytecode 线路都没有退成背景。 | `6.2/10` |
| `单节点与单边 Review` | `5.4/10` | `6.0/10` | 单节点和局部路由拖分明显，尤其是 `Computer` 缺字、右侧双入射拥堵、`.scl.csv` 周边转折过密。 | `5.7/10` |

## Top 3 fixes

1. 重新确立唯一主脊柱，把 `stablepc.csv -> + -> stable.upipelinecache -> Phone` 压成全页最强线路，顶部 `rec` 和底部旧 bytecode 链必须降权。
2. 重做 `Phone` 左侧 landing，把 `stable` 与 `.ushaderbytecode` 两条入射拉开垂直间距，并让 stable 落线更直、更终局。
3. 修正电脑侧锚点与 merge 区：恢复 `Computer` 明确标签，清理 `.scl.csv -> +` 的转折密度，让左半边真正像“整理端”而不是一团过路节点。

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `computer-shell` | 电脑侧整理端，是 stable band 的起点。 | 与更早主线里的 `Computer` 连续 | 设备壳体 | 语义本该非常稳，但当前壳体缺字，continuity 识别被削弱。 | `6.6/10` | 当前最低分主节点之一，source 端像空壳，锚点感不成立。 | `4.8/10` |
| `rec-node` | 顶部回流中继，提醒 stable band 来自运行时收集。 | 连续承接 page15 回流语义 | 文件胶囊 | continuity 清楚。 | `8.3/10` | 这页里它抢戏过多，横向回路比应有的更像主线。 | `6.0/10` |
| `scl-node` | `.scl.csv` 参与电脑侧整理。 | 连续承接前页同名文件语义 | 文件胶囊 | 语义连续清楚。 | `8.0/10` | 位置尚可，但夹在两个 `+` 与多条转折线之间，局部拥挤。 | `5.9/10` |
| `stablepc-node` | `expand` 阶段中间产物。 | 承接 page17 的 `stablepc.csv` 语义 | 文件胶囊 | 是全页最清楚的连续节点之一。 | `8.5/10` | 中段链路成立，节点本身较稳。 | `7.1/10` |
| `stable-upipe-node` | 最终稳定产物，是送回手机前的最后落点。 | 连续承接 page17 输出并连接 page19 输入 | 强调文件胶囊 | continuity 明确。 | `8.8/10` | 当前页最稳的主角节点，但还没有压过所有旧路径。 | `7.5/10` |
| `phone-shell` | 最终使用端，闭环必须收束到这里。 | 与前序主线 `Phone` 连续 | 设备壳体 | 语义连续成立。 | `8.4/10` | 右侧体量过重，且同时承担两条入射和内部骨架，收束感被冲散。 | `6.0/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `rec-to-computer` | 回流最终落回电脑侧，准备整理。 | 连续承接 page15 / page17 的回流记忆 | 橙色长回路箭头 | continuity 很清楚。 | `8.5/10` | 过长且过醒目，这页应退为背景辅助线。 | `5.8/10` |
| `computer-to-expand-merge` | 电脑侧接入 stable band。 | 新增到本页的主线启动边 | 橙色主链箭头 | 主向成立。 | `8.0/10` | 由于 source 端锚点偏弱，这条边的起势不够稳。 | `5.9/10` |
| `scl-to-expand-merge` | `.scl.csv` 补入第一个 `+`。 | 连续承接前页 `.scl.csv` 参与整理的语义 | 橙色辅链箭头 | 关系成立。 | `7.9/10` | 转折密、入射区挤，是当前页局部最乱的一段边之一。 | `5.4/10` |
| `expand-merge-to-stablepc` | `expand` 输出 `stablepc.csv`。 | 与前页 `expand` 解释连续 | 橙色主链箭头 | continuity 清楚。 | `8.2/10` | 中段最顺的一段边，基本成立。 | `7.1/10` |
| `stablepc-to-stable-upipe` | `stablepc.csv` 与第二个 `+`、`stable.upipelinecache` 组成稳定产物主带。 | 连续承接 page17 build 结果 | 橙色主链箭头 | 是这页真正应该成为主脊柱的部分。 | `8.6/10` | 结构是对的，但还没有被压成唯一第一主线。 | `6.8/10` |
| `stable-upipe-to-phone` | stable 结果回到 `Phone` 使用。 | 本页最关键的闭环完成边 | 橙色 landing 箭头 | 语义极强，是本页终局。 | `8.8/10` | 当前却不够果断，落点被 bytecode 入射挤压，终局感不足。 | `5.6/10` |
| `bytecode-to-phone` | 旧 bytecode 路径仍然保留在图中。 | 连续承接更早主线 | 橙色旧路径箭头 | 语义本身没错。 | `7.8/10` | 这页里存在感过高，和 stable landing 抢同一侧边界。 | `5.2/10` |

## 审批状态

`review 账本已恢复。当前页尚未通过美感验收：最大问题不是“逻辑错”，而是闭环收束没有压成唯一主线，电脑侧锚点偏弱，右侧 `Phone` 入口过于拥堵。`
