# 第 19 页正式动画：`page_19`

> 文件类型：`正式动画页 / placeholder onepage`
> Step / Sketch：`page_19` / `-`
> 上一页：`page_18_img`


## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `8.0/10` | 来源：`三层 Review 总表` 的 `整体布局 Review` 行 |
| Node 分数 | `6.8/10` | 来源：`节点剧本与 Review 表` 综合分数列均值 |
| Edge 分数 | `6.9/10` | 来源：`边剧本与 Review 表` 综合分数列均值 |
| 动画分数 | `8.8/10` | 自动 probe：`page_18_img -> page_19`，时长 `1.8s`，durationVerdict=`in_range` |

## 过渡时长复核（2026-04-19）

- 目标过渡：`Step 22 -> Step 23`（对应 `page_18_img -> page_19`）
- 调整前：`2086 -> 2146`（`60` 帧，`1.0s`）
- 调整后：`2086 -> 2194`（`108` 帧，`1.8s`）
- 约束策略：只拉长该段并整体顺延后续锚点，不压缩原有后续段时长

## Page goal

- 在 page18 把 stable 闭环讲完整之后，插入最后一张 onepage 内容页，解释 precompile 如何消费 `stable.upipelinecache`。
- 这页负责把“闭环之后还会发生什么”讲清，而不是继续改动当前拓扑。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `预编译如何发生` |
| 页面副标题 | `stable.upipelinecache` 只是稳定输入，真正的预编译工作量仍然强烈取决于 API 的显式程度。 |
| 当前页核心信息 | precompile 消费的是稳定化后的 cache 表达，但 `OpenGL` 和 `Metal` 的 compile-count 会因为 API 显式性差异而明显不同。 |
| 页面类型 | `placeholder onepage 内容页` |
| 与上一页关系 | 承接 page18 的 stable 闭环，暂停主流程推进，专门解释 precompile 输入、处理过程和 compile-count 差异。 |
| 与下一段 narrative 关系 | 为后续 precompile / benchmark / API 讨论提供解释桥梁，不再改动当前闭环骨架。 |
| 这一页新增了什么 | precompile input / process / comparison 三栏内容和底部 takeaway strip。 |
| 这一页删除了什么 | 不新增新的闭环 routing，不重新讨论 `expand / build` 细节。 |
| 这一页保留了什么 | 保留 page18 主舞台作为淡化背景记忆，不改变当前章节主骨架。 |
| 需要强调的元素 | `stable.upipelinecache`、precompile process、`OpenGL` vs `Metal` compile-count。 |
| 暂不实现 / 暂不出现 | 新的拓扑节点、额外文件类型、更多 runtime 支线。 |

## Onepage 内容结构

| 区块 | 内容 | 讲述重点 |
| --- | --- | --- |
| 顶部标题行 | 标题 + 一句 thesis | 强调 precompile 消费 stable 输入，但工作量仍和 API 显式度强相关。 |
| 左侧内容卡 | precompile input: `stable.upipelinecache` | 讲清输入是稳定 cache 表达，不是原始 runtime hash。 |
| 中部内容卡 | `StableKey / State -> build pipeline -> precompile` | 讲清处理过程。 |
| 右侧内容卡 | compile-count comparison | `OpenGL` 通常更少，`Metal` 通常更多。 |
| 底部 takeaway strip | explicit APIs cost more upfront but give stronger control | 收束成一句更偏章节结论的话。 |

## 插页转场规则

1. 主流程页整体缩小并淡出。
2. onepage 内容板从中心轻微放大浮入，标题、三张内容卡、底部 takeaway 分两拍落位。
3. 离场时 onepage 整体缩小并淡出，为下一段章节内容留白。

## 版式与内容约束

- 三栏必须形成清晰的 `input -> process -> comparison` 顺序，不能只是三块孤立文字卡。
- `OpenGL` 和 `Metal` 的对比要落在 compile target 数量与 API 显式性，而不是泛泛性能判断。
- 底部 takeaway 要比标题更偏“结论”，让观众带着一句完整判断离开这页。
- 页面仍要保持 explanation board 的轻量感，不能回退成主流程拓扑。

## Acceptance checklist

1. 一眼读得出 precompile 消费的是 `stable.upipelinecache`。
2. `input -> process -> comparison` 的三段关系必须清楚。
3. `OpenGL` 通常更少、`Metal` 通常更多的 compile-count 结论必须能单独复述。
4. 页面要像章节解释板，而不是 page18 的续画。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | `page18_img -> page19` 前半段 | `page18_img` 证据页整体淡出，主舞台记忆层轻量接入。 | previous flow frame | 先把证据页收回。 |
| 2 | `page18 -> page19` 中段 | 标题、三张内容卡从中心浮入。 | title row、three content cards | 点名 stable cache 之后还有 precompile 这一步。 |
| 3 | `page18 -> page19` 后半段 | 底部 takeaway strip 落稳。 | takeaway strip | 用 API 显式性与 compile-count 的差异收束。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 主体形态 | 三栏 onepage explanation board |
| 背景策略 | page18 缩小淡出后保留为透明背景下的记忆接点 |
| 内容块数量 | `3` 张内容卡 + `1` 条底部 takeaway |
| 动画节奏 | 先缩主流程，再进三栏内容，最后压出 takeaway |
| 风险点 | 如果右侧 comparison 卡写成大段论述，这页会失去“一眼比较”的解释效率。 |

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260413-1815-rework3/page19-rework3.png)

- 当前 URL：`http://127.0.0.1:4179/?variant=bus-clean&step=page_19&debugFrame=1110`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260413-1815-rework3/page19-rework3.txt`

## Blockers

- 这页已经不是纯粹的拓扑图问题，但 still 有明显 explanation board 既视感，左侧讲稿与右侧三卡板的语气没有收成一个中心论点。
- `Precompile` 作为中心枢纽还不够强，四向连接齐了，但不像全页唯一锚点；左右 `OpenGL` / `Metal` 卡片的样式力度也不完全对称。
- `.ushaderbytecode` 盒体的文字与框体呼吸感偏差，视觉上有点“切边”或发脏，拖低节点美感分。

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `7.4/10` | `8.5/10` | 纵向主轴和左右对照关系都成立，整体比前两张稳，但板书感还没有被完全消掉。 | `8.0/10` |
| `模块空间结构 Review` | `7.2/10` | `8.0/10` | `stable.upipelinecache -> Precompile -> OpenGL/Metal/.ushaderbytecode` 的模块关系清楚，但中心锚点与对比模块的戏剧性仍偏弱。 | `7.6/10` |
| `单节点与单边 Review` | `6.3/10` | `7.4/10` | 单条边已经比较克制，但节点细节质量不齐，尤其是 `.ushaderbytecode` 和左右卡片的语气差异明显。 | `6.9/10` |

## Top 3 fixes

1. 强化 `Precompile` 的中心锚点地位，让四向辐射更像围绕同一个机械核心，而不是四个说明对象各自对着中间。
2. 统一 `OpenGL` 和 `Metal` 两张卡的视觉语法，包括标题层级、内文密度、装饰点和轮廓力度，收紧左右对称。
3. 重做 `.ushaderbytecode` 的字号、行高、框内边距与垂直居中，去掉“切边发脏”的观感。

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `stable-cache-node` | 承接 page18 的 `stable.upipelinecache`，作为预编译输入。 | 连续承接 page18 电脑侧稳定缓存输出 | 强调文件胶囊 | continuity 清楚，观众能认出它来自上一页闭环。 | `8.8/10` | 位置稳，但和中心之间的情绪对比还不够强。 | `7.8/10` |
| `precompile-node` | 预编译中心动作。 | 新增动作节点 | 中心胶囊 | 新动作合理。 | `8.2/10` | 是本页骨架中心，但体量和气场还不够压住全页。 | `6.8/10` |
| `opengl-card` | OpenGL 一侧说明“compile target 更少”。 | 新增对比模块 | 左侧说明卡 | 新模块合理。 | `8.0/10` | 左卡比较稳，但信息组织仍偏像说明文字，不够像清晰结论块。 | `6.9/10` |
| `metal-card` | Metal 一侧说明“compile count 通常更高”。 | 新增对比模块 | 右侧说明卡 | 新模块合理。 | `8.0/10` | 与左卡的视觉风格略不统一，强调点更散。 | `6.6/10` |
| `bytecode-node` | `.ushaderbytecode` 作为下游编译产物或落地表达。 | 与更早主线里 `.ushaderbytecode` 语义相关，但与 page18 不直接连续 | 下方文件胶囊 | 新页回调旧语义合理。 | `7.5/10` | 当前最低分节点，文字离边界太近，垂直居中和框体呼吸感不足。 | `5.9/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `stable-to-precompile` | 稳定缓存输入预编译。 | 连续承接 page18 闭环后的输入关系 | 纵向主链箭头 | continuity 明确，是本页最自然的承接边。 | `8.8/10` | 这条边最稳，但仍可更强调为唯一主轴。 | `7.8/10` |
| `precompile-to-opengl` | 预编译对 OpenGL 的 compile target 分支。 | 新增解释边 | 左向灰色箭头 | 新边合理。 | `8.0/10` | 方向明确，但与右侧分支的视觉力度不完全对称。 | `6.8/10` |
| `precompile-to-metal` | 预编译对 Metal 的 compile count 分支。 | 新增解释边 | 右向灰色箭头 | 新边合理。 | `8.0/10` | 和左支路相比更靠近卡片，呼吸感略紧。 | `6.7/10` |
| `precompile-to-bytecode` | 预编译落到 `.ushaderbytecode`。 | 新增输出边 | 纵向橙色箭头 | 新边合理。 | `8.0/10` | 线本身清楚，但被下方低质节点拖低整体观感。 | `6.4/10` |

## 审批状态

`review 账本已恢复。当前页结构分不算低，但仍未通过审美验收：整体能看懂，问题集中在 explanation board 感过重、中心锚点不够强，以及 `.ushaderbytecode` 节点质量明显偏低。`
