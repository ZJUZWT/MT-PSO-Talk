# 第 19 页正式动画：`page_19`

> 文件类型：`正式动画页 / merged diagram`
> Step / Sketch：`page_19` / `page19-r1`
> 上一页：`page_18_img`
> 下一页：`page_21`


## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `6.7/10` | 几何事实：左侧改成 `stable` 父容器 + `UE PSO` 子树后，`leftRightMassDelta` 从约 `30%` 降到约 `20%`，但 `minNodeGap=32px` 仍低于 comfort floor。 |
| Node 分数 | `8.0/10` | 几何事实：`mechanicalScore=8.0`，`childOutOfBoundsCount=0`、`nodePierce=0`、`minRenderedFontPx=18`。 |
| Edge 分数 | `9.3/10` | 几何事实：`lineStraightness=9`、`crossingRisk=9`、`offCenterAnchorCount=0`，主链改成 `stable -> GPU` 后仍保持纯直线。 |
| 动画分数 | `in_range` | 自动 probe：`page_18_img -> page_19` 为 `in_range`；`page_19 -> page_21` 现在直接收口到 `75` 帧，标准时序 `in_range`。 |

## Page goal

- 把旧 `page_19` 和旧 `page_20` 压成一页，直接说明 `stable.upipelinecache` 如何在左侧展开成 `UE PSO` 子树，再经过 `page_14` 同款 `VertexData -> GPU -> Pixels` 竖链，落到不同图形 API 的内存态与本地缓存。
- 保留 `page_14` 那种“横向主轴 + GPU 竖列”的读图方式，并把 GPU 线路重新对齐到 `VertexData / GPU / Pixels` 三段。
- `Metal Binary Archive 2` 先保留问号，只作为待验证分支提及，不在本页讲成社区已经收敛的确定答案。
- 删除独立 `page_20` 后，不再额外保留删页停顿预算，`page_19 -> page_21` 直接进入下一页。

## 页面改动说明

| 项目 | 内容 |
| --- | --- |
| 新增页面/元素 | 新的合并版 `page_19` 正式图；左侧 `stable` 父容器内新增 `UE PSO` 内部标题与三条子节点；右侧保留 `内存中 PSO` / `硬盘中的 PSO` 与三条横向虚线缓存边。 |
| 移动节点 | `GPU` 整体左移，`内存中 PSO` 与 `硬盘中的 PSO` 两组整体左移；`disk-vk` 额外加宽 `4px` 以消除前台 Edge 的真实 text overflow。 |
| 文案调整 | `stable.upipelinecache` 回到单行父容器标题，并在容器内部单独保留 `UE PSO` 小标题；GPU 竖链只保留 `GPU` 文案，`VertexData` 与 `Pixels` 继续用纯图标表达；`Metal Binary Archive 2` 仍只保留为待验证分支。 |
| 新增 node | `stable` 父容器内的 `UE PSO` 内部标题。 |
| 删除 node | 左侧独立第二列 `UE PSO` 外层容器；独立的旧 `page_20` 前置页整体删除。 |
| 新增 edge | `stable -> GPU`、`GPU -> 内存中 PSO` 主链；三条从内存态指向硬盘态的虚线缓存边。 |
| 删除 edge | 原 `page_19 -> page_20 -> page_21` 的跨页叙事边不再作为独立页面存在。 |
| 替换过渡逻辑 | 删除旧 `page_20` 后，`page_19 -> page_21` 改成直接 handoff，不再保留独立删页停顿。 |

## 连续语义审计

| 连续对象 | 是否连续 | 处理方式 | 说明 |
| --- | --- | --- | --- |
| `stable.upipelinecache` | 是 | 允许重建 | 上一页 `page_18_img` 是证据/插图页，不保留共享渲染载体，先淡出图片再进入本页正式图。 |
| `stable -> precompile` 章节语义 | 是 | 合并进主中轴 | 本页直接把旧 `page_19` 的输入说明和旧 `page_20` 的过渡停顿一起吸收。 |
| `page_20` 的章节停顿 | 否 | 直接删除 | 本页已经吸收原 `page_20` 的信息，不再额外保留停顿预算。 |

## Node inventory

| node id | 页面文本 | 角色 | 当前渲染载体 |
| --- | --- | --- | --- |
| `stable-parent` | `stable.upipelinecache` / `UE PSO` / `PSO 1` / `PSO 2` / `PSO ...` | 左侧强父子树；stable 作为父容器承载 UE PSO 子树 | `StageBox` + 行内 `StageBox` |
| `gpu-stack` | `Vertex icon` / `GPU` / `Pixel icon` | 和 `page_14` 同语义的 GPU 竖向计算栈，但只保留中段文字 | `VertexTriangles` + `text` + `PixelGrid` |
| `mem-group` | `内存中 PSO` | API 运行时内存态容器 | `StageBox` |
| `mem-gl-node` | `OpenGL` / `Program Binary` | OpenGL 内存态 | `StageBox` + `StackedLabel` |
| `mem-vk-node` | `Vulkan` / `Pipeline Cache` | Vulkan 内存态 | `StageBox` + `StackedLabel` |
| `mem-metal-node` | `Metal` / `Binary Archive` / `系统管理` | Metal 内存态 | `StageBox` + `StackedLabel` |
| `disk-group` | `硬盘中的 PSO` | 本地缓存容器 | `StageBox` |
| `disk-gl-node` | `Program Binary` / `Cache` | OpenGL 落盘缓存 | `StageBox` + `StackedLabel` |
| `disk-vk-node` | `VulkanPSO.cache` | Vulkan 落盘缓存 | `StageBox` |
| `disk-metal-node` | `BinaryArchive` / `functions.data` | Metal 落盘缓存 | `StageBox` + `StackedLabel` |

## Edge inventory

| edge id | 起点 -> 终点 | 类型 | 当前渲染载体 |
| --- | --- | --- | --- |
| `stable-to-gpu` | `stable.upipelinecache(含 UE PSO 子树) -> GPU` | 主轴实线 | `StrokeArrow` |
| `gpu-vertex-to-text` | `VertexData -> GPU` | GPU 竖向内链 | `StrokeArrow` |
| `gpu-text-to-pixels` | `GPU -> Pixels` | GPU 竖向内链 | `StrokeArrow` |
| `gpu-to-mem` | `GPU -> 内存中 PSO` | 主轴实线 | `StrokeArrow` |
| `gl-to-disk` | `OpenGL / Program Binary -> Program Binary / Cache` | 横向虚线缓存边 | `StrokeArrow` |
| `vk-to-disk` | `Vulkan / Pipeline Cache -> VulkanPSO.cache` | 横向虚线缓存边 | `StrokeArrow` |
| `metal-to-disk` | `Metal / Binary Archive / 系统管理 -> BinaryArchive / functions.data` | 横向虚线缓存边 | `StrokeArrow` |

## 空间约束

- 主中轴：`mainAxisY = 360`
- 三个主模块中心：`stableCenterY = 360`、`memCenterY = 360`、`diskCenterY = 360`
- GPU 竖轴：`gpuVerticalAxisX = 498`
- 几何硬事实：`overlapCount=0`、`crossingCount=0`、`nodePierceCount=0`、`badEndpointCount=0`
- 树结构硬事实：`childOutOfBoundsCount=0`、`siblingOverlapCount=0`
- 结构间距：`minNodeGap=32`、`minMargin=44`、`topMargin=150`、`bottomMargin=150`
- 路由质量：`primaryLineBendCount=0`、`offCenterAnchorCount=0`
- 当前主要风险：blocker 已经清掉，当前剩余问题是 `layoutDensity=6.7` 与 `minNodeGap=32` 仍未达到 comfort floor；但 `leftRightMassDelta` 已降到约 `20%`，说明强父子树方向是对的。

## 元素样式约束

- `stable.upipelinecache` 改成强调型父容器，保持本页唯一强输入锚点。
- `UE PSO` 不再占独立一列，而是在 `stable` 容器内部作为橙色小标题出现。
- `stable`、`内存中 PSO`、`硬盘中的 PSO` 用同一套浅底分组框，保证层级语义一致。
- 组标题、行标题和 `GPU` 字号要比上一版更大，保证投影阅读时不会显得发虚。
- `VertexData` 与 `Pixels` 不再单独写字，只保留三角形和像素块图标。
- 三个 API 内存节点与三个硬盘节点全部横向对齐，箭头端点必须居中。
- `OpenGL` 与 `Program Binary` 合并为同一节点的上下双行文案，不拆成两个块。
- `Metal` 节点必须同时出现 `Binary Archive` 与 `系统管理`；硬盘节点必须同时出现 `BinaryArchive` 与 `functions.data`。
- 主轴箭头使用小号三角头，缓存边使用虚线，不允许出现折线、偏心锚点或 oversized arrowhead。

## Assembly order

1. 落左侧 `stable` 父容器与内部 `UE PSO` 子树。
2. 落中间 `VertexData -> GPU -> Pixels` 竖列。
3. 左移后的 `内存中 PSO` 三行与 `硬盘中的 PSO` 三行一起落位。
4. 补齐 `stable -> GPU -> 内存中 PSO` 主中轴。
5. 最后补三条到本地缓存的虚线。

## Acceptance checklist

1. 观众一眼能按 `stable(含 UE 子树) -> VertexData / GPU / Pixels -> 内存中 PSO -> 硬盘中的 PSO` 的顺序读完。
2. `GPU` 必须和 `page_14` 一样保持竖向三段，不允许横着摊开。
3. `OpenGL` 与 `Program Binary` 必须合并在同一节点；`Metal` 必须同时写出 `Binary Archive` 与 `系统管理`。
4. 三条本地缓存边必须是同一风格的横向虚线，并且终点统一落进 `硬盘中的 PSO` 容器。
5. 删除旧 `page_20` 后，`page_19 -> page_21` 不再保留额外停顿，直接进入下一页。

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260420-page19-strong-parent/page19-strong-parent.png)

- 当前 URL：`http://127.0.0.1:4174/?variant=bus-clean&step=page_19&debugFrame=2322`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260420-page19-strong-parent/page19-strong-parent.txt`
- Capture size：`1366 x 768`
- 前台 Edge probe：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/front-browser-page19-strong-parent/page19-probe.json`

## 参考链接与保留判断

- `Metal Binary Archive 2` 当前只作为“先打问号”的补充线索，不当作本页结论。
- 参考链接：[知乎：Metal Binary Archive 2](https://zhuanlan.zhihu.com/p/587988966)
- 当前口径：社区讨论并不稳定，所以这里维持保留态度，只先把 `Binary Archive / 系统管理 -> BinaryArchive / functions.data` 这条主线讲清楚。

## Facts & Metrics

| 指标 | 值 | 说明 |
| --- | --- | --- |
| `mechanicalScore` | `8.0/10` | 强父子树把旧左二列压回左单树后，机械综合分保持在稳定线以上。 |
| `verdict` | `Open the layout before critic pass` | 当前不再是穿刺/交叉 blocker，而是“仍有局部偏紧”的非 blocker 提示。 |
| `layoutDensity` | `6.7/10` | `minNodeGap` 从旧版 `26px` 提升到 `32px`，整体呼吸感更松，但还没到 `48px` comfort floor。 |
| `balance` | `6.7/10` | `leftRightMassDelta` 从约 `30%` 降到约 `20%`，右侧偏重明显缓解。 |
| `primaryLineClarity` | `9/10` | 主轴阅读链现在没有 blocker 干扰，读法很稳。 |
| `lineStraightness` | `9/10` | 主链与 GPU 竖链都保持直线，三条缓存虚线也继续水平。 |
| `crossingRisk` | `9/10` | `crossingCount=0` 且 `nodePierceCount=0`，风险已经回到安全区。 |
| `childOutOfBoundsCount` | `0` | `UE PSO` 三个子节点都稳定落在 `stable` 父容器内部。 |
| `frontProbeTightest` | `0.9px` | 前台 Edge 复核后，最紧文本已经收敛到 `disk-vk` 的 `0.9px` 左右 padding，不再负值穿边。 |
| `minRenderedFontPx` | `18px` | 当前最小可见字号比上一轮再抬了一档。 |

## 三层 Review 总表

| Review 层级 | 事实依据 | 综合判断 | 综合分数 |
| --- | --- | --- | --- |
| `整体布局 Review` | `stageLayout=9`、`top/bottomMargin=150`、`leftRightMassDelta≈20%` | 左树 + 右流的质量分布比旧版更均衡，但右半区仍略重。 | `6.7/10` |
| `模块空间结构 Review` | `layoutDensity=6.7`、`minNodeGap=32`、`childOutOfBoundsCount=0` | 树结构语义更清楚，右侧组块也更松，但距离 comfort floor 仍差一截。 | `6.7/10` |
| `单节点与单边 Review` | `nodePierceCount=0`、`badEndpointCount=0`、`crossingCount=0`、前台 Edge probe 无负 overflow | 节点与边保持干净，真实浏览器里最紧的 `disk-vk` 也已经回到正 padding。 | `9.3/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 几何 / 美感 Review | 综合判断 |
| --- | --- | --- | --- | --- | --- | --- |
| `stable-parent` | `stable.upipelinecache` 作为父容器，在其内部直接收住 `UE PSO` 子树。 | 承接 `page_18` 的 stable 章节语义 | `StageBox` + 内部标题 + 子节点 | 语义连续，但上一页是插图页，允许重建载体。 | 左侧不再需要第二列，真实浏览器里标题 tightest padding=`23.1px`。 | `通过` |
| `ue-subtree` | `PSO 1 / 2 / ...` 作为 stable 的三个孩子。 | 新增树内子层，不要求跨页共享载体 | `StageBox` 行节点 | 逻辑比旧版“独立 UE 列”更贴语义。 | `childOutOfBoundsCount=0`，三格都稳定落在父容器内。 | `通过` |
| `gpu-stack` | 承接 UE PSO 后的 `VertexData -> GPU -> Pixels` 竖列。 | 继承 `page_14` 的 GPU 表达习惯 | `VertexTriangles` + `text` + `PixelGrid` | 连续语义很强，是本页中心对照轴。 | 竖向链路已经回到 page14 的读法。 | `通过` |
| `mem-group` | 三种 API 的内存中 PSO 表达。 | 新增总结容器 | `StageBox` | 与本页讲述目标完全匹配。 | 纵向拉开后，三行之间的呼吸感明显更稳。 | `通过` |
| `mem-gl-node` | OpenGL 以 Program Binary 形式驻留内存。 | 新增 API 对照节点 | `StageBox` + `StackedLabel` | 文案满足“OpenGL 与 Program Binary 同节点”。 | 双行文本居中，空间最稳。 | `通过` |
| `mem-vk-node` | Vulkan 以内存态 Pipeline Cache 表达。 | 新增 API 对照节点 | `StageBox` + `StackedLabel` | 与 OpenGL / Metal 形成平行对照。 | 双行标签清楚。 | `通过` |
| `mem-metal-node` | Metal 由 Binary Archive 与系统管理共同表达。 | 新增 API 对照节点 | `StageBox` + `StackedLabel` | 满足用户要求，信息最完整。 | 现在仍是右半区最密的一格，但 padding 已经过线。 | `通过 / 后续可继续精修` |
| `disk-group` | 所有本地缓存统一落在一个硬盘容器里。 | 承接本地缓存语义 | `StageBox` | 合理替代了分散的多个末端缓存盒子。 | 这次不再出现机械 penetration blocker，当前主要只是整体仍偏紧。 | `通过` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 几何 / 美感 Review | 综合判断 |
| --- | --- | --- | --- | --- | --- | --- |
| `stable-to-gpu` | `stable` 父容器直接把 UE 子树结果送进 `VertexData / GPU / Pixels` 竖列。 | 承接 stable 输入语义 | `StrokeArrow` | 新主线清楚。 | 主轴仍保持纯水平直线，并且腾出了右侧左移空间。 | `通过` |
| `gpu-vertex-to-text` | `VertexData` 下压到 `GPU`。 | 继承 page14 GPU 竖列表达 | `StrokeArrow` | 连续成立。 | 垂直链条与文字重心对齐。 | `通过` |
| `gpu-text-to-pixels` | `GPU` 下压到 `Pixels`。 | 继承 page14 GPU 竖列表达 | `StrokeArrow` | 连续成立。 | 同轴、无偏心。 | `通过` |
| `gpu-to-mem` | GPU 产出进入 API 内存态。 | 本页主链新增 | `StrokeArrow` | 逻辑清楚。 | 是右半区的主入口，直线质量高。 | `通过` |
| `gl-to-disk` | OpenGL 内存态缓存到 Program Binary Cache。 | 本页新增缓存边 | `StrokeArrow` | 映射一一对应。 | 虚线节奏统一，端点居中。 | `通过` |
| `vk-to-disk` | Vulkan 内存态缓存到 `VulkanPSO.cache`。 | 本页新增缓存边 | `StrokeArrow` | 映射一一对应。 | 与上下两条虚线间距均匀。 | `通过` |
| `metal-to-disk` | Metal 内存态缓存到 `BinaryArchive / functions.data`。 | 本页新增缓存边 | `StrokeArrow` | 映射一一对应。 | 在最下层仍保持水平与居中，没有歪斜。 | `通过` |

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | `page_18_img -> page_19` 前段 | 证据页淡出，本页主中轴开始建立。 | `page_18_img` overlay、stable/UE PSO 左半带 | 先把插图页收走，再回到正式结构。 |
| 2 | 中段 | GPU、内存组、硬盘组依次落位。 | GPU 竖列、内存中 PSO、硬盘中的 PSO | 用 page14 同款 GPU 读法接住中间转折。 |
| 3 | 后段 | 主中轴和三条缓存虚线全部补齐。 | 主轴箭头、三条本地缓存虚线 | 这一步完成“两页压一页”的核心信息闭环。 |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_18_img -> page_19）

- 帧窗口：`2206 -> 2314`（`108` frames @ 60fps）
- 总时长：`1.800s`
- 时长评估：`in_range`（目标区间 `0.90s ~ 1.80s`）
- 调整建议：目标帧长 `108`，相对当前 `Δ+0` frames
- 标准时序评估：`in_range`（标准下限 `1.621s`，上限 `2.189s`）
- 标准调整建议：目标帧长 `108`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`1.621s`
- 标准允许上限：`2.189s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 120.0 | 120.0 | - |
| fade_out | 120.0 | 340.0 | 220.0 | fade_out_evidence_overlay |
| node_motion | 340.0 | 729.0 | 389.0 | move_left_band, move_gpu_memory_disk |
| edge_motion | 729.0 | 1221.5 | 492.5 | grow_main_spine, grow_disk_cache_routes |
| fade_in | 1221.5 | 1441.5 | 220.0 | fade_in_labels |
| hold_after | 1441.5 | 1621.5 | 180.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_evidence_overlay | fade_out | fade_out | - | 220.0 | formula |
| move_left_band | node_move | node_motion | distancePx=160.0 | 332.0 | formula |
| move_gpu_memory_disk | node_move | node_motion | distancePx=220.0 | 389.0 | formula |
| grow_main_spine | edge_grow | edge_motion | lengthPx=470.0 | 492.5 | formula |
| grow_disk_cache_routes | edge_grow | edge_motion | lengthPx=286.0 | 354.5 | formula |
| fade_in_labels | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 2206 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 2233 | 0.450 | move_left_band(node_move)、move_gpu_memory_disk(node_move) | 无显著边变化 |
| Mid | 2260 | 0.900 | 无显著节点变化 | grow_main_spine(edge_grow)、grow_disk_cache_routes(edge_grow) |
| Q3 | 2287 | 1.350 | fade_in_labels(fade_in) | 无显著边变化 |
| End | 2314 | 1.800 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- `page_18_img -> page_19` 已通过公式时长审查；当前不需要再挪 `embed.ts` 锚点。
- `page_19 -> page_21` 已改成直接 handoff，当前窗口为 `2314 -> 2389`（`75` frames / `1.25s`），probe verdict=`in_range`。
- 2026-04-20 额外复核：`page_18_img` 的退场和 `page_19` 的退场都改成跟随“下一页实际 visible”再退出，`2238` 与 `2332` 两个中途帧不再漏出旧 loop stage。
- 2026-04-20 几何微调：保留当前横向读法不变，改用“纵向释压”把 `UE PSO`、`内存中 PSO`、`硬盘中的 PSO` 三个大组整体增高，并重新分配内部三行的高度与间距。
- 2026-04-20 GPU 微调：`Vertex -> GPU` 与 `GPU -> Pixels` 两条竖边重新锚到 GPU 边界，formal sketch 的 `nodePierceCount` 已从 `2` 降到 `0`。
- 2026-04-20 强父子版：左侧独立 `UE PSO` 外列被收回 `stable` 父容器内部，主链改成 `stable -> GPU -> 内存 -> 硬盘`，右侧两组随之整体左移。
- 2026-04-20 前台 Edge 复核：真实浏览器里曾暴露 `disk-vk` 左右 padding 约 `-1px` 的轻微穿边，随后把三条 disk 子框统一加宽到 `174px`，目前 `disk-vk` tightest padding=`0.9px`。
- 当前残留问题不再是 blocker，而是 `layoutDensity=6.7`、`minNodeGap=32` 这类“方向已经对了，但还能继续松开”的密度项。
