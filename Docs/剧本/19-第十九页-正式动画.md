# 第 19 页正式动画：`page_19`

> 文件类型：`正式动画页 / merged diagram`
> Step / Sketch：`page_19` / `page19-r1`
> 上一页：`page_18_img`
> 下一页：`page_21`

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `4.7/10` | 几何事实：三大主节点已收成 `262px x 228px` 的更轻列，`x=142 / 509 / 876`，中间 `内存中GfxPSO` 中轴稳定在 `640`；白色子卡全部删掉后，页面重心明显变轻，但仍有 `minMargin=28px` 与左右质量差 `35%` 的余量问题。 |
| 模块分数 | `5.3/10` | 几何事实：`childOutOfBoundsCount=0`、`minInternalPadding≈7px`，三列只保留橙色主锚点与底部三条 note；“组内列表”被彻底拿掉后，模块关系比旧版更干净。 |
| 节点与边分数 | `9.3/10` | 几何事实：`nodePierce=0`、`crossing=0`、`badEndpoint=0`、`shortSegmentCount=0`，`GPU` 与 `内存中GfxPSO` 同轴；右侧缓存表达已收成单条 dashed handoff。 |
| 动画分数 | `in_range` | 自动 probe：`page_18_img -> page_19` 为 `in_range`；`page_19 -> page_21` 的标准时序 verdict 也为 `in_range`。 |

## Page goal

- 把 `page_19` 收成真正的极简三段：左 `UE PSO / stable.upipelinecache`、中 `内存中GfxPSO`、右 `硬盘中的 PSO`，不再在三大橙框内部展开任何白色子卡。
- `VertexData / GPU / Pixels` 继续保持上方横排，只承担“被 `内存中GfxPSO` 向上驱动”的执行链角色。
- 右侧缓存关系收成一条简洁的 dashed handoff，不再把 OpenGL / Vulkan / Metal 的落盘细节堆在主舞台里。
- `Metal Binary Archive 2` 先保留问号，只作为待验证分支提及，不在本页讲成社区已经收敛的确定答案。
- 删除独立 `page_20` 后，新增一个真正可见的 `page_21` 玩家视角页；`page_19` 负责把复杂主图讲满，下一页再切回玩家拿到结果资产的视角。
- 当前页客观事实维持：`本地缓存的二进制与OS、驱动、芯片强相关，PSO只能在机器上现编，不能稳定分发。`

## 页面改动说明

| 项目 | 内容 |
| --- | --- |
| 新增页面/元素 | 未新增页面；保留合并版 `page_19`，但把舞台收成三个 slimmer 的橙色主节点与一条单虚线 handoff。 |
| 移动节点 | 左中右三个大框统一缩到 `262px x 228px`，位置改为 `x=142 / 509 / 876`、`y=206`；底部三条 note 同步收窄到 `262px` 并下移到 `y=500`；`Metal Binary Archive 2 ?` badge 保持在第三条 note 正下方。 |
| 文案调整 | 白色子卡文案全部退出主舞台；橙色主节点只保留 `UE PSO / stable.upipelinecache`、`内存中GfxPSO`、`硬盘中的 PSO` 三层主标题。 |
| 当前页客观事实 | `本地缓存的二进制与OS、驱动、芯片强相关，PSO只能在机器上现编，不能稳定分发。` |
| 新增 node | 无新增 node；顶部横排的 `VertexData / GPU / Pixels`、底部三条 note 和右下 badge 继续沿用。 |
| 删除 node | 删除三大橙框内部的 9 个白色子卡：左侧 `PSO 1/2/...`、中间 `OpenGL / Vulkan / Metal`、右侧 `Program Binary Cache / VulkanPSO.cache / BinaryArchive functions.data`。 |
| 新增 edge | 无新增 edge。 |
| 删除 edge | 删除原先三条从 API 子卡指向硬盘子卡的横向虚线缓存边，收成 1 条 `内存中GfxPSO -> 硬盘中的 PSO` dashed handoff。 |
| 替换过渡逻辑 | 无新过渡类型；`page_18_img -> page_19` 和 `page_19 -> page_21` 的 phase 顺序保持不变，本轮只做结构瘦身，不压缩原有时长。 |

## 连续语义审计

| 连续对象 | 是否连续 | 处理方式 | 说明 |
| --- | --- | --- | --- |
| `stable.upipelinecache` | 是 | 允许重建 | 上一页 `page_18_img` 是证据/插图页，不保留共享渲染载体，先淡出图片再进入本页正式图。 |
| `stable -> precompile` 章节语义 | 是 | 合并进主中轴 | 本页继续承接旧 `page_19` 的输入说明，但可视化层级已经从“展开细项”收成“只保留三大锚点”。 |
| `page_20` 的章节停顿 | 否 | 直接删除 | 本页已经吸收原 `page_20` 的信息。 |
| `page_21` 的玩家视角 | 是 | 作为下一页承接 | 本页退场后不再直接跳总结板，而是先落到“玩家拿到结果资产”的视角。 |

## Node inventory

| node id | 页面文本 | 角色 | 当前渲染载体 |
| --- | --- | --- | --- |
| `stable-parent` | `UE PSO` / `stable.upipelinecache` | 左侧输入锚点；只保留章节身份，不再展示组内白卡 | `StageBox` + 双行标题 |
| `gpu-strip` | `Vertex icon` / `GPU` / `Pixel icon` | 顶部上提的一排执行链；只保留中段 `GPU` 文字 | `VertexTriangles` + `text` + `PixelGrid` |
| `gfx-group` | `内存中GfxPSO` | 中间主节点，作为本页真正的“内存中 PSO”主表达 | `StageBox` |
| `disk-group` | `硬盘中的 PSO` | 右侧落盘锚点；只保留“本地缓存态”总概念 | `StageBox` |
| `binary-notes` | `Shader / State` / `codegen / 映射` / `OS / Driver / GPU / API` | 下边界解释层，说明 why local binary is unstable | `StageBox` + 多行文本 |
| `binary-archive2` | `Metal` / `Binary Archive 2 ?` | 保守补充标签，只保留问号态 | `StageBox` + 双行文本 |

## Edge inventory

| edge id | 起点 -> 终点 | 类型 | 当前渲染载体 |
| --- | --- | --- | --- |
| `stable-to-gfx` | `UE PSO / stable.upipelinecache -> 内存中GfxPSO` | 主轴实线 | `StrokeArrow` |
| `vertex-to-gpu` | `VertexData -> GPU` | 顶部横向内链 | `StrokeArrow` |
| `gpu-to-pixel` | `GPU -> Pixels` | 顶部横向内链 | `StrokeArrow` |
| `gfx-to-gpu` | `内存中GfxPSO 顶端 -> GPU` | 主轴上行箭头 | `StrokeArrow` |
| `gfx-to-disk` | `内存中GfxPSO -> 硬盘中的 PSO` | 横向虚线 handoff | `StrokeArrow` |

## 空间约束

- 主中轴：`stableCenterY = 320`、`gfxCenterY = 320`、`diskCenterY = 320`
- 三大框列系统：`stableBox = (142, 206, 262, 228)`、`gfxBox = (509, 206, 262, 228)`、`diskBox = (876, 206, 262, 228)`
- 横向中位：`gfxCenterX = 640`；左右两列关于该中轴镜像，列间可见 gap=`105`
- 顶部执行链：`topRowY = 78`
- note 行约束：`noteSize = 262 x 78`、`noteY = 500`、`badge = (924, 648, 166, 44)`
- 几何硬事实：`overlapCount=0`、`crossingCount=0`、`nodePierceCount=0`、`badEndpointCount=0`、`textOverflowCount=0`
- 树结构硬事实：`childOutOfBoundsCount=0`
- 结构间距：`minMargin=28`、`topMargin=34`、`bottomMargin=28`
- 路由质量：`primaryLineBendCount=0`、`offCenterAnchorCount=0`
- 当前主要风险：formal blocker 已全部清空；当前主要剩余项是 `minInternalPadding≈7`、`minMargin=28` 与左右质量差 `35%`，不再是子卡密度或 route 扭结问题。

## 元素样式约束

- 左侧大节点继续保持本页唯一强输入锚点，但标题层级已对调为 `UE PSO` 主标题在上、`stable.upipelinecache` 次标题在下。
- `UE PSO` 与 `stable.upipelinecache` 都使用黑色文字；`UE PSO` 为顶部居中大字，`stable.upipelinecache` 为其下方的小一号黑色副标题。
- `stable`、`内存中GfxPSO`、`硬盘中的 PSO` 三个大框维持同一套浅底分组框，保证左中右三段读法一致。
- `内存中GfxPSO` 与 `硬盘中的 PSO` 现都只保留主标题，不再保留任何组内白色子卡或列表文案。
- `VertexData` 与 `Pixels` 不再单独写字，只保留三角形和像素块图标。
- 顶部 `VertexData / GPU / Pixels` 必须整体上提，落在主舞台的空旷区，而不是压在中轴内部。
- 主轴箭头使用小号三角头；右侧缓存关系只保留 1 条虚线，不再画 3 条并行细节边。

## Assembly order

1. 落左侧大节点：顶部居中主标题 `UE PSO` 与其下黑色副标题 `stable.upipelinecache`。
2. 落中间大框 `内存中GfxPSO` 与右侧 `硬盘中的 PSO`，形成三列主锚点。
3. 把 `VertexData -> GPU -> Pixels` 整体上提到上方空白区。
4. 补齐 `stable -> 内存中GfxPSO` 主轴和从 `内存中GfxPSO` 头部顶端往上接到 `GPU` 的上行箭头。
5. 最后只补 1 条 `内存中GfxPSO -> 硬盘中的 PSO` 的 dashed handoff。

## Acceptance checklist

1. 观众一眼能按 `stable -> 内存中GfxPSO -> GPU / 硬盘中的 PSO` 的顺序读完。
2. `VertexData / GPU / Pixels` 必须横向摆在上方空白区，不能再回到中间竖列。
3. 左到右必须只剩三个大节点：`stable`、`内存中GfxPSO`、`硬盘中的 PSO`。
4. 三大橙框内部不得再出现任何白色字卡或替代型列表。
5. 右侧缓存表达必须只剩 1 条 dashed handoff，不能再恢复成三条并行细节边。
6. 删除旧 `page_20` 后，`page_19` 必须先 handoff 到新的 `page_21` 玩家视角页，再进入 `page_22`。

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260422-095405/review-capture.png)

- 当前 URL：`http://127.0.0.1:4174/?step=page_19&debugFrame=2412`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260422-095405/review-capture.txt`
- Capture size：`4096 x 2404`
- 前台 Edge probe：本轮未执行；以 `browser-api page` 导出作为视觉复核基准。

## 参考链接与保留判断

- `Metal Binary Archive 2` 当前只作为“先打问号”的补充线索，不当作本页结论。
- 当前口径：本页不再在主舞台内展示 OpenGL / Vulkan / Metal 的细节载体，只保留“本地 binary / cache 不是稳定接口”这条判断。

## Facts & Metrics

| 指标 | 值 | 说明 |
| --- | --- | --- |
| `mechanical hard gates` | `pass` | `overlap=0`、`crossing=0`、`nodePierce=0`、`badEndpoint=0`、`textOverflow=0`。 |
| `minRenderedFontPx` | `12.5px` | 最小字号来自 `Metal Binary Archive 2 ?` 的保留 badge。 |
| `minInternalPadding` | `≈7px` | 当前最紧的仍是右下 badge 与 note 区域；主舞台三大橙框本身已明显松开。 |
| `equal-width columns` | `262 / 262 / 262` | 左中右三个主节点已统一成更轻的同宽同高列。 |
| `column x positions` | `142 / 509 / 876` | 中间 `内存中GfxPSO` 继续对准 `x=640` 中轴，左右两列镜像。 |
| `badge placement` | `x=924, y=648` | `Metal Binary Archive 2 ?` 继续挂在第三条 note 正下方，并与其保持中心对齐。 |
| `top/bottom margin` | `34px / 28px` | 顶部执行链仍悬在空旷区；页脚安全边界由 badge 保持。 |

## 三层 Review 总表

| Review 层级 | 事实依据 | 综合判断 | 综合分数 |
| --- | --- | --- | --- |
| `整体布局 Review` | `threeColumnWidth=262/262/262`、`columnX=142/509/876`、`gfxCenterX=640`、`mechanicalScore=7.0` | 去掉九张白卡后，主舞台终于真正回到“三锚点 + 上下辅助层”的干净结构；剩余问题主要是页边留白和左右质量差。 | `4.7/10` |
| `模块空间结构 Review` | `minInternalPadding≈7`、`childOutOfBoundsCount=0`、`singleDashedHandoff=1` | 模块层级已经从“组内表格”收成“章节锚点”，读法更快更直；底部 note 和 badge 仍略密。 | `5.3/10` |
| `单节点与单边 Review` | `nodePierceCount=0`、`badEndpointCount=0`、`crossingCount=0`、`shortSegmentCount=0` | 主轴、上行箭头和单条 dashed handoff 都保持干净，route penalty 维持清零。 | `9.3/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 几何 / 美感 Review | 综合判断 |
| --- | --- | --- | --- | --- | --- | --- |
| `stable-parent` | 左侧大节点以 `UE PSO` 作为主标题、`stable.upipelinecache` 作为次标题，整块只承担“输入锚点”职责。 | 承接 `page_18` 的 stable 章节语义 | `StageBox` + 双行标题 | 语义连续，但上一页是插图页，允许重建载体。 | 现在不再承担细项展示，体量显著变轻。 | `通过` |
| `gpu-strip` | 顶部横排的 `VertexData -> GPU -> Pixels` 执行链。 | 继承 `page_14` 的 GPU 表达习惯 | `VertexTriangles` + `text` + `PixelGrid` | 连续语义很强，本页继续维持上方横排。 | GPU 被抬到空白区后，主舞台三大节点终于不再互相挤占。 | `通过` |
| `gfx-group` | `内存中GfxPSO` 作为中间主节点，只承担“内存中 PSO”这一个概念。 | 新增总结容器 | `StageBox` | 与本页讲述目标完全匹配。 | 当前已经对准主舞台中轴，成为三列系统的真正中心。 | `通过` |
| `disk-group` | 右侧 `硬盘中的 PSO` 作为本地缓存锚点。 | 承接本地缓存语义 | `StageBox` | 合理替代了旧版分散的多个落盘盒子。 | 与左中两框保持同宽同高，并与左侧形成镜像列。 | `通过` |
| `binary-notes` | 底部三条 note 分别解释 `Shader / State`、`codegen / 映射`、`OS / Driver / GPU / API`。 | 延续 page19 的“失效原因说明”语义 | `StageBox` + 多行文本 | 连续成立。 | 虽然仍略紧，但比旧版卡片堆叠更克制。 | `通过 / 后续可继续精修` |
| `binary-archive2` | `Metal Binary Archive 2 ?` 问号标签。 | 保守补充线索 | `StageBox` + 双行文本 | 作为保守态存在合理。 | 当前已经脱离主舞台，只留在右下角作附注。 | `通过` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 几何 / 美感 Review | 综合判断 |
| --- | --- | --- | --- | --- | --- | --- |
| `stable-to-gfx` | 左侧 `UE PSO / stable.upipelinecache` 直接把章节主语义送进 `内存中GfxPSO`。 | 承接 stable 输入语义 | `StrokeArrow` | 新主线清楚。 | 左侧水平主轴稳定，也给上方 GPU 留出了空白区。 | `通过` |
| `vertex-to-gpu` | `VertexData` 横向进入 `GPU`。 | 继承 page14 GPU 语义，但改成上方横排 | `StrokeArrow` | 连续成立。 | 左侧顶排箭头已经明显拉长，读者更容易先看到“输入进入 GPU”。 | `通过` |
| `gpu-to-pixel` | `GPU` 横向输出到 `Pixels`。 | 继承 page14 GPU 语义，但改成上方横排 | `StrokeArrow` | 连续成立。 | 右侧顶排箭头也被明显展开，GPU 的输出方向更清楚。 | `通过` |
| `gfx-to-gpu` | `内存中GfxPSO` 从头部顶端向上驱动 `GPU`。 | 本页主链新增 | `StrokeArrow` | 逻辑清楚。 | `GPU` 现已与 `内存中GfxPSO` 同轴，这条上行主轴已不再吃 short-segment penalty。 | `通过` |
| `gfx-to-disk` | `内存中GfxPSO` 的本地缓存关系被压成 1 条横向 dashed handoff。 | 本页缓存边精简版 | `StrokeArrow` | 简化后仍然成立。 | 现在比旧版三条并行虚线更干净，也更符合“这页不展开细节”的目标。 | `通过` |

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | `page_18_img -> page_19` 前段 | 证据页淡出，本页主中轴开始建立。 | `page_18_img` overlay、stable/UE PSO 左半带 | 先把插图页收走，再回到正式结构。 |
| 2 | 中段 | `内存中GfxPSO` 与 `硬盘中的 PSO` 两个大组先落位，再把顶部 GPU strip 上提。 | `内存中GfxPSO`、`硬盘中的 PSO`、顶部 `VertexData / GPU / Pixels` | 先建立三个主锚点，再把上层执行链悬到空白区。 |
| 3 | 后段 | 主中轴、上行箭头和单条 dashed handoff 补齐。 | 主轴箭头、`内存中GfxPSO -> 硬盘中的 PSO` 虚线 | 这一步完成更简版的预编译闭环。 |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_18_img -> page_19）

- 帧窗口：`2296 -> 2404`（`108` frames @ 60fps）
- 总时长：`1.800s`
- 时长评估：`in_range`（目标区间 `0.90s ~ 1.80s`）
- 调整建议：目标帧长 `108`，相对当前 `Δ+0` frames
- 标准时序评估：`in_range`（标准下限 `1.710s`，上限 `2.308s`）
- 标准调整建议：目标帧长 `108`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`1.710s`
- 标准允许上限：`2.308s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 120.0 | 120.0 | - |
| fade_out | 120.0 | 340.0 | 220.0 | fade_out_evidence_overlay |
| node_motion | 340.0 | 729.0 | 389.0 | move_left_band, move_gpu_memory_disk |
| edge_motion | 729.0 | 1310.0 | 581.0 | grow_main_spine, grow_disk_cache_routes |
| fade_in | 1310.0 | 1530.0 | 220.0 | fade_in_labels |
| hold_after | 1530.0 | 1710.0 | 180.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_evidence_overlay | fade_out | fade_out | - | 220.0 | formula |
| move_left_band | node_move | node_motion | distancePx=160.0 | 332.0 | formula |
| move_gpu_memory_disk | node_move | node_motion | distancePx=220.0 | 389.0 | formula |
| grow_main_spine | edge_grow | edge_motion | lengthPx=588.0 | 581.0 | formula |
| grow_disk_cache_routes | edge_grow | edge_motion | lengthPx=286.0 | 354.5 | formula |
| fade_in_labels | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 2296 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 2323 | 0.450 | move_left_band(node_move)、move_gpu_memory_disk(node_move) | 无显著边变化 |
| Mid | 2350 | 0.900 | 无显著节点变化 | grow_main_spine(edge_grow)、grow_disk_cache_routes(edge_grow) |
| Q3 | 2377 | 1.350 | fade_in_labels(fade_in) | 无显著边变化 |
| End | 2404 | 1.800 | 无显著节点变化 | 无显著边变化 |

## 过渡动画剧本（page_19 -> page_21）

- 帧窗口：`2404 -> 2618`（`214` frames @ 60fps）
- 总时长：`3.567s`
- 时长评估：`too_long`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `156`，相对当前 `Δ-58` frames
- 标准时序评估：`in_range`（标准下限 `3.552s`，上限 `4.795s`）
- 标准调整建议：目标帧长 `214`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> callback_node_motion -> callback_edge_motion -> callback_hold -> callback_shift_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`3.552s`
- 标准允许上限：`4.795s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 120.0 | 120.0 | - |
| fade_out | 120.0 | 340.0 | 220.0 | fade_out_page19_focus |
| callback_node_motion | 340.0 | 730.9 | 390.9 | restore_page18_callback_stage |
| callback_edge_motion | 730.9 | 1491.1 | 760.2 | grow_page18_callback_loop |
| callback_hold | 1491.1 | 1811.1 | 320.0 | hold_restored_page18_stage |
| callback_shift_out | 1811.1 | 2270.4 | 459.3 | shift_out_page18_callback_stage, fade_out_page18_callback_stage |
| node_motion | 2270.4 | 2720.2 | 449.8 | move_page21_player_phone |
| edge_motion | 2720.2 | 3191.7 | 471.5 | grow_page21_asset_routes |
| fade_in | 3191.7 | 3411.7 | 220.0 | fade_in_page21_player_view |
| hold_after | 3411.7 | 3551.7 | 140.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_page19_focus | fade_out | fade_out | - | 220.0 | formula |
| restore_page18_callback_stage | node_move | callback_node_motion | distancePx=222.0 | 390.9 | formula |
| grow_page18_callback_loop | edge_grow | callback_edge_motion | lengthPx=827.0 | 760.2 | formula |
| hold_restored_page18_stage | node_move | callback_hold | distancePx=0.0 | 320.0 | manual |
| shift_out_page18_callback_stage | node_move | callback_shift_out | distancePx=294.0 | 459.3 | formula |
| fade_out_page18_callback_stage | fade_out | callback_shift_out | - | 220.0 | formula |
| move_page21_player_phone | node_move | node_motion | distancePx=284.0 | 449.8 | formula |
| grow_page21_asset_routes | edge_grow | edge_motion | lengthPx=442.0 | 471.5 | formula |
| fade_in_page21_player_view | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 2404 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 2458 | 0.900 | 无显著节点变化 | grow_page18_callback_loop(edge_grow) |
| Mid | 2511 | 1.783 | hold_restored_page18_stage(node_move) | 无显著边变化 |
| Q3 | 2564 | 2.667 | move_page21_player_phone(node_move) | 无显著边变化 |
| End | 2618 | 3.567 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- `page_18_img -> page_19` 已通过公式时长审查；当前不需要再挪 `embed.ts` 锚点。
- `page_19 -> page_21` 当前原始时长带内 verdict=`too_long`，但 Harness 标准时序 verdict=`in_range`；本轮没有新增前置动画，也没有压缩原有 base 动画。
- 2026-04-22 本轮几何更新：`page_19` 三大橙框统一缩到 `262px x 228px`，九张白色子卡全部移除，右侧缓存关系收成单条 dashed handoff。
- 2026-04-22 视觉复核：browser capture 已更新到 `ignore/browser-api-captures/20260422-024058/review-capture.png`，当前主舞台读序是稳定的 `stable -> 内存中GfxPSO -> 硬盘中的 PSO`。
- 当前残留问题不再是 blocker，而是 `minInternalPadding≈7`、`minMargin=28` 和左右质量差 `35%` 这类留白精修项。
