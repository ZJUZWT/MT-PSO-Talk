# 第 19 页正式动画：`page_19`

> 文件类型：`正式动画页 / merged diagram`
> Step / Sketch：`page_19` / `page19-r1`
> 上一页：`page_18_img`
> 下一页：`page_21`


## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `3.7/10` | 几何事实：三大主节点仍保持 `324px x 392px` 的瘦列，`x=108 / 478 / 848`，中间 `GfxPSO` 中轴稳定在 `640`；这轮把 `Metal Binary Archive 2 ?` 改到 `OS / Driver / GPU / API` 说明框正下方后，当前主要剩余项变成 `minNodeGap=6px` 与 `minMargin=28px`。 |
| 模块分数 | `4.3/10` | 几何事实：`childOutOfBoundsCount=0`、`minInternalPadding=6.8px`，左侧 `PSO` 行与中右两组子卡仍保持统一；为给 badge 腾出合法位置，底部三张 note 一起上提，badge 与右侧 note 的局部间距现在是 `6px`。 |
| 节点与边分数 | `9.3/10` | 几何事实：`nodePierce=0`、`crossing=0`、`badEndpoint=0`、`shortSegmentCount=0`，`GPU` 与 `GfxPSO` 同轴，右侧三条虚线现在直接落到对应硬盘子卡边界。 |
| 动画分数 | `in_range` | 自动 probe：`page_18_img -> page_19` 为 `in_range`；`page_19 -> page_21` 现在为 `214` 帧玩家视角 handoff，标准时序 `in_range`。 |

## Page goal

- 把旧 `page_19` 和旧 `page_20` 压成一页，直接说明左侧大节点如何以 `UE PSO` 为居中主标题、以 `stable.upipelinecache` 为黑色次标题，再向下展开三个 `PSO` 子项，随后进入中间大节点 `GfxPSO`，最后落到右侧的 `硬盘中的 PSO`。
- `VertexData / GPU / Pixels` 不再竖着堆在中间，而是整体上提到主舞台上方的空白区，作为被 `GfxPSO` 向上驱动的执行链。
- `Metal Binary Archive 2` 先保留问号，只作为待验证分支提及，不在本页讲成社区已经收敛的确定答案。
- 删除独立 `page_20` 后，新增一个真正可见的 `page_21` 玩家视角页；`page_19` 负责把复杂主图讲满，下一页再切回玩家拿到结果资产的视角。

## 页面改动说明

| 项目 | 内容 |
| --- | --- |
| 新增页面/元素 | 保留合并版 `page_19`；左侧大节点改成 `UE PSO` 主标题 + `stable.upipelinecache` 次标题 + 三条 `PSO` 子节点；中间大框改成 `GfxPSO`；上方单独悬出 `VertexData / GPU / Pixels` 一排。 |
| 移动节点 | `VertexData / GPU / Pixels` 整体继续上提到上方空白区；`GPU` 横向改到与 `GfxPSO` 主盒同轴；左右两侧图标同步外拉，让 `Vertex -> GPU` 与 `GPU -> Pixels` 两条边显著展开；左中右三个大框现统一为 `324px x 392px`，位置为 `x=108 / 478 / 848`；`GfxPSO` 与 `硬盘中的 PSO` 内部三张子卡也同步收成 `280px x 70px` 同宽同高；底部三张 note 统一上移到 `y=564`；`Metal Binary Archive 2 ?` 现已移到 `OS / Driver / GPU / API` 说明框正下方。 |
| 文案调整 | 大框标题从 `API Cache / Binary` 改成 `GfxPSO`；主逻辑从“GPU 竖列接内存中 PSO”改成“`GfxPSO` 向上驱动 GPU”；`VertexData` 与 `Pixels` 继续只用图标表达。 |
| 新增 node | 顶部横排的 `VertexData / GPU / Pixels` 作为独立上层执行链继续保留。 |
| 删除 node | 中间偏下的独立小 `GfxPSO` 卡片删除。 |
| 新增 edge | `stable -> GfxPSO` 主轴；`GfxPSO` 头部顶端向上接到 `GPU` 的上行箭头；三条从 `GfxPSO` 内部 API 子项指向硬盘态的虚线缓存边。 |
| 删除 edge | 原 `page_19 -> page_20` 的跨页叙事边不再作为独立可见页面存在。 |
| 替换过渡逻辑 | 删除旧 `page_20`，并把 `page_21` 改成真正可见的玩家视角页；`page_19` 退场后先进入 `page_21`，再进入 `page_22`。 |

## 连续语义审计

| 连续对象 | 是否连续 | 处理方式 | 说明 |
| --- | --- | --- | --- |
| `stable.upipelinecache` | 是 | 允许重建 | 上一页 `page_18_img` 是证据/插图页，不保留共享渲染载体，先淡出图片再进入本页正式图。 |
| `stable -> precompile` 章节语义 | 是 | 合并进主中轴 | 本页直接把旧 `page_19` 的输入说明和旧 `page_20` 的过渡停顿一起吸收。 |
| `page_20` 的章节停顿 | 否 | 直接删除 | 本页已经吸收原 `page_20` 的信息。 |
| `page_21` 的玩家视角 | 是 | 作为下一页承接 | 本页退场后不再直接跳总结板，而是先落到“玩家拿到结果资产”的视角。 |

## Node inventory

| node id | 页面文本 | 角色 | 当前渲染载体 |
| --- | --- | --- | --- |
| `stable-parent` | `UE PSO` / `stable.upipelinecache` / `PSO 1` / `PSO 2` / `PSO ...` | 左侧强父子树；`UE PSO` 作为主标题，`stable.upipelinecache` 作为次标题，共同承载三个子项 | `StageBox` + 行内 `StageBox` |
| `gpu-strip` | `Vertex icon` / `GPU` / `Pixel icon` | 顶部上提的一排执行链；只保留中段 `GPU` 文字 | `VertexTriangles` + `text` + `PixelGrid` |
| `gfx-group` | `GfxPSO` | 中间大节点，作为本页真正的“内存中 PSO”主表达 | `StageBox` |
| `gfx-gl-node` | `OpenGL` / `Program Binary` | `GfxPSO` 内部的 OpenGL 运行态表达 | `StageBox` + `StackedLabel` |
| `gfx-vk-node` | `Vulkan` / `Pipeline Cache` | `GfxPSO` 内部的 Vulkan 运行态表达 | `StageBox` + `StackedLabel` |
| `gfx-metal-node` | `Metal` / `Binary Archive` / `系统管理` | `GfxPSO` 内部的 Metal 运行态表达 | `StageBox` + `StackedLabel` |
| `disk-group` | `硬盘中的 PSO` | 本地缓存容器 | `StageBox` |
| `disk-gl-node` | `Program Binary` / `Cache` | OpenGL 落盘缓存 | `StageBox` + `StackedLabel` |
| `disk-vk-node` | `VulkanPSO.cache` | Vulkan 落盘缓存 | `StageBox` |
| `disk-metal-node` | `BinaryArchive` / `functions.data` | Metal 落盘缓存 | `StageBox` + `StackedLabel` |

## Edge inventory

| edge id | 起点 -> 终点 | 类型 | 当前渲染载体 |
| --- | --- | --- | --- |
| `stable-to-gfx` | `UE PSO / stable.upipelinecache(含三个 PSO 子项) -> GfxPSO` | 主轴实线 | `StrokeArrow` |
| `vertex-to-gpu` | `VertexData -> GPU` | 顶部横向内链 | `StrokeArrow` |
| `gpu-to-pixel` | `GPU -> Pixels` | 顶部横向内链 | `StrokeArrow` |
| `gfx-to-gpu` | `GfxPSO 顶端 -> GPU` | 主轴上行箭头 | `StrokeArrow` |
| `gl-to-disk` | `OpenGL / Program Binary -> Program Binary / Cache` | 横向虚线缓存边 | `StrokeArrow` |
| `vk-to-disk` | `Vulkan / Pipeline Cache -> VulkanPSO.cache` | 横向虚线缓存边 | `StrokeArrow` |
| `metal-to-disk` | `Metal / Binary Archive / 系统管理 -> BinaryArchive / functions.data` | 横向虚线缓存边 | `StrokeArrow` |

## 空间约束

- 主中轴：`stableCenterY = 360`、`gfxCenterY = 360`、`diskCenterY = 360`
- 三大框列系统：`stableBox = (108, 164, 324, 392)`、`gfxBox = (478, 164, 324, 392)`、`diskBox = (848, 164, 324, 392)`
- 横向中位：`gfxCenterX = 640`；左右两列关于该中轴镜像，列间可见 gap=`46`
- 顶部执行链：`topRowY = 78`
- 子卡约束：`ueRowWidth = 280`、`apiCardSize = 280 x 70`、`diskCardSize = 280 x 70`、`verticalGap = 38`、`leftPad = rightPad = 22`
- 几何硬事实：`overlapCount=0`、`crossingCount=0`、`nodePierceCount=0`、`badEndpointCount=0`、`textOverflowCount=0`
- 树结构硬事实：`childOutOfBoundsCount=0`、`siblingOverlapCount=0`
- 结构间距：`minNodeGap=6`、`minMargin=28`、`topMargin=34`、`bottomMargin=28`
- 路由质量：`primaryLineBendCount=0`、`offCenterAnchorCount=0`
- 当前主要风险：formal blocker 已全部清空；当前主要剩余项是 `Metal Binary Archive 2 ?` 与 `OS / Driver / GPU / API` 说明框之间的 `minNodeGap=6`，以及 `minInternalPadding=6.8`、`minMargin=28` 这种密度问题，不再有 short segment 或 anchor offset 残留。

## 元素样式约束

- 左侧大节点继续保持本页唯一强输入锚点，但标题层级已对调为 `UE PSO` 主标题在上、`stable.upipelinecache` 次标题在下。
- `UE PSO` 与 `stable.upipelinecache` 都使用黑色文字；`UE PSO` 为顶部居中大字，`stable.upipelinecache` 为其下方的小一号黑色副标题。
- `stable`、`GfxPSO`、`硬盘中的 PSO` 三个大框维持同一套浅底分组框，保证左中右三段读法一致。
- `GfxPSO` 标题是中间大框的主标题，不再额外放一个独立小卡。
- `VertexData` 与 `Pixels` 不再单独写字，只保留三角形和像素块图标。
- 顶部 `VertexData / GPU / Pixels` 必须整体上提，落在主舞台的空旷区，而不是压在中轴内部。
- 三个 API 子节点与三个硬盘节点全部横向对齐，箭头端点必须居中。
- `OpenGL` 与 `Program Binary` 合并为同一节点的上下双行文案，不拆成两个块。
- `Metal` 节点必须同时出现 `Binary Archive` 与 `系统管理`；硬盘节点必须同时出现 `BinaryArchive` 与 `functions.data`。
- 主轴箭头使用小号三角头，缓存边使用虚线，不允许出现折线、偏心锚点或 oversized arrowhead。

## Assembly order

1. 落左侧大节点：顶部居中主标题 `UE PSO`、其下黑色副标题 `stable.upipelinecache`，以及下方三个 `PSO` 子项。
2. 落中间大框 `GfxPSO`，并把内部三条 API 子项排好。
3. 把 `VertexData -> GPU -> Pixels` 整体上提到上方空白区。
4. 补齐 `stable -> GfxPSO` 主轴和从 `GfxPSO` 头部顶端往上接到 `GPU` 的上行箭头。
5. 最后补三条到本地缓存的虚线。

## Acceptance checklist

1. 观众一眼能按 `stable(含 UE 子树) -> GfxPSO -> GPU / 本地缓存` 的顺序读完。
2. `VertexData / GPU / Pixels` 必须横向摆在上方空白区，不能再回到中间竖列。
3. 左到右必须只剩三个大节点：`stable`、`GfxPSO`、`硬盘中的 PSO`。
4. `GfxPSO` 必须直接占原先 `API Cache / Binary` 那个大框位置，不能再额外出现第二个独立小 `GfxPSO` 盒子。
5. `OpenGL` 与 `Program Binary` 必须合并在同一节点；`Metal` 必须同时写出 `Binary Archive` 与 `系统管理`。
6. 三条本地缓存边必须是同一风格的横向虚线，并且终点统一落在对应硬盘子卡边界。
7. 删除旧 `page_20` 后，`page_19` 必须先 handoff 到新的 `page_21` 玩家视角页，再进入 `page_22`。
## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260421-213309/review-capture.png)

- 当前 URL：`http://127.0.0.1:4174/?step=page_19&debugFrame=2412`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260421-213309/review-capture.txt`
- Capture size：`4096 x 2404`
- 前台 Edge probe：本轮未执行；以 `browser-api page` 导出作为视觉复核基准。

## 参考链接与保留判断

- `Metal Binary Archive 2` 当前只作为“先打问号”的补充线索，不当作本页结论。
- 参考链接：[知乎：Metal Binary Archive 2](https://zhuanlan.zhihu.com/p/587988966)
- 当前口径：社区讨论并不稳定，所以这里维持保留态度，只先把 `Binary Archive / 系统管理 -> BinaryArchive / functions.data` 这条主线讲清楚。

## Facts & Metrics

| 指标 | 值 | 说明 |
| --- | --- | --- |
| `mechanical hard gates` | `pass` | `overlap=0`、`crossing=0`、`nodePierce=0`、`badEndpoint=0`、`textOverflow=0`。 |
| `minRenderedFontPx` | `12.5px` | 最小字号来自 `Metal Binary Archive 2 ?` 的保留 badge。 |
| `minInternalPadding` | `6.8px` | 当前最紧的仍是 `Metal Binary Archive 2 ?` badge；主舞台已经没有文本 overflow blocker。 |
| `equal-width columns` | `324 / 324 / 324` | 左中右三个大节点已经统一成同宽同高列。 |
| `column x positions` | `108 / 478 / 848` | 中间 `GfxPSO` 已对准 `x=640` 中轴，左右两列镜像。 |
| `badge placement` | `x=927, y=648` | `Metal Binary Archive 2 ?` 现已移到 `OS / Driver / GPU / API` 说明框正下方，并与该说明框保持同轴。 |
| `top/bottom margin` | `34px / 28px` | 顶部执行链继续悬在空旷区；为给右下 badge 腾空间，底部 note 行整体上提。 |

## 三层 Review 总表

| Review 层级 | 事实依据 | 综合判断 | 综合分数 |
| --- | --- | --- | --- |
| `整体布局 Review` | `threeColumnWidth=324/324/324`、`columnX=108/478/848`、`gfxCenterX=640`、`mechanicalScore=6.5` | 左中右三段仍然对称，这轮主要是把问号标签改挂到右下说明框下面；代价是右下角的最小间距和底边距进一步收紧。 | `3.7/10` |
| `模块空间结构 Review` | `minNodeGap=6`、`minInternalPadding=6.8`、`childOutOfBoundsCount=0`、中右子卡 `280x70 / gap 38 / padX 22` | 大框和子卡列系统仍然统一，但右下角因为“说明框 + 补充标签”的竖向组合而更紧。 | `4.3/10` |
| `单节点与单边 Review` | `nodePierceCount=0`、`badEndpointCount=0`、`crossingCount=0`、`shortSegmentCount=0` | 主轴和三条缓存边都已经直接挂在目标边界上，route penalty 已清零。 | `9.3/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 几何 / 美感 Review | 综合判断 |
| --- | --- | --- | --- | --- | --- | --- |
| `stable-parent` | 左侧大节点以 `UE PSO` 作为顶部居中主标题，`stable.upipelinecache` 作为其下方黑色次标题，并继续收住三个 `PSO` 子项。 | 承接 `page_18` 的 stable 章节语义 | `StageBox` + 内部标题 + 子节点 | 语义连续，但上一页是插图页，允许重建载体。 | 现已与中右两组统一成同宽主框，不再出现“左大右小”的版式漂移。 | `通过` |
| `ue-subtree` | `PSO 1 / 2 / ...` 作为左侧 `UE PSO / stable.upipelinecache` 大节点的三个孩子。 | 新增树内子层，不要求跨页共享载体 | `StageBox` 行节点 | 逻辑比旧版“独立 UE 列”更贴语义。 | `childOutOfBoundsCount=0`，三格都稳定落在父容器内。 | `通过` |
| `gpu-strip` | 顶部横排的 `VertexData -> GPU -> Pixels` 执行链。 | 继承 `page_14` 的 GPU 表达习惯 | `VertexTriangles` + `text` + `PixelGrid` | 连续语义很强，但本页改成了上方横排。 | GPU 被抬到空白区后，主舞台三大节点终于不再互相挤占。 | `通过` |
| `gfx-group` | `GfxPSO` 作为中间主节点，内部再挂三种 API 子项。 | 新增总结容器 | `StageBox` | 与本页讲述目标完全匹配。 | 当前已经对准主舞台中轴，成为三列系统的真正中心。 | `通过` |
| `gfx-gl-node` | OpenGL 以 Program Binary 形式在 `GfxPSO` 内部表达。 | 新增 API 对照节点 | `StageBox` + `StackedLabel` | 文案满足“OpenGL 与 Program Binary 同节点”。 | 现与另外两张卡同尺寸、同宽度、同左右 padding。 | `通过` |
| `gfx-vk-node` | Vulkan 以内存态 Pipeline Cache 表达。 | 新增 API 对照节点 | `StageBox` + `StackedLabel` | 与 OpenGL / Metal 形成平行对照。 | 现与上下两张卡等高、等距。 | `通过` |
| `gfx-metal-node` | Metal 由 Binary Archive 与系统管理共同表达。 | 新增 API 对照节点 | `StageBox` + `StackedLabel` | 满足用户要求，信息最完整。 | 已收成与上两张卡同尺寸，三张卡现在形成规整的中间纵向序列。 | `通过 / 后续可继续精修` |
| `disk-group` | 所有本地缓存统一落在一个硬盘容器里。 | 承接本地缓存语义 | `StageBox` | 合理替代了分散的多个末端缓存盒子。 | 已同步拉到与左中两框同宽，并与左侧形成镜像列。 | `通过` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 几何 / 美感 Review | 综合判断 |
| --- | --- | --- | --- | --- | --- | --- |
| `stable-to-gfx` | 左侧 `UE PSO / stable.upipelinecache` 大节点先把三个 `PSO` 子项结果送进 `GfxPSO`。 | 承接 stable 输入语义 | `StrokeArrow` | 新主线清楚。 | 左侧水平主轴稳定，也给上方 GPU 留出了空白区。 | `通过` |
| `vertex-to-gpu` | `VertexData` 横向进入 `GPU`。 | 继承 page14 GPU 语义，但改成上方横排 | `StrokeArrow` | 连续成立。 | 左侧顶排箭头已经明显拉长，读者更容易先看到“输入进入 GPU”。 | `通过` |
| `gpu-to-pixel` | `GPU` 横向输出到 `Pixels`。 | 继承 page14 GPU 语义，但改成上方横排 | `StrokeArrow` | 连续成立。 | 右侧顶排箭头也被明显展开，GPU 的输出方向更清楚。 | `通过` |
| `gfx-to-gpu` | `GfxPSO` 从头部顶端向上驱动 `GPU`。 | 本页主链新增 | `StrokeArrow` | 逻辑清楚。 | `GPU` 现已与 `GfxPSO` 同轴，这条上行主轴已不再吃 short-segment penalty。 | `通过` |
| `gl-to-disk` | OpenGL 内存态缓存到 Program Binary Cache。 | 本页新增缓存边 | `StrokeArrow` | 映射一一对应。 | 虚线节奏统一，端点居中。 | `通过` |
| `vk-to-disk` | Vulkan 内存态缓存到 `VulkanPSO.cache`。 | 本页新增缓存边 | `StrokeArrow` | 映射一一对应。 | 与上下两条虚线间距均匀。 | `通过` |
| `metal-to-disk` | Metal 内存态缓存到 `BinaryArchive / functions.data`。 | 本页新增缓存边 | `StrokeArrow` | 映射一一对应。 | 在最下层仍保持水平与居中，没有歪斜。 | `通过` |

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | `page_18_img -> page_19` 前段 | 证据页淡出，本页主中轴开始建立。 | `page_18_img` overlay、stable/UE PSO 左半带 | 先把插图页收走，再回到正式结构。 |
| 2 | 中段 | `GfxPSO` 与 `硬盘中的 PSO` 两个大组先落位，再把顶部 GPU strip 上提。 | `GfxPSO`、`硬盘中的 PSO`、顶部 `VertexData / GPU / Pixels` | 先建立三个大节点，再把上层执行链悬到空白区。 |
| 3 | 后段 | 主中轴和三条缓存虚线全部补齐。 | 主轴箭头、三条本地缓存虚线 | 这一步完成“两页压一页”的核心信息闭环。 |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_18_img -> page_19）

- 帧窗口：`2296 -> 2404`（`108` frames @ 60fps）
- 总时长：`1.800s`
- 时长评估：`in_range`（目标区间 `0.90s ~ 1.80s`）
- 调整建议：目标帧长 `108`，相对当前 `Δ+0` frames
- 标准时序评估：`in_range`（标准下限 `1.710s`，上限 `2.308s`）
- 标准调整建议：目标帧长 `108`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`1.710s`
- 标准允许上限：`2.308s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 120.0 | 120.0 | - |
| fade_out | 120.0 | 340.0 | 220.0 | fade_out_evidence_overlay |
| node_motion | 340.0 | 729.0 | 389.0 | move_left_band, move_gpu_memory_disk |
| edge_motion | 729.0 | 1310.0 | 581.0 | grow_main_spine, grow_disk_cache_routes |
| fade_in | 1310.0 | 1530.0 | 220.0 | fade_in_labels |
| hold_after | 1530.0 | 1710.0 | 180.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_evidence_overlay | fade_out | fade_out | - | 220.0 | formula |
| move_left_band | node_move | node_motion | distancePx=160.0 | 332.0 | formula |
| move_gpu_memory_disk | node_move | node_motion | distancePx=220.0 | 389.0 | formula |
| grow_main_spine | edge_grow | edge_motion | lengthPx=588.0 | 581.0 | formula |
| grow_disk_cache_routes | edge_grow | edge_motion | lengthPx=286.0 | 354.5 | formula |
| fade_in_labels | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 2296 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 2323 | 0.450 | move_left_band(node_move)、move_gpu_memory_disk(node_move) | 无显著边变化 |
| Mid | 2350 | 0.900 | 无显著节点变化 | grow_main_spine(edge_grow)、grow_disk_cache_routes(edge_grow) |
| Q3 | 2377 | 1.350 | fade_in_labels(fade_in) | 无显著边变化 |
| End | 2404 | 1.800 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- `page_18_img -> page_19` 已通过公式时长审查；当前不需要再挪 `embed.ts` 锚点。
- `page_19 -> page_21` 当前窗口为 `2404 -> 2618`（`214` frames / `3.567s`），Harness 标准时序 verdict=`in_range`；新过渡会先完整回到带蓝色闭环的 `page_18` 主舞台，停一拍，再把旧舞台左移退场。
- 2026-04-21 节奏微调：`page_19` 退场更快让出舞台，`page_18` 完整态会更早出现，并在左移退场前多停留一段。
- `page_21` 顶部保留下来的 `stable.upipelinecache`、`ShaderLibrary`、`Phone` 和两条输入边，现已改成复用 callback 还原阶段同一批 shared carrier，不再额外生成一套 duplicate 节点 / 边。
- 2026-04-21 额外复核：`page_18_img -> page_19` 的校验帧现在跟随 step frame 计算，交接区间以 `2296 -> 2404` 为准，不再使用已经失效的旧裸帧号 `2238`。
- 2026-04-21 最新几何更新：左中右三个大节点已统一成 `324px x 392px` 的瘦列，`x=108 / 478 / 848`；中间 `GfxPSO` 中轴稳定在 `640`；右侧 `硬盘中的 PSO` 与中间盒保持同宽同高；顶部 `VertexData / GPU / Pixels` 跟随中轴整体回正。
- 2026-04-21 补充节点下移：`Metal Binary Archive 2 ?` 已移到 `OS / Driver / GPU / API` 说明框正下方；为了避免它掉进页脚装饰区，底部三张 note 统一上移到 `y=564`。
- 2026-04-20 GPU 微调：`Vertex -> GPU` 与 `GPU -> Pixels` 两条竖边重新锚到 GPU 边界，formal sketch 的 `nodePierceCount` 已从 `2` 降到 `0`。
- 2026-04-20 强父子版：左侧独立 `UE PSO` 外列被收回左侧大节点内部，形成当前这版 `UE PSO / stable.upipelinecache -> GfxPSO -> GPU / 硬盘` 的主逻辑骨架。
- 2026-04-21 标题层级修正：左侧大节点现以 `UE PSO` 为顶部居中大字主标题，`stable.upipelinecache` 为其下方黑色副标题；两者都不再使用强调色。
- 2026-04-21 边界复核：`stable -> GfxPSO` 主轴与三条右侧缓存虚线现在都直接挂到目标节点边界，`anchor offset=0`、`shortSegmentCount=0`。
- 当前残留问题不再是 blocker，而是右下角局部的 `minNodeGap=6`，以及 `minInternalPadding=6.8`、`minMargin=28` 这种密度层面的精修项。
