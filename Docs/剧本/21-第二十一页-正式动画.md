# 第 21 页

> 文件类型：`正式动画页`
> Step / Sketch：`page_21` / `formal-page21`
> 上一页：`page_19`
> 下一页：`page_22`

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## Page goal

- 把 `page_21` 从旧的“上下双卡入口页”改成和后面策略页一致的 `左主卡 + 右侧三卡 + footer`。
- 先用一句话收束“缓存效果由四个边界共同决定”，再拆开讲收集、首启、平台 / 环境三个边界。
- 保留 `page_19 -> page_21` 的淡入淡出逻辑，不改 frame anchor，不引入额外闪回。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `PSO 缓存有效性边界` |
| 新增元素 | 左侧主卡 `left-card`、右侧三张边界卡 `right-1/2/3`、底部 `footer` |
| 移动的节点 | 旧 `goal-card` / `bridge-card` 的语义从“上下叠放”改为“左主卡总结 + 右侧分解” |
| 删除的元素 | 删除旧版 `这一页讲什么`、`和下一页怎么衔接` 两张独立竖排卡 |
| 新增 node / edge | 新增 node：`left-card`、`right-1`、`right-2`、`right-3`、`footer`；新增隐式阅读 edge：`read-left-to-right`、`read-right-stack`、`read-to-footer` |
| 删除 node / edge | 删除 node：`goal-card`、`bridge-card`；删除 edge：`reading-flow-a`、`reading-flow-b` |
| 过渡逻辑替换 | 未改 `page_19 -> page_21` 的 frame anchor；仍是 `page_19` 淡出后直接淡入 summary 板，无主图闪回 |

## Node inventory

| id | text | semantic role | visual kind | anchor region | emphasis | alignment constraints | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `left-card` | `缓存效果由四个边界共同决定` | 本页主判断 | lead card | 左侧主区 | 高 | 与 footer 居中轴对齐 | 承接 `page_19` 的稳定缓存话题 | 不要求同载体 |
| `right-1` | `收集边界` | 边界卡 1 | info card | 右上 | 中 | 与右侧另外两卡同宽同左边线 | 新增 | 不要求同载体 |
| `right-2` | `首启边界` | 边界卡 2 | info card | 右中 | 中 | 与 `right-1/3` 同宽 | 新增 | 不要求同载体 |
| `right-3` | `平台 / 环境边界` | 边界卡 3 | info card | 右下 | 中 | 与 `right-1/2` 同宽 | 新增 | 不要求同载体 |
| `footer` | `边界讲清，后面的策略页才不会被误读成默认正确。` | 收束条 | footer bar | 底部 | 中 | 与主舞台水平居中 | 承接 `page_22` | 不要求同载体 |

## Edge inventory

| id | from | to | meaning | route grammar | max bends | segment label | arrow style | sibling / symmetry note | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `read-left-to-right` | `left-card` | `right-1` | 从总判断进入右侧分解 | implicit horizontal | 0 | - | none | 左主右辅 | 是 | 否 |
| `read-right-stack` | `right-1` | `right-3` | 右侧三张卡由上往下阅读 | implicit vertical | 0 | - | none | 同一列内顺序阅读 | 否 | 否 |
| `read-to-footer` | `right-3` | `footer` | 从分解边界收束到底部结论 | implicit vertical | 0 | - | none | 收口阅读链 | 是 | 否 |

## Spatial constraints

- 左主卡明显比右侧单卡更重，确保先读结论再读拆解。
- 右侧三卡保持同宽、同左边线、同圆角语法，形成 `page24+` 一致的策略页侧栏感。
- footer 必须独立于主卡和三卡，不允许与右侧卡组挤成一列。

## Element styles

- 左主卡使用暖色强调描边，右侧三卡延续后面策略页的统一 info-card 语法。
- 不新增大色块背景，不改变外层工作台壳体。
- 边界页只依赖版式阅读流，不再补额外箭头。

## Acceptance checklist

1. 这页必须看起来和 `page_24+` 是同一家族，而不是旧模板残留。
2. 观众先读到“缓存效果由四个边界共同决定”，再读右侧三张边界卡。
3. `page_19 -> page_21` 仍然不能出现 loop-stage 闪回。

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260420-page21-page22-form/page_21.png)

- 当前 URL：`http://127.0.0.1:4174/?variant=bus-clean&step=page_21`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260420-page21-page22-form/page_21.txt`
- Capture size：`4096 x 2242`

## 三层 Review 总表

| Review 层级 | Harness 结果 | 说明 |
| --- | --- | --- |
| `整体布局 Review` | `3.7/10` | 当前版式已经同构于后面页面，但左右质量仍偏差较大，stage 仍有再平衡空间。 |
| `模块空间结构 Review` | `4.7/10` | 左主卡 + 右三卡 + footer 的模块关系已稳定建立。 |
| `单节点与单边 Review` | `9.3/10` | 无 overlap / crossing / nodePierce / textOverflow，单节点与阅读链很干净。 |

- `mechanicalScore = 6.5/10`
- `overlapCount = 0`
- `crossingCount = 0`
- `nodePierceCount = 0`
- `textOverflowCount = 0`
- `minNodeGap = 20px`
- `minMargin = 62px`
- `leftRightMassDelta = 32%`

## 节点剧本与 Review 表

> 本页节点表只记录 harness 可追溯事实，不再人工编造单节点分数。

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 几何事实 | 当前 Review |
| --- | --- | --- | --- | --- | --- |
| `left-card` | 先用一句话收住“边界共同决定缓存效果”。 | 承接 `page_19` 的稳定缓存话题 | lead card | `directionalTightestPx=28`、`internalTightestPx=95` | 主卡本身很稳，是当前页最清楚的阅读起点。 |
| `right-1` | 讲收集边界：少而全，别暴力 Permute。 | 新增 | info card | `directionalTightestPx=20`、`internalTightestPx=44` | 与下两卡之间的垂直间距偏紧，是当前页最紧的局部。 |
| `right-2` | 讲首启边界：stable.upipelinecache 把压力前置。 | 新增 | info card | `directionalTightestPx=20`、`internalTightestPx=44` | 与上下卡保持统一，但仍受右列间距约束。 |
| `right-3` | 讲平台 / 环境边界：状态模型不同，环境变化可触发失效。 | 新增 | info card | `directionalTightestPx=20`、`internalTightestPx=54` | 高度足够，文本安全；右列整体的紧感仍来自模块间距。 |
| `footer` | 最后一句把后面策略页的阅读口径钉住。 | 承接 `page_22` | footer bar | `directionalTightestPx=60`、`internalTightestPx=12` | 与上层内容有清晰断带，承担收束职责。 |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 几何事实 | 当前 Review |
| --- | --- | --- | --- | --- | --- |
| `read-left-to-right` | 从左主判断进入右上第一张边界卡。 | 是 | implicit reading flow | 无显式线段，因此 `crossing=0`、`nodePierce=0` | 先主后辅的阅读方向成立。 |
| `read-right-stack` | 右侧三张边界卡由上往下阅读。 | 否 | implicit reading flow | 无显式线段，因此 `crossing=0`、`nodePierce=0` | 模块语义已统一，但 20px 的卡间距仍偏紧。 |
| `read-to-footer` | 右列读完后收到底部 footer。 | 是 | implicit reading flow | footer 与上层分离明显，无遮挡 | 收束路径清楚，没有旧版双卡的断裂感。 |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_19 -> page_21）

- 帧窗口：`2314 -> 2389`（`75` frames @ 60fps）
- 总时长：`1.250s`
- 时长评估：`in_range`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `75`，相对当前 `Δ+0` frames
- 标准时序评估：`in_range`（标准下限 `1.238s`，上限 `1.671s`）
- 标准调整建议：目标帧长 `75`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`1.238s`
- 标准允许上限：`1.671s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 120.0 | 120.0 | - |
| fade_out | 120.0 | 340.0 | 220.0 | fade_out_page19_focus |
| node_motion | 340.0 | 520.0 | 180.0 | move_page21_title |
| edge_motion | 520.0 | 837.8 | 317.8 | grow_page21_reading_flow |
| fade_in | 837.8 | 1057.8 | 220.0 | fade_in_page21_cards |
| hold_after | 1057.8 | 1237.8 | 180.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_page19_focus | fade_out | fade_out | - | 220.0 | formula |
| move_page21_title | node_move | node_motion | distancePx=0.0 | 180.0 | manual |
| grow_page21_reading_flow | edge_grow | edge_motion | lengthPx=237.0 | 317.8 | formula |
| fade_in_page21_cards | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 2314 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 2333 | 0.317 | fade_out_page19_focus(fade_out) | 无显著边变化 |
| Mid | 2352 | 0.633 | 无显著节点变化 | grow_page21_reading_flow(edge_grow) |
| Q3 | 2370 | 0.933 | fade_in_page21_cards(fade_in) | 无显著边变化 |
| End | 2389 | 1.250 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- `page_19 -> page_21` 的标准时序 verdict 为 `in_range`，这次没有改 frame anchor。
- 图片页退场后直接淡入当前 summary 板，没有旧 loop-stage 闪回。

## 审批状态

`结构统一目标已完成；机械门槛通过。若下一轮继续抬 harness 总分，优先动作是拉开右侧三卡间距并继续平衡左右质量。`
