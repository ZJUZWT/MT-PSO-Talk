# 第 21 页

> 文件类型：`正式动画页`
> Step / Sketch：`page_21` / `formal-page21`
> 上一页：`page_19`
> 下一页：`page_22`

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## Page goal

- 把 `page_21` 固定成和后面策略页同一家族的 summary 板：`左主卡 + 右侧三卡 + footer`。
- 左主卡只回答“什么情况下会失效”，右侧三卡分别拆成 `内容 / 状态变了`、`版本 / 构建变了`、`环境变了`。
- 保留 `page_19 -> page_21` 的淡入淡出逻辑，不改 frame anchor，不引入额外闪回。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `PSO 缓存有效性边界` |
| 新增元素 | 左侧主卡 `left-card`、右侧三张失效条件卡 `right-1/2/3`、底部 `footer` |
| 移动的节点 | 左主卡从旧版 `596px` 收到 `568px` 并右移到 `x=104`；右侧三卡重排成 `102 / 102 / 114` 的高度组合，卡间距统一拉到 `48px` |
| 删除的元素 | 删除旧版“收集边界 / 首启边界 / 平台环境边界”的命名口径 |
| 新增 node / edge | 新增 node：`left-card`、`right-1`、`right-2`、`right-3`、`footer`；新增隐式阅读 edge：`read-left-to-right`、`read-right-stack`、`read-to-footer` |
| 删除 node / edge | 删除 node：`goal-card`、`bridge-card`；删除 edge：`reading-flow-a`、`reading-flow-b` |
| 过渡逻辑替换 | 未改 `page_19 -> page_21` 的 frame anchor；仍是 `page_19` 淡出后直接淡入 summary 板，无主图闪回 |

## Node inventory

| id | text | semantic role | visual kind | anchor region | emphasis | alignment constraints | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `left-card` | `什么时候会失效？` | 本页主判断 | lead card | 左侧主区 | 高 | 与 footer 中轴对齐，右侧留出 `48px` 阅读廊道 | 承接 `page_19` 的 stable/cache 语义 | 不要求同载体 |
| `right-1` | `内容 / 状态变了` | 失效条件卡 1 | info card | 右上 | 中 | 与 `right-2/3` 同宽同左边线，竖向间距 `48px` | 新增 | 不要求同载体 |
| `right-2` | `版本 / 构建变了` | 失效条件卡 2 | info card | 右中 | 中 | 与 `right-1/3` 同宽 | 新增 | 不要求同载体 |
| `right-3` | `环境变了` | 失效条件卡 3 | info card | 右下 | 中 | 与 `right-1/2` 同宽，和 footer 保持 `48px` 间距 | 新增 | 不要求同载体 |
| `footer` | `缓存不是永不失效，而是把可复用边界和重建边界讲清楚。` | 收束条 | footer bar | 底部 | 中 | 与主舞台水平居中 | 承接 `page_22` | 不要求同载体 |

## Edge inventory

| id | from | to | meaning | route grammar | max bends | segment label | arrow style | sibling / symmetry note | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `read-left-to-right` | `left-card` | `right-1` | 从总判断进入右侧分解 | implicit horizontal | 0 | - | none | 左主右辅 | 是 | 否 |
| `read-right-stack` | `right-1` | `right-3` | 右侧三张卡由上往下阅读 | implicit vertical | 0 | - | none | 同一列内顺序阅读 | 否 | 否 |
| `read-to-footer` | `right-3` | `footer` | 从失效条件收束到底部结论 | implicit vertical | 0 | - | none | 收口阅读链 | 是 | 否 |

## Spatial constraints

- 当前 formal review 事实：`minNodeGap=48px`、`minMargin=62px`、`leftMargin=104px`、`rightMargin=152px`。
- 左主卡必须比右侧单卡更重，保持“先读结论，再读拆解”的阅读顺序。
- footer 继续独立于主卡和右列，不允许和右列挤成假四卡。

## Element styles

- 左主卡使用暖色强调描边，右侧三卡沿用后续策略页的统一 info-card 语法。
- 不新增大色块背景，不改变外层工作台壳体。
- 这页只靠版式阅读流完成解释，不额外补箭头。

## Acceptance checklist

1. 这页必须看起来和 `page_24+` 是同一家族，而不是旧模板残留。
2. 观众先读到“什么时候会失效”，再读内容 / 构建 / 环境三个失效条件。
3. `page_19 -> page_21` 仍然不能出现 loop-stage 闪回。

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260420-page21-page22-invalidity-pass-v2/page_21/page_21.png)

- 当前 URL：`http://127.0.0.1:4174/?variant=bus-clean&step=page_21`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260420-page21-page22-invalidity-pass-v2/page_21/page_21.txt`
- Capture size：`4096 x 2242`

## 三层 Review 总表

| Review 层级 | Harness 结果 | 说明 |
| --- | --- | --- |
| `整体布局 Review` | `6/10` | 右列间距已经拉到 `48px`，这页不再是“右侧三卡挤成一列”的旧状态。 |
| `模块空间结构 Review` | `7/10` | 左主卡 + 右三卡 + footer 的模块关系已稳定，阅读顺序和 `page_24+` 同构。 |
| `单节点与单边 Review` | `9.3/10` | 无 overlap / crossing / nodePierce / textOverflow，节点关系干净。 |

- `mechanicalScore = 7.7/10`
- `overlapCount = 0`
- `crossingCount = 0`
- `nodePierceCount = 0`
- `textOverflowCount = 0`
- `minNodeGap = 48px`
- `minMargin = 62px`
- `leftRightMassDelta = 36%`
- `minInternalPadding = 12px`

## 节点剧本与 Review 表

> 本页节点表只记录 harness 可追溯事实，不再人工编造单节点分数。

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 几何事实 | 当前 Review |
| --- | --- | --- | --- | --- | --- |
| `left-card` | 先用一句话收住“缓存会失效，而且要看失效条件”。 | 承接 `page_19` 的稳定缓存语义 | lead card | `directionalTightestPx=48`、`internalTightestPx=12.8` | 作为主判断已经稳定，宽度收口后也没有文字拥塞。 |
| `right-1` | 讲内容 / 状态变化导致旧缓存不再对应同一个 PSO。 | 新增 | info card | `directionalTightestPx=48`、`internalTightestPx=38` | 右列起点清楚，卡间距已经回到 harness 舒适线。 |
| `right-2` | 讲 SharedCode / codegen / Hash / scl 变化带来的重建。 | 新增 | info card | `directionalTightestPx=48`、`internalTightestPx=38` | 和上下卡形成稳定节奏，不再是旧版 20px 紧叠。 |
| `right-3` | 讲 OS / Driver / GPU / API 变化造成的本地缓存失效。 | 新增 | info card | `directionalTightestPx=48`、`internalTightestPx=44` | 作为右列收束卡已经足够稳，和 footer 之间也留出了清楚断带。 |
| `footer` | 最后一句把“复用边界 / 重建边界”钉住。 | 承接 `page_22` | footer bar | `directionalTightestPx=48`、`internalTightestPx=12` | 不再挤在右列下面，承担独立收束职责。 |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 几何事实 | 当前 Review |
| --- | --- | --- | --- | --- | --- |
| `read-left-to-right` | 从左主判断进入右上第一张失效条件卡。 | 是 | implicit reading flow | 无显式线段，因此 `crossing=0`、`nodePierce=0` | 左主右辅的阅读方向成立。 |
| `read-right-stack` | 右侧三张卡由上往下阅读。 | 否 | implicit reading flow | 无显式线段，因此 `crossing=0`、`nodePierce=0` | 右列节奏已经稳定到 `48px` 间距，不再发紧。 |
| `read-to-footer` | 右列读完后收到底部结论。 | 是 | implicit reading flow | footer 与上层分离明显，无遮挡 | 收束路径清楚，没有旧版双卡断裂感。 |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_19 -> page_21）

- 帧窗口：`2314 -> 2389`（`75` frames @ 60fps）
- 总时长：`1.250s`
- 时长评估：`in_range`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `75`，相对当前 `Δ+0` frames
- 标准时序评估：`in_range`（标准下限 `1.238s`，上限 `1.671s`）
- 标准调整建议：目标帧长 `75`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`1.238s`
- 标准允许上限：`1.671s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 120.0 | 120.0 | - |
| fade_out | 120.0 | 340.0 | 220.0 | fade_out_page19_focus |
| node_motion | 340.0 | 520.0 | 180.0 | move_page21_title |
| edge_motion | 520.0 | 837.8 | 317.8 | grow_page21_reading_flow |
| fade_in | 837.8 | 1057.8 | 220.0 | fade_in_page21_cards |
| hold_after | 1057.8 | 1237.8 | 180.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_page19_focus | fade_out | fade_out | - | 220.0 | formula |
| move_page21_title | node_move | node_motion | distancePx=0.0 | 180.0 | manual |
| grow_page21_reading_flow | edge_grow | edge_motion | lengthPx=237.0 | 317.8 | formula |
| fade_in_page21_cards | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 2314 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 2333 | 0.317 | fade_out_page19_focus(fade_out) | 无显著边变化 |
| Mid | 2352 | 0.633 | 无显著节点变化 | grow_page21_reading_flow(edge_grow) |
| Q3 | 2370 | 0.933 | fade_in_page21_cards(fade_in) | 无显著边变化 |
| End | 2389 | 1.250 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- `page_19 -> page_21` 的默认时长带和标准时序都为 `in_range`，这次没有改 frame anchor。
- 图片页退场后直接淡入当前 summary 板，没有旧 loop-stage 闪回。

## 审批状态

`page_21 已经从“内容改了但结构还紧”的状态收口到“文案与几何一致”。下一轮若继续抬分，优先动作不再是拉卡间距，而是继续平衡左右质量。`
