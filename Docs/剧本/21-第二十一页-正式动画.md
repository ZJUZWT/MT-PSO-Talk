# 第 21 页正式动画：`page_21`

> 文件类型：`正式动画页`
> Step / Sketch：`page_21` / `formal-page21`
> 上一页：`page_19`
> 下一页：`page_22`

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `5.3/10` | 几何事实：玩家视角已经立住，但左侧结果卡与手机之间仍有 `minNodeGap=10px` 的紧口袋。 |
| Node 分数 | `9.0/10` | 连续性事实：`stable.upipelinecache`、`ShaderLibrary`、`Phone` 现在沿用 callback 还原阶段同一批 shared carrier，不再复制一套 callback duplicate。 |
| Edge 分数 | `9.3/10` | 几何事实：两条输入边保持上下对称，且复用 callback 还原阶段同一批 shared edge carrier，`crossing=0`、`nodePierce=0`。 |
| 动画分数 | `in_range` | 自动 probe：`page_19 -> page_21` 当前为 `214` 帧 / `3.567s`，Harness 标准时序 verdict=`in_range`。 |

## Page goal

- 把镜头从 `page_19` 的复杂预编译主图里抽出来，先完整还原带蓝色 `cook / rec / stable` 闭环的 `page_18` 主舞台，停一拍，再切到玩家真正感知到的一层。
- 画面只保留两份直接发到设备上的结果资产：`stable.upipelinecache` 和 `ShaderLibrary`。
- 两张资产卡上下对称地指向一个左移放大的手机，表示玩家拿到的是结果，而不是现场再做 `cook / compile / link`。
- 这一页不是讲“从此绝不卡顿”，而是讲“最重的整理工作已经前移，所以运行时更不容易卡顿”。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `玩家视角：直接拿到预构建结果` |
| 新增元素 | `page18-callback-stage`、`callback-computer`、`callback-scl`、`callback-rec`、`callback-stablepc`、完整蓝色 `cook / rec / stable` 回路线、底部收束 `footer`；`stable.upipelinecache / ShaderLibrary / Phone` 改成 shared carrier 形变层，不再额外复制一套 callback 节点 |
| 移动的节点 | `stable.upipelinecache`、`ShaderLibrary`、`Phone` 这 3 个 node 先以 `page_18` 还原位置出现，再沿同一批 shared carrier morph 到玩家视角终态；`Phone` 同时左移放大 |
| 删除的节点 | 最终玩家视角页里不再保留 `Computer`、`rec.upipelinecache`、`stablepc.csv`、`+` merge 点、蓝色 `cook` 支线、`.scl.csv`；也不再单独生成 `callback-phone / callback-shader / callback-pso` 这套重复载体 |
| 新增 edge | callback 阶段补回 `cook` 分叉、`Phone -> rec`、`rec -> Computer`、`expand/build/stable`；最终页保留 `pso-to-phone`、`shader-to-phone` 两条结果资产输入线，且它们直接复用 callback 还原阶段的 shared edge carrier |
| 删除 edge | 最终玩家视角页不再保留 `cook` 分叉、`expand`、`build`、`rec -> Computer` 那整套过程边；`callback-pso-to-phone / callback-shader-to-phone` 也不再作为独立重复边存在 |
| 替换过渡逻辑 | 不再把 `page_21` 当 summary 板；现在是 `page_19` 淡出后，先完整回到 `page_18` 主舞台并停一拍，再让旧舞台左移淡出，最后由玩家视角页接管并进入 `page_22` 的概念总结 |

## Node inventory

| id | text | semantic role | visual kind | anchor region | emphasis | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `pso-card` | `stable.upipelinecache` / `预构建 PSO` | 玩家拿到的 PSO 结果资产 | artifact card | 左上 | 高 | 承接 `page_19` 左侧 stable 输入语义 | 必须复用 `page_18` 还原阶段同一 shared carrier |
| `shader-card` | `ShaderLibrary` / `.ushaderbytecode` | 玩家拿到的 Shader 结果资产 | artifact card | 左下 | 高 | 承接更早章节里的 `ShaderLibrary -> .ushaderbytecode` 语义 | 必须复用 `page_18` 还原阶段同一 shared carrier |
| `phone-shell` | `玩家` | 玩家设备与运行时承载端 | device shell | 右中 | 高 | 承接主舞台里的 `Phone` 语义，但本页改成玩家视角 | 必须复用 `page_18` 还原阶段同一 shared carrier |
| `footer` | `玩家拿到 stable.upipelinecache + ShaderLibrary 后，运行时更不容易卡顿。` | 收束条 | footer bar | 底部 | 中 | 新增 | 不要求同载体 |

## Edge inventory

| id | from | to | meaning | route grammar | max bends | arrow style | sibling / symmetry note | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `pso-to-phone` | `pso-card` | `phone-shell` | 预构建 PSO 结果直接进入玩家设备 | horizontal straight segment | 0 | accent solid | 与 `shader-to-phone` 成上下对称兄弟边 | 是 | 必须复用 `page_18` 还原阶段同一 shared edge carrier |
| `shader-to-phone` | `shader-card` | `phone-shell` | ShaderLibrary 结果直接进入玩家设备 | horizontal straight segment | 0 | accent solid | 与 `pso-to-phone` 成上下对称兄弟边 | 是 | 必须复用 `page_18` 还原阶段同一 shared edge carrier |

## Spatial constraints

- 两张左侧资产卡必须围绕手机中线做上下对称，不能回到 `page_19` 那种树状展开。
- 手机必须左移并放大，形成这一页唯一的玩家视角锚点。
- `footer` 只能承担结论收束，不能再和手机或左卡发生压叠。
- 当前 formal review 事实：`mechanicalScore=7.3/10`、`minNodeGap=10px`、`minMargin=32px`、`overlapCount=0`、`crossingCount=0`、`nodePierceCount=0`、`textOverflowCount=0`。

## Element styles

- 两张资产卡统一使用橙色强调描边，表示它们都属于“预先准备好的结果”。
- 不新增大色块背景，继续沿用现有工作台背景和白底舞台。
- 手机内部仍保留 `Vertex -> GPU -> Pixels` 的运行时骨架，但不再出现 `RUNTIME` 标签与玩家文字重叠。

## Acceptance checklist

1. 观众必须一眼读出“玩家拿到的是结果资产，而不是后台整理过程”。
2. 两张左侧资产卡必须直接使用技术名：`stable.upipelinecache`、`ShaderLibrary`。
3. 蓝色 `cook` 及电脑侧整理节点必须完全离场，不能在这一页残留。
4. `page_19 -> page_21` 新增一页后，总时长必须增长，不能压缩原有 `page_19` 退场节奏。
5. `stable.upipelinecache`、`ShaderLibrary`、`Phone` 和两条输入边必须沿用还原阶段同一批 shared carrier，不允许额外 fade in 一套副本。

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260421-page21-player-view/page21-player-view.png)

- 当前 URL：`http://127.0.0.1:4174/?step=page_21`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260421-page21-player-view/page21-player-view.txt`
- Capture size：`4096 x 2404`

## 三层 Review 总表

| Review 层级 | Harness 结果 | 说明 |
| --- | --- | --- |
| `整体布局 Review` | `5.3/10` | 画面语义已经很明确，但左卡与手机之间的最近间隙仍偏紧。 |
| `模块空间结构 Review` | `6/10` | 两张资产卡 + 手机 + footer 的四段关系已经成立，比旧 summary 板更贴近本页叙事。 |
| `单节点与单边 Review` | `9.3/10` | 无 overlap / crossing / nodePierce / textOverflow，单节点与两条输入边都保持干净。 |

- `mechanicalScore = 7.3/10`
- `verdict = Open the layout before critic pass`
- Top fixes：
  1. `minNodeGap=10px`，仍低于 `48px` comfort floor。
  2. `minMargin=32px`，仍低于 `40px` safety floor。
  3. 还有 `2` 个局部口袋的 side clearance 低于 `48px`。

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 几何事实 | 当前 Review |
| --- | --- | --- | --- | --- | --- |
| `pso-card` | 玩家拿到的预构建 PSO 结果。 | 承接 `page_19` 的 stable 输入语义 | artifact card | 左上强调卡，formal review 无 text overflow；从 `STABLE_UPIPE_BOX` 同一 shared carrier 形变而来 | 文字和职责都清楚，而且不再额外生成 callback duplicate。 |
| `shader-card` | 玩家拿到的 Shader 结果资产，补 `.ushaderbytecode`。 | 承接 `ShaderLibrary` 章节语义 | artifact card | 左下强调卡，双行技术名 fit 通过；从 `BYTECODE_BOX` 同一 shared carrier 形变而来 | 和上卡形成对称，读图关系稳定。 |
| `phone-shell` | 玩家设备与运行时执行端。 | 承接主舞台 `Phone` 语义 | device shell | 手机左移放大后无越界，但与左卡间距仍偏紧；沿用主舞台 `Phone` 同一 carrier 左移放大 | 本页最强焦点，玩家视角已经立住。 |
| `footer` | 用一句话把“更不容易卡顿”收住。 | 新增 | footer bar | 已从手机底部拉开，不再重叠 | 收束清楚，但仍有继续上提手机或拉开底边的空间。 |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 几何事实 | 当前 Review |
| --- | --- | --- | --- | --- | --- |
| `pso-to-phone` | `stable.upipelinecache` 直接送进玩家设备。 | 是 | accent solid arrow | horizontal straight segment、无 crossing / nodePierce；沿用 callback 还原阶段上路输入线同一 carrier 变形 | 上路输入现在是平直水平线，而且不再叠一条新的 callback duplicate。 |
| `shader-to-phone` | `ShaderLibrary` 直接送进玩家设备。 | 是 | accent solid arrow | horizontal straight segment、与上边形成对称；沿用 callback 还原阶段下路输入线同一 carrier 变形 | 下路输入和上路节奏一致，页面语义一眼可读。 |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_19 -> page_21）

- 帧窗口：`2404 -> 2618`（`214` frames @ 60fps）
- 总时长：`3.567s`
- 时长评估：`too_long`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `156`，相对当前 `Δ-58` frames
- 标准时序评估：`in_range`（标准下限 `3.552s`，上限 `4.795s`）
- 标准调整建议：目标帧长 `214`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> callback_node_motion -> callback_edge_motion -> callback_hold -> callback_shift_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`3.552s`
- 标准允许上限：`4.795s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 120.0 | 120.0 | - |
| fade_out | 120.0 | 340.0 | 220.0 | fade_out_page19_focus |
| callback_node_motion | 340.0 | 730.9 | 390.9 | restore_page18_callback_stage |
| callback_edge_motion | 730.9 | 1491.1 | 760.2 | grow_page18_callback_loop |
| callback_hold | 1491.1 | 1811.1 | 320.0 | hold_restored_page18_stage |
| callback_shift_out | 1811.1 | 2270.4 | 459.3 | shift_out_page18_callback_stage, fade_out_page18_callback_stage |
| node_motion | 2270.4 | 2720.2 | 449.8 | move_page21_player_phone |
| edge_motion | 2720.2 | 3191.7 | 471.5 | grow_page21_asset_routes |
| fade_in | 3191.7 | 3411.7 | 220.0 | fade_in_page21_player_view |
| hold_after | 3411.7 | 3551.7 | 140.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_page19_focus | fade_out | fade_out | - | 220.0 | formula |
| restore_page18_callback_stage | node_move | callback_node_motion | distancePx=222.0 | 390.9 | formula |
| grow_page18_callback_loop | edge_grow | callback_edge_motion | lengthPx=827.0 | 760.2 | formula |
| hold_restored_page18_stage | node_move | callback_hold | distancePx=0.0 | 320.0 | manual |
| shift_out_page18_callback_stage | node_move | callback_shift_out | distancePx=294.0 | 459.3 | formula |
| fade_out_page18_callback_stage | fade_out | callback_shift_out | - | 220.0 | formula |
| move_page21_player_phone | node_move | node_motion | distancePx=284.0 | 449.8 | formula |
| grow_page21_asset_routes | edge_grow | edge_motion | lengthPx=442.0 | 471.5 | formula |
| fade_in_page21_player_view | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 2404 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 2458 | 0.900 | 无显著节点变化 | grow_page18_callback_loop(edge_grow) |
| Mid | 2511 | 1.783 | hold_restored_page18_stage(node_move) | 无显著边变化 |
| Q3 | 2564 | 2.667 | move_page21_player_phone(node_move) | 无显著边变化 |
| End | 2618 | 3.567 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- `page_19 -> page_21` 现在不再直接淡入最终玩家页，而是补上了“完整还原 `page_18` 蓝色闭环 -> 停一拍 -> 旧舞台左移淡出 -> 玩家页接管”的完整四段。
- `page_21` 的 frame anchor 已从 `2578` 后移到 `2618`，新增 callback 动画没有压缩原有玩家视角落位节奏。
- 2026-04-21 节奏微调：`page_19` 的淡出与 callback 恢复现在前置了一点，`page_21` 外层板子的淡入按两倍速度收紧，旧舞台会更快回到完整 `page_18` 状态；同时左移退场被推后，所以不再是“刚回到 page18 几帧就离开”。
- 顶部 `stable.upipelinecache`、`ShaderLibrary`、`Phone` 和两条输入边现在沿用 callback 还原阶段的同一批 shared carrier 一路 morph 到终态，不再单独 fade in 一套重复元素。
- 当前 duration band 仍显示 `too_long`，但 Harness 标准时序 verdict 已经回到 `in_range`；这次以“完整表达 callback 逻辑”为优先。

## 审批状态

`page_21` 已经从旧的“缓存边界复述板”改成“玩家视角结果页”。当前主要剩余问题不是语义，而是几何仍然偏紧：左卡到手机、手机到底栏的距离都还能继续松开。
