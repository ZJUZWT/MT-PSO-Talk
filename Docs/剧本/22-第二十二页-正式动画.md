# 第 22 页正式动画：`page_22`

> 文件类型：`正式动画页`
> Step / Sketch：`page_22` / `formal-page22`
> 上一页：`page_21`
> 下一页：`page_24`

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `4.7/10` | 主表已经重算到 `left/right=64px` 外边距并把横向占宽压回 harness 阈值内，但左右质量差仍有 `31%`。 |
| Node 分数 | `5.7/10` | 三列五行反证表的读取顺序稳定，左右箭道已经固定到 `52px`，当前最紧垂直间隔仍是主表到底部 footer 的 `34px`。 |
| Edge 分数 | `9.3/10` | 两条暖色纵向箭线现在没有 crossing / nodePierce / overflow blocker，表内阅读流也更明确。 |
| 动画分数 | `in_range` | `page_21 -> page_22` 仍是 `72` 帧 / `1.200s`，Harness v1.0 verdict 没变，当前改动只发生在页内版式。 |

## Page goal

- `page_21` 已经把“玩家拿到的是结果资产”钉住，`page_22` 现在不再做抽象定义，而是把前文收成一张 `5 行反证表`。
- 左列统一写“非要这么干？”的头铁选项，中列改回和 `NotesPanel` 同模型的数字圈圈，只保留真实累计客观事实编号，右列直接写硬后果。
- 5 行逻辑必须按 `SharedShaderCode -> 预编译 -> 构建机二进制 -> cook 全量统计 -> 新包直接吃旧 rec` 递进。
- 去掉顶部 `前文收束` 小标题，三列标题都改成居中。
- 中列横向收窄，左右两列同步放大，左右文案字号也一起抬高；右列后果句不再手动拆成两行，交给版面自然换行；第 1 行左列 `不打开 SharedShaderCode` 也保持单行。
- 左右两个 gap 箭道要从旧版 `24px` 拉到 `52px`，箭线与分隔线一起从冷灰改成暖色高对比，让阅读方向更像一条从上到下的通道。
- 左列这条论点实际用到的事实圈需要做发光命中态；左侧 NotesPanel 里对应的累计客观事实编号也要同步发光。
- footer 继续保留一句收束；下方继续保留 `样本 B` 图，但去掉 `样本 B` 这行字。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `我的理解：PSO 与 PSO Cache` |
| 本轮主要修正 | 把 `left-card + right-1/2/3` 改成 `left-column + facts-column + right-column` 三列五行反证表；中列改回数字圈圈，并严格使用真实累计客观事实编号 `6 / 8`、`5`、`12 / 13`、`2 / 9`、`10 / 11`；同时把主表重算成 `left=(64,138,400,434)`、`facts=(516,138,248,434)`、`right=(816,138,400,434)`，把左右箭道从 `24px` 拉到 `52px`，并把箭线与分隔线由冷灰改成暖色高对比；右列后果句不再手动硬拆两行；footer 下方保留 `样本 B` 图，但不显示图标题。 |
| 移动的节点 | 原有 `left-card / right-1 / right-2 / right-3` 被替换为三列容器；本轮又把左列整体右移 `4px`，左右主列各收 `4px`，以保住 `52px` 箭道同时让 harness 的 stage envelope 过线；footer 保留。 |
| 新增元素 | 左右两个 gap 的纵向箭头、三列内部分隔线、5 个发光事实圈底光、footer 下方的无标题 `样本 B` 图。 |
| 删除元素 | `样本 B` 图上的标题字。 |
| 新增 node / edge | 左右两个 gap 的纵向递进箭头、5 个 glow node、`sample-b-strip`。 |
| 删除 node / edge | 删除旧的“三步主链 lead card + 三张补充卡”读法。 |
| 替换的行为 | `page_22` 不再回顾 `SharedCode -> rec -> stable` 三步，而是改成“五个不能 + 事实链 + 后果”的反证读法；左右 gap 的纵向箭头也从“细灰提示线”换成“暖色主通道”，专门承担从上到下的递进感。 |

## Node inventory

| id | text | semantic role | visual kind | 当前页职责 |
| --- | --- | --- | --- | --- |
| `left-column` | `非要这么干？` | 左列问题带 | column card | 连续抛出 5 个“不能这么做”的选项。 |
| `facts-column` | `事实` | 中列证据带 | column card | 用和 `NotesPanel` 一致的数字圈圈承载真实累计客观事实编号，把前文约束压成链路索引。 |
| `right-column` | `那就会这样` | 右列后果带 | column card | 把每一行反着做的硬后果直接写出来。 |
| `fact-glow-*` | `发光事实圈` | 事实命中强调 | glow underlay | 中列事实圈发光，同时左侧 NotesPanel 里对应的累计客观事实编号也同步发光，帮助观众先抓主依据。 |
| `footer` | `PSO 的成本不会消失，只会转移。` | 收束条 | footer bar | 把整页结论压成一句可复述的话。 |
| `sample-b-strip` | `样本 B 图（无标题）` | 底部例子 | image strip | 保留 footer 下方那张图，但不再显示 `样本 B` 这行字。 |

## Edge inventory

| id | from | to | meaning | 当前页说明 |
| --- | --- | --- | --- | --- |
| `read-left-to-facts` | `left-column` | `facts-column` | 每一行先抛问题，再去中列找依据 | 先看“别这么干”，再看对应的真实客观事实编号。 |
| `read-facts-to-right` | `facts-column` | `right-column` | 中列事实导向右列后果 | 读完依据以后，自然落到“那就会这样”。 |
| `read-top-to-bottom` | `row-1` | `row-5` | 五行逻辑按顺序递进 | 左右两个 gap 里的纵向箭头一起强调从上到下深入。 |
| `read-to-footer` | `right-column` | `footer` | 从五行反证收束到底部总结 | 最后落到“成本不会消失，只会转移”。 |

## Spatial constraints

- 三列主框：`left=(64,138,400,434)`、`facts=(516,138,248,434)`、`right=(816,138,400,434)`；左右外边距统一收成 `64px`。
- 5 行统一由分隔线切开；左右两个 gap 里各放 1 条从上到下的箭头，箭道宽度固定为 `52px`，强调递进。
- 中列 5 行编号固定映射为：`6 / 8`、`5`、`12 / 13`、`2 / 9`、`10 / 11`，并以数字圈圈横向居中排布。
- 左列论点实际用到的事实圈都要带发光底光；本页当前映射下即 `6 / 8`、`5`、`12 / 13`、`2 / 9`、`10 / 11` 全部发光；左侧 NotesPanel 的累计事实编号 `2 / 5 / 6 / 8 / 9 / 10 / 11 / 12 / 13` 也同步发光。
- `left / right` 两列宽度必须一致；三列标题都要居中。
- 两条 gap 箭线使用暖色高对比主色，分隔线也同步去灰化，不能再读成背景缝隙。
- footer 使用 `box=(204,606,872,54)`；下方继续保留 `sample-b-strip=(426,674,428,72)`，水平居中。
- 第 1 行视觉权重可略高，因为它承担“系统源头开关”的职责。

## Acceptance checklist

1. 第 1 行必须先回答 `SharedShaderCode`，因为它是整套系统的源头开关。
2. 中列必须只保留真实累计客观事实编号，且使用数字圈圈承载，不能再出现 `A1 / A2` 或手写顺序号，也不重复写事实句。
3. 第 5 行必须明确读出“新包不能直接吃旧 `.rec.upipelinecache`”。
4. 5 行逻辑必须从上到下递进，不能被读成并列吐槽。
5. 左右两列要明显比中列更宽，左右文案也要明显比旧版更大。
6. 左列论点实际用到的事实圈必须发光，左侧 NotesPanel 里对应的累计客观事实编号也必须同步发光。
7. `page_21 -> page_22` 仍然不能出现旧 loop-stage 回闪。

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260422-page22-flow-pass2/page22-flow-pass2.png)

- 当前 URL：`http://127.0.0.1:4174/?step=page_22`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260422-page22-flow-pass2/page22-flow-pass2.txt`
- Capture size：`4096 x 2404`
- `2026-04-22` 当前这轮已经补抓到新截图；截图里可以直接看到左右两个 `52px` 暖色箭道和收窄后的中列都已落地。

## 三层 Review 总表

| Review 层级 | Harness 结果 | 说明 |
| --- | --- | --- |
| `整体布局 Review` | `4.7/10` | 当前 formal sketch 已经把左右外边距收成 `64px` 并把主表宽度压回 harness 阈值内，但 family 级别的左右质量差仍有 `31%`。 |
| `模块空间结构 Review` | `5.7/10` | 三列五行反证表的阅读顺序稳定，左右箭道已经拉到 `52px`；当前最紧的垂直间隔是主表到底部 footer 的 `34px`，仍低于 `48px` comfort floor。 |
| `单节点与单边 Review` | `9.3/10` | 无 overlap / crossing / nodePierce / textOverflow；前台 Edge probe 也确认所有卡片仍有正 padding。 |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 几何事实 | 当前 Review |
| --- | --- | --- | --- | --- | --- |
| `left-column` | 左列统一抛出 5 个“不能这么做”的选项。 | 承接 `page_21` 之后的总结需求 | column card | 与中右两列等高对齐；当前框为 `x=64, width=400`，内部 5 行靠分隔线切开 | 从旧 summary 卡改成问题带，同时给更大的字号留出空间，并给两侧箭道让出稳定的 `52px`。 |
| `facts-column` | 中列只保留真实累计客观事实编号。 | 新增 | column card | `6 / 8`、`5`、`12 / 13`、`2 / 9`、`10 / 11` 以数字圈圈整体居中；列宽收窄到 `248`，左右各让出 `52px` 箭道，左列论点实际用到的事实圈带 glow | 页面中间更像“索引层”，不会和左右结论抢戏。 |
| `right-column` | 每一行都给出反着做的硬后果。 | 新增 | column card | 与左列 5 行逐行对齐；当前框为 `x=816, width=400`，分隔线和轮廓同步去灰化 | 右列不再抽象总结，而是直接给出“翻车后果”，阅读更利落。 |
| `fact-glow-*` | 左列论点所用事实圈的发光底光。 | 新增 | glow underlay | 跟随对应事实圈位置，不额外抢主叙事 | 帮助观众先抓住每行主依据。 |
| `footer` | `PSO 的成本不会消失，只会转移。` | 承接后续策略页的 tradeoff 主题 | footer bar | 前台 Edge `tightestPaddingPx=11.9` | 结论收口清楚，没有贴边。 |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 几何事实 | 当前 Review |
| --- | --- | --- | --- | --- | --- |
| `read-left-to-facts` | 每一行先看左侧“头铁选项”，再去中列找依据。 | 是 | implicit reading flow | 无显式线段，因此 `crossing=0`、`nodePierce=0` | 三列结构把阅读顺序压得更死。 |
| `read-facts-to-right` | 中列事实继续导向右列后果。 | 是 | implicit reading flow | 无显式线段，因此 `crossing=0`、`nodePierce=0` | 观众能自然读成“问题 -> 事实 -> 后果”。 |
| `read-top-to-bottom` | 5 行按顺序逐级递进。 | 否 | gap arrows + implicit reading flow | 左右两个 `52px` gap 的纵向箭头从上到下贯穿主体高度，颜色改成暖色高对比 | SharedShaderCode 在最上，新包旧 rec 在最下，递进关系成立，而且更容易被读成“主通道”。 |
| `read-to-footer` | 用 footer 把五行反证压成一句 tradeoff。 | 是 | implicit reading flow | footer 与主体保持清晰分离 | 收束关系稳定。 |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_21 -> page_22）

- 帧窗口：`2618 -> 2690`（`72` frames @ 60fps）
- 总时长：`1.200s`
- 时长评估：`in_range`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `72`，相对当前 `Δ+0` frames
- 标准时序评估：`in_range`（标准下限 `1.125s`，上限 `1.519s`）
- 标准调整建议：目标帧长 `72`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`1.125s`
- 标准允许上限：`1.519s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 80.0 | 80.0 | - |
| fade_out | 80.0 | 300.0 | 220.0 | fade_out_player_page |
| node_motion | 300.0 | 571.2 | 271.2 | move_page22_summary_board |
| edge_motion | 571.2 | 825.2 | 254.0 | grow_page22_reading_flow |
| fade_in | 825.2 | 1045.2 | 220.0 | fade_in_page22_cards |
| hold_after | 1045.2 | 1125.2 | 80.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_player_page | fade_out | fade_out | - | 220.0 | formula |
| move_page22_summary_board | node_move | node_motion | distancePx=96.0 | 271.2 | formula |
| grow_page22_reading_flow | edge_grow | edge_motion | lengthPx=152.0 | 254.0 | formula |
| fade_in_page22_cards | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 2618 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 2636 | 0.300 | fade_out_player_page(fade_out)、move_page22_summary_board(node_move) | 无显著边变化 |
| Mid | 2654 | 0.600 | 无显著节点变化 | grow_page22_reading_flow(edge_grow) |
| Q3 | 2672 | 0.900 | fade_in_page22_cards(fade_in) | 无显著边变化 |
| End | 2690 | 1.200 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- `page_21 -> page_22` 的标准时序 verdict 仍是 `in_range`，当前 frame anchor 为 `2618 -> 2690`。
- 当前修改发生在 `page_22` 的内部版式：从“左大卡 + 右三卡”改成“三列五行反证表”，不影响这段 handoff 的总时长。
- 页面切换继续保持整板 crossfade，没有旧 loop-stage 闪回。

## 审批状态

`page_22` 的内容口径已经改成“三列五行反证表”：左列抛出 5 个“不能这么做”的选项，中列改回和 `NotesPanel` 一致的数字圈圈，只保留真实累计客观事实编号 `6 / 8`、`5`、`12 / 13`、`2 / 9`、`10 / 11`，右列直接写反着做的后果；左右两个 gap 再用纵向箭头把递进关系压在版式里。其中第 1 行 `SharedShaderCode` 被提升为整套系统的源头开关。这一轮又进一步把主表重算成 `400 / 248 / 400` 三列，左右箭道固定到 `52px`，箭线与分隔线统一去灰化成暖色高对比，并把左列整体右移 `4px` 让 formal harness 的 stage layout 回到预算内；第 1 行左列 `不打开 SharedShaderCode` 不再拆成两行，右列后果句也不再手动拆成两行，改成一句话自然换行。footer 下方继续保留 `样本 B` 图，但只留图片、不显示 `样本 B` 这行字。当前剩余风险主要转成 5 行文案密度、三列对齐和 footer 落点控制，不再是旧 summary family 的叙事偏差。
