# 第 22 页

> 文件类型：`正式动画页`
> Step / Sketch：`page_22` / `formal-page22`
> 上一页：`page_21`
> 下一页：`page_24`

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## Page goal

- 把 `page_22` 从旧的单大卡观点页改成和 `page_24+` 相同的 summary 家族。
- 左主卡只做一个核心判断，右侧三卡分别拆成 `对象 / 方法 / 工程取舍`。
- 保留 `page_21 -> page_22` 的交接节奏，不改 anchor，只替换板内组织方式。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `我的理解：PSO 与 PSO Cache` |
| 新增元素 | 左侧主卡 `left-card`、右侧三张卡 `right-1/2/3`、底部 `footer` |
| 移动的节点 | 旧 `narrative-box` 的内容被拆成“左侧主判断 + 右侧三类分解” |
| 删除的元素 | 删除旧版单大卡 typeset 结构 |
| 新增 node / edge | 新增 node：`left-card`、`right-1`、`right-2`、`right-3`、`footer`；新增隐式阅读 edge：`read-left-to-right`、`read-right-stack`、`read-to-footer` |
| 删除 node / edge | 删除 node：`narrative-box`；删除 edge：`read-kicker-to-body`、`read-body-to-footer` |
| 过渡逻辑替换 | 未改 `page_21 -> page_22` 的 frame anchor；仍然沿用整板淡出 / 淡入，不引入旧主图闪回 |

## Node inventory

| id | text | semantic role | visual kind | anchor region | emphasis | alignment constraints | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `left-card` | `预编译的 PSO 不会消失，只会转移。` | 本页主判断 | lead card | 左侧主区 | 高 | 与 footer 居中轴对齐 | 承接 `page_21` 的边界结论 | 不要求同载体 |
| `right-1` | `对象` | 分解卡 1 | info card | 右上 | 中 | 与右侧另外两卡同宽同左边线 | 新增 | 不要求同载体 |
| `right-2` | `方法` | 分解卡 2 | info card | 右中 | 中 | 与 `right-1/3` 同宽 | 新增 | 不要求同载体 |
| `right-3` | `工程取舍` | 分解卡 3 | info card | 右下 | 中 | 与 `right-1/2` 同宽 | 新增 | 不要求同载体 |
| `footer` | `PSO Cache 是工程方法，不是让代价凭空消失的魔法。` | 收束条 | footer bar | 底部 | 中 | 与主舞台水平居中 | 承接 `page_24` | 不要求同载体 |

## Edge inventory

| id | from | to | meaning | route grammar | max bends | segment label | arrow style | sibling / symmetry note | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `read-left-to-right` | `left-card` | `right-1` | 从核心判断进入右侧概念拆解 | implicit horizontal | 0 | - | none | 左主右辅 | 是 | 否 |
| `read-right-stack` | `right-1` | `right-3` | 右侧三张卡由上往下阅读 | implicit vertical | 0 | - | none | 同列阅读链 | 否 | 否 |
| `read-to-footer` | `right-3` | `footer` | 从工程取舍收束到底部总结 | implicit vertical | 0 | - | none | 收束阅读链 | 是 | 否 |

## Spatial constraints

- 左主卡必须比右侧三卡更重，让“核心判断”先落地。
- 右侧三卡和 `page_24+` 保持同一列节奏，避免这页成为孤立样式。
- footer 只负责一句话收束，不和主卡争权重。

## Element styles

- 左主卡延续暖色强调描边，但不额外引入新背景底板。
- 右侧三卡沿用后面策略页的 info-card 语法。
- 本页不新增流程图，不回到旧的大段排版模板。

## Acceptance checklist

1. 这页必须和 `page_24+` 一眼同构。
2. 观众先读到“预编译的 PSO 不会消失，只会转移”，再读对象 / 方法 / 工程取舍。
3. `page_21 -> page_22` 仍然不能出现旧 loop-stage 回闪。

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260420-page21-page22-form/page_22.png)

- 当前 URL：`http://127.0.0.1:4174/?variant=bus-clean&step=page_22`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260420-page21-page22-form/page_22.txt`
- Capture size：`4096 x 2242`

## 三层 Review 总表

| Review 层级 | Harness 结果 | 说明 |
| --- | --- | --- |
| `整体布局 Review` | `3.7/10` | 外层结构已统一，但左右质量和高度占比仍有继续平衡空间。 |
| `模块空间结构 Review` | `4.7/10` | 左主卡 + 右三卡 + footer 的模块关系已成型。 |
| `单节点与单边 Review` | `9.3/10` | 无 overlap / crossing / nodePierce / textOverflow，单节点与阅读链干净。 |

- `mechanicalScore = 6.5/10`
- `overlapCount = 0`
- `crossingCount = 0`
- `nodePierceCount = 0`
- `textOverflowCount = 0`
- `minNodeGap = 20px`
- `minMargin = 62px`
- `leftRightMassDelta = 32%`

## 节点剧本与 Review 表

> 本页节点表只记录 harness 可追溯事实，不再人工编造单节点分数。

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 几何事实 | 当前 Review |
| --- | --- | --- | --- | --- | --- |
| `left-card` | 先钉住“预编译的 PSO 不会消失，只会转移”。 | 承接 `page_21` 的边界总结 | lead card | `directionalTightestPx=28`、`internalTightestPx=74.6` | 主判断足够醒目，是这一页的阅读锚点。 |
| `right-1` | 讲对象：PSO 是运行时对象。 | 新增 | info card | `directionalTightestPx=20`、`internalTightestPx=44` | 右列起点清楚，但与下卡间距仍偏紧。 |
| `right-2` | 讲方法：PSO Cache 是工程方法。 | 新增 | info card | `directionalTightestPx=20`、`internalTightestPx=44` | 语义承上启下，版式紧度与 `right-1` 一致。 |
| `right-3` | 讲工程取舍：优先换掉运行时尖峰，再回收首启与空间开销。 | 新增 | info card | `directionalTightestPx=20`、`internalTightestPx=54` | 高度足够，说明性文本没有溢出。 |
| `footer` | 用一句话把“工程方法”定性。 | 承接 `page_24` 的策略展开 | footer bar | `directionalTightestPx=60`、`internalTightestPx=12` | 与主体区分清楚，收束语义明确。 |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 几何事实 | 当前 Review |
| --- | --- | --- | --- | --- | --- |
| `read-left-to-right` | 从核心判断进入右上第一张分解卡。 | 是 | implicit reading flow | 无显式线段，因此 `crossing=0`、`nodePierce=0` | 左主右辅的阅读顺序成立。 |
| `read-right-stack` | 右侧三张卡从对象读到工程取舍。 | 否 | implicit reading flow | 无显式线段，因此 `crossing=0`、`nodePierce=0` | 逻辑清楚，仍建议后续继续拉开右列间距。 |
| `read-to-footer` | 右列读完后收到底部总结。 | 是 | implicit reading flow | footer 与上层分离清楚 | 收束路径顺，没有旧单大卡的“段落压住”问题。 |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_21 -> page_22）

- 帧窗口：`2389 -> 2460`（`71` frames @ 60fps）
- 总时长：`1.183s`
- 时长评估：`too_short`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `72`，相对当前 `Δ+1` frames
- 标准时序评估：`in_range`（标准下限 `0.993s`，上限 `1.341s`）
- 标准调整建议：目标帧长 `71`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`0.993s`
- 标准允许上限：`1.341s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 100.0 | 100.0 | - |
| fade_out | 100.0 | 320.0 | 220.0 | fade_out_validity_board |
| node_motion | 320.0 | 633.0 | 313.0 | move_bridge_title, move_summary_block |
| fade_in | 633.0 | 853.0 | 220.0 | fade_in_bridge_copy |
| hold_after | 853.0 | 993.0 | 140.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_validity_board | fade_out | fade_out | - | 220.0 | formula |
| move_bridge_title | node_move | node_motion | distancePx=110.0 | 284.5 | formula |
| move_summary_block | node_move | node_motion | distancePx=140.0 | 313.0 | formula |
| fade_in_bridge_copy | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 2389 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 2407 | 0.300 | fade_out_validity_board(fade_out) | 无显著边变化 |
| Mid | 2424 | 0.583 | move_bridge_title(node_move)、move_summary_block(node_move) | 无显著边变化 |
| Q3 | 2442 | 0.883 | hold_after 静置 | hold_after 静置 |
| End | 2460 | 1.183 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- `page_21 -> page_22` 的标准时序 verdict 为 `in_range`，这次没有改 frame anchor。
- 一般时长带给出 `Δ+1 frame` 建议，但当前标准动作链已在允许范围内，因此先保留现有节奏，不触碰后续整段动画。
- 页面切换继续保持整板 crossfade，没有旧 loop-stage 闪回。

## 审批状态

`结构统一目标已完成；机械门槛通过。若下一轮继续抬 harness 总分，优先动作仍是拉开右侧三卡间距并继续平衡左右质量。`
