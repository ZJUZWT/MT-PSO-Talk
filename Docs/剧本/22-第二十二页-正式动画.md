# 第 22 页正式动画：`page_22`

> 文件类型：`正式动画页`
> Step / Sketch：`page_22` / `formal-page22`
> 上一页：`page_21`
> 下一页：`page_24`

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## Page goal

- `page_21` 已经把“玩家拿到的是结果资产”钉住，`page_22` 负责继续回答“这套工程应该怎么理解”。
- 左主卡先钉住核心区分：`PSO 是对象，PSO Cache 是方法。`
- 右侧三卡分别补：`PSO` 的对象属性、`PSO Cache` 对 `Shader Module` 的依赖、以及这套方法的代价与适用边界。
- footer 只负责一句收束：`PSO 的成本不会消失，只会转移。`

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `我的理解：PSO 与 PSO Cache` |
| 本轮主要修正 | 把旧版“预编译的 PSO 不会消失，只会转移”改成更明确的对象 / 方法区分，并补上 Shader 依赖、文件参与、适用边界。 |
| 移动的节点 | 无。沿用原来的 `left-card + right-1/2/3 + footer` 几何结构。 |
| 新增元素 | 无。 |
| 删除元素 | 无。 |
| 新增 node / edge | 无。 |
| 删除 node / edge | 无。 |
| 替换的行为 | 只替换卡片内部文案与讲述分工，并把过渡入口改成新的 `page_21 -> page_22` handoff。 |

## Node inventory

| id | text | semantic role | visual kind | 当前页职责 |
| --- | --- | --- | --- | --- |
| `left-card` | `PSO 是对象，PSO Cache 是方法。` | 主判断 | lead card | 先区分对象与方法，再补 tradeoff 和“不是所有项目都需要”。 |
| `right-1` | `PSO：对象` | 对象卡 | info card | 说明 PSO 是被创建、绑定、命中的运行时对象。 |
| `right-2` | `PSO Cache：方法` | 依赖卡 | info card | 说明 PSO Cache 依赖 `Shader Module` 提供数据，`.ushaderbytecode / .scl.csv` 会参与。 |
| `right-3` | `代价 / 适用` | 边界卡 | info card | 说明不是所有项目都需要，而且代价往往比想象中更大。 |
| `footer` | `PSO 的成本不会消失，只会转移。` | 收束条 | footer bar | 把整页结论压成一句可复述的话。 |

## Edge inventory

| id | from | to | meaning | 当前页说明 |
| --- | --- | --- | --- | --- |
| `read-left-to-right` | `left-card` | `right-1` | 从核心区分进入右侧补充卡 | 先讲区分，再讲对象。 |
| `read-right-stack` | `right-1` | `right-3` | 右侧三张卡自上而下阅读 | 从对象读到依赖，再读到代价 / 适用。 |
| `read-to-footer` | `right-3` | `footer` | 从边界卡收束到底部总结 | 最后落到“成本不会消失，只会转移”。 |

## Spatial constraints

- formal review 事实：`overlapCount=0`、`crossingCount=0`、`nodePierceCount=0`、`textOverflowCount=0`
- 当前机械指标：`mechanicalScore=7/10`
- 当前布局指标：`minNodeGap=24px`、`leftRightMassDelta=37%`
- 当前舞台边界：`top=146px`、`right=144px`、`bottom=60px`、`left=88px`
- 当前前台 Edge 文本 padding：`left-card=13.9px`、`right-1=12.5px`、`right-2=12.5px`、`right-3=12.5px`、`footer=11.9px`

## Acceptance checklist

1. 观众必须先读到 `PSO 是对象，PSO Cache 是方法。`
2. 页面里必须明确说出 `PSO Cache` 依赖 `Shader Module`，并让 `.ushaderbytecode / .scl.csv` 进入叙事。
3. 必须说明“不是所有项目都需要 PSO Cache”。
4. 必须把结论收束到“成本不会消失，只会转移”。
5. `page_21 -> page_22` 仍然不能出现旧 loop-stage 回闪。

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260420-page22-meaning-pass1/page22-meaning-pass1.png)

- 当前 URL：`http://127.0.0.1:4174/?step=page_22`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260420-page22-meaning-pass1/page22-meaning-pass1.txt`
- Capture size：`4096 x 2242`
- 前台 Edge probe：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/front-browser-page22-meaning-pass1/page22-probe.json`

## 三层 Review 总表

| Review 层级 | Harness 结果 | 说明 |
| --- | --- | --- |
| `整体布局 Review` | `4.7/10` | 当前页的主要问题仍然是 family 级别的左右质量差与外边界不均，不是内容本身。 |
| `模块空间结构 Review` | `5.7/10` | `left-card + right-1/2/3 + footer` 的阅读顺序稳定，但 `minNodeGap=24px` 仍低于 `48px` comfort floor。 |
| `单节点与单边 Review` | `9.3/10` | 无 overlap / crossing / nodePierce / textOverflow；前台 Edge probe 也确认所有卡片仍有正 padding。 |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 几何事实 | 当前 Review |
| --- | --- | --- | --- | --- | --- |
| `left-card` | 先说清 `PSO` 和 `PSO Cache` 不是同一个层级的东西。 | 承接 `page_21` 的玩家结果视角 | lead card | 前台 Edge `tightestPaddingPx=13.9` | 主判断已经对齐用户口径。 |
| `right-1` | `PSO` 是对象，不是方法。 | 新增 | info card | 前台 Edge `tightestPaddingPx=12.5` | 右列起点清楚。 |
| `right-2` | `PSO Cache` 依赖 `Shader Module`，`.ushaderbytecode / .scl.csv` 会参与。 | 新增 | info card | 前台 Edge `tightestPaddingPx=12.5` | 依赖关系已经补进页面。 |
| `right-3` | 不是所有项目都需要，而且代价往往比想象中更大。 | 新增 | info card | 前台 Edge `tightestPaddingPx=12.5` | 适用边界和代价已经讲清。 |
| `footer` | `PSO 的成本不会消失，只会转移。` | 承接后续策略页的 tradeoff 主题 | footer bar | 前台 Edge `tightestPaddingPx=11.9` | 结论收口清楚，没有贴边。 |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 几何事实 | 当前 Review |
| --- | --- | --- | --- | --- | --- |
| `read-left-to-right` | 从核心区分进入右侧对象卡。 | 是 | implicit reading flow | 无显式线段，因此 `crossing=0`、`nodePierce=0` | 阅读顺序成立。 |
| `read-right-stack` | 从对象读到方法依赖，再读到代价 / 适用。 | 否 | implicit reading flow | 无显式线段，因此 `crossing=0`、`nodePierce=0` | 右列逻辑顺序成立。 |
| `read-to-footer` | 用 footer 把 tradeoff 总结压成一句。 | 是 | implicit reading flow | footer 与主体保持清晰分离 | 收束关系稳定。 |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_21 -> page_22）

- 帧窗口：`2618 -> 2690`（`72` frames @ 60fps）
- 总时长：`1.200s`
- 时长评估：`in_range`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `72`，相对当前 `Δ+0` frames
- 标准时序评估：`in_range`（标准下限 `1.125s`，上限 `1.519s`）
- 标准调整建议：目标帧长 `72`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`1.125s`
- 标准允许上限：`1.519s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 80.0 | 80.0 | - |
| fade_out | 80.0 | 300.0 | 220.0 | fade_out_player_page |
| node_motion | 300.0 | 571.2 | 271.2 | move_page22_summary_board |
| edge_motion | 571.2 | 825.2 | 254.0 | grow_page22_reading_flow |
| fade_in | 825.2 | 1045.2 | 220.0 | fade_in_page22_cards |
| hold_after | 1045.2 | 1125.2 | 80.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_player_page | fade_out | fade_out | - | 220.0 | formula |
| move_page22_summary_board | node_move | node_motion | distancePx=96.0 | 271.2 | formula |
| grow_page22_reading_flow | edge_grow | edge_motion | lengthPx=152.0 | 254.0 | formula |
| fade_in_page22_cards | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 2618 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 2636 | 0.300 | fade_out_player_page(fade_out)、move_page22_summary_board(node_move) | 无显著边变化 |
| Mid | 2654 | 0.600 | 无显著节点变化 | grow_page22_reading_flow(edge_grow) |
| Q3 | 2672 | 0.900 | fade_in_page22_cards(fade_in) | 无显著边变化 |
| End | 2690 | 1.200 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- `page_21 -> page_22` 的标准时序 verdict 仍是 `in_range`，当前 frame anchor 为 `2618 -> 2690`。
- 当前修改只发生在过渡入口和 page22 卡片内部文案与讲述分工，不影响 summary 板本身节奏。
- 页面切换继续保持整板 crossfade，没有旧 loop-stage 闪回。

## 审批状态

`page_22` 的内容口径已经改回“PSO 是对象，PSO Cache 是方法”，并补齐了 Shader 依赖、文件参与、代价和适用边界。当前剩余问题主要还是这套 summary family 的几何密度与左右质量差，不是这一页理解本身的偏差。
