# 第 24 页

> 文件类型：`正式动画页`
> Step / Sketch：`page_24` / `formal-page24`
> 上一页：`page_22`
> 下一页：`page_25`
> 2026-04-21 density 复审版：保留“左 `ShaderCode` 压缩、右 UE 中 PSO `LRU + mmap`”语义不变，同时把 page24 外边距压到约 `58 / 44 / 40 / 44`，左半去掉总头和平台外套框，改成算法行自己标 `(压缩/)解压`，右半继续保留更清楚的策略骨架。

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `5.0/10` | 双栏更满，外边距已收进到 `58 / 44 / 40 / 44`，但局部 pill 间距仍是 comfort floor。 |
| Node 分数 | `5.7/10` | 12 个平台 pill 继续纳入 formal review；最紧局部间距现在抬到 `6px`。 |
| Edge 分数 | `9.3/10` | `swap-out / fill-back` 依旧没有 crossing / nodePierce / overflow blocker。 |
| 动画分数 | `in_range` | `page_22 -> page_24` 与 `page_24 -> page_25` 的 Harness v1.0 verdict 都是 `in_range`；本轮未改 frame anchor。 |

## Page goal

- 把这一页做成“主语明确”的正式双栏，而不是只看结构猜语义。
- 左半明确写成 `ShaderCode` 压缩，并展示 `Windows / macOS / Android / iOS` 四个平台数据。
- 右半明确写成 UE 中 PSO 的 `LRU + mmap` 策略，同时继续保留内存结构和底部方法条。
- 仍然避免解释性段落，所有新增内容都必须是结构标签或真实数据。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `工程策略总览：包体 / 内存` |
| 页面副标题 | 正式双栏图：左边是 `ShaderCode` 压缩数据，右边是 UE 中 PSO 的内存策略。 |
| 当前页核心信息 | `包体` 看 `ShaderCode` 压缩与平台数据，`内存` 看 `LRU + mmap` 下的驻留 / 换出 / 回填。 |
| 与上一页关系 | 从 `page_22` 的理解页切到工程策略页；这次不只保留“数据板 + 主图”，而是把左半压缩矩阵和右半策略骨架都做厚，同时把左半读法再收得更直接。 |
| 新增了哪些页面 / 元素 | 左半新增每条算法名旁边的 `(压缩/)解压` 提示；右半继续保留 `驻留层 / 换出 / 回填 / 映射 / 载体` 结构条与中间 flow lane。 |
| 移动了哪些节点 | `left-card` 改到 `x=44, y=58, 520x560`；`right-card` 改到 `x=588, y=58, 648x560`；`package-row-1/2/3` 再上提到 `y=140 / 280 / 420` 且高度保持 `132px`；`memory`/`disk` 保持 `268x248`；`memory-method-1/2/3` 保持 `190x108`；`footer` 上收至 `y=628`。 |
| 增加 / 删除了哪些 node / edge | 保留 node：`left-card`、`package-row-1/2/3`、`package-row-1/2/3-windows/macos/android/ios`、`right-card`、`memory`、`flow-lane`、`disk`、`memory-method-1/2/3`、`footer`；删除视觉元素：左半 `算法 / 压缩比 / 平台数据` 总头与平台外套框；保留 edge：`swap-out`、`fill-back`。 |
| 替换过渡逻辑 | 页面间过渡逻辑不变；被替换的是页内 review 颗粒度，原先只审 `package-row-*` 外层盒，现在改成“row shell + 12 个平台 pill”一起进入 formal review。 |

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/headless-captures/20260421-page24-density-v3/page_24.png)

- 当前 URL：`http://127.0.0.1:4174/?step=page_24`
- 捕获方式：`browser-api page capture`
- Capture size：`4096 x 2404`
- Capture metadata：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/headless-captures/20260421-page24-density-v3/page_24.txt`
- Mechanical summary：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/slide-review/page_19_plus/pages/page_24.geometry.json`

## Facts & Metrics

| 指标 | 值 | 说明 |
| --- | --- | --- |
| `mechanicalScore` | `7.2/10` | 外边距压缩后仍无 blocker，footer 再上收后把最小外边距拉回到 `40px`。 |
| `verdict` | `Open the layout before critic pass` | 当前剩余问题仍是 comfort floor，不是 blocker。 |
| `minNodeGap` | `6px` | 最紧的是平台 pill 在各自 row 内的局部安全距离。 |
| `minInternalPadding` | `7.4px` | 最紧的是平台 pill 的文字呼吸空间，但仍无 overflow blocker。 |
| `textOverflowCount` | `0` | 本轮 formal review 无文字超框。 |
| `top/right/bottom/left margin` | `58 / 44 / 40 / 44` | 外边距被显著压缩，接近用户要求的“减半”。 |

## 三层 Review 总表

| Review 层级 | 事实依据 | 综合判断 | 综合分数 |
| --- | --- | --- | --- |
| `整体布局 Review` | `top/right/bottom/left margin = 58/44/40/44` | 双栏更满，标题区空白明显减少，footer 也回到 40px 安全线。 | `5.0/10` |
| `模块空间结构 Review` | 左侧 `ShaderCode` 压缩矩阵 + 12 个平台 pill + 右侧 `LRU + mmap` 主图 + flow lane + 底部 3 张方法条 + footer | 读序从“压缩数据 -> 平台数据 -> 驻留/换出/映射 -> 方法条”连续成立。 | `5.7/10` |
| `单节点与单边 Review` | `overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`、`childOutOfBounds=0` | 双向箭头、平台 pill、数据行和方法条都稳定。 | `9.3/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 当前渲染载体 | 真实浏览器事实 | 当前 Review |
| --- | --- | --- | --- | --- |
| `left-card` | 左半正式数据板。 | board shell | 标题放大到 `包体`，并补 `ShaderCode 压缩`。 | 通过 |
| `package-row-1` | `LZ4` 的多平台压缩数据外层 row。 | data row | 作为父 row 继续承载压缩比与 4 个平台 pill，算法名旁补 `(压缩/)解压`。 | 通过 |
| `package-row-2` | `zstd` 的多平台压缩数据外层 row。 | data row | 长数字仍无 overflow。 | 通过 |
| `package-row-3` | `Oodle Leviathan` 的多平台压缩数据外层 row。 | data row | 长算法名和 4 个平台 pill 仍在父 row 内。 | 通过 |
| `package-row-1/2/3-windows`、`package-row-1/2/3-macos`、`package-row-1/2/3-android`、`package-row-1/2/3-ios` | 左侧 12 个平台 pill。 | platform pill | 这 12 个 child node 继续纳入 formal review；当前只保留独立 pill，平台名左、时间右。 | 通过 |
| `right-card` | 右半正式结构板。 | board shell | 标题放大到 `内存`，并补 `UE 中 PSO：LRU + mmap` 与一条策略层次条。 | 通过 |
| `memory` | 高频常驻 / 可替换缓存所在区。 | board cluster | 作为内存主图主体继续稳定。 | 通过 |
| `flow-lane` | 中间的换出 / 回填通道。 | narrow connector lane | 把原本漂浮的中间空区收成正式策略骨架。 | 通过 |
| `disk` | 映射视图 / 文件载体所在区。 | board cluster | 与 `memory` 对照关系清楚。 | 通过 |
| `memory-method-1` | `选取策略` 方法条。 | method chip card | 先把 `LRU / Clock / Pin` 放进正式图。 | 通过 |
| `memory-method-2` | `回填路径` 方法条。 | method chip card | `mmap / paging` 被结构化地挂到图里。 | 通过 |
| `memory-method-3` | `外存载体` 方法条。 | method chip card | `file / SQL / KV` 不再只存在于口播。 | 通过 |
| `footer` | 页尾极短收束句。 | footer bar | 仍保持短句，不回到长段说明。 | 通过 |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 当前渲染载体 | 真实浏览器事实 | 当前 Review |
| --- | --- | --- | --- | --- |
| `swap-out` | 从内存把内容换到外存。 | horizontal arrow | 文字与箭头仍未贴边。 | 通过 |
| `fill-back` | 从外存按需回填到内存。 | horizontal arrow | 与 `swap-out` 成对出现，没有交叉。 | 通过 |
| `read-left-to-right` | 先读左侧数据，再读右侧结构。 | implicit reading flow | 符合“左包体、右内存”的主要求。 | 通过 |
| `read-structure-to-method-strip` | 读完右侧主图后再读底部方法条。 | implicit reading flow | 新增内容仍然顺着结构逻辑展开。 | 通过 |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_22 -> page_24）

- 帧窗口：`2550 -> 2621`（`71` frames @ 60fps）
- 总时长：`1.183s`
- 时长评估：`too_short`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `72`，相对当前 `Δ+1` frames
- 标准时序评估：`in_range`（标准下限 `1.020s`，上限 `1.376s`）
- 标准调整建议：目标帧长 `71`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`1.020s`
- 标准允许上限：`1.376s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 100.0 | 100.0 | - |
| fade_out | 100.0 | 320.0 | 220.0 | fade_out_bridge_summary |
| node_motion | 320.0 | 659.6 | 339.6 | move_package_board, move_memory_board |
| fade_in | 659.6 | 879.6 | 220.0 | fade_in_package_memory_footer |
| hold_after | 879.6 | 1019.6 | 140.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_bridge_summary | fade_out | fade_out | - | 220.0 | formula |
| move_package_board | node_move | node_motion | distancePx=132.0 | 305.4 | formula |
| move_memory_board | node_move | node_motion | distancePx=168.0 | 339.6 | formula |
| fade_in_package_memory_footer | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 2550 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 2568 | 0.300 | fade_out_bridge_summary(fade_out) | 无显著边变化 |
| Mid | 2586 | 0.600 | move_package_board(node_move)、move_memory_board(node_move) | 无显著边变化 |
| Q3 | 2603 | 0.883 | hold_after 静置 | hold_after 静置 |
| End | 2621 | 1.183 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- 2026-04-21 density 复审版：`page_22 -> page_24` 的标准时序 verdict 仍为 `in_range`，不需要改 `page_24` frame anchor。
- `page_24 -> page_25` 的标准时序 verdict 同样为 `in_range`；当前右半底部方法条可直接承接到 page25 的三张方法卡。
- 这一轮没有压缩原有 base animation；`page_24 = 2621`、`page_25 = 2692` 维持不变。

## 审批状态

`density 复审版通过。当前剩余问题只在 comfort floor，不存在 overlap / crossing / nodePierce / textOverflow / childOutOfBounds blocker；page24 已经从“安全但偏空”收成“更满的正式矩阵 + 策略骨架”。`
