# 第 24 页

> 文件类型：`正式动画页`
> Step / Sketch：`page_24` / `-`
> 上一页：`page_22`
> 下一页：`page_25`

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `7.9/10` | 左表右卡结构清楚，左表与右侧卡组最小间距 `28px`，无重叠。 |
| Node 分数 | `7.5/10` | 表头、数据行、三张策略卡层级清楚。 |
| Edge 分数 | `7.2/10` | 主要依赖版式阅读流，底部 footer 把视线重新收口。 |
| 动画分数 | `in_range` | `page_22 -> page_24` 标准时序 verdict=`in_range`。 |

## Page goal

- 以真实压缩数据讲明白：压缩不是白赚，而是典型的时空互换。
- 从 `Compression` 提升到 `Precompute / Preload / Algorithm` 三类更一般的方法。
- 顺手点明：AI 方法也可视作压缩，`PCA` 和模型都在把原过程压进更紧的表示。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `改资源形态：时空互换` |
| 页面副标题 | 左侧是真实实测，右侧是策略扩展，并顺手补一个 AI 压缩视角。 |
| 当前页核心信息 | 压缩、预处理、PCA / 模型和算法切换都属于“重新分配时间与空间”。 |
| 与上一页关系 | 从观点页进入第一张策略页。 |
| 这一页新增了什么 | 压缩表、三张策略卡、`Algorithm` 卡里的 `PCA / 模型` 补充句、右下角 GDC 参考链接。 |
| 这一页删除了什么 | 不再保留旧 `page_23`。 |
| 这一页保留了什么 | late-tail 外层工作台结构与简洁背景。 |
| 本轮移动了什么 | 把表格右上角的单位说明下移成第二行注释，并把列头整体下移，避免与 `release/results` 顶栏标题重叠；同时整体放大左侧数据表和右侧三张策略卡，让第一页策略页更靠近主舞台可读尺度。 |
| 需要强调的元素 | `release/results`、`Compression`、`Precompute / Preload`、`Algorithm`、`PCA`、`模型`。 |
| 暂不实现 / 暂不出现 | 压缩算法细节推导，只保留一句可复述的 AI 压缩提示。 |

## Node inventory

| id | text | semantic role | visual kind | anchor region | emphasis | alignment constraints | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `compression-table` | 实测表格 | 真实证据 | data table | 左侧 | 高 | 与标题左对齐 | 承接上一页观点落地 | 否 |
| `compression-card` | `Compression` | 策略卡 1 | info card | 右上 | 高 | 与另外两卡同宽 | 新增 | 否 |
| `precompute-card` | `Precompute / Preload` | 策略卡 2 | info card | 右中 | 中 | 与另外两卡同宽 | 新增 | 否 |
| `algorithm-card` | `Algorithm` | 策略卡 3 | info card | 右下 | 中 | 与另外两卡同宽 | 新增 | 否 |
| `reference-link` | `参考：GDC Vault / ML / Physics / Kolmogorov` | 外部参考 | underlined link | 右下卡片下沿 | 低 | 与 `algorithm-card` 左对齐，位于 footer 上方 | 新增 | 否 |
| `footer-bar` | 底部结论句 | 收束 | footer bar | 底部 | 中 | 居中 | 承接下一页 | 否 |

## Edge inventory

| id | from | to | meaning | route grammar | max bends | segment label | arrow style | sibling / symmetry note | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `read-left-to-right` | `compression-table` | `compression-card` | 从实测进入策略理解 | implicit horizontal | 0 | - | none | 横向主阅读 | 是 | 否 |
| `read-right-stack` | `compression-card` | `algorithm-card` | 在右侧三张卡内向下阅读 | implicit vertical | 0 | - | none | 右侧卡组内阅读 | 否 | 否 |
| `read-to-footer` | `algorithm-card` | `footer-bar` | 从策略扩展收束到底部结论 | implicit vertical | 0 | - | none | 收束阅读链 | 是 | 否 |

## Spatial constraints

- 左表必须明显比右侧单卡更重，保证“先证据后扩展”。
- 右侧三张卡统一宽度与对齐线，避免看起来像零散便签。
- `reference-link` 只能做补充来源，不能抢过 `Algorithm` 卡本身的阅读优先级。
- footer 不能贴到表格底部，必须保留独立收束带。

## Element styles

- 左表用浅底和细分割线，不引入花哨图形。
- 三张策略卡维持同一视觉语法，只让第一张卡加重强调。
- 参考链接使用同色细下划线小字，只当注脚，不做第四张卡。

## Assembly order

1. 淡出上一页观点页。
2. 左表与右侧三卡一起进入。
3. 最后补 footer。

## Acceptance checklist

1. 观众先看左侧数据，再理解右侧策略扩展。
2. `Compression` 只是其中一种，不应被讲成全部答案。
3. `PCA / 模型也是压缩` 这件事要被点到，但不能把这页讲成 AI 专题页。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | 前段 | 观点页淡出 | `page_22` | 从观点切到第一张策略页。 |
| 2 | 中段 | 左表与右卡进入 | `compression-table`、三张策略卡 | 先举实测，再抬成一般方法。 |
| 3 | 尾段 | footer 落位 | `footer-bar` | 用一句话锁住“重新分配时空”。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | 压缩表格行、三张策略卡、GDC 参考链接 |
| 需要复用的元素 | session rail、step pills 与 footer 样式 |
| 当前共享元素 / 连续语义 carrier | 章节逻辑连续，不共享具体节点 |
| 是否需要镜头推进 / 缩放 | 否 |
| 是否需要逐步显隐 | 是 |
| 风险点 | 表格列宽一旦漂移，会立即损伤投影可读性。 |

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260420-page24-scale-up/page_24.png)

- 当前 URL：`http://127.0.0.1:4174/?variant=bus-clean&step=page_24`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260420-page24-scale-up/page_24.png`
- Capture size：`4096 x 2242`

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `7.8/10` | `8.0/10` | 左重右轻的策略页结构稳定。 | `7.9/10` |
| `模块空间结构 Review` | `7.6/10` | `7.8/10` | 表格、三卡、footer 分层明确。 | `7.7/10` |
| `单节点与单边 Review` | `7.3/10` | `7.6/10` | 表格密度较高，但总体可读。 | `7.5/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `compression-table` | 真实压缩实测表。 | 承接上一页“方法是工程折中” | data table | 证据支撑成立。 | `7.8/10` | 列宽清楚、投影可读。 | `7.5/10` |
| `compression-card` | 第一张策略卡，强调压缩。 | 从左表提升到一般策略 | info card | 关系清楚。 | `7.6/10` | 强调得当。 | `7.4/10` |
| `precompute-card` | 第二张策略卡。 | 扩展到空间换时间 | info card | 连续成立。 | `7.4/10` | 与首卡平衡良好。 | `7.3/10` |
| `algorithm-card` | 第三张策略卡，并补一句 `PCA / 模型也是压缩`。 | 再向上抬到更一般的时空互换 | info card | 连续成立。 | `7.3/10` | 文案比上一版更满，但仍在可投影阅读范围内。 | `7.1/10` |
| `reference-link` | 指向 GDC Vault 的补充来源。 | 新增引用，不承担主叙事 | underlined link | 来源合理。 | `7.0/10` | 位置克制，没有抢 footer。 | `7.0/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `read-left-to-right` | 从左侧实测进到右侧策略理解。 | 是 | implicit reading flow | 阅读链自然。 | `7.3/10` | 横向重心转换顺。 | `7.2/10` |
| `read-right-stack` | 右侧三张策略卡由上往下展开。 | 否 | implicit reading flow | 内部层次清楚。 | `7.1/10` | 卡间节奏统一。 | `7.1/10` |
| `read-to-footer` | 从策略卡收到底部结论。 | 是 | implicit reading flow | 结论收束成立。 | `7.2/10` | footer 没有抢走主阅读。 | `7.2/10` |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_22 -> page_24）

- 帧窗口：`2460 -> 2531`（`71` frames @ 60fps）
- 总时长：`1.183s`
- 时长评估：`too_short`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `72`，相对当前 `Δ+1` frames
- 标准时序评估：`in_range`（标准下限 `0.993s`，上限 `1.341s`）
- 标准调整建议：目标帧长 `71`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`0.993s`
- 标准允许上限：`1.341s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 100.0 | 100.0 | - |
| fade_out | 100.0 | 320.0 | 220.0 | fade_out_bridge_summary |
| node_motion | 320.0 | 633.0 | 313.0 | move_compression_table, move_strategy_cards |
| fade_in | 633.0 | 853.0 | 220.0 | fade_in_tradeoff_footer |
| hold_after | 853.0 | 993.0 | 140.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_bridge_summary | fade_out | fade_out | - | 220.0 | formula |
| move_compression_table | node_move | node_motion | distancePx=110.0 | 284.5 | formula |
| move_strategy_cards | node_move | node_motion | distancePx=140.0 | 313.0 | formula |
| fade_in_tradeoff_footer | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 2460 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 2478 | 0.300 | fade_out_bridge_summary(fade_out) | 无显著边变化 |
| Mid | 2496 | 0.600 | move_compression_table(node_move)、move_strategy_cards(node_move) | 无显著边变化 |
| Q3 | 2513 | 0.883 | hold_after 静置 | hold_after 静置 |
| End | 2531 | 1.183 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- 2026-04-20 收紧版复审：已重抓当前页并复跑 transition probe，frame anchors 未改，当前时序表保持有效。
- 标准时序已通过；当前无需进一步拉长 anchor。
- 复核帧 `2724` 的旧 loop-stage 闪回问题已消失，交接按 visible 直接 crossfade。
- 2026-04-20 内容补充：`Algorithm` 卡追加“`PCA / 模型也可视作压缩`”的提示，并在其下方增加 GDC Vault 参考链接 `Machine Learning, Physics Simulation, Kolmogorov`。
- 2026-04-20 表头审校：`release/results` 标题与单位说明改成上下两行，列头同步下移，表头已无重叠。
- 2026-04-20 整体放大：左侧表格扩到 `760 x 430`，右侧三卡同步放大到 `320px` 宽，同一页的信息密度不变，但投影阅读尺度更接近后续策略页。

## 审批状态

`通过。作为四个策略页的第一页已稳定。`
