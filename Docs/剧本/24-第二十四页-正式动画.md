# 第 24 页正式动画：`page_24`

> 文件类型：`正式动画页 / placeholder onepage`
> Step / Sketch：`page_24` / `-`
> 上一页：`page_23`
> 下一页：`page_25`


## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `7.4/10` | 来源：`2026-04-17` 截图复核（`ignore/review-refresh-20260417/frame-1779.png`） |
| Node 分数 | `7.1/10` | 标题与双卡节点清楚，层级稳定 |
| Edge 分数 | `6.7/10` | 页面以文本卡流向为主，显式边语义偏弱 |
| 动画分数 | `8.5/10` | 自动 probe：page_23 -> page_24，durationVerdict=in_range |

## Page goal

- 收束 `Metal` 与 `OpenGL` 差异来源：状态来源路径与显式程度不同。
- 避免误读为“某 API 绝对更优”的结论页。

## 页面核心信息

- 左栏：`OpenGL PSO`（更多驱动侧隐式状态）。
- 右栏：`Metal PSO`（更多显式状态组合）。
- 中桥：`State Source`。
- 底结论：差异来自状态模型，不是单一优劣标签。

## 讲述备注

- 这一页先给机制归因，后续若有实测页可挂到这一页之后。
- 允许后续追加“统计口径说明”子卡片。

## Acceptance checklist

1. 观众能说出差异根因是“状态来源与显式程度”。
2. 这一页不能被读成 API 性能口号对比。

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `7.3/10` | `7.5/10` | 与上一页一致的双卡模版，节奏稳定。 | `7.4/10` |
| `模块空间结构 Review` | `7.0/10` | `7.2/10` | 主卡讲 BCache，次卡讲下一页，模块关系明确。 | `7.1/10` |
| `单节点与单边 Review` | `6.9/10` | `7.0/10` | 字体和间距合格，边语义仍主要依赖阅读顺序。 | `7.0/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `title-node` | 优化方向 2（BCache 策略）标题节点。 | 承接上一页优化方向 1 | 标题节点 | 章节延续明确。 | `7.3/10` | 标题清晰、位置稳定。 | `7.3/10` |
| `core-card-node` | BCache 核心策略说明卡。 | 承接优化叙事主线 | 主卡片 | 语义聚焦。 | `7.1/10` | 文本可读，密度适中。 | `7.1/10` |
| `bridge-card-node` | 下页衔接卡（编译加速/UsageMask）。 | 承接下一页优化方向 3 | 次卡片 | 过渡关系清楚。 | `6.9/10` | 卡片结构稳定。 | `6.9/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `title-to-core` | 标题进入主卡的阅读边。 | 承接章节推进 | 隐式阅读边 | 阅读关系稳定。 | `6.8/10` | 依赖版式，不是显式箭头。 | `6.8/10` |
| `core-to-bridge` | 主卡进入下方过渡卡。 | 承接本页收束 | 隐式阅读边 | 逻辑顺滑。 | `6.7/10` | 视觉上自然，但缺显式边。 | `6.7/10` |
| `bridge-to-next` | 过渡卡导向下一页主题。 | 承接后续章节 | 隐式章节边 | 导向语义清楚。 | `6.6/10` | 主要靠文案表达。 | `6.6/10` |

## 过渡动画剧本与时长审查

- 当前状态：`待补`
- 说明：请运行 `probe_transition_timeline.py --from-step <prev> --to-step <current> --workload-json <workload-json> --emit-markdown` 后回填。

## 2026-04-20 转场复核补记

- 复核帧：`2724`，位于 `page_24 -> page_25` 的第 `19 / 71` 帧。
- 之前的实际问题：这一帧会闪回旧 loop-stage（`rec.upipelinecache / stablepc.csv / .scl.csv / .ushaderbytecode / runtime GPU`）。
- 本次替换行为：late placeholder 链的旧 stage suppress 从“看 focus”改成“看 visible”，同时 `page_21 -> page_29` 的占位页交接改成按相邻页可见度直接 crossfade。
- 修复后结果：`2724` 不再闪回旧 loop，也不会掉成空背景，而是继续停留在策略占位卡内容上。

![2724 frame after fix](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/slide-stage-captures/20260420-frame2724-final/frame2724-final.png)
