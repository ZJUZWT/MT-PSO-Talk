# 第 24 页

> 文件类型：`正式动画页`
> Step / Sketch：`page_24` / `formal-page24`
> 上一页：`page_22`
> 下一页：`page_26`
> 2026-04-23 列重排版：双主卡继续保持 `y=68 / h=532`，左侧压缩表改成 `Win / Mac` 左列、`And / iOS` 右列，并把算法列分隔线左移一点；`Oodle Leviathan` 继续双行承载。

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 最新 formal review：`ignore/slide-review/page_19_plus/pages/page_24.geometry.md`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `5.7/10` | 双主卡抬高到 `y=68, h=532` 后，页面终于不再顶底打架，整体版心也回到了安全 margin。 |
| Node 分数 | `6.0/10` | 左侧三行比较表改成平台分列后，`Win / Mac` 与 `And / iOS` 的扫描顺序更顺，`Oodle Leviathan` 也不再挤压数据列。 |
| Edge 分数 | `9.3/10` | `换出 / 按需回填` 两条关系线清楚、无 crossing / pierce blocker。 |
| 动画分数 | `in_range` | `page_22 -> page_24` 的 Harness v1.0 标准时序仍为 `in_range`，未改 frame anchor。 |

## Page goal

- 把“资源太重怎么办”先抽成两种更高层的方法，而不是直接掉进 UE 细节。
- 左半明确讲 `减体积`：PSO / ShaderCode 为什么满足“高体积、高重复、体积成本敏感”。
- 右半明确讲 `虚拟化`：PSO 为什么满足“冷热分化明显、物理资源受限、允许按需回填”。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `资源过重：减体积 / 虚拟化` |
| 当前页核心信息 | 资源太重时，先想能不能 `减体积`，再想能不能 `虚拟化`。 |
| 新增了哪些元素 | 本轮没有新增页面元素。 |
| 移动了哪些节点 | formal 主节点坐标保持不变；`package-row-1/2/3` 内部表格改成 `Win / Mac` 左列、`And / iOS` 右列，算法分隔线从 `x=rowBox.x+154` 左移到 `x=rowBox.x+146`；`Oodle Leviathan` 继续用双行承载。 |
| 增加 / 删除了哪些 node / edge | 新增 node：无；删除 node：无；保留 node：`left-card`、`package-row-*`、`right-card`、`virtual-guide`、`memory`、`disk`、`footer`；保留 edge：`swap-out`、`fill-back`。 |
| 替换了什么行为 | 没有替换页间过渡逻辑；替换的是左表格的承载方式：从“Win/Mac 顶排，And/iOS 底排”的横向混排改成“Win/Mac 左列，And/iOS 右列”的纵向分列。 |

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260423-page24-page26-layout-pass/page_24/page24-layout-pass.png)

- 当前 URL：`http://127.0.0.1:4173/?step=page_24`
- 捕获方式：`browser-api page capture`
- Capture size：`4096 x 2404`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260423-page24-page26-layout-pass/page_24/page24-layout-pass.txt`
- Mechanical summary：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/slide-review/page_19_plus/pages/page_24.geometry.md`

## Facts & Metrics

| 指标 | 值 | 说明 |
| --- | --- | --- |
| `mechanicalScore` | `7.5/10` | formal review 可通过，无 blocker。 |
| `verdict` | `Open the layout before critic pass` | 这一轮已经把超框和最明显的拥挤点解掉，剩余问题主要是局部间距还没到 comfort floor。 |
| `minRenderedFontPx` | `19.5` | `Oodle Leviathan` 双行后仍保留在安全可读范围。 |
| `minInternalPadding` | `14` | 当前最紧区域仍保在安全值之上。 |
| `minMargin` | `56` | 双卡上提后，页面重新回到健康的外边距。 |
| `nodePierce / crossing / overflow` | `0 / 0 / 0` | 当前无硬 blocker。 |

## 三层 Review 总表

| Review 层级 | 事实依据 | 综合判断 | 综合分数 |
| --- | --- | --- | --- |
| `整体布局 Review` | 双主卡外轮廓调整为 `y=68, h=532`，footer 仍留在 `y=610`，外边距 `56px` | 双卡增高后不再挤压 footer，整体版心更稳 | `5.7/10` |
| `模块空间结构 Review` | 左侧比较表 + 右侧主说明块 + 双盒 + footer | page24 现在既保留三行数据证据，也让长算法名不再挤爆列宽 | `6.0/10` |
| `单节点与单边 Review` | `nodePierce=0`、`crossing=0`、`textOverflow=0` | 线和盒子关系干净 | `9.3/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 当前渲染载体 | 真实浏览器事实 | 当前 Review |
| --- | --- | --- | --- | --- |
| `left-card` | 左半方法卡，负责讲 `减体积`。 | board shell | 标题、判据、类比三层读序稳定。 | 通过 |
| `package-row-1` | `LZ4` 对比行。 | compare row | 现在左列固定放 `Win / Mac`，右列固定放 `And / iOS`，读法更稳定。 | 通过 |
| `package-row-2` | `zstd` 对比行。 | compare row | 平台数据按列分组后，不再需要先横扫再回头对平台。 | 通过 |
| `package-row-3` | `Oodle Leviathan` 对比行。 | compare row | 长算法名继续双行承载，同时给平台列留出了更稳定的横向空间。 | 通过 |
| `right-card` | 右半方法卡，负责讲 `虚拟化`。 | board shell | 双主卡骨架与 `page_26` 对齐，视觉配对更明确。 | 通过 |
| `virtual-guide` | `UE 中 PSO 如何使用这个优化` 的主说明块。 | guide box | `LRU / mmap` 的职责被拉到卡片上半区，不再藏在角落。 | 通过 |
| `memory` | 热数据常驻区。 | major node | 用一块大盒子讲“热点优先常驻”。 | 通过 |
| `disk` | 冷数据外存区。 | major node | 用一块大盒子讲“冷数据按需装入”。 | 通过 |
| `footer` | 页底收束句。 | footer bar | 只保留一句模式化总结。 | 通过 |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 当前渲染载体 | 真实浏览器事实 | 当前 Review |
| --- | --- | --- | --- | --- |
| `swap-out` | 从常驻区换出到外存。 | horizontal arrow | 语义单一，没有和别的线缠绕。 | 通过 |
| `fill-back` | 从外存按需回填。 | horizontal arrow | 与 `swap-out` 成对，方向区分明确。 | 通过 |
| `read-left-to-right` | 从 `减体积` 读到 `虚拟化`。 | implicit reading flow | 观众先看证据，再看第二种方法。 | 通过 |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_22 -> page_24）

- 帧窗口：`2690 -> 2761`（`71` frames @ 60fps）
- 总时长：`1.183s`
- 时长评估：`too_short`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `72`，相对当前 `Δ+1` frames
- 标准时序评估：`in_range`（标准下限 `1.020s`，上限 `1.376s`）
- 标准调整建议：目标帧长 `71`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`1.020s`
- 标准允许上限：`1.376s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 100.0 | 100.0 | - |
| fade_out | 100.0 | 320.0 | 220.0 | fade_out_bridge_summary |
| node_motion | 320.0 | 659.6 | 339.6 | move_package_board, move_memory_board |
| fade_in | 659.6 | 879.6 | 220.0 | fade_in_package_memory_footer |
| hold_after | 879.6 | 1019.6 | 140.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_bridge_summary | fade_out | fade_out | - | 220.0 | formula |
| move_package_board | node_move | node_motion | distancePx=132.0 | 305.4 | formula |
| move_memory_board | node_move | node_motion | distancePx=168.0 | 339.6 | formula |
| fade_in_package_memory_footer | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 2690 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 2708 | 0.300 | fade_out_bridge_summary(fade_out) | 无显著边变化 |
| Mid | 2726 | 0.600 | move_package_board(node_move)、move_memory_board(node_move) | 无显著边变化 |
| Q3 | 2743 | 0.883 | hold_after 静置 | hold_after 静置 |
| End | 2761 | 1.183 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- `audit_transition_timings.py` 最新结果里，`page_22 -> page_24` 的 Harness v1.0 verdict 仍是 `in_range`。
- 本轮没有压缩或重分配原有 base animation；`page_24` frame anchor 保持不动。
- 被替换的是页内承载方式：左侧表格改成平台分列、算法分隔线略微左移；页间过渡语义保持不动。

## 审批状态

`通过。当前 page24 的超框与拥挤问题已经和 render / formal review / 剧本说明保持一致地修正；剩余只在局部 gap 还没到 comfort floor，不存在 overlap / crossing / nodePierce / textOverflow blocker。`
