# 第 26 页

> 文件类型：`正式动画页`
> Step / Sketch：`page_26` / `-`
> 上一页：`page_25`
> 下一页：`page_27`

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `8.1/10` | 左侧双 UsageMask 结构与右侧三卡分区稳定，左右间距 `26px`。 |
| Node 分数 | `7.7/10` | 双 mask、地图 A/B 示例与下载时编译提示都清楚。 |
| Edge 分数 | `7.3/10` | 本页以时机解耦为主，箭头压力较低。 |
| 动画分数 | `in_range` | `page_25 -> page_26` 标准时序 verdict=`in_range`。 |

## Page goal

- 讲清楚 `Game UsageMask` 与 `Compile UsageMask` 的解耦。
- 把 UsageMask 从 UE 原生限制提升成“改变发生时机”的更一般策略。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `改发生时机：收集与编译解耦` |
| 页面副标题 | 优化的不是做不做，而是什么时候做。 |
| 当前页核心信息 | `Game UsageMask = A` 时，`Compile UsageMask` 仍可提前覆盖 `A + B`。 |
| 与上一页关系 | 从“放在哪里”转到“什么时候做”。 |
| 这一页新增了什么 | 双 mask 对照、地图 A/B 行、三张延迟处理卡。 |
| 这一页删除了什么 | 不再延续内存/外存的双区结构。 |
| 这一页保留了什么 | late-tail 标题、右侧三卡、footer。 |
| 需要强调的元素 | `Game UsageMask`、`Compile UsageMask`、`Lazy Load`、`Streaming`、`Deferred Compile`。 |
| 暂不实现 / 暂不出现 | 过多 UE 底层名词。 |

## Node inventory

| id | text | semantic role | visual kind | anchor region | emphasis | alignment constraints | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `game-mask-box` | `Game UsageMask` | 收集时机 | compare card | 左上 | 高 | 与 compile box 同高 | 承接“决策条件”讨论 | 否 |
| `compile-mask-box` | `Compile UsageMask` | 编译时机 | compare card | 左上偏右 | 高 | 与 game box 同高 | 是 | 否 |
| `lazy-card` | `Lazy Load` | 延迟处理卡 1 | info card | 右上 | 中 | 与另外两卡同宽 | 新增 | 否 |
| `streaming-card` | `Streaming` | 延迟处理卡 2 | info card | 右中 | 中 | 与另外两卡同宽 | 新增 | 否 |
| `deferred-card` | `Deferred / Async Compile` | 延迟处理卡 3 | info card | 右下 | 高 | 与另外两卡同宽 | 新增 | 否 |

## Edge inventory

| id | from | to | meaning | route grammar | max bends | segment label | arrow style | sibling / symmetry note | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `mask-row-a` | `game-mask-box` | `compile-mask-box` | 地图 A 在收集与编译中都可出现 | implicit row compare | 0 | `地图 A` | none | 与下一行构成对照 | 是 | 否 |
| `mask-row-b` | `game-mask-box` | `compile-mask-box` | 地图 B 可只进入编译 | implicit row compare | 0 | `地图 B` | none | 与上一行成对 | 是 | 否 |
| `read-left-to-right` | `compile-mask-box` | `lazy-card` | 从双 mask 提升到一般延迟处理方式 | implicit horizontal | 0 | - | none | 右侧三卡入口 | 是 | 否 |

## Spatial constraints

- 左侧双 box 必须先被读成一个“对照关系”，不能被拆成两个独立卡片。
- 下载时编译的提示条不能压到地图 A/B 行上。

## Element styles

- 双 mask 用更接近对比板的视觉，不用复杂拓扑线。
- 右侧三卡沿用前两页的统一卡片语法。

## Assembly order

1. 淡出上一页。
2. 先落左侧双 mask 对照。
3. 再落右侧三张延迟处理卡和 footer。

## Acceptance checklist

1. 观众能看懂 `A` 与 `A+B` 的区别。
2. 这页必须讲“时机”，不要滑到“并行”。 

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | 前段 | 存储页淡出 | `page_25` | 切到时机策略。 |
| 2 | 中段 | 双 mask 进入 | 左侧对照区 | 先讲地图 A/B 例子。 |
| 3 | 尾段 | 三卡与 footer 进入 | 右侧三卡、footer | 再提升到一般延迟处理。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | 双 mask 对照板、三张延迟卡 |
| 需要复用的元素 | late-tail 卡片与 footer |
| 当前共享元素 / 连续语义 carrier | 逻辑连续即可 |
| 是否需要镜头推进 / 缩放 | 否 |
| 是否需要逐步显隐 | 是 |
| 风险点 | 如果地图 A/B 对照不够清楚，会直接丢掉本页核心。 |

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260420-late-tail-tightened/page_26/page_26.png)

- 当前 URL：`http://127.0.0.1:4173/?variant=bus-clean&step=page_26`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260420-late-tail-tightened/page_26/page_26.txt`
- Capture size：`4096 x 2242`

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `8.0/10` | `8.1/10` | 对照板 + 三卡结构稳定。 | `8.1/10` |
| `模块空间结构 Review` | `7.8/10` | `7.9/10` | 左侧例子、右侧方法分类和提示条都分开了。 | `7.8/10` |
| `单节点与单边 Review` | `7.5/10` | `7.6/10` | 地图 A/B 与右侧三卡都清楚。 | `7.6/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `game-mask-box` | 当前收集视角。 | 承接策略链 | compare card | 成立。 | `7.7/10` | 标签与行都清楚。 | `7.5/10` |
| `compile-mask-box` | 提前编译视角。 | 是 | compare card | 成立。 | `7.8/10` | 对照关系稳定。 | `7.6/10` |
| `deferred-card` | 延迟编译的泛化表达。 | 从例子升到方法 | info card | 成立。 | `7.5/10` | 在三卡中最关键。 | `7.4/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `mask-row-a` | 地图 A 的双语义对照。 | 是 | implicit row compare | 清楚。 | `7.3/10` | 读图简单。 | `7.2/10` |
| `mask-row-b` | 地图 B 只进入编译。 | 是 | implicit row compare | 清楚。 | `7.4/10` | 对比关系成立。 | `7.3/10` |
| `read-left-to-right` | 从双 mask 进入延迟处理三卡。 | 是 | implicit flow | 清楚。 | `7.1/10` | 版式顺滑。 | `7.0/10` |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_25 -> page_26）

- 帧窗口：`2602 -> 2673`（`71` frames @ 60fps）
- 总时长：`1.183s`
- 时长评估：`too_short`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `72`，相对当前 `Δ+1` frames
- 标准时序评估：`in_range`（标准下限 `0.993s`，上限 `1.341s`）
- 标准调整建议：目标帧长 `71`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`0.993s`
- 标准允许上限：`1.341s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 100.0 | 100.0 | - |
| fade_out | 100.0 | 320.0 | 220.0 | fade_out_storage_strategy |
| node_motion | 320.0 | 633.0 | 313.0 | move_dual_mask_board, move_deferred_cards |
| fade_in | 633.0 | 853.0 | 220.0 | fade_in_timing_footer |
| hold_after | 853.0 | 993.0 | 140.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_storage_strategy | fade_out | fade_out | - | 220.0 | formula |
| move_dual_mask_board | node_move | node_motion | distancePx=110.0 | 284.5 | formula |
| move_deferred_cards | node_move | node_motion | distancePx=140.0 | 313.0 | formula |
| fade_in_timing_footer | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 2602 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 2620 | 0.300 | fade_out_storage_strategy(fade_out) | 无显著边变化 |
| Mid | 2638 | 0.600 | move_dual_mask_board(node_move)、move_deferred_cards(node_move) | 无显著边变化 |
| Q3 | 2655 | 0.883 | hold_after 静置 | hold_after 静置 |
| End | 2673 | 1.183 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- 2026-04-20 收紧版复审：已重抓当前页并复跑 transition probe，frame anchors 未改，当前时序表保持有效。
- 标准时序通过。
- 图像页与解释页之间没有背景闪回。 

## 审批状态

`通过。`
