# 第 26 页

> 文件类型：`正式动画页`
> Step / Sketch：`page_26` / `-`
> 上一页：`page_25`
> 下一页：`page_27`

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `8.1/10` | 左侧双 UsageMask 结构与右侧三卡分区稳定，左右间距 `26px`。 |
| Node 分数 | `7.7/10` | 双 mask、原生限制提示与“单一存储表达”说明形成闭环。 |
| Edge 分数 | `7.3/10` | 本页以时机解耦为主，箭头压力较低。 |
| 动画分数 | `in_range` | `page_25 -> page_26` 标准时序 verdict=`in_range`。 |

## Page goal

- 讲清楚双 `UsageMask` 的目标是按地图调度编译，而不是造两套 PSO。
- 说明 UE 原生 `UsageMask` 不够，所以拆的是调度语义；写回 PSO 时仍然只有一种表达。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `改发生时机：按地图调度编译` |
| 页面副标题 | 优化的不是做不做，而是把地图下载变成对应 PSO 的编译触发点。 |
| 当前页核心信息 | `Game UsageMask = A` 时，地图 B 下载完成后，`Compile UsageMask` 可以提前覆盖 `A + B`。 |
| 与上一页关系 | 从“放在哪里”转到“什么时候做”。 |
| 这一页新增了什么 | 事件链、`A / A+B` 结果对比、两张结论卡。 |
| 这一页删除了什么 | 不再延续内存/外存的双区结构。 |
| 这一页保留了什么 | late-tail 标题、右侧结论卡、footer。 |
| 需要强调的元素 | `Game UsageMask`、`Compile UsageMask`、`UE Native UsageMask`、`Per-Map Compile`、`Single Usage Expression`。 |
| 暂不实现 / 暂不出现 | 并行化结论、两套 PSO 存储的误导。 |

## Node inventory

| id | text | semantic role | visual kind | anchor region | emphasis | alignment constraints | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `event-strip` | `当前在地图 A -> 地图 B 下载完成 -> 开始编译地图 B 的 PSO` | 事件链 | flow strip | 左上 | 高 | 需要先于 mask 被读懂 | 新增 | 否 |
| `game-mask-box` | `Game UsageMask = A` | 当前游戏视角 | compare card | 左中 | 高 | 与 compile box 同高 | 承接“决策条件”讨论 | 否 |
| `compile-mask-box` | `Compile UsageMask = A + B` | 编译调度视角 | compare card | 左中偏右 | 高 | 与 game box 同高 | 是 | 否 |
| `context-note` | `当前游戏 = A；编译调度 = A + B` | 两视角同时存在 | note pill | 左下 | 高 | 需完整承载两行总结 | 新增 | 否 |
| `why-native-fails-card` | `为什么 UE 原生不够` | 原生机制限制 | info card | 右上 | 高 | 与下卡同宽 | 新增 | 否 |
| `single-expression-card` | `但 PSO 存储没变` | 持久化边界 | info card | 右下 | 高 | 与上卡同宽 | 新增 | 否 |

## Edge inventory

| id | from | to | meaning | route grammar | max bends | segment label | arrow style | sibling / symmetry note | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `event-a-to-b` | `event-strip/current` | `event-strip/download` | 先明确当前地图，再引入下载事件 | horizontal arrow | 0 | - | arrow | 与下一条构成连续因果 | 否 | 否 |
| `event-b-to-c` | `event-strip/download` | `event-strip/compile` | 地图下载完成后触发该图 PSO 编译 | horizontal arrow | 0 | - | arrow | 与上一条连成事件链 | 否 | 否 |
| `read-left-to-right` | `compile-mask-box` | `why-native-fails-card` | 从结果对比进入原生限制与持久化边界 | implicit horizontal | 0 | - | none | 右侧结论卡入口 | 是 | 否 |

## Spatial constraints

- 左侧必须先被读成事件链，再读成 `A / A+B` 的结果对比。
- `Game = A` 与 `Compile = A + B` 必须被读成同一时刻的两个视角，而不是两个阶段。

## Element styles

- 双 mask 用更接近对比板的视觉，不用复杂拓扑线。
- 顶部事件链要像“发生了什么”，不是像术语目录。
- 底部提示条要像结果总结，不要像 action button。
- 右侧两卡沿用统一卡片语法，但内容只保留“原生限制 / 存储边界”。

## Assembly order

1. 淡出上一页。
2. 先落顶部事件链。
3. 再落 `Game = A / Compile = A + B` 对比与两视角总结。
4. 最后落右侧两张结论卡和 footer。

## Acceptance checklist

1. 观众必须先看懂“地图 B 下载完成 -> 开始编译地图 B 的 PSO”。
2. 观众能看懂 `A` 与 `A+B` 代表的是两个视角，不是两个阶段或两套存储。
3. 这页必须讲按地图触发编译，不要滑到并行。 

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | 前段 | 存储页淡出 | `page_25` | 切到时机策略。 |
| 2 | 中段前半 | 事件链进入 | 左上事件链 | 先讲“地图 B 下载完成后，要不要立刻编这张图的 PSO”。 |
| 3 | 中段后半 | `Game = A / Compile = A + B` 对比与总结进入 | 左中对比区 | 再讲两个视角为什么不同。 |
| 4 | 尾段 | 右侧结论卡与 footer 进入 | 右侧两卡、footer | 最后讲为什么原生不够，以及存储边界没变。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | 事件链、结果对比板、两视角总结、两张结论卡 |
| 需要复用的元素 | late-tail 卡片与 footer |
| 当前共享元素 / 连续语义 carrier | 逻辑连续即可 |
| 是否需要镜头推进 / 缩放 | 否 |
| 是否需要逐步显隐 | 是 |
| 风险点 | 如果观众在事件链还没读懂前就被 `UsageMask` 术语绊住，这页就失败了。 |

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260420-page26-event-first/page_26.png)

- 当前 URL：`http://127.0.0.1:4173/?step=page_26`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260420-page26-event-first/page_26.txt`
- Capture size：`4096 x 2404`

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `8.0/10` | `8.1/10` | 事件链、结果对比、结论卡三层阅读顺序稳定。 | `8.1/10` |
| `模块空间结构 Review` | `7.8/10` | `7.9/10` | 左侧先讲发生了什么，再讲两个视角，右侧收结论。 | `7.8/10` |
| `单节点与单边 Review` | `7.5/10` | `7.6/10` | `A / A+B` 与“存储不变”能直接读出来。 | `7.6/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `event-strip` | 当前在地图 A，地图 B 下载完成，于是开始编译地图 B 的 PSO。 | 新增事件入口 | flow strip | 成立。 | `7.8/10` | 把抽象术语换成了先讲事件。 | `7.6/10` |
| `game-mask-box` | 当前游戏地图视角。 | 承接策略链 | compare card | 成立。 | `7.7/10` | 直接落成 `Game = A`。 | `7.5/10` |
| `compile-mask-box` | 地图下载后的编译调度视角。 | 是 | compare card | 成立。 | `7.8/10` | 直接落成 `Compile = A + B`。 | `7.6/10` |
| `context-note` | 同一时刻存在两种视角。 | 从事件过渡到结论 | note pill | 成立。 | `7.5/10` | 把两种视角的并置关系说清楚了。 | `7.4/10` |
| `single-expression-card` | PSO 存储仍保持一种表达。 | 从结果升到边界 | info card | 成立。 | `7.5/10` | 把“不是两套存储”补清楚了。 | `7.4/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `event-a-to-b` | 从当前地图进入下载事件。 | 否 | horizontal arrow | 清楚。 | `7.4/10` | 事件链读起来更顺。 | `7.3/10` |
| `event-b-to-c` | 下载完成后触发该图 PSO 编译。 | 否 | horizontal arrow | 清楚。 | `7.5/10` | 核心动作被直接看见。 | `7.4/10` |
| `read-left-to-right` | 从 `A / A+B` 对比进入原生限制与持久化边界。 | 是 | implicit flow | 清楚。 | `7.1/10` | 版式顺滑。 | `7.0/10` |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_25 -> page_26）

- 帧窗口：`2602 -> 2673`（`71` frames @ 60fps）
- 总时长：`1.183s`
- 时长评估：`too_short`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `72`，相对当前 `Δ+1` frames
- 标准时序评估：`in_range`（标准下限 `0.993s`，上限 `1.341s`）
- 标准调整建议：目标帧长 `71`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`0.993s`
- 标准允许上限：`1.341s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 100.0 | 100.0 | - |
| fade_out | 100.0 | 320.0 | 220.0 | fade_out_storage_strategy |
| node_motion | 320.0 | 633.0 | 313.0 | move_dual_mask_board, move_deferred_cards |
| fade_in | 633.0 | 853.0 | 220.0 | fade_in_timing_footer |
| hold_after | 853.0 | 993.0 | 140.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_storage_strategy | fade_out | fade_out | - | 220.0 | formula |
| move_dual_mask_board | node_move | node_motion | distancePx=110.0 | 284.5 | formula |
| move_deferred_cards | node_move | node_motion | distancePx=140.0 | 313.0 | formula |
| fade_in_timing_footer | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 2602 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 2620 | 0.300 | fade_out_storage_strategy(fade_out) | 无显著边变化 |
| Mid | 2638 | 0.600 | move_dual_mask_board(node_move)、move_deferred_cards(node_move) | 无显著边变化 |
| Q3 | 2655 | 0.883 | hold_after 静置 | hold_after 静置 |
| End | 2673 | 1.183 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- 2026-04-20 事件优先版复审：已重抓当前页并复跑 transition probe，frame anchors 未改，当前时序表保持有效。
- 标准时序通过。
- 图像页与解释页之间没有背景闪回。 

## 审批状态

`通过。`
