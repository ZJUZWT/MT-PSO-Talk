# 第 26 页

> 文件类型：`正式动画页`
> Step / Sketch：`page_26` / `formal-page26`
> 上一页：`page_24`
> 下一页：`page_28`
> 2026-04-22 外框增高版：纠正“最外层 node 太矮”的问题；双主卡整体上提到 `y=136` 并把高度拉到 `432`，footer 下让开，让页面不再像扁平的两块板。

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 最新 formal review：`ignore/slide-review/page_19_plus/pages/page_26.geometry.md`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `4.7/10` | 双主卡骨架继续和 `page_24` 对齐；这轮主要修的是最外层大卡本体过矮。 |
| Node 分数 | `5.7/10` | 右半导读条、层级表、总结条的字号和条高都放松了，左半触发链也不再显得过小。 |
| Edge 分数 | `9.3/10` | 仅保留左侧事件链箭头，关系简单清楚。 |
| 动画分数 | `in_range` | `page_24 -> page_26` 的 Harness v1.0 标准时序为 `in_range`。 |

## Page goal

- 把“编译过慢怎么办”先抽成两种更一般的方法，而不是继续讲 UE 内部拓扑。
- 左半明确讲 `分集合`：先决定这一轮做哪些。
- 右半明确讲 `并行化`：在集合不变的前提下，怎样更快做完。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `编译过慢：分集合 / 并行化` |
| 当前页核心信息 | `分集合` 决定做什么，`并行化` 决定怎样更快做完。 |
| 新增了哪些元素 | 本轮没有新增页面元素。 |
| 移动了哪些节点 | `usage-mask-path / parallel-path` 整体调整到 `y=136, h=432`；`event-strip` 调整到 `y=224, 508x84`；`game-mask / compile-mask` 调整到 `y=322, 246x136`；`usage-mask-note` 调整到 `y=470, 508x54`；`parallel-guide` 调整到 `x=682, y=224, 490x96`；`parallel-layers` 调整到 `x=682, y=332, 490x154`；`parallel-summary` 调整到 `x=682, y=496, 490x40`；`footer` 调整到 `x=164, y=610, 948x54`。 |
| 增加 / 删除了哪些 node / edge | 新增 node：无；删除 node：无；保留 node：`usage-mask-path`、`event-strip`、`game-mask`、`compile-mask`、`usage-mask-note`、`parallel-path`、`parallel-guide`、`parallel-layers`、`parallel-summary`、`footer`；保留 edge：`event-a-to-b`、`event-b-to-c`。 |
| 替换了什么行为 | 没有替换页间过渡逻辑；替换的是页内最外层 card 的垂直几何，让左右两块大卡不再显得太矮。 |

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260422-outer-card-height/page_26/page26-outer-card-height.png)

- 当前 URL：`http://127.0.0.1:4173/?step=page_26`
- 捕获方式：`browser-api page capture`
- Capture size：`4096 x 2404`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260422-outer-card-height/page_26/page26-outer-card-height.txt`
- Mechanical summary：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/slide-review/page_19_plus/pages/page_26.geometry.md`

## Facts & Metrics

| 指标 | 值 | 说明 |
| --- | --- | --- |
| `mechanicalScore` | `7.0/10` | formal review 可通过，无 blocker。 |
| `verdict` | `Reframe the stage before critic pass` | 主要问题是舒适度和边距，不是结构错误。 |
| `minRenderedFontPx` | `15` | 最小字号已从上一轮的 `13.5px` 拉回安全线。 |
| `minInternalPadding` | `5.5` | 最紧处仍偏紧，但已经不再贴边。 |
| `minMargin` | `56` | 外边距仍安全；相比上一版，主要换来更高的最外层卡片。 |
| `nodePierce / crossing / overflow` | `0 / 0 / 0` | 当前无硬 blocker。 |

## 三层 Review 总表

| Review 层级 | 事实依据 | 综合判断 | 综合分数 |
| --- | --- | --- | --- |
| `整体布局 Review` | 双主卡外轮廓与 `page_24` 对齐，并统一到 `y=136, h=432`，外边距 `56px` | 外层 card 更舒展，但整页仍不是“压实型” | `4.7/10` |
| `模块空间结构 Review` | 左侧触发链 + 双视角，右侧导读块 + 层级表 + 总结条 | 方法论读序更完整 | `5.7/10` |
| `单节点与单边 Review` | `nodePierce=0`、`crossing=0`、`textOverflow=0` | 线和节点关系干净 | `9.3/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 当前渲染载体 | 真实浏览器事实 | 当前 Review |
| --- | --- | --- | --- | --- |
| `usage-mask-path` | 左半方法卡，负责讲 `分集合`。 | board shell | 标题、判据、类比都已经前置。 | 通过 |
| `event-strip` | 上下文触发链。 | flow strip | 条高放松后，三段事件块终于不是“挤在一起的按钮”。 | 通过 |
| `game-mask` | 当前运行视角。 | compare card | `Game UsageMask = A` 读序稳定。 | 通过 |
| `compile-mask` | 编译调度视角。 | compare card | `Compile UsageMask = A + B` 与左卡同层对比。 | 通过 |
| `usage-mask-note` | 两个视角同时成立的总结。 | summary strip | 总结条拉高后，两行结论不再贴边。 | 通过 |
| `parallel-path` | 右半方法卡，负责讲 `并行化`。 | board shell | 不再保留旧的 queue/worker 图。 | 通过 |
| `parallel-guide` | 并行化的导读条。 | guide box | 三列导读词和副句都放大后，读法终于不需要凑近看。 | 通过 |
| `parallel-layers` | `SIMD / Thread / GPU` 三层并行层级。 | major node | 行式层级表比旧的 worker 拓扑更平、更易扫读。 | 通过 |
| `parallel-summary` | 并行化总结条。 | summary strip | 总结条不再被压成细细一条。 | 通过 |
| `footer` | 页底收束句。 | footer bar | 页底只保留一句模式化总结。 | 通过 |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 当前渲染载体 | 真实浏览器事实 | 当前 Review |
| --- | --- | --- | --- | --- |
| `event-a-to-b` | 从当前地图进入下载事件。 | horizontal arrow | 事件链第一跳清楚。 | 通过 |
| `event-b-to-c` | 从下载完成进入预编译触发。 | horizontal arrow | 事件链第二跳清楚。 | 通过 |
| `read-left-to-right` | 从 `分集合` 读到 `并行化`。 | implicit reading flow | 先讲“做什么”，再讲“怎么更快做完”。 | 通过 |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_24 -> page_26）

- 帧窗口：`2761 -> 2832`（`71` frames @ 60fps）
- 总时长：`1.183s`
- 时长评估：`too_short`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `72`，相对当前 `Δ+1` frames
- 标准时序评估：`in_range`（标准下限 `1.020s`，上限 `1.376s`）
- 标准调整建议：目标帧长 `71`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`1.020s`
- 标准允许上限：`1.376s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 100.0 | 100.0 | - |
| fade_out | 100.0 | 320.0 | 220.0 | fade_out_package_memory_board |
| node_motion | 320.0 | 659.6 | 339.6 | move_dual_mask_board, move_compile_throughput_cards |
| fade_in | 659.6 | 879.6 | 220.0 | fade_in_compile_footer |
| hold_after | 879.6 | 1019.6 | 140.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_package_memory_board | fade_out | fade_out | - | 220.0 | formula |
| move_dual_mask_board | node_move | node_motion | distancePx=132.0 | 305.4 | formula |
| move_compile_throughput_cards | node_move | node_motion | distancePx=168.0 | 339.6 | formula |
| fade_in_compile_footer | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 2761 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 2779 | 0.300 | fade_out_package_memory_board(fade_out) | 无显著边变化 |
| Mid | 2796 | 0.583 | move_dual_mask_board(node_move)、move_compile_throughput_cards(node_move) | 无显著边变化 |
| Q3 | 2814 | 0.883 | hold_after 静置 | hold_after 静置 |
| End | 2832 | 1.183 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- `audit_transition_timings.py` 最新结果里，`page_24 -> page_26` 的 Harness v1.0 verdict 仍是 `in_range`。
- 本轮没有改 frame anchor；`page_26` 继续复用原 late-tail 进入时长。
- 被替换的是入场后的右侧解释对象：旧的 queue/worker 拓扑退场，新的并行层级卡入场。

## 审批状态

`通过。当前 page26 的方法论重构已经和 render / formal review / 剧本说明保持一致；剩余问题只在舒适度和边距，不存在 overlap / crossing / nodePierce / textOverflow blocker。`
