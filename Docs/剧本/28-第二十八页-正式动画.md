# 第 28 页

> 文件类型：`正式动画页`
> Step / Sketch：`page_28` / `-`
> 上一页：`page_26`
> 下一页：`page_29`

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `8.3/10` | 左侧双图上下堆叠，右侧单张结构卡解释 `PSO State`，舞台利用率更高。 |
| Node 分数 | `7.9/10` | 匿名样本图、`PSO = Shader + State` 结构卡和底部结论条层级明确。 |
| Edge 分数 | `7.5/10` | 阅读顺序从左侧现象进入右侧结构，再收口到底部。 |
| 动画分数 | `in_range` | 新的 `page_26 -> page_28` 直连为 `71` 帧，标准时序 verdict=`in_range`。 |

## Page goal

- 左侧先用两张匿名样本图把“同样内容，结果不同”讲成一个现象，再补一个 `VertexBuffer` layout inset，把视线慢慢推向输入侧差异。
- 右侧再用 `PSO = Shader + State` 解释：平台差异不是只有 shader，state 里的很多维度也会进入 PSO。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `平台差异的表象：同样内容，未必落成同一组 PSO` |
| 页面副标题 | 平台差异是真实存在的，因此 PSO 数量并不是一个完全平台无关的问题。 |
| 当前页核心信息 | 左侧先给出现象，再用一个 `VertexBuffer` layout inset 补足输入侧证据；右侧解释 `State` 内部维度如何共同进入 PSO。 |
| 与上一页关系 | 从“预编译速度优化”直接切到“为什么很多问题其实是治理问题”。 |
| 这一页新增了什么 | 两张匿名样本图、一张与上方两张同宽的 `VertexBuffer` layout inset、`PSO = Shader + State` 结构卡、底部收束句。 |
| 这一页删除了什么 | 不再保留并行页的队列、worker 和注意事项卡。 |
| 这一页保留了什么 | 左侧 session rail、底部 step pills 与统一背景。 |
| 需要强调的元素 | `样本 A`、`样本 B`、`VertexBuffer` inset、`PSO = Shader + State`、`Vertex Decl / Input Layout`。 |
| 暂不实现 / 暂不出现 | 平台名字、API 优劣争论、具体根因代码。 |

## Node inventory

| id | text | semantic role | visual kind | anchor region | emphasis | alignment constraints | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `sample-a-shot` | `样本 A` | 第一张现象图 | screenshot card | 主舞台左上 | 高 | 与样本 B 同宽 | 从策略层切到平台差异事实层 | 否 |
| `sample-b-shot` | `样本 B` | 第二张现象图 | screenshot card | 主舞台左下 | 高 | 与样本 A 同宽 | 是 | 否 |
| `vertex-buffer-shot` | `VertexBuffer` | 输入布局证据 inset | screenshot card | 主舞台左下补充带 | 中 | 与 `sample-a-shot / sample-b-shot` 同宽，贴着样本 B 下沿，但不得压到 footer | 从现象继续推向 `Vertex Decl / Input Layout` | 否 |
| `pso-structure-card` | `PSO = Shader + State` | 结构解释卡 | rounded card | 主舞台右侧 | 高 | 与左侧双图形成左右对读 | 从现象进入解释 | 否 |
| `footer-strip` | `平台差异是客观存在的...` | 收束句 | footer pill | 主舞台底部 | 中 | 与主舞台中心轴对齐 | 作为治理页的前置结论 | 否 |

## Edge inventory

| id | from | to | meaning | route grammar | max bends | segment label | arrow style | sibling / symmetry note | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `samples-to-structure` | `sample-a-shot / sample-b-shot` | `pso-structure-card` | 从现象图进入结构解释 | implicit horizontal | 0 | - | none | 左现象右解释 | 是 | 否 |
| `structure-to-footer` | `pso-structure-card` | `footer-strip` | 从结构卡收束到底部结论 | implicit vertical | 0 | - | none | 单轴收口 | 是 | 否 |

## Spatial constraints

- 左侧两张截图必须上下对齐、同宽、同视觉权重，但不提前暴露平台名字。
- `VertexBuffer` inset 必须贴着 `sample-b-shot` 下沿补入，且外框与上方两张图同宽；只做输入布局提示，不得长成第三张主图。
- 右侧结构卡要足够大，观众一眼能看到 `Vertex Decl / Input Layout`、`RT Format`、`Depth/Blend` 等 state 项。
- 若左列三张图整体高度高于右侧说明卡，则优先拉高右侧说明卡到底边对齐，不压缩左列图片。
- 底部结论条只能做收束，不得抢左侧现象和右侧结构主体。

## Element styles

- 截图外只保留轻标签 `样本 A / B`，不写平台名字。
- 右侧结构卡采用统一浅底，`Vertex Decl / Input Layout` 做强调，为下一页埋钩子。
- 不引入大底板或无关装饰，保持全局背景一致。

## Assembly order

1. 淡出上一页并行化结构。
2. 左侧双样本图先进入。
3. 左下 `VertexBuffer` inset 与右侧 `PSO = Shader + State` 结构卡、底部收束条同段补入，不插旧页面闪回。

## Acceptance checklist

1. 观众先从左侧两张图看出“同内容不同结果”，但不会立即被平台名字剧透。
2. `VertexBuffer` inset 要把视线从现象慢慢推向输入布局，而不是变成第三张主图。
3. 右侧结构卡要把 `state` 里到底收了什么讲清楚。
4. 这一页讲完后，观众应该接受“PSO 不是完全平台无关的常数”。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | 前段 | `page_26` 淡出 | 时机策略页 | 从执行方式切到治理层。 |
| 2 | 中段 | 左侧双截图进入 | `sample-a-shot`、`sample-b-shot` | 先让观众自己看出差异，不急着说平台名字。 |
| 3 | 尾段 | 左下 `VertexBuffer` inset、右侧结构卡与底部结论补上 | `vertex-buffer-shot`、`pso-structure-card`、`footer-strip` | 用 `VertexBuffer -> Vertex Decl / Input Layout -> PSO State` 把差异原因的入口讲出来。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | 双样本截图、`VertexBuffer` inset、`PSO = Shader + State` 结构卡、底部收束条 |
| 需要复用的元素 | session rail、footer pills |
| 当前共享元素 / 连续语义 carrier | 外层工作台结构连续即可 |
| 是否需要镜头推进 / 缩放 | 否 |
| 是否需要逐步显隐 | 是，双图先于说明卡出现 |
| 风险点 | 如果 `VertexBuffer` inset 过大，会把左侧现象区误读成三张并列主图；如果右侧说明卡不跟左列底边对齐，页面会出现明显上下失衡；如果右侧 state 结构讲不清，下一页 `Vertex Decl` 会失去钩子。 |

## 2026-04-21 补图复核

- 新增元素：`image-3 / vertex-buffer-shot`，资源为 `SlideApp/public/supplement/VertexBuffer.png`
- 移动节点：右侧 `pso-structure-card` 底边下拉到与左列第三张图对齐；`sample-a-shot / sample-b-shot / footer-strip` 位置保持不动
- 增加 / 删除 node / edge：新增 node `vertex-buffer-shot`；edge 无新增、无删除
- 行为替换：原“左侧双样本图 -> 右侧结构卡 + footer”补入逻辑，替换为“左侧双样本图 -> 左下同宽 `VertexBuffer` inset + 右侧加高结构卡 + footer”同段补入；保留 `page_26 -> page_28` 直连时长不变
- Harness 事实：本轮 formal review artifact 复核为 `mechanicalScore=7.7`、`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`，当前 `minNodeGap=10`

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260421-page28-vertexbuffer-big/page28.png)

- 当前 URL：`http://127.0.0.1:4173/?step=page_28`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260421-page28-vertexbuffer-big/page28.txt`
- Capture size：`4096 x 2404`

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `8.1/10` | `8.2/10` | 双图对照和下方解释区的重心稳定，信息入口非常清楚。 | `8.2/10` |
| `模块空间结构 Review` | `7.8/10` | `7.9/10` | 并排证据图、解释卡、结论条三层关系明确，没有主图闪回。 | `7.9/10` |
| `单节点与单边 Review` | `7.4/10` | `7.5/10` | 单点内容可读，隐式阅读流成立。 | `7.5/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `sample-a-shot` | 左上匿名样本图。 | 从“并行执行”切到“平台差异事实” | screenshot card | 语义切换清楚。 | `7.8/10` | 尺寸已足够阅读。 | `7.7/10` |
| `sample-b-shot` | 左下匿名样本图。 | 与上图共同构成现象对照 | screenshot card | 成立。 | `7.8/10` | 与上图宽度统一，比较关系明确。 | `7.7/10` |
| `vertex-buffer-shot` | 左下第三张 `VertexBuffer` 输入布局证据图。 | 从现象层继续推向输入布局 | screenshot inset | 成立。 | `7.6/10` | 与上方两图同宽后，左侧证据带更完整，也把右侧 `Vertex Decl / Input Layout` 的阅读入口钉得更牢。 | `7.6/10` |
| `pso-structure-card` | 右侧 `PSO = Shader + State` 结构卡。 | 把事实层抬成治理命题 | rounded card | 成立。 | `7.7/10` | `Vertex Decl` 高亮清楚，能自然接到下一页。 | `7.6/10` |
| `footer-strip` | 底部收束句。 | 作为下一页根因分析的前置总结 | footer pill | 成立。 | `7.3/10` | 权重克制。 | `7.2/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `samples-to-structure` | 从左侧样本图进入右侧结构解释。 | 从现象进入解释 | implicit reading flow | 成立。 | `7.5/10` | 左现象右解释的阅读路径清楚。 | `7.4/10` |
| `structure-to-footer` | 从结构卡收束到底部结论。 | 为下一页原因分析做垫子 | implicit reading flow | 成立。 | `7.1/10` | 不需显式箭头。 | `7.0/10` |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_26 -> page_28）

- 帧窗口：`2629 -> 2700`（`71` frames @ 60fps）
- 总时长：`1.183s`
- 时长评估：`too_short`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `72`，相对当前 `Δ+1` frames
- 标准时序评估：`in_range`（标准下限 `1.091s`，上限 `1.473s`）
- 标准调整建议：目标帧长 `71`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`1.091s`
- 标准允许上限：`1.473s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 120.0 | 120.0 | - |
| fade_out | 120.0 | 340.0 | 220.0 | fade_out_compile_acceleration |
| node_motion | 340.0 | 691.0 | 351.0 | move_platform_samples, move_pso_state_card |
| fade_in | 691.0 | 911.0 | 220.0 | fade_in_platform_footer |
| hold_after | 911.0 | 1091.0 | 180.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_compile_acceleration | fade_out | fade_out | - | 220.0 | formula |
| move_platform_samples | node_move | node_motion | distancePx=120.0 | 294.0 | formula |
| move_pso_state_card | node_move | node_motion | distancePx=180.0 | 351.0 | formula |
| fade_in_platform_footer | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 2629 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 2647 | 0.300 | fade_out_compile_acceleration(fade_out) | 无显著边变化 |
| Mid | 2664 | 0.583 | move_platform_samples(node_move)、move_pso_state_card(node_move) | 无显著边变化 |
| Q3 | 2682 | 0.883 | fade_in_platform_footer(fade_in) | 无显著边变化 |
| End | 2700 | 1.183 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- 2026-04-21 按“第三张图与前两张同宽、右卡底边跟左列对齐”重抓当前页并复跑 transition probe，frame anchors 已切换为 `page_26 -> page_28` 直连，当前时序表保持有效。
- 2026-04-21 补入并放大 `VertexBuffer` inset 后，继续沿用原 `surface_page_fade_in` 相位落位，不新增 frame anchor，也不压缩原转场时长。
- `page_26 -> page_28` 当前时长与标准时序都为 `in_range`。
- 转场采用“先淡出并行页，再淡入双截图、`VertexBuffer` inset 和解释卡”的方式，没有图片页之间的无关主图闪回。

## 审批状态

`通过。可作为治理章节的第一张现象页进入正式讲述。`
