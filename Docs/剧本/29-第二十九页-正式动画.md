# 第 29 页

> 文件类型：`正式动画页`
> Step / Sketch：`page_29` / `-`
> 上一页：`page_28`
> 下一页：`page_29_data`

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `8.1/10` | 保持外层骨架不动，把共享编译参数做成更薄的居中条，再把代码、IA 与 footer 整组上提，纵向链路更平衡。 |
| Node 分数 | `7.8/10` | `VertexDescriptor / InitRHI` 与 `LocalVertexFactory.ush` 的层级清楚。 |
| Edge 分数 | `7.3/10` | 依赖左右对照和隐式阅读流，不需要显式箭头，信息路径依然稳定。 |
| 动画分数 | `in_range` | `page_28 -> page_29` 为 `80` 帧，标准时序 verdict=`in_range`。 |

- 2026-04-22 机械评估：`7.3/10`，`min node gap` 从上一轮的 `2px` 提升到 `12px`，纵向最紧的链路已从“参数条贴代码”改成“可读但仍可继续放松”的状态。

## Page goal

- 用代码层证据解释上一页的现象：输入布局本身会进入管线状态，因此输入侧差异会直接进入 PSO 维度。
- 这一页要把“同一个Material作用于不同的Mesh也会产生不同的PSO”讲成根因，而不是一句口号。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `平台差异的来源：输入侧差异会直接进入 PSO 维度` |
| 页面副标题 | 同一个Material作用于不同的Mesh也会产生不同的PSO。 |
| 当前页核心信息 | 上中统一写 `NUM_MATERIAL_TEXCOORDS_VERTEX = 2`，左右 IA 截图只承担 Mesh 输入差异：左 `1 个 UV`、右 `2 个 UV`，从而说明输入布局差异为什么会直接影响 PSO。 |
| 与上一页关系 | 从“平台差异的表象”过渡到“平台差异的来源”，随后先进入 `page_29_data` 的原始数据对比页。 |
| 这一页新增了什么 | 上中共享编译参数条、左右代码块（轻量语法高亮）、底部总结条。 |
| 这一页删除了什么 | 不再保留双平台截图与上一页说明卡。 |
| 这一页保留了什么 | 左侧 session rail、底部 step pills 与统一背景。 |
| 需要强调的元素 | `VertexDescriptor`、`LocalVertexFactory`、底部结论句。 |
| 暂不实现 / 暂不出现 | 更深的 shader 编译细节、平台驱动实现差异。 |

## Node inventory

| id | text | semantic role | visual kind | anchor region | emphasis | alignment constraints | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `compile-param` | `NUM_MATERIAL_TEXCOORDS_VERTEX = 2` | 上中共享编译参数 | accent token | 主舞台上中 | 高 | 必须压在两段代码正中间，作为两侧共同前提 | 新增统一前提 | 否 |
| `cpu-code-card` | `VertexDescriptor / InitRHI` | CPU 侧根因证据 | code card | 主舞台左上 | 高 | 与左侧图片同宽，并与右侧代码卡同高；整体与共享参数条保持明确间距，视觉中心回到主舞台中段 | 承接上一页现象的左侧解释 | 否 |
| `shader-code-card` | `LocalVertexFactory.ush` | Shader 输入侧证据 | code card | 主舞台右上 | 高 | 与右侧图片同宽，并与左侧代码卡同高；整体与共享参数条保持明确间距，视觉中心回到主舞台中段，代码内容额外内缩一档 | 是 | 否 |
| `left-image` | `Mesh：1 个 UV` | 左侧 IA 证据 | bare image | 主舞台左下 | 中 | 左右边界与左代码框完全对齐，标题与图片中轴对齐，图片仅放大不裁切 | 承接左侧代码的直接结果 | 否 |
| `right-image` | `Mesh：2 个 UV` | 右侧 IA 证据 | bare image | 主舞台右下 | 中 | 左右边界与右代码框完全对齐，标题与图片中轴对齐，图片仅放大不裁切 | 承接右侧代码的直接结果 | 否 |
| `footer-strip` | `同一个Material作用于不同的Mesh也会产生不同的PSO` | 底部收束句 | footer pill | 主舞台底部 | 中 | 与主舞台中心轴对齐，并和图片区拉开 | 为下一页的原始数据对比页预埋收口 | 否 |

## Edge inventory

| id | from | to | meaning | route grammar | max bends | segment label | arrow style | sibling / symmetry note | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `cpu-to-shader` | `cpu-code-card` | `shader-code-card` | 从 CPU 侧设置进入 shader 输入声明 | implicit horizontal | 0 | - | none | 左到右阅读 | 是 | 否 |
| `shader-to-footer` | `shader-code-card` | `footer-strip` | 从代码结论收束到底部总结 | implicit vertical | 0 | - | none | 单轴收束 | 是 | 否 |

## Spatial constraints

- 左右代码块必须同一视觉层级，不让任何一侧成为装饰。
- 共享编译参数条必须保持居中、横向拉长，并与左右代码卡之间留出可见呼吸带，不能再贴边。
- 两张图的左右边界与测量宽度都必须严格贴齐对应代码框，形成上下成对阅读；图片标题必须压在各自图片中轴上，并通过增高字号来放大证据，不做裁切。
- 底部总结条需要足够克制，且要与图片区拉开。

## Element styles

- 代码块延续统一浅底边框，但不额外加外层大框。
- 代码行保留真实缩进，并只给关键词 / 宏 / 字符串做轻量语法高亮，不能做成花哨 IDE 风格。
- 标题始终在页面外层，不写进代码图或截图内部。

## Assembly order

1. 淡出上一页双截图页。
2. 左右代码块一起进入，先完成“根因并排”。
3. 补底部总结句，完成抽象收束。

## Acceptance checklist

1. 观众要能明白“输入布局差异”就是 PSO 维度的一部分。
2. 左右代码块必须可读，不能缩到只剩装饰感。
3. 底部那句“同一个Material作用于不同的Mesh也会产生不同的PSO”要成为本页记忆点。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | 前段 | `page_28` 淡出 | 双平台截图页 | 现象页退场，开始讲根因。 |
| 2 | 中段 | 上中共享编译参数先给出，再落左右代码 | `compile-param`、`cpu-code-card`、`shader-code-card` | 先明确“两边共用同一个编译参数”，再讲输入差异。 |
| 3 | 后段 | IA 图与底部总结条补齐 | `left-image`、`right-image`、`footer-strip` | 左右只保留 Mesh 差异，最后收束结论。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | 两个代码块、底部结论条 |
| 需要复用的元素 | session rail、footer pills |
| 当前共享元素 / 连续语义 carrier | 外层工作台结构连续即可 |
| 是否需要镜头推进 / 缩放 | 否 |
| 是否需要逐步显隐 | 是，代码块优先，footer 后补 |
| 风险点 | IA 图如果再缩回小窗大小，这页会重新变得“像解释页，不像证据页”。 |

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260422-171901/review-capture.png)

- 当前 URL：`http://127.0.0.1:4174/?step=page_29`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260422-171901/review-capture.txt`
- Capture size：`4096 x 2404`

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `7.9/10` | `8.0/10` | 左右代码证据等宽居中后，参数条、代码、截图、footer 的纵向链路更顺。 | `8.0/10` |
| `模块空间结构 Review` | `7.7/10` | `7.8/10` | 两块代码卡、对应 IA 图和总结条的层级关系明确。 | `7.8/10` |
| `单节点与单边 Review` | `7.4/10` | `7.5/10` | 单点结构干净，左右对照容易读懂。 | `7.5/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `compile-param` | 上中统一展示共享编译参数 `NUM_MATERIAL_TEXCOORDS_VERTEX = 2`。 | 本页新增统一前提 | accent token | 成立。 | `7.8/10` | 更薄的横条把“共同条件”钉住后，左右对照不再误读成两次不同编译。 | `7.9/10` |
| `cpu-code-card` | CPU 侧 `VertexDescriptor / InitRHI` 代码证据。 | 承接上一页左侧现象图的“为什么” | code card | 成立。 | `7.6/10` | 等宽拉长并上提后，左侧代码和下方 IA 图的对照关系更稳定。 | `7.7/10` |
| `shader-code-card` | `LocalVertexFactory.ush` 输入结构证据。 | 承接上一页右侧现象图的“为什么” | code card | 成立。 | `7.7/10` | 右侧代码保持等宽居中，并把正文整体内缩一档，用轻量语法高亮把 `#if / ATTRIBUTE` 信息提出来。 | `7.8/10` |
| `left-image` | 左侧 IA 截图，标题改为 `Mesh：1 个 UV`。 | 承接左侧代码结果 | bare image | 成立。 | `7.5/10` | 现在左图测量宽度与上方代码框完全一致，标题也抬大到更容易扫读。 | `7.8/10` |
| `right-image` | 右侧 IA 截图，标题改为 `Mesh：2 个 UV`。 | 承接右侧代码结果 | bare image | 成立。 | `7.5/10` | 右图也回到“Mesh 差异”本身，并与上方代码框保持严格同宽。 | `7.8/10` |
| `footer-strip` | 底部总结句。 | 为下一页的独立阅读页收口 | footer pill | 成立。 | `7.2/10` | 略微上提后不再压底，和图片区的呼吸感更稳定。 | `7.3/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `cpu-to-shader` | 从 CPU 侧设置过渡到 shader 输入。 | 现象开始落到根因 | implicit reading flow | 成立。 | `7.4/10` | 左右阅读自然。 | `7.2/10` |
| `shader-to-footer` | 从右侧代码收束到底部结论。 | 为下一页的独立阅读页服务 | implicit reading flow | 成立。 | `7.1/10` | 不需要显式箭头。 | `7.0/10` |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_28 -> page_29）

- 帧窗口：`2903 -> 2983`（`80` frames @ 60fps）
- 总时长：`1.333s`
- 时长评估：`in_range`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `80`，相对当前 `Δ+0` frames
- 标准时序评估：`in_range`（标准下限 `1.112s`，上限 `1.501s`）
- 标准调整建议：目标帧长 `80`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`1.112s`
- 标准允许上限：`1.501s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 120.0 | 120.0 | - |
| fade_out | 120.0 | 340.0 | 220.0 | surface_page_fade_out |
| node_motion | 340.0 | 672.0 | 332.0 | code_block_move, attribute_tokens_move |
| fade_in | 672.0 | 892.0 | 220.0 | source_page_fade_in |
| hold_after | 892.0 | 1112.0 | 220.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| surface_page_fade_out | fade_out | fade_out | - | 220.0 | formula |
| code_block_move | node_move | node_motion | distancePx=160.0 | 332.0 | formula |
| attribute_tokens_move | node_move | node_motion | distancePx=100.0 | 275.0 | formula |
| source_page_fade_in | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 2903 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 2923 | 0.333 | surface_page_fade_out(fade_out) | 无显著边变化 |
| Mid | 2943 | 0.667 | code_block_move(node_move) | 无显著边变化 |
| Q3 | 2963 | 1.000 | hold_after 静置 | hold_after 静置 |
| End | 2983 | 1.333 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- 2026-04-22 版式复审：共享编译参数条改成更薄的居中条，左右代码卡保持等宽并上提，IA 截图与 footer 同步上提；frame anchors 未改，当前时序表仍保持有效。
- `page_28 -> page_29` 当前时长与标准时序都为 `in_range`。
- `page_29` 结束后，现在会先淡入 `page_29_data` 的双平台原始数据表，再从数据页淡出去 `page_30`。
- 本轮新增 `page_29_data` 后，后续 `page_30`、`page_31`、`page_32` 的 frame anchor 统一顺延 `+71` frames，总时长同步补长，未压缩原有后续转场时长。
- 现象页淡出后直接进入代码证据页，没有发生双图页与代码页之间的无关内容闪回。
- 2026-04-22 逻辑修正：代码块继续保留真实缩进，并增加轻量语法高亮；本轮把右上代码正文再内缩一档，把下方图片的测量宽度严格收回到与上方代码框一致，同时把 `Mesh` 标题字号放大，提升上下对照的可读性。

## 审批状态

`通过。可作为平台差异根因页进入正式讲述。`
