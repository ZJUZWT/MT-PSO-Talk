# 第 29 页正式动画：`page_29`

> 文件类型：`正式动画页 / closing quote`
> Step / Sketch：`page_29` / `-`
> 上一页：`page_28`
> 下一页：`-`


## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `人工快审` | 纯引句收束，视觉重心集中，没有多余信息干扰 |
| Node 分数 | `人工快审` | 引句主块、出处、尾注三层关系明确 |
| Edge 分数 | `人工快审` | 页面依赖排版阅读流，不需要额外显式边 |
| 动画分数 | `in_range` | 自动 probe：`page_28 -> page_29`，标准时序 verdict=`in_range` |

## Page goal

- 让整场分享在一句真正留得住的话上停下来。
- 去掉所有工程解释，只保留《庄子·逍遥游》引句与出处。

## 页面核心信息

- 顶部小标题：`《庄子·逍遥游》`
- 中间主块：四行引句
- 底部：`以此作为这次分享的最后一句。`

## 讲述备注

- 这一页不再给“结论总结”，而是把分享停在开放空间里。
- 引句字号要明显大于前一页，确保最后一页的视觉重心足够干净。

## Acceptance checklist

1. 观众进入这一页后，不再被额外信息打断。
2. 引句四行的行距与留白足以支撑停顿感。
3. 结尾句和出处应退到次级，不和引句本身抢重心。

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/Docs/剧本/.snapshots/page29-closing.png)

- 当前 URL：`http://127.0.0.1:4175/?step=page_29`
- 捕获方式：`headless-stage`

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `人工快审` | `人工快审` | 极简收束有效，留白充分。 | `快审通过` |
| `模块空间结构 Review` | `人工快审` | `人工快审` | 出处、正文、尾注三层稳定，不互相抢位。 | `快审通过` |
| `单节点与单边 Review` | `人工快审` | `人工快审` | 引句四行视觉统一，阅读路径自然。 | `快审通过` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `source-node` | 顶部出处节点。 | 由上一页的推荐页收拢到一个更高层的文本来源 | 顶部小标题文本 | 情绪连续，但信息层级主动收缩。 | `快审` | 位置克制，不抢主句。 | `快审通过` |
| `quote-block` | 四行引句主块。 | 故意打断上一页的双栏结构，进入单焦点结尾 | 中央大字文本组 | 结构切换合理。 | `快审` | 焦点充分集中。 | `快审通过` |
| `footer-node` | 底部收尾句。 | 承接整场分享最终停顿 | 底部淡灰尾注 | 收尾成立。 | `快审` | 视觉退后，符合结尾语气。 | `快审通过` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `source-to-quote` | 从出处进入正文主块的阅读边。 | 从推荐页过渡到引句中心 | 隐式阅读边 | 路径自然。 | `快审` | 单轴阅读清晰。 | `快审通过` |
| `quote-to-footer` | 从引句进入底部尾注的收束边。 | 承接最后停顿 | 隐式阅读边 | 收束关系成立。 | `快审` | 不需要显式箭头。 | `快审通过` |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_28 -> page_29）

- 帧窗口：`3006 -> 3086`（`80` frames @ 60fps）
- 总时长：`1.333s`
- 时长评估：`in_range`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `80`，相对当前 `Δ+0` frames
- 标准时序评估：`in_range`（标准下限 `1.112s`，上限 `1.501s`）
- 标准调整建议：目标帧长 `80`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`1.112s`
- 标准允许上限：`1.501s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 120.0 | 120.0 | - |
| fade_out | 120.0 | 340.0 | 220.0 | reading_page_fade_out |
| node_motion | 340.0 | 672.0 | 332.0 | quote_block_move, quote_source_move |
| fade_in | 672.0 | 892.0 | 220.0 | quote_page_fade_in |
| hold_after | 892.0 | 1112.0 | 220.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| reading_page_fade_out | fade_out | fade_out | - | 220.0 | formula |
| quote_block_move | node_move | node_motion | distancePx=160.0 | 332.0 | formula |
| quote_source_move | node_move | node_motion | distancePx=100.0 | 275.0 | formula |
| quote_page_fade_in | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 3006 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 3026 | 0.333 | reading_page_fade_out(fade_out) | 无显著边变化 |
| Mid | 3046 | 0.667 | quote_block_move(node_move) | 无显著边变化 |
| Q3 | 3066 | 1.000 | hold_after 静置 | hold_after 静置 |
| End | 3086 | 1.333 | 无显著节点变化 | 无显著边变化 |
