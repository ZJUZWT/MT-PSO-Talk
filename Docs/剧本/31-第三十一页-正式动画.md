# 第 31 页

> 文件类型：`正式动画页`
> Step / Sketch：`page_31` / `-`
> 上一页：`page_30`
> 下一页：`page_32`

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `6.0/10` | formal harness 当前 `stageLayout=6`；主回环已经成立，但右侧辅助卡仍然是紧凑信息区。 |
| Node 分数 | `6.3/10` | formal harness 当前 `mechanicalScore=6.3`；已无 `textOverflow`、`crossing`、`nodePierce` blocker。 |
| Edge 分数 | `9.0/10` | 主回环箭头顺时针闭合，formal facts 下 `crossing=0`、`nodePierce=0`。 |
| 动画分数 | `in_range` | `page_30 -> page_31` 标准时序 verdict=`in_range`。 |

## Page goal

- 在推荐页之前，把仓库里现在真实在跑的自动改版回环直接亮出来。
- 这一页不再只讲“评分链路”或“数学约束”，而是要让观众看清：`Agent 修改 -> hook / workflow gate -> 前台 Edge 真实取数 -> geometryReviewArtifact -> geometryMetrics -> geometryScorePolicy -> 回写下一轮修改`。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `项目 Harness 自动回环：Agent 改完，自动取数再计算` |
| 页面副标题 | Agent 修改页面后，会经 `hook / workflow_gate.py` 触发前台 Edge 取数，再走 geometry review 链，把结果回写成下一轮修改建议。 |
| 当前页核心信息 | 左侧是显式顺时针闭环，右侧是 `hook / workflow_gate.py / review:mechanical / front Edge probe` 与硬门槛说明。 |
| 与上一页关系 | 从“前置治理决定上限”切到“页面和动画是怎么被自动约束和回写的”。 |
| 这一页新增了什么 | `loop-card`、六个闭环节点、六条闭环箭头、`helper-card`、四个 trigger token、四个 hard-gate token。 |
| 这一页移动了什么 | 原 `评分链路` 从单列纵排改为顺时针回环；右侧说明区缩成辅助卡，不再与主循环争主次。 |
| 这一页删除了什么 | 删除旧的 `chain-1/2/3` 纵排链路卡结构与“只讲公式”的右卡叙事。 |
| 这一页保留了什么 | session rail、底部 step pills 与统一背景。 |
| 增加 / 删除的 edge | 新增 `agent->edge`、`edge->artifact`、`artifact->metrics`、`metrics->policy`、`policy->feedback`、`feedback->agent`；删除旧的隐式 `chain-to-formula` / `cards-to-footer` 阅读流描述。 |
| 被替换的过渡逻辑 | 从“左右双卡并列说明”替换成“主闭环 + 辅助卡”的阅读结构；转场仍保持整板 crossfade，不引入旧 loop-stage 闪回。 |
| 需要强调的元素 | `hook`、`workflow_gate.py`、`review:mechanical`、`front Edge probe`、`geometryReviewArtifact`、`geometryMetrics`、`geometryScorePolicy`、`overlap=0`。 |
| 暂不实现 / 暂不出现 | 完整 JSON 报告、逐项 raw metrics dump、前台 Edge 取数脚本的所有参数。 |

## Node inventory

| id | text | semantic role | visual kind | anchor region | emphasis | alignment constraints | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `loop-card` | `完整回环` | 主循环容器 | rounded card | 主舞台左侧 | 高 | 与右侧辅助卡同高 | 从治理结论切到自动约束流程 | 否 |
| `agent-node` | `Agent 修改页面` | 回环起点 | rounded node | 左上 | 高 | 与 `feedback-node` 共列 | 将结论重新送回改版动作 | 否 |
| `edge-node` | `前台 Edge 真实取数` | 真实取数节点 | rounded node | 上中 | 高 | 与 `policy-node` 共列 | 把主观修改转成真实浏览器事实 | 否 |
| `artifact-node` | `geometryReviewArtifact` | facts 节点 | rounded node | 右上 | 高 | 与 `metrics-node` 共列 | 从真实取数进入几何 facts | 否 |
| `metrics-node` | `geometryMetrics` | 指标节点 | rounded node | 右下 | 高 | 与 `artifact-node` 垂直对齐 | 从 facts 压缩成指标 | 否 |
| `policy-node` | `geometryScorePolicy` | 评分 / 门槛节点 | rounded node | 下中 | 高 | 与 `edge-node` 垂直对齐 | 根据指标决定放行或继续改 | 否 |
| `feedback-node` | `回写下一轮修改` | 回写节点 | rounded node | 左下 | 高 | 与 `agent-node` 垂直对齐 | 把评分结果重新送回 Agent | 否 |
| `helper-card` | `Hook / 真实取数` | 辅助说明卡 | rounded card | 主舞台右侧 | 中 | 宽度收窄，不压过主循环 | 补清触发入口与硬门槛 | 否 |
| `footer-strip` | `hook 会带着真实 Edge 数据...` | 底部收束句 | footer pill | 主舞台底部 | 中 | 与主舞台中心轴对齐 | 为推荐页做收口 | 否 |

## Edge inventory

| id | from | to | meaning | route grammar | max bends | segment label | arrow style | sibling / symmetry note | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `agent-to-edge` | `agent-node` | `edge-node` | 改完后进入真实取数 | horizontal | 0 | - | arrow | 顶部主链第一段 | 是 | 否 |
| `edge-to-artifact` | `edge-node` | `artifact-node` | 真实取数进入 facts | horizontal | 0 | - | arrow | 顶部主链第二段 | 是 | 否 |
| `artifact-to-metrics` | `artifact-node` | `metrics-node` | facts 压成 metrics | vertical | 0 | - | arrow | 右侧竖向主链 | 是 | 否 |
| `metrics-to-policy` | `metrics-node` | `policy-node` | 指标进入 policy | horizontal | 0 | - | arrow | 底部主链第一段 | 是 | 否 |
| `policy-to-feedback` | `policy-node` | `feedback-node` | policy 生成回写建议 | horizontal | 0 | - | arrow | 底部主链第二段 | 是 | 否 |
| `feedback-to-agent` | `feedback-node` | `agent-node` | 建议回送 Agent | vertical | 0 | - | arrow | 左侧竖向闭环 | 是 | 否 |

## Spatial constraints

- 主回环必须明显强于右侧辅助卡，不能重新退化成“两张同权说明卡”。
- 六个闭环节点要能一眼读成顺时针回环，不能把路径读丢。
- 右侧必须同时露出 trigger 方案与四个 hard-gate token，但不能因为信息过满压塌内边距。
- 底部收束句只做总结，不得压住主循环或辅助卡。

## Element styles

- 主循环节点沿用 late-tail 背景与橙色描边，只把 `Agent` 与 `回写` 做成强调节点。
- 箭头全部用轻量橙色直线，强调“它真的在跑一圈”，不做装饰性曲线。
- 右侧辅助卡继续保持工程感，但从“大公式卡”改成“hook + Edge + hard gate”说明区。
- 不新增底板，不破坏晚段统一背景。

## Assembly order

1. 淡出上一页治理结论页。
2. 左侧主回环与右侧辅助卡一起进入，建立“自动回环”主命题。
3. 底部收束句补齐，强调“真实 Edge 数据 + 数学门槛会回写下一轮修改”。

## Acceptance checklist

1. 观众能一眼看出这是一个顺时针的完整闭环，而不是又一页双栏说明。
2. `Agent 修改 -> 前台 Edge 真实取数 -> geometryReviewArtifact -> geometryMetrics -> geometryScorePolicy -> 回写修改` 必须完整成立。
3. 右侧必须明确露出 `hook`、`workflow_gate.py`、`review:mechanical` 与四个 hard gate。
4. formal harness 下 `crossing=0`、`nodePierce=0`、`textOverflow=0` 必须继续为 0。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | 前段 | `page_30` 淡出 | 治理结论页 | 从“前置治理”切到“改完以后谁来自动验收”。 |
| 2 | 中段 | 主回环和辅助卡一起进入 | `loop-card`、`helper-card` | 左边讲闭环主链，右边补 hook 与真实取数。 |
| 3 | 尾段 | 底部收束句进入 | `footer-strip` | 用一句话压住“真实数据 + 约束 -> 回写修改”。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | `loop-card`、六个回环节点、六条箭头、`helper-card`、trigger token、hard-gate token、footer |
| 需要复用的元素 | session rail、footer pills |
| 当前共享元素 / 连续语义 carrier | 外层工作台结构连续即可 |
| 是否需要镜头推进 / 缩放 | 否 |
| 是否需要逐步显隐 | 是，主回环与辅助卡先出现，footer 后补 |
| 风险点 | 如果右侧辅助卡再次做得太重，页面会退回“双栏说明”，看不出自动回环才是真主角。 |

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260420-page31-loop-refresh/page31-loop.png)

- 当前 URL：`http://127.0.0.1:4173/?variant=bus-clean&step=page_31`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260420-page31-loop-refresh/page31-loop.txt`
- Capture size：`4096 x 2242`

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `3.3/10` | `3.3/10` | formal harness 认为左右质量仍偏左重，右侧辅助区也还紧。 | `3.3/10` |
| `模块空间结构 Review` | `4.3/10` | `4.3/10` | 闭环结构成立，但最紧节点间距仍只有 `14px`。 | `4.3/10` |
| `单节点与单边 Review` | `9.3/10` | `9.3/10` | 当前已无 `crossing`、`nodePierce`、`textOverflow` blocker。 | `9.3/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `loop-card` | 主循环容器。 | 从治理结论切到自动回环 | rounded card | 成立。 | `7.4/10` | 主意图已经明确。 | `6.8/10` |
| `agent-node` | Agent 修改页面。 | 从“治理”回到“修改动作” | rounded node | 成立。 | `7.2/10` | 强调位清楚。 | `7.0/10` |
| `edge-node` | 前台 Edge 真实取数。 | 把修改动作接到真实数据 | rounded node | 成立。 | `7.4/10` | 语义直接。 | `6.9/10` |
| `artifact-node` | geometryReviewArtifact。 | 从真实数据进入 facts | rounded node | 成立。 | `7.1/10` | 与 metrics/policy 顺序清楚。 | `6.8/10` |
| `metrics-node` | geometryMetrics。 | 从 facts 进入指标 | rounded node | 成立。 | `7.1/10` | 与 artifact 对位稳定。 | `6.8/10` |
| `policy-node` | geometryScorePolicy。 | 从指标进入门槛判断 | rounded node | 成立。 | `7.1/10` | 位于闭环下中，读法自然。 | `6.8/10` |
| `feedback-node` | 回写下一轮修改。 | 从 policy 回送 Agent | rounded node | 成立。 | `7.3/10` | 回环收口清楚。 | `7.0/10` |
| `helper-card` | hook / 真实取数 / hard gate 说明卡。 | 给主循环补触发入口 | rounded card | 成立。 | `7.0/10` | 当前偏紧，是后续可继续放松的区块。 | `6.3/10` |
| `footer-strip` | 底部收束句。 | 为推荐页做情绪缓冲 | footer pill | 成立。 | `7.0/10` | 位置克制。 | `6.9/10` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `agent-to-edge` | Agent 改完后进入真实取数。 | 从动作进入事实采集 | arrow | 成立。 | `7.5/10` | 顶部主链清楚。 | `8.0/10` |
| `edge-to-artifact` | 真实取数进入 facts。 | 从浏览器结果进入 artifact | arrow | 成立。 | `7.5/10` | 顶部第二段读法直接。 | `8.0/10` |
| `artifact-to-metrics` | facts 压成 metrics。 | 从 facts 到指标 | arrow | 成立。 | `7.4/10` | 右侧竖向主链清楚。 | `8.2/10` |
| `metrics-to-policy` | 指标进入 policy。 | 从指标到门槛 | arrow | 成立。 | `7.4/10` | 底部右段稳定。 | `8.0/10` |
| `policy-to-feedback` | policy 生成回写建议。 | 从判断到建议 | arrow | 成立。 | `7.4/10` | 底部左段稳定。 | `8.0/10` |
| `feedback-to-agent` | 建议回送 Agent。 | 闭环闭合 | arrow | 成立。 | `7.5/10` | 左侧竖向闭环明确。 | `8.2/10` |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_30 -> page_31）

- 帧窗口：`2973 -> 3044`（`71` frames @ 60fps）
- 总时长：`1.183s`
- 时长评估：`too_short`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `72`，相对当前 `Δ+1` frames
- 标准时序评估：`in_range`（标准下限 `0.993s`，上限 `1.341s`）
- 标准调整建议：目标帧长 `71`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`0.993s`
- 标准允许上限：`1.341s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 100.0 | 100.0 | - |
| fade_out | 100.0 | 320.0 | 220.0 | fade_out_governance_page |
| node_motion | 320.0 | 633.0 | 313.0 | move_harness_loop, move_harness_helper_card |
| fade_in | 633.0 | 853.0 | 220.0 | fade_in_harness_footer |
| hold_after | 853.0 | 993.0 | 140.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_governance_page | fade_out | fade_out | - | 220.0 | formula |
| move_harness_loop | node_move | node_motion | distancePx=120.0 | 294.0 | formula |
| move_harness_helper_card | node_move | node_motion | distancePx=140.0 | 313.0 | formula |
| fade_in_harness_footer | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 2973 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 2991 | 0.300 | fade_out_governance_page(fade_out) | 无显著边变化 |
| Mid | 3008 | 0.583 | move_harness_loop(node_move)、move_harness_helper_card(node_move) | 无显著边变化 |
| Q3 | 3026 | 0.883 | hold_after 静置 | hold_after 静置 |
| End | 3044 | 1.183 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- 2026-04-20 收紧版复审：已重抓当前页并复跑 transition probe，frame anchors 未改，当前时序表保持有效。
- `page_30 -> page_31` 的通用时长带宽显示 `too_short`，但 harness 标准时序 verdict 为 `in_range`，当前 anchor 可接受。
- 治理结论页淡出后直接进入“左侧主回环 + 右侧辅助卡”，没有出现旧证据图回闪。

## 审批状态

`通过。可作为项目内延伸阅读页进入正式讲述。`
