# 第 31 页

> 文件类型：`正式动画页`
> Step / Sketch：`page_31` / `-`
> 上一页：`page_30`
> 下一页：`page_32`

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `8.0（formal）` | 单大圆环成立，四个主节点挂在圆环四边，主读法明显强于贴边 token。 |
| Node 分数 | `6.7（mechanical）` | formal sketch 已通过 `overlap/crossing/nodePierce/textOverflow=0`，贴边 token 与中心题眼均在安全框内。 |
| Edge 分数 | `pass` | 4 条顺时针曲线箭头形成闭环，没有交叉与穿透。 |
| 动画分数 | `标准 in_range` | `page_30 -> page_31` 在 Harness 标准时序中为 `in_range`。 |

## Page goal

- 把仓库里真实在跑的 harness 压成一个更直观的单大圆环，而不是左右两张解释卡。
- 让观众先读到闭环本身：`Hook 进入 -> 网页数据评分 -> 网页图片评分 -> 回执循环`。
- 再把 `workflow gate`、`front probe`、`browser capture`、`blind critics`、`通过则停止 / 不通过继续` 吸到圆环边上，作为真实抓手提示。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `项目 Harness：先看真实结果，再决定停或继续` |
| 页面副标题 | 真实回环不再拆成主卡和 helper 卡，而是压成单大圆环。 |
| 当前页核心信息 | 圆环四边是 4 个主节点，圆环内部是题眼，圆环边上是工程抓手 token。 |
| 与上一页关系 | 从 `PSO 延伸阅读` 资料页切回“项目里怎么让页面自己被真实结果约束”。 |
| 这一页新增了什么 | 新增单大圆环、中心题眼、4 个主节点、4 条顺时针曲线箭头、4 个 source token、2 个 decision token。 |
| 这一页移动了什么 | 把旧版右侧 helper 卡拆散，分别贴到圆环对应边上。 |
| 这一页删除了什么 | 删除旧版 `loop-card + helper-card + footer` 的双卡结构。 |
| 增加 / 删除的 node / edge | 新增 `hook/data/image/receipt` 4 主节点与 4 条环向箭头；删除旧的 `loop-card`、`helper-card`、footer bar。 |
| 被替换的过渡逻辑 | 由 `page_30` 淡出后，先成立大圆环和主节点，再补边上的 token 与中心题眼。 |

## Node inventory

| id | text | semantic role | visual kind | anchor region | emphasis | 与上一页连续语义 |
| --- | --- | --- | --- | --- | --- | --- |
| `hook-node` | `Hook 进入` | 回环起点 | rounded node | 上侧 | 高 | 把页面修改送进自动流程 |
| `data-node` | `网页数据评分` | 数据/文本评分 | rounded node | 右侧 | 中高 | 真实网页取数 |
| `image-node` | `网页图片评分` | 图像评分 | rounded node | 下侧 | 中高 | 真实截图与视觉复核 |
| `receipt-node` | `回执循环` | 回写与决策 | rounded node | 左侧 | 高 | blocker / top fixes / 通过与否回写 |
| `source-1` | `workflow gate` | 入口抓手 | micro token | 上侧外沿 | 中高 | 把任务送进 loop |
| `source-2` | `front probe` | 数据抓手 | micro token | 右上外沿 | 中 | 真实页面读数 |
| `source-3` | `browser capture` | 截图抓手 | micro token | 下侧外沿 | 中 | 真实画面 capture |
| `source-4` | `blind critics` | 视觉抓手 | micro token | 左上外沿 | 中 | 盲审视觉复核 |
| `decision-1` | `通过则停止` | stop 条件 | micro token | 左下外沿 | 高 | 闭环放行条件 |
| `decision-2` | `不通过继续` | continue 条件 | micro token | 左下外沿 | 高 | 闭环继续修改 |
| `loop-core-title` | `先看真实结果 / 再决定停或继续` | 中心题眼 | center copy | 圆环内部 | 高 | 压实这一页主旨 |

## Edge inventory

| id | from | to | meaning | route grammar | max bends | 与上一页连续语义 |
| --- | --- | --- | --- | --- | --- | --- |
| `hook-to-data` | `hook-node` | `data-node` | Hook 后先进入网页数据评分 | curved arc | 1 | 成立 |
| `data-to-image` | `data-node` | `image-node` | 数据评分进入图片评分 | curved arc | 1 | 成立 |
| `image-to-receipt` | `image-node` | `receipt-node` | 图片评分进入回执循环 | curved arc | 1 | 成立 |
| `receipt-to-hook` | `receipt-node` | `hook-node` | 回执重新送回 Hook | curved arc | 1 | 成立 |

## Spatial constraints

- 圆环必须是绝对主视觉，不允许再退回左右双栏同权。
- 4 个主节点要清楚落在圆环四边，顺时针读法不能歧义。
- token 只做提示，不得抢掉主节点与中心题眼。
- 不新增大色块背景，继续沿用晚段统一浅底。

## Element styles

- 主节点沿用晚段圆角浅底语言，`Hook` 与 `回执循环` 用强调态。
- 圆环采用双层轻描边，不引入新的底板。
- token 继续保持工程感，但只承担“真实 carrier 名称提示”。

## Assembly order

1. `page_30` 的阅读卡整体淡出。
2. 单大圆环与 4 个主节点一起成立。
3. 边上的 source / decision token 补入。
4. 中心题眼淡入，压实“先看真实结果，再决定停或继续”。

## Acceptance checklist

1. 观众一眼能读出这是单个闭环，而不是两张解释卡。
2. `Hook 进入 -> 网页数据评分 -> 网页图片评分 -> 回执循环` 必须完整成立。
3. `workflow gate / front probe / browser capture / blind critics` 必须全部露出。
4. `通过则停止 / 不通过继续` 必须贴在回执这一侧，闭环真正闭上。

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260422-page31-live-harness/page31-live-harness.png)

- 当前 URL：`http://127.0.0.1:4173/?step=page_31`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260422-page31-live-harness/page31-live-harness.txt`

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 |
| --- | --- | --- | --- |
| `整体布局 Review` | `browser capture` | `formal harness` | 大圆环已经取代双卡成为主视觉，中心题眼清楚。 |
| `模块空间结构 Review` | `browser capture` | `formal harness` | 四个主节点与边上 token 分层明确，读法顺畅。 |
| `单节点与单边 Review` | `browser capture` | `formal harness` | formal 指标 `overlap/crossing/nodePierce/textOverflow=0`，曲线箭头闭环清楚。 |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 当前渲染载体 | 连续语义 Review | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- |
| `hook-node` | 把页面修改送进 loop。 | rounded node | 成立 | 顶部起点清楚 | `通过` |
| `data-node` | 真实网页数据评分。 | rounded node | 成立 | 右侧第二拍明确 | `通过` |
| `image-node` | 真实网页图片评分。 | rounded node | 成立 | 底部收口自然 | `通过` |
| `receipt-node` | 回写 stop / continue 决策。 | rounded node | 成立 | 左侧闭环成立 | `通过` |
| `source-1..4` | workflow gate / probe / capture / critics 提示。 | micro token | 成立 | 贴边而不抢主环 | `通过` |
| `decision-1..2` | stop / continue 提示。 | micro token | 成立 | 与回执节点关系清楚 | `通过` |
| `loop-core-title` | 中心题眼。 | center copy | 成立 | 主旨压实 | `通过` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 当前渲染载体 | 连续语义 Review | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- |
| `hook-to-data` | Hook 后先做数据评分。 | curved arrow | 成立 | 上右象限读法清楚 | `通过` |
| `data-to-image` | 数据评分进入图片评分。 | curved arrow | 成立 | 右下象限衔接自然 | `通过` |
| `image-to-receipt` | 图片评分进入回执循环。 | curved arrow | 成立 | 下左象限回送稳定 | `通过` |
| `receipt-to-hook` | 回执重新送回 Hook。 | curved arrow | 成立 | 左上象限闭合顺畅 | `通过` |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_30 -> page_31）

- 帧窗口：`3125 -> 3196`（`71` frames @ 60fps）
- 总时长：`1.183s`
- 时长评估：`too_short`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `72`，相对当前 `Δ+1` frames
- 标准时序评估：`in_range`（标准下限 `1.027s`，上限 `1.387s`）
- 标准调整建议：目标帧长 `71`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`1.027s`
- 标准允许上限：`1.387s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 100.0 | 100.0 | - |
| fade_out | 100.0 | 320.0 | 220.0 | fade_out_pso_reading_page |
| node_motion | 320.0 | 667.2 | 347.2 | move_harness_ring, move_ring_helpers |
| fade_in | 667.2 | 887.2 | 220.0 | fade_in_loop_copy |
| hold_after | 887.2 | 1027.2 | 140.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_pso_reading_page | fade_out | fade_out | - | 220.0 | formula |
| move_harness_ring | node_move | node_motion | distancePx=176.0 | 347.2 | formula |
| move_ring_helpers | node_move | node_motion | distancePx=148.0 | 320.6 | formula |
| fade_in_loop_copy | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 3125 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 3143 | 0.300 | fade_out_pso_reading_page(fade_out) | 无显著边变化 |
| Mid | 3160 | 0.583 | move_harness_ring(node_move)、move_ring_helpers(node_move) | 无显著边变化 |
| Q3 | 3178 | 0.883 | fade_in_loop_copy(fade_in) | 无显著边变化 |
| End | 3196 | 1.183 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- `page_30` 的阅读资料页退场后，当前页先立住大圆环，再补边上的 token，没有回到旧的双卡解释格式。
- 通用 profile 仍提示比 `1.20s` 下限短 `1` 帧，但 Harness 标准时序 verdict 已为 `in_range`，当前 anchor 可接受。
- 这页现在承担的是“真实 harness 闭环”而不是“工程解释列表”。

## 审批状态

`通过。可作为尾声部分的 live harness 闭环页进入正式讲述。`
