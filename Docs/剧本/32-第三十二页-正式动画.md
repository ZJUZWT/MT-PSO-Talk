# 第 32 页

> 文件类型：`正式动画页`
> Step / Sketch：`page_32` / `-`
> 上一页：`page_31`
> 下一页：`page_33`

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `stageLayout=7` | 工程资料抽走后，顶部改成 1 张横向 `书与视频` 卡，下方仍保留 `推荐游戏`。 |
| Node 分数 | `mechanicalScore=7.3` | `culture-card + game-card` 两段结构清楚，formal harness 无 blocker。 |
| Edge 分数 | `pass` | 本页继续依赖阅读流，不再需要工程资料双栏之间的平衡。 |
| 动画分数 | `in_range` | `page_31 -> page_32` 标准时序 verdict=`in_range`。 |

## Page goal

- 在 `page_30` 抽走 PSO 工程资料之后，这一页只保留书、视频和游戏入口。
- 结尾语气要继续轻一点，不再重复工程链接，也不再像两栏百科页。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `延伸阅读与另一些推荐` |
| 页面副标题 | 把 PSO 工程资料单独抽走以后，这一页只保留书、视频和游戏入口。 |
| 当前页核心信息 | 顶部为横向 `书与视频` 卡，内部改成 2x2 分布；底部继续保留 `推荐游戏` 卡。 |
| 与上一页关系 | 从项目内 harness 回环切到更轻的阅读 / 观看 / 游玩入口。 |
| 这一页新增了什么 | `culture-card` 的双列排布、重新居中的 `game-card`。 |
| 这一页移动了什么 | 原 `书与视频` 右栏扩成全宽顶卡；`推荐游戏` 继续留在底部。 |
| 这一页删除了什么 | 删除旧 `工程延伸` 左栏及其 4 条 PSO 链接。 |
| 这一页保留了什么 | `《银河帝国》`、`《反杜林论》`、`马克思主义哲学`、`重读资本论`、`星际拓荒`、`Type Help`。 |
| 增加 / 删除的 edge | 删除旧的 `engineering-card -> game-card` 阅读关系；保留从顶卡到游戏卡的单轴收束。 |
| 被替换的过渡逻辑 | 从“工程 + 书 / 视频双栏 + 游戏”替换为“书 / 视频全宽卡 + 游戏卡”，但仍保持整板淡入淡出。 |
| 需要强调的元素 | `书与视频`、`推荐游戏`、`星际拓荒`。 |

## Node inventory

| id | text | semantic role | visual kind | anchor region | emphasis | alignment constraints | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `culture-card` | `书与视频` | 顶部推荐主卡 | rounded card | 主舞台上半区 | 高 | 顶卡横向占满主舞台，内部两列均衡 | 从工程方法切到更宽视角的入口 | 否 |
| `culture-link-1..4` | `银河帝国 / 反杜林论 / 马克思主义哲学 / 重读资本论` | 书和视频条目 | link item | 顶卡内部 | 中高 | 左右两列成对排布 | 保留“继续往下看”的入口 | 否 |
| `game-card` | `推荐游戏` | 底部体验入口 | rounded card | 主舞台下半区 | 中高 | 横跨页面下部，接住顶卡之后的视线 | 从阅读再过渡到体验 | 否 |
| `game-link-1..2` | `星际拓荒 / Type Help` | 游戏条目 | link item | 底卡内部 | 中高 | 左右分栏，等距排布 | 收尾页前的最后一个轻入口 | 否 |

## Edge inventory

| id | from | to | meaning | route grammar | max bends | segment label | arrow style | sibling / symmetry note | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `culture-to-game` | `culture-card` | `game-card` | 从阅读入口过渡到体验入口 | implicit vertical | 0 | - | none | 顶卡读完后自然落到底卡 | 是 | 否 |

## Spatial constraints

- 顶卡必须横向铺开，明确告诉观众“工程资料已经被抽走，这页只剩其它推荐”。
- 顶卡内部两列不能互相压迫，4 条条目必须仍然清楚可扫读。
- `推荐游戏` 要继续成为独立底卡，不能重新挤回顶卡。

## Element styles

- 顶卡延续浅底边框，但不额外引入新的强调色块。
- 条目继续保持真实链接样式。
- 底卡沿用晚段推荐页的安静节奏，不新增额外总结 footer。

## Assembly order

1. 淡出 `page_31` 的 harness 说明页。
2. 顶部 `书与视频` 卡进入。
3. 底部 `推荐游戏` 卡与条目淡入。

## Acceptance checklist

1. 本页不再出现任何 PSO 工程资料重复项。
2. `书与视频` 与 `推荐游戏` 必须仍然是两个明确分组。
3. 顶卡拓宽后，阅读节奏不能散掉。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | 前段 | `page_31` 淡出 | harness 回环页 | 从项目内方法回到轻一些的推荐。 |
| 2 | 中段 | 顶部推荐卡进入 | `culture-card` | 这里只保留书和视频。 |
| 3 | 后段 | 游戏卡与条目进入 | `game-card` | 再补一个体验入口。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | `culture-card` 全宽布局、`culture-link-1..4` 双列排布、`game-link-1..2` |
| 需要复用的元素 | `ENDING_CULTURE_LINKS`、`ENDING_GAME_LINKS`、session rail、footer pills |
| 当前共享元素 / 连续语义 carrier | 外层工作台结构连续即可 |
| 是否需要镜头推进 / 缩放 | 否 |
| 是否需要逐步显隐 | 是，顶卡先到位，底卡后补 |
| 风险点 | 如果顶部仍留出一个“左栏空位”，这页会显得像漏删元素，而不是完成拆页后的新结构。 |

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260421-page32-reading-tail/page_32.png)

- 当前 URL：`http://127.0.0.1:4173/?variant=bus-clean&step=page_32`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260421-page32-reading-tail/page_32.txt`

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `formal harness` | `stageLayout=7` | 顶卡 + 底卡两段结构清楚，页面不再留出“工程栏空位”。 | `通过` |
| `模块空间结构 Review` | `formal harness` | `mechanicalScore=7.3` | 顶卡双列与底卡双列都过了机械门槛。 | `通过` |
| `单节点与单边 Review` | `formal harness` | `overlap=0 / crossing=0 / nodePierce=0 / textOverflow=0` | 节点和阅读流都过硬门槛。 | `通过` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `culture-card` | 顶部全宽 `书与视频` 卡。 | 从 harness 说明切到更轻的推荐页 | rounded card | 成立。 | `通过` | stageLayout=7。 | `通过` |
| `culture-link-1..4` | 4 条书 / 视频条目。 | 从工程方法转向外部视角 | link item | 成立。 | `通过` | textOverflow=0。 | `通过` |
| `game-card` | 底部 `推荐游戏` 卡。 | 继续给出体验入口 | rounded card | 成立。 | `通过` | overlap/crossing 全为 0。 | `通过` |
| `game-link-1..2` | 2 条游戏条目。 | 作为收尾前最后一个轻入口 | link item | 成立。 | `通过` | 排布稳定。 | `通过` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `culture-to-game` | 从顶卡收束到底部游戏卡。 | 从书 / 视频继续落到体验入口 | implicit reading flow | 成立。 | `通过` | 单轴阅读自然。 | `通过` |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_31 -> page_32）

- 帧窗口：`3125 -> 3196`（`71` frames @ 60fps）
- 总时长：`1.183s`
- 时长评估：`too_short`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `72`，相对当前 `Δ+1` frames
- 标准时序评估：`in_range`（标准下限 `0.993s`，上限 `1.341s`）
- 标准调整建议：目标帧长 `71`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`0.993s`
- 标准允许上限：`1.341s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 100.0 | 100.0 | - |
| fade_out | 100.0 | 320.0 | 220.0 | fade_out_harness_page |
| node_motion | 320.0 | 633.0 | 313.0 | move_culture_card, move_reading_games |
| fade_in | 633.0 | 853.0 | 220.0 | fade_in_reading_links |
| hold_after | 853.0 | 993.0 | 140.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_harness_page | fade_out | fade_out | - | 220.0 | formula |
| move_culture_card | node_move | node_motion | distancePx=140.0 | 313.0 | formula |
| move_reading_games | node_move | node_motion | distancePx=120.0 | 294.0 | formula |
| fade_in_reading_links | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 3125 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 3143 | 0.300 | fade_out_harness_page(fade_out) | 无显著边变化 |
| Mid | 3160 | 0.583 | move_culture_card(node_move)、move_reading_games(node_move) | 无显著边变化 |
| Q3 | 3178 | 0.883 | hold_after 静置 | hold_after 静置 |
| End | 3196 | 1.183 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- 2026-04-21 抽页同步：原 `工程延伸` 左栏已经整体前移到 `page_30`，本页只保留书 / 视频与推荐游戏。
- 顶部结构从双栏改成 1 张全宽 `culture-card`，避免抽走工程栏后留下空白。
- `page_31 -> page_32` 的通用时长带宽显示 `too_short`，但 harness 标准时序 verdict 为 `in_range`，当前 anchor 可接受。

## 审批状态

`通过。可作为 page30 之后的轻量推荐页进入正式讲述。`
