# 第 33 页

> 文件类型：`正式动画页`
> Step / Sketch：`page_33` / `-`
> 上一页：`page_32`
> 下一页：`-`

## Harness 公式快照

- 公式版本：`Harness Registry v1.0`
- 统一总表：`Docs/SkillFramework/higher-level/slide-geometry-harness/references/harness-formula-registry.md`
- 几何硬门槛：`overlap=0`、`crossing=0`、`nodePierce=0`、`textOverflow=0`
- 动画标准：`node_move / edge_grow / fade` + phase `hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 时长判定：`allowedMinSec = requiredSec`，`allowedMaxSec = requiredSec * 1.35`

## 页面评估看板（四项）

| 指标 | 分数 | 说明 |
| --- | --- | --- |
| 布局分数 | `2.3/10（latest mechanical）` | `quote-body / quote-footer` 与左二中二维码右二的三栏入口层次清楚，但下半区仍然偏贴底边。 |
| Node 分数 | `5.8/10（latest mechanical）` | quote 文本、左右 4 个入口与中间二维码通过 overlap / overflow 检查，当前主要压力在底部 margin。 |
| Edge 分数 | `pass` | 本页不靠技术边，只有从引句落到推荐入口的隐式阅读流。 |
| 动画分数 | `标准 in_range` | `page_32 -> page_33` 的 profile 和 Harness 标准时序 verdict 都为 `in_range`。 |

## Page goal

- 恢复成真正独立的终页，让最后一句与推荐入口不再挤在 bridge page 上。
- 上半区只负责最后一句，下半区只负责给观众留两个继续往下走的方向。
- 保持“已经结束”的气质，而不是重新回到工程讲解。

## 页面改动表

| 项目 | 内容 |
| --- | --- |
| 页面标题 | `最后一句与后续入口` |
| 页面副标题 | 上半只保留 4 行引句和尾注，下半改成左二中二维码右二的三栏入口。 |
| 当前页核心信息 | 上半是 4 行引句和尾注；下半是左侧 2 个入口、中间仓库二维码、右侧 2 个入口。 |
| 与上一页关系 | 从 bridge page 的思考停顿，落到全场真正的最后一句和最后两个延伸方向。 |
| 这一页新增了什么 | 4 行引句、`quote-footer`、左右 4 个入口项、`repo-qr`、`repo-url`。 |
| 这一页移动了什么 | 把原 merged ending 里的 quote 和入口整体从 `page_32` 拆出；删除页内 `《逍遥游》` 题眼；把二维码和左右入口整体再下压一点；右侧两项改成右对齐。 |
| 这一页删除了什么 | 删除旧 merge 里“bridge 与终页合在一页”的做法。 |
| 这一页保留了什么 | session rail、底部 step pills、晚段统一背景、现有推荐内容。 |
| 增加 / 删除的 node / edge | 新增 `quote-*`、`left-link-*`、`right-link-*`、`repo-qr`、`repo-url`；删除旧的左右推荐卡容器语义。 |
| 被替换的过渡逻辑 | `page_32` 不再直接兼做最后一页，而是在 bridge 停顿后，让当前页单独淡入并保持终场停留。 |
| 需要强调的元素 | `无所可用，安所困苦哉！`、`以此作为这次分享的最后一句。`、中间仓库二维码、左右两组推荐入口。 |

## Node inventory

| id | text | semantic role | visual kind | anchor region | emphasis | alignment constraints | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `quote-body` | 4 行《逍遥游》引句 | 最后一句主体 | text block | 主舞台上半区 | 最高 | 4 行共中轴、无外框 | 整场分享的最后一句 | 否 |
| `quote-footer` | `以此作为这次分享的最后一句。` | 尾注 | text only | 引句下方 | 中 | 与引句共中轴 | 把终句钉住 | 否 |
| `left-link-1..2` | `《银河帝国》 / 人类高质量思政课` | 左侧推荐条目 | link item | 主舞台左下 | 中 | 两条目等步进、共左起点，并整体向中轴内收 | 推荐继续阅读 / 观看 | 否 |
| `repo-qr` | `仓库二维码` | 中间统一出口 | framed image | 主舞台下半中央 | 高 | 与页面中轴对齐；外框必须直角 | 给观众一个立即可扫的出口 | 否 |
| `repo-url` | `github.com/ZJUZWT/MT-PSO-Talk` | 二维码文字落款 | text only | 二维码下方 | 中 | 与二维码共中轴 | 为扫码失败时保留可读 URL | 否 |
| `right-link-1..2` | `星际拓荒 / Type Help` | 右侧推荐条目 | link item | 主舞台右下 | 中 | 两条目等步进、统一右对齐，并整体向中轴内收 | 推荐继续体验 | 否 |

## Edge inventory

| id | from | to | meaning | route grammar | max bends | segment label | arrow style | sibling / symmetry note | 与上一页连续语义 | 同元素承载要求 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `quote-to-links` | `quote-body` | `left-link-* / repo-qr / right-link-*` | 从终句自然落到三栏延伸入口 | implicit vertical | 0 | - | none | 终句之后分成左二中一右二的对称入口 | 是 | 否 |

## Spatial constraints

- 引句区必须无框，终页的中心只能由字号和留白形成。
- 4 行正文和 `quote-footer` 必须共用一条中轴。
- 左右两组入口与中间二维码要明显下压到主舞台下半区，不能飘回中部。
- 删除 `《逍遥游》` 页内题眼后，上半区要留下更完整的引句呼吸空间。
- 左右两列必须保持对称，外边距与到二维码的间距都要一起向内收。
- 右列标题与副标题统一右对齐，右缘收口要明显比旧版更整齐。
- `repo-qr` 必须是直角框，不允许圆角裁切。

## Element styles

- 引句区只用文字层级，不再加外框或额外容器。
- 左右入口只保留文字层级，不再加左右大卡容器。
- `repo-qr` 使用轻描边白底直角框。
- 终页保持克制，不新增大色块背景。

## Assembly order

1. `page_32` 的 bridge page 淡出。
2. 4 行引句和尾注先成立。
3. 左右两列入口与中间二维码随后落位，完成整场终页。

## Acceptance checklist

1. 这一页必须显式成为单独终页，而不是 bridge page 的延长。
2. 引句区必须无框，且 4 行正文保持大字号终场气质。
3. 左右入口与二维码必须留在下半区，不得挤回正文附近。
4. `以此作为这次分享的最后一句。` 必须作为尾注明确出现。
5. 二维码必须无圆角，右列文字必须右对齐，左右两列必须对称往中间收一点。

## 动画剧本

| Beat | 时间 | 画面变化 | 屏幕元素 | 讲述词 / 备注 |
| --- | --- | --- | --- | --- |
| 1 | 前段 | `page_32` 淡出 | bridge page | 从“人的学习”落到最终收束。 |
| 2 | 中段 | 引句与尾注进入 | 4 行引句、`quote-footer` | 先把最后一句立住。 |
| 3 | 后段 | 左右入口与二维码进入 | `left-link-*`、`repo-qr`、`right-link-*` | 最后再给出继续往下走的几个方向。 |

## Remotion 实现备注

| 项目 | 内容 |
| --- | --- |
| 需要新增的节点 / 元素 | 4 行引句、`quote-footer`、左右 4 个入口项、`repo-qr`、`repo-url` |
| 需要复用的元素 | `ENDING_CULTURE_LINKS`、`ENDING_GAME_LINKS`、session rail、footer pills |
| 当前共享元素 / 连续语义 carrier | 外层工作台结构延续即可 |
| 是否需要镜头推进 / 缩放 | 否 |
| 是否需要逐步显隐 | 是，引句先成立，左右入口与二维码后补 |
| 风险点 | 如果左右两列重新外扩、右列不右对齐，或二维码回到圆角框，终页的出口感会立刻变散。 |

## Current screenshot

![Current screenshot](/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260422-160302/review-capture.png)

- 当前 URL：`http://127.0.0.1:4174/?step=page_33`
- 捕获方式：`browser-api page`
- 捕获来源记录：`/Users/swannzhang/Workspace/AIProjects/MT-PSO-Talk/ignore/browser-api-captures/20260422-160302/review-capture.txt`
- Capture size：`2.8M PNG`

## 三层 Review 总表

| Review 层级 | Art Critic | Geometry Critic | 综合判断 | 综合分数 |
| --- | --- | --- | --- | --- |
| `整体布局 Review` | `browser capture` | `formal harness` | 删除页内 `《逍遥游》` 后，上半区更舒展，但当前下半区仍然贴近底边。 | `2.3/10` |
| `模块空间结构 Review` | `browser capture` | `formal harness` | 引句区与三栏入口层次分明，二维码与左右入口下压后更像终页出口，但底部余量仍偏小。 | `4.3/10` |
| `单节点与单边 Review` | `browser capture` | `formal harness` | quote 文本、二维码与左右入口保持无 overlap / crossing / text overflow，结构是干净的。 | `9.3/10` |

## 节点剧本与 Review 表

| 节点 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `quote-body` | 4 行引句正文。 | 整场最后一句主体 | text block | 成立 | `通过` | 删除页内题眼后，正文区更舒展，终场感更直接。 | `通过` |
| `quote-footer` | 最后一句尾注。 | 把终句收住 | text | 成立 | `通过` | 层级弱于正文但清楚可见 | `通过` |
| `left-link-1..2` | 《银河帝国》与思政课。 | 提供继续阅读 / 观看方向 | link item | 成立 | `通过` | 向中轴内收后更像一组稳定入口 | `通过` |
| `repo-qr` | 仓库二维码。 | 终页的中间出口 | framed image | 成立 | `通过` | 直角边框更干净，不和页面圆角语言抢戏 | `通过` |
| `repo-url` | 仓库 URL。 | 为二维码补一个可读落款 | text | 成立 | `通过` | 中轴清楚，弱于二维码主体 | `通过` |
| `right-link-1..2` | 星际拓荒与 Type Help。 | 提供继续体验方向 | link item | 成立 | `通过` | 右对齐后右缘收口更稳 | `通过` |

## 边剧本与 Review 表

| 边 | 中文剧本描述 | 与上一页连续语义 | 当前渲染载体 | 连续语义 Review | 连续语义分数 | 几何 / 美感 Review | 综合分数 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `quote-to-links` | 从终句自然落到左二中二维码右二的三栏入口。 | bridge 落点之后的最终延伸 | implicit reading flow | 成立 | `通过` | 自然、克制 | `通过` |

## 过渡动画剧本与时长审查

## 过渡动画剧本（page_32 -> page_33）

- 帧窗口：`3267 -> 3409`（`142` frames @ 60fps）
- 总时长：`2.367s`
- 时长评估：`in_range`（目标区间 `1.20s ~ 2.60s`）
- 调整建议：目标帧长 `142`，相对当前 `Δ+0` frames
- 标准时序评估：`in_range`（标准下限 `1.868s`，上限 `2.522s`）
- 标准调整建议：目标帧长 `142`，相对当前 `Δ+0` frames

### Harness 动画标准（v1.0）

- 标准相位顺序：`hold_before -> fade_out -> node_motion -> edge_motion -> fade_in -> hold_after`
- 标准推荐最短时长：`1.868s`
- 标准允许上限：`2.522s`

| Phase | 开始(ms) | 结束(ms) | 时长(ms) | 动作 |
|---|---:|---:|---:|---|
| hold_before | 0.0 | 220.0 | 220.0 | - |
| fade_out | 220.0 | 440.0 | 220.0 | fade_out_feedback_bridge |
| node_motion | 440.0 | 1228.0 | 788.0 | move_quote_block, move_link_groups |
| fade_in | 1228.0 | 1448.0 | 220.0 | fade_in_final_links |
| hold_after | 1448.0 | 1868.0 | 420.0 | - |

| 动作 id | 类型 | 相位 | 指标 | 计算时长(ms) | 来源 |
|---|---|---|---|---:|---|
| fade_out_feedback_bridge | fade_out | fade_out | - | 220.0 | formula |
| move_quote_block | node_move | node_motion | distancePx=640.0 | 788.0 | formula |
| move_link_groups | node_move | node_motion | distancePx=560.0 | 712.0 | formula |
| fade_in_final_links | fade_in | fade_in | - | 220.0 | formula |

| 时间点 | 帧 | 相对时间(s) | Node 行为 | Edge 行为 |
|---|---:|---:|---|---|
| Start | 3267 | 0.000 | hold_before 静置 | hold_before 静置 |
| Q1 | 3302 | 0.583 | move_quote_block(node_move)、move_link_groups(node_move) | 无显著边变化 |
| Mid | 3338 | 1.183 | move_quote_block(node_move) | 无显著边变化 |
| Q3 | 3374 | 1.783 | hold_after 静置 | hold_after 静置 |
| End | 3409 | 2.367 | 无显著节点变化 | 无显著边变化 |

## Animation review verdict

- `page_32` 的 bridge page 在前半段先完整退场，当前页随后独立成立，没有再把终页和桥页压在一起。
- 当前 frame span `142` frames，同步满足 profile 与 Harness 标准时序的 `in_range` 判定。
- 左右入口与二维码的进入被明确留到了终页自身，不再提前打断 bridge 页的停顿。
- 删除 `《逍遥游》` 页内题眼后，4 行引句获得了更完整的顶部空间，没有再被额外标题切断。
- 顶部页标题同步改成 `最后一句与后续入口`，不再重复 `《逍遥游》` 这个词。
- `repo-qr` 改成直角框后仍保持中轴稳定；右列改成右对齐并对称内收后，再把下半区略微下压，三栏出口的收口更干净，但当前底部 outer margin 仍只有 `9px`。

## 审批状态

`通过。当前改动满足本轮用户要求，但后续若继续打磨，优先处理 page_33 底部 outer margin 偏紧的问题。`
