# Test Benchmarks

This directory contains the benchmark-first test harness for the PSO and
compression measurement project.

The initial scaffold keeps all benchmark code under `Test/` so later tasks can
add runners, schemas, platform adapters, and assets without mixing them into a
product-style application layout.
